/*
  SINET Audio Lekar — App Core
  File: js/app.js
  Version: 16.0.0.69 (System + Studio pipeline)n audit + filters + backup)
  Author: miuchins | Co-author: SINET AI
*/

// Cache-bust audio engine updates (NO-SW mode relies on browser cache)
import { SinetAudioEngine } from './audio/audio-engine.js?v=16.0.0.69';
import { renderProtocolToWavBlobURL, estimateWavBytes } from './audio/ios-rendered-track.js?v=16.0.0.69';
import { normalizeCatalogPayload } from './catalog/stl-adapter.js?v=16.0.0.69';

const SINET_APP_VERSION = "16.0.0.69";



// v15.7.3.0 — default segment length for long sessions (esp. iOS PRO rendered WAV)
const DEFAULT_SEGMENT_MIN = 40; // minutes
// Repeat scopes
const REPEAT_SCOPE_ITEM = "item";

// Play modes (Queue/List)
const PLAY_MODE_SEQ = 'seq';
const PLAY_MODE_RANDOM = 'random';
   // (A) ponovi jedan simptom/protokol
const REPEAT_SCOPE_QUEUE = "queue"; // (B) ponovi celu Queue listu

/** iOS detection (iPhone/iPad/iPod + iPadOS masquerading as Mac) */
function isIOSDevice() {
  const ua = navigator.userAgent || "";
  const isAppleMobile = /iP(hone|od|ad)/.test(ua);
  const isIpadOS13Plus = (navigator.platform === "MacIntel" && navigator.maxTouchPoints > 1);
  return isAppleMobile || isIpadOS13Plus;
}

// 0.25s silent WAV (base64) — used to keep audio session alive on iOS when screen is off.
const SILENT_WAV_DATA_URI = "data:audio/wav;base64,";

// 0.25s audible BEEP WAV (base64) — used for iPhone one-tap self-test.
const BEEP_WAV_DATA_URI = "data:audio/wav;base64,";

class IosAudioSessionKeeper {
  constructor(enabled) {
    this.enabled = !!enabled;
    this.audioEl = null;
    this.started = false;
  }

  _ensureEl() {
    if (this.audioEl) return this.audioEl;
    const el = document.createElement("audio");
    el.src = SILENT_WAV_DATA_URI;
    el.loop = true;
    el.preload = "auto";
    el.playsInline = true;
    el.setAttribute("playsinline", "");
    // Keep very low volume (not muted) — some iOS setups stop muted audio in background.
    el.volume = 0.0001;
    el.muted = false;
    el.style.display = "none";
    document.body.appendChild(el);
    this.audioEl = el;
    return el;
  }

  start() {
    if (!this.enabled) return;
    const el = this._ensureEl();
    // Must be triggered by a user gesture (we call this from Play click).
    try {
      const p = el.play();
      if (p && typeof p.catch === "function") p.catch(() => {});
      this.started = true;
    } catch(_) {}
  }

  stop() {
    if (!this.audioEl) return;
    try {
      this.audioEl.pause();
      this.audioEl.currentTime = 0;
    } catch(_) {}
    this.started = false;
  }
}


class App {
  constructor() {
    window.app = this;

    this.audio = new SinetAudioEngine();


    // Audio Engine v2 prefs
    this.audioPrefs = { master: 0.55, boost: false, wave: "sine" };
    try {
      const p = JSON.parse(localStorage.getItem("sinet_audio_prefs_v1") || "null");
      if (p && typeof p === "object") this.audioPrefs = { ...this.audioPrefs, ...p };
    } catch(_) {}

    // Playlist selection
    this.playlistSelected = new Set();
    this.favoritesSelected = new Set();
    this.userSymptomsSelected = new Set();
    this._playlistBackup = null;

    // Play mode (Queue/List)
    try { this.playMode = (localStorage.getItem('SINET_PLAYMODE') || PLAY_MODE_SEQ); } catch(_) { this.playMode = PLAY_MODE_SEQ; }

    // iOS background hardening
    this._isIOS = isIOSDevice();

    // iOS: background playback in browsers is NOT guaranteed.
    // We expose an explicit (experimental) toggle for the MediaStream->HTMLAudio routing.
    this._iosBgExperimental = false;
    this._iosBgRendered = false;
    try {
      const expStored = localStorage.getItem("sinet_ios_bg_experimental");
      const proStored = localStorage.getItem("sinet_ios_bg_rendered");

      // ✅ iOS default: enable best-effort background + bluetooth path.
      // Rationale:
      //  - Updates often require clearing site data, which resets toggles.
      //  - iOS/Safari tends to pause WebAudio in background; HTMLMediaElement is more reliable.
      //  - PRO rendered mode uses <audio> and improves bluetooth routing consistency.
      // Users can disable this in Settings.
      if (this._isIOS && expStored === null && proStored === null) {
        this._iosBgExperimental = false;
        this._iosBgRendered = true;
        try {
          localStorage.setItem("sinet_ios_bg_experimental", "0");
          localStorage.setItem("sinet_ios_bg_rendered", "1");
          localStorage.setItem("sinet_ios_bg_defaulted", "1");
        } catch (_) {}
      } else {
        this._iosBgExperimental = (expStored === "1");
        this._iosBgRendered = (proStored === "1");
      }
    } catch(_) {}
    // iOS: we use a silent HTMLAudio keep-alive to (a) unlock playback via user gesture and
    // (b) improve the chance that iOS keeps the session alive on lock-screen.
    // Enabled for BOTH experimental and PRO rendered modes.
    this._iosKeeper = new IosAudioSessionKeeper(this._isIOS && (this._iosBgExperimental || this._iosBgRendered));
    this._iosHintShown = false;

    // iOS: optional MediaStream -> <audio> output (best-effort)
    this._iosMediaOutEl = null;

    // iOS PRO: Rendered WAV player (<audio> src=blob:)
    this._iosRenderedEl = null;
    this._rendered = { active: false };
    this._renderedRaf = null;
    this._renderAbort = null;
    this._renderedBound = false;

    // Media Session (best-effort, varies by browser/iOS)
    this._mediaSessionReady = false;

    // Heartbeat that tries to keep AudioContext resumed while playing (best-effort)
    this._ctxHeartbeat = null;
    this.db = window.db;

    // Resume/session (user-friendly) — OFF only if user disables
    this._resumeEnabled = true;
    this._sessionId = null;
    this._sessionStartedAt = null;
    this._sessionTitle = null;
    this._sessionSource = null;
    this._sessionGeneratedBy = null;
    this._sessionNote = "";
    this._sessionAudit = null;
    this._playOrigin = null;
    this._sessionLastPersistAt = 0;


    this.catalogItems = [];
    this._catalogLoad = { state: "init", err: null, count: 0, url: null, ts: 0 };
    this.playlist = [];
    this.isPlaylistActive = false;
    this.currentPlaylistIndex = 0;

    this.protocolTotalTimeSec = 0;
    this.protocolBaseElapsedSec = 0;

    // v15.7.1.5 — Repeat/Loop controls (default: (A) "lekarski" — jedan simptom)
    this.repeat = {
      scope: REPEAT_SCOPE_ITEM,
      infinite: false,
      count: 1,        // N
      remaining: 1,    // runtime counter
      cycle: 1,        // current cycle number (1-based)
      totalCycles: 1
    };

    // v15.7.1.5 — Repeat runtime + protocol timer control
    this._repeatRt = { baseProtocolSec: 0, queueCycle: 1, itemCycle: 1 };
    this._protocolTimerEnabled = true;

    // v15.7.1.5 — iOS PRO segmented rendered playback buffers
    this._renderSeg = {
      enabled: true,
      segMin: DEFAULT_SEGMENT_MIN,
      // runtime
      active: false,
      itemTotalSec: 0,
      perFreqSec: 0,
      startOffsetSec: 0,
      // pre-rendered next
      next: null, // { url, bytes, totalSec, segmentState }
      preparing: false
    };

    this.selectedItem = null;
    this.currentModalId = null;

    this._lastStats = null;

    this.ui = {};

    /* AUDIO_PREFS_BINDINGS */
    try {
      this.applyAudioPrefs();
      const vol = document.getElementById("p-vol");
      if (vol) {
        vol.value = String(Math.round((Number(this.audioPrefs?.master)||0.55)*100));
        vol.addEventListener("input", (e)=>{
          const v = Number(e.target.value||0)/100;
          this.setMasterVolumeUI(v);
        });
      }
      const boostBtn = document.getElementById("p-boost");
      if (boostBtn) boostBtn.innerText = `⚡ Boost: ${this.audioPrefs?.boost ? "ON" : "OFF"}`;
      const wave = document.getElementById("p-wave");
      if (wave) {
        wave.value = String(this.audioPrefs?.wave || "sine");
        wave.addEventListener("change", (e)=> this.setWaveform(e.target.value));
      }
    } catch(_) {}


    this.acupressureRegistry = null;
    this._modalItem = null;

    // v15.3 — Senior presets + user symptoms + overrides
    this.seniorPresets = null;
    this.seniorShowAll = false;
    this.homeEntryMode = null;
    this.activeOblast = null;
    this.lastCatalogViewItems = null;
    this.lastCatalogViewTitle = "";

    this.userSymptoms = [];
    this.overrides = {};

    // Nav helpers (elder-friendly)
    this.currentPageId = 'home';
    this.navStack = [];
    this._toastTimer = null;



    // v15.5 — Moji Protokoli (user-defined frequency sequences)
    this.protocols = [];
    this._protoEdit = null;
    // v15.4.7 — cached favorite IDs
    this._favSet = new Set();
  }

  async init() {
    console.log('SINET v16.0.0.73 Init');
    this.cacheUI();
    try { this._ensureSpaPageTools(); } catch(_) {}
    try { this._ensureMenuPriorityOrder(); } catch(_) {}

    // v15.7.1.5 — restore Loop/Repeat settings (dock + playlist + modal)
    try { this._restoreRepeatFromStorage(); } catch(_) {}

    try {
      const cb = document.getElementById('ios-bg-toggle');
      if (cb) cb.checked = this.isIosBgExperimentalEnabled();
      const cb2 = document.getElementById('ios-bg-render-toggle');
      if (cb2) cb2.checked = this.isIosBgRenderedEnabled();
      const card = document.getElementById('ios-settings-card');
      if (card && !this._isIOS) card.style.display = 'none';

      // Visual hint: iPhone MODE button active when PRO rendered is enabled.
      const iphoneBtn = document.getElementById('iphone-mode-btn');
      if (iphoneBtn) {
        if (this._isIOS && this.isIosBgRenderedEnabled()) iphoneBtn.classList.add('active');
        else iphoneBtn.classList.remove('active');
      }

      // One-time toast if we defaulted iOS mode after a cache/storage clear.
      try {
        const didDefault = localStorage.getItem('sinet_ios_bg_defaulted') === '1';
        const shown = localStorage.getItem('sinet_ios_bg_default_toast') === '1';
        if (this._isIOS && didDefault && !shown) {
          localStorage.setItem('sinet_ios_bg_default_toast', '1');
          this.showToast('🍏 iPhone: uključen je "iPhone MODE" (pozadina + bluetooth, best‑effort). Ako ne želiš, isključi u Podešavanjima.', { timeoutMs: 9000 });
        }
      } catch (_) {}
    } catch(_) {}

    // iOS / background / lock-screen best-effort helpers
    this._setupMediaSession();
    this._bindBackgroundRecovery();

    // ✅ ensure DB ready BEFORE any favorites / playlist read
    if (this.db) await this.db.init();
    // System audit retention (7 days)
    try { await this.db.rotateAudit(this.getAuditRetentionSettings()); } catch(_) {}

    // Frequency catalog (shared)
    try { await this.loadFrequencyCatalog(); } catch(_) {}

    // Global error hooks (audit)
    try { this._installGlobalErrorHooks(); } catch(_) {}


    // v15.4.7 — preload favorites set for fast UI toggles
    await this.refreshFavoritesSet();
    await this.loadCatalogData();
    await this.loadPlaylistFromDB();
    await this.loadProtocolsFromDB();

    // optional offline registry (images, licensing)
    await this.loadAcupressureRegistry();

    // v15.3 — load senior presets + user symptoms + overrides (never break init)
    await this.loadSeniorPresets();
    await this.loadUserSymptoms();
    await this.loadOverrides();
    this.applyUserDataToCatalog();
    try { this.renderWorkbenchPicker(''); } catch(_) {}

    this.renderSystemPresets();
    this.showCatalogHome();
    this.renderPlaylistUI();

    // Engine hooks
    this.audio.onTick = (stats) => this.onAudioTick(stats);
    this.audio.onComplete = () => this.onAudioComplete();
    this.audio.onSkip = (freqObj, stats) => this.onAudioSkip(freqObj, stats);
    this.audio.onFreqChange = (freqObj, stats) => this.onFreqChange(freqObj, stats);

    await this.renderResumeHint();
    try { this._loadResumePref(); this._syncResumeButtonUI(); } catch(_) {}
    try { await this.renderHomeResumePanel(); } catch(_) {}
    try { await this.maybeToastResumeOnStartup(); } catch(_) {}

    
// iOS UX: show 🍏 iPhone MODE button only on iOS devices
try {
  const b1 = document.getElementById("iphone-mode-btn");
  if (b1) b1.style.display = this._isIOS ? "inline-flex" : "none";
} catch(_) {}
    try { await this.renderAuditPreview(); } catch(_) {}
    // Restore UX hardening: after successful restore/import we reload once; on next boot show a toast.
    try {
      if (localStorage.getItem("sinet_restore_completed") === "1") {
        localStorage.removeItem("sinet_restore_completed");
        this.showToast("Restore uspešan ✅ Aplikacija je osvežena radi stabilnosti.", { timeoutMs: 6000 });
      }
    } catch(_) {}
    this.log("APP", "Init", "OK");
  }

// iOS/Safari: explicit user-gesture unlock (now with an audible self-test)
async unlockAudio() {
  try { this.audio.init(); } catch(_) {}
  try { this._ensurePlaybackSession(); } catch(_) {}

  // Best-effort: start iOS keeper (must be user gesture)
  try {
    if (this._isIOS && this._iosKeeper) {
      this._iosKeeper.enabled = true;
      this._iosKeeper.start();
    }
  } catch(_) {}

  const res = await this._iosAudioSelfTest();

  if (res.ok) {
    try {
      const b = document.getElementById("unlock-audio-btn");
      if (b) b.style.display = "none";
    } catch(_) {}
    this.showToast("✅ iPhone/Safari: audio je aktiviran. Sada možeš ▶ POKRENI.", { timeoutMs: 6000 });
  } else {
    this.showToast("❌ iPhone: nema zvuka. Proveri 🔇 silent switch + Volume, pa tapni 🍏 iPhone MODE.", { timeoutMs: 12000 });
    try {
      const b = document.getElementById("unlock-audio-btn");
      if (b) b.style.display = "inline-flex";
    } catch(_) {}
  }

  try { this._showIosDiag(res.detail); } catch(_) {}
}

// 🍏 One-tap iPhone Mode: enables best web-only background path + runs self-test
async iphoneMode() {
  try { this.setIosBgExperimentalEnabled(false); } catch(_) {}
  try { this.setIosBgRenderedEnabled(true); } catch(_) {}

  try { this.audio.init(); } catch(_) {}
  try { this._ensurePlaybackSession(); } catch(_) {}

  // Must be within user gesture
  try {
    if (this._isIOS && this._iosKeeper) {
      this._iosKeeper.enabled = true;
      this._iosKeeper.start();
    }
  } catch(_) {}

  try {
    const btn = document.getElementById("iphone-mode-btn");
    if (btn) btn.classList.add("active");
  } catch(_) {}

  const res = await this._iosAudioSelfTest();

  if (res.ok) {
    this.showToast("🍏 iPhone MODE: uključen ✅  Sada izaberi simptom i pritisni ▶ POKRENI.", { timeoutMs: 9000 });
    try { const b = document.getElementById("unlock-audio-btn"); if (b) b.style.display = "none"; } catch(_) {}
  } else {
    this.showToast("🍏 iPhone MODE: iOS još blokira zvuk ❌  1) isključi 🔇 silent switch 2) pojačaj Volume 3) ponovi 🍏 MODE.", { timeoutMs: 14000 });
    try { const b = document.getElementById("unlock-audio-btn"); if (b) b.style.display = "inline-flex"; } catch(_) {}
  }

  try { this._showIosDiag(res.detail); } catch(_) {}
}

async _iosAudioSelfTest() {
  const detail = {
    isIOS: !!this._isIOS,
    ua: (navigator.userAgent || "").slice(0, 160),
    ctxState: null,
    ios: {
      proRendered: !!this.isIosBgRenderedEnabled?.(),
      experimental: !!this.isIosBgExperimentalEnabled?.()
    },
    catalog: null,
    webAudio: { ok: false, err: null },
    htmlAudio: { ok: false, err: null },
    prime: { renderedEl: false, mediaOutEl: false, err: null }
  };

  // Snapshot catalog state (useful when clicks do nothing because catalog isn't loaded)
  try {
    const c = this._catalogLoad || {};
    detail.catalog = {
      state: c.state || "unknown",
      count: Number(c.count || 0),
      url: c.url || null,
      err: c.err || null
    };
  } catch(_) {}

  // WebAudio beep (may be muted by iOS silent switch)
  try {
    this.audio.init();
    const ctx = this.audio.audioContext;
    detail.ctxState = ctx ? ctx.state : null;
    if (ctx && ctx.state === "suspended") {
      await ctx.resume().catch(() => {});
      detail.ctxState = ctx.state;
    }
    if (ctx) {
      const osc = ctx.createOscillator();
      osc.type = "sine";
      osc.frequency.value = 440;

      const g = ctx.createGain();
      g.gain.value = 0.55;

      osc.connect(g);
      g.connect(ctx.destination);

      osc.start();
      osc.stop(ctx.currentTime + 0.18);

      detail.webAudio.ok = true;
    }
  } catch (e) {
    detail.webAudio.ok = false;
    detail.webAudio.err = String(e?.message || e);
  }

  // HTMLAudio beep (should play even with silent switch off; varies by device settings)
  try {
    const a = new Audio(BEEP_WAV_DATA_URI);
    a.preload = "auto";
    a.loop = false;
    a.playsInline = true;
    a.setAttribute && a.setAttribute("playsinline", "");
    a.volume = 1.0;
    a.muted = false;

    const p = a.play();
    if (p && typeof p.then === "function") await p;
    setTimeout(() => { try { a.pause(); } catch(_) {} }, 300);

    detail.htmlAudio.ok = true;
  } catch (e) {
    detail.htmlAudio.ok = false;
    detail.htmlAudio.err = String(e?.message || e);
  }

  // IMPORTANT: Prime the SAME hidden <audio> elements that will later be used for iOS playback.
  // iOS sometimes unlocks playback per-element; priming a different element may not help.
  try {
    if (this._isIOS) {
      if (detail.ios.proRendered) {
        const el = this._ensureIosRenderedEl();
        el.src = SILENT_WAV_DATA_URI;
        el.loop = true;
        el.preload = "auto";
        el.playsInline = true;
        el.setAttribute && el.setAttribute("playsinline", "");
        el.volume = 0.0001;
        el.muted = false;

        const pr = el.play();
        if (pr && pr.catch) await pr.catch(() => {});
        detail.prime.renderedEl = true;
      }

      if (detail.ios.experimental && !detail.ios.proRendered) {
        const el2 = this._ensureIosMediaOutEl();
        // prime with silent src; later we switch to srcObject
        el2.src = SILENT_WAV_DATA_URI;
        el2.loop = true;
        el2.preload = "auto";
        el2.playsInline = true;
        el2.setAttribute && el2.setAttribute("playsinline", "");
        el2.volume = 0.0001;
        el2.muted = false;

        const pm = el2.play();
        if (pm && pm.catch) await pm.catch(() => {});
        detail.prime.mediaOutEl = true;
      }
    }
  } catch (e) {
    detail.prime.err = String(e?.message || e);
  }

  // Refresh catalog snapshot post-actions
  try {
    const c = this._catalogLoad || {};
    detail.catalog = {
      state: c.state || "unknown",
      count: Number(c.count || 0),
      url: c.url || null,
      err: c.err || null
    };
  } catch(_) {}

  return { ok: !!(detail.webAudio.ok || detail.htmlAudio.ok), detail };
}

_showIosDiag(detail) {
  if (!detail) return;
  const boxId = "ios-diag-box";
  let box = document.getElementById(boxId);
  if (!box) {
    box = document.createElement("div");
    box.id = boxId;
    box.style.position = "fixed";
    box.style.left = "12px";
    box.style.right = "12px";
    box.style.bottom = "110px";
    box.style.zIndex = "9999";
    box.style.background = "rgba(20,20,20,0.92)";
    box.style.color = "#fff";
    box.style.padding = "10px 12px";
    box.style.borderRadius = "14px";
    box.style.fontFamily = "monospace";
    box.style.fontSize = "12px";
    box.style.maxHeight = "22vh";
    box.style.overflow = "auto";
    box.style.boxShadow = "0 10px 25px rgba(0,0,0,0.35)";
    box.addEventListener("click", () => { try { box.remove(); } catch(_) {} });
    document.body.appendChild(box);
  }

  const lines = [];
  lines.push("🍏 iOS DIAG (tap to close)");
  lines.push("ctx=" + (detail.ctxState || "n/a") +
    " webAudio=" + (detail.webAudio?.ok ? "OK" : "FAIL") +
    " htmlAudio=" + (detail.htmlAudio?.ok ? "OK" : "FAIL") +
    " catalog=" + ((detail.catalog?.state || "n/a") + ":" + (detail.catalog?.count ?? "0")) +
    " pro=" + (detail.ios?.proRendered ? "1" : "0") +
    " exp=" + (detail.ios?.experimental ? "1" : "0") +
    " primeR=" + (detail.prime?.renderedEl ? "1" : "0") +
    " primeM=" + (detail.prime?.mediaOutEl ? "1" : "0")
  );
  if (detail.webAudio && detail.webAudio.err) lines.push("webAudio.err=" + detail.webAudio.err);
  if (detail.htmlAudio && detail.htmlAudio.err) lines.push("htmlAudio.err=" + detail.htmlAudio.err);
  lines.push("UA=" + (detail.ua || ""));
  box.textContent = lines.join("\n");
}

  _ensureIosMediaOutEl() {
    if (this._iosMediaOutEl) return this._iosMediaOutEl;
    const el = document.createElement("audio");
    el.preload = "auto";
    el.playsInline = true;
    el.setAttribute("playsinline", "");
    el.volume = 1.0;
    el.muted = false;
    el.style.display = "none";
    document.body.appendChild(el);
    this._iosMediaOutEl = el;
    return el;
  }

  _ensureIosRenderedEl() {
    if (this._iosRenderedEl) return this._iosRenderedEl;
    const el = document.createElement("audio");
    el.preload = "auto";
    el.playsInline = true;
    el.setAttribute("playsinline", "");
    el.controls = false;
    el.volume = 1.0;
    el.muted = false;
    el.style.display = "none";
    document.body.appendChild(el);
    this._iosRenderedEl = el;
    return el;
  }

  _isRenderedPlaying() {
    try {
      const el = this._iosRenderedEl;
      return !!(el && this._rendered && this._rendered.active && !el.paused && !el.ended);
    } catch(_) { return false; }
  }
  async _startRenderedTrack(params = {}) {
    // params: { sequence, durationsSec, totalSec, resumeTrackSec, titleText, loopAll, segmentMin }
    if (!this._isIOS) throw new Error('Rendered mode is intended for iOS only.');

    const fullSeq = Array.isArray(params.sequence) ? params.sequence : [];
    const fullDurs = Array.isArray(params.durationsSec) ? params.durationsSec : null;
    const fullTotalSec = Math.max(0, Number(params.totalSec) || 0);
    const resumeTrackSec = Math.max(0, Number(params.resumeTrackSec) || 0);

    if (!fullSeq.length || !fullTotalSec) throw new Error('Empty rendered track.');

    const segMin = Math.max(5, Number(params.segmentMin) || Number(this._renderSeg?.segMin) || DEFAULT_SEGMENT_MIN);
    let segLenSec = segMin * 60;

    // 🍏 iOS PRO (Rendered): for lock-screen/background, JS timers can be throttled.
    // If we render the whole remaining plan into a single WAV file, playback can
    // continue without relying on JS to stitch segments.
    try{
      if (this.isIosBgRenderedEnabled && this.isIosBgRenderedEnabled()) {
        const wantSingle = (localStorage.getItem('sinet_ios_bg_render_single') !== '0');
        if (wantSingle) {
          const HARD_MB = 160; // must match _startRenderedSegment safeguard
          const estBytesFull = estimateWavBytes(fullTotalSec, 22050, 1, 16);
          const estMBFull = estBytesFull / (1024 * 1024);
          if (estMBFull <= HARD_MB) {
            segLenSec = Math.max(segLenSec, fullTotalSec + 0.01);
            if (!this._iosSingleRenderHinted) {
              this._iosSingleRenderHinted = true;
              this.showToast('🍏 iPhone PRO: renderujem ceo plan kao 1 fajl (bolje za pozadinu).', { timeoutMs: 6500 });
            }
          } else {
            // Too large for a single buffer -> keep segmented render (foreground recommended).
            if (!this._iosSingleRenderTooBigHinted) {
              this._iosSingleRenderTooBigHinted = true;
              this.showToast('🍏 iPhone PRO: plan je predugačak za 1 fajl. Radiće u segmentima (pozadina nije zagarantovana).', { timeoutMs: 9000 });
            }
          }
        }
      }
    }catch(_){}


    try { this.audio?.stop?.(); } catch(_) {}
    try { this._teardownPlaybackSession('render_start'); } catch(_) {}

    const el = this._ensureIosRenderedEl();

    try {
      if (this._iosKeeper) {
        this._iosKeeper.enabled = true;
        this._iosKeeper.start();
      }
    } catch(_) {}

    try { if (this._renderAbort) this._renderAbort.abort(); } catch(_) {}
    this._renderAbort = new AbortController();

    if (this.ui.playerDock) { this.ui.playerDock.style.display = 'block'; requestAnimationFrame(() => this._syncDockPadding(false)); }
    if (this.ui.nowPlayingPanel) this.ui.nowPlayingPanel.style.display = 'block';
    if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = '⏳';

    const r = await this._startRenderedSegment({
      fullSeq,
      fullDurs,
      fullTotalSec,
      resumeTrackSec,
      segLenSec,
      titleText: (params.titleText || '').toString(),
      signal: this._renderAbort.signal
    });

    return r;
  }

  _durationAt(i, fullDurs, perStep) {
    try {
      if (fullDurs && fullDurs.length) return Math.max(0, Number(fullDurs[i]) || 0);
      return Math.max(0, perStep);
    } catch (_) {
      return Math.max(0, perStep);
    }
  }

  _computeRenderedSegment(fullSeq, fullDurs, fullTotalSec, resumeTrackSec, segLenSec) {
    const n = fullSeq.length;
    const perStep = n ? (fullTotalSec / n) : 0;

    let startIdx = 0;
    let startOffset = 0;
    for (let i = 0; i < n; i++) {
      const d = this._durationAt(i, fullDurs, perStep);
      if (resumeTrackSec < startOffset + d) { startIdx = i; break; }
      startOffset += d;
      startIdx = Math.min(n - 1, i + 1);
    }

    startIdx = Math.max(0, Math.min(n - 1, startIdx));

    let endIdx = startIdx;
    let segTotal = 0;
    for (let i = startIdx; i < n; i++) {
      const d = this._durationAt(i, fullDurs, perStep);
      segTotal += d;
      endIdx = i;
      if (segTotal >= segLenSec && i > startIdx) break;
    }

    let segSeq = fullSeq.slice(startIdx, endIdx + 1);
    // 🍏 iOS PRO: apply skip selections by zeroing frequency values for the next rendered segments.
    // (Current segment is already rendered; skip becomes effective from next segment render.)
    try {
      const skips = this._renderedSkipSet;
      if (skips && skips.size && Array.isArray(segSeq) && segSeq.length) {
        segSeq = segSeq.map((it, j) => {
          const gi = startIdx + j;
          if (!skips.has(gi)) return it;
          // create a shallow clone and force silence
          const o = Object.assign({}, it);
          o.value = 0;
          o.hz = 0;
          return o;
        });
      }
    } catch(_) {}
    const segDurs = fullDurs ? fullDurs.slice(startIdx, endIdx + 1) : null;

    const resumeInSeg = Math.max(0, resumeTrackSec - startOffset);
    const endOffset = Math.min(fullTotalSec, startOffset + segTotal);

    return {
      startIdx,
      endIdx,
      startOffsetSec: startOffset,
      endOffsetSec: endOffset,
      segSeq,
      segDurs,
      segTotalSec: Math.max(0, segTotal),
      resumeInSegSec: resumeInSeg
    };
  }

  async _startRenderedSegment({ fullSeq, fullDurs, fullTotalSec, resumeTrackSec, segLenSec, titleText, signal }) {
    const el = this._ensureIosRenderedEl();

    const seg = this._computeRenderedSegment(fullSeq, fullDurs, fullTotalSec, resumeTrackSec, segLenSec);

    const estBytes = estimateWavBytes(seg.segTotalSec, 22050, 1, 16);
    const estMB = estBytes / (1024 * 1024);
    const HARD_MB = 160;
    if (estMB > HARD_MB) {
      this.showToast(`⚠️ iOS PRO: Segment je prevelik za RAM render (~${estMB.toFixed(0)} MB). Prelazim na standardni režim (foreground).`, { timeoutMs: 10000 });
      this._iosBgRendered = false;
      try { localStorage.setItem('sinet_ios_bg_rendered', '0'); } catch (_) {}
      try { const el2 = document.getElementById('ios-bg-render-toggle'); if (el2) el2.checked = false; } catch (_) {}
      try { this._hideStartOverlay(); this._setStartButtonsEnabled(true); } catch (_) {}
      return { fallback: true };
    }

    try {
      const fromMin = Math.floor(seg.startOffsetSec / 60);
      const toMin = Math.ceil(Math.min(fullTotalSec, seg.endOffsetSec) / 60);
      this.showToast(`🍏 iOS PRO: Renderujem segment ${fromMin}–${toMin} min (~${estMB.toFixed(0)} MB)`, { timeoutMs: 4500 });
    } catch (_) {}

    let rendered;
    try {
      rendered = await renderProtocolToWavBlobURL({
        sequence: seg.segSeq,
        durationsSec: seg.segDurs,
        totalSec: seg.segTotalSec,
        sampleRate: 22050,
        channels: 1,
        gain: (this.audio?.masterGain?.gain?.value) ? Math.max(0.05, Math.min(0.9, this.audio.masterGain.gain.value)) : 0.25,
        subCarrierHz: this.audio?.subCarrierHz || 200,
        subThresholdHz: this.audio?.subCarrierThresholdHz || 50,
        fadeMs: 12,
        signal
      });
    } catch (e) {
      this.showToast('❌ iOS PRO: render nije uspeo. Vraćam standardni režim.', { timeoutMs: 9000 });
      try { this._hideStartOverlay(); this._setStartButtonsEnabled(true); } catch (_) {}
      return { fallback: true, error: String(e?.message || e) };
    }

    try {
      if (this._rendered && this._rendered.url) URL.revokeObjectURL(this._rendered.url);
    } catch (_) {}

    el.src = rendered.url;
    el.loop = false;
    try { el.currentTime = Math.max(0, seg.resumeInSegSec); } catch (_) {}

    this._rendered = {
      active: true,
      url: rendered.url,
      bytes: rendered.bytes,
      totalSec: seg.segTotalSec,
      fullTotalSec,
      sampleRate: rendered.sampleRate,
      channels: rendered.channels,
      sequence: seg.segSeq,
      durationsSec: seg.segDurs,
      fullSequence: fullSeq,
      fullDurationsSec: fullDurs,
      segStartIndex: seg.startIdx,
      segEndIndex: seg.endIdx,
      segStartOffsetSec: seg.startOffsetSec,
      segEndOffsetSec: seg.endOffsetSec,
      lastIndex: -1,
      titleText: titleText || ''
    };

    // Fill "FREKVENCIJE" panel immediately (iOS PRO rendered mode)
    try { this.renderNowPlayingList({ currentIndex: Number(this._rendered.segStartIndex||0) }); } catch(_) {}


    if (!this._renderedBound) {
      this._renderedBound = true;
      el.addEventListener('ended', () => {
        try { this._handleRenderedEnded(); } catch (_) {}
      });
    }

    this._startRenderedTicker();

    try {
      const p = el.play();
      if (p && typeof p.catch === 'function') {
        p.catch(() => {
          try {
            this.showToast('🍏 iPhone blokira ▶ dok se audio ne aktivira. Tapni 🔊 AKTIVIRAJ, pa opet ▶.', { timeoutMs: 12000 });
            const b = document.getElementById('unlock-audio-btn');
            if (b) b.style.display = 'inline-flex';
          } catch (_) {}
        });
      }
    } catch (_) {}

    if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = '⏸';

    try { this._hideStartOverlay(); this._setStartButtonsEnabled(true); } catch (_) {}

    this.log('PLAYER', 'Play (iOS PRO Rendered Seg)', this.selectedItem?.id || '');

    return { fallback: false };
  }

  async _handleRenderedEnded() {
    try {
      if (!this._rendered || !this._rendered.active) return;

      const nextResume = Number(this._rendered.segEndOffsetSec) || 0;
      const fullTotal = Number(this._rendered.fullTotalSec) || Number(this._rendered.totalSec) || 0;

      if (nextResume >= (fullTotal - 0.01)) {
        this._rendered.active = false;
        this.onAudioComplete();
        return;
      }

      try {
        this._showStartOverlay('⏳ PRIPREMAM…', 'Sledeći segment (iOS PRO)…');
        this._setStartButtonsEnabled(false);
      } catch (_) {}

      await this._startRenderedSegment({
        fullSeq: this._rendered.fullSequence || [],
        fullDurs: this._rendered.fullDurationsSec || null,
        fullTotalSec: fullTotal,
        resumeTrackSec: nextResume,
        segLenSec: Math.max(5*60, Number(this._renderSeg?.segMin || DEFAULT_SEGMENT_MIN) * 60),
        titleText: this._rendered.titleText || '',
        signal: (this._renderAbort && this._renderAbort.signal) ? this._renderAbort.signal : undefined
      });
    } catch (e) {
      try { this._rendered.active = false; } catch (_) {}
      try { this._hideStartOverlay(); this._setStartButtonsEnabled(true); } catch (_) {}
      this.onAudioComplete();
    }
  }

  _startRenderedTicker() {
    if (this._renderedRaf) cancelAnimationFrame(this._renderedRaf);
    const el = this._iosRenderedEl;

    const loop = () => {
      try {
        if (!this._rendered || !this._rendered.active || !el) return;
        if (document.hidden) { this._renderedRaf = requestAnimationFrame(loop); return; }

        const tSeg = Math.max(0, Number(el.currentTime) || 0);
        const base = Math.max(0, Number(this._rendered.segStartOffsetSec) || 0);
        const tFull = Math.max(0, Math.min((Number(this._rendered.fullTotalSec) || 0), base + tSeg));

        const seqFull = this._rendered.fullSequence || this._rendered.sequence || [];
        const dursFull = this._rendered.fullDurationsSec || this._rendered.durationsSec;
        const fullTotalSec = Number(this._rendered.fullTotalSec || this._rendered.totalSec || 0);
        const perStep = (!dursFull && seqFull.length) ? (fullTotalSec / seqFull.length) : 0;

        let idx = 0;
        let acc = 0;
        for (; idx < seqFull.length; idx++) {
          const d = dursFull ? (Number(dursFull[idx]) || 0) : perStep;
          if (tFull < acc + d) break;
          acc += d;
        }
        idx = Math.min(idx, Math.max(0, seqFull.length - 1));
        const durCur = dursFull ? (Number(dursFull[idx]) || 0) : perStep;
        const elapsedInFreq = Math.max(0, tFull - acc);

        const stats = {
          currentIndex: idx,
          totalItems: seqFull.length,
          enabledTotalItems: seqFull.length,
          currentPos: idx,
          elapsedInFreq,
          durationPerFreq: durCur,
          durationCurrentSec: durCur,
          totalDurationSec: fullTotalSec,
          totalTrackSec: fullTotalSec,
          elapsedTrackSec: tFull,
          hasPerFreqDurations: !!dursFull
        };

        if (this._rendered.lastIndex !== idx) {
          this._rendered.lastIndex = idx;
          this.onFreqChange(seqFull[idx] || {}, stats);
        }

        this.onAudioTick(stats);

        this._lastStats = {
          currentIndex: stats.currentIndex,
          totalItems: stats.totalItems,
          elapsedInFreq: stats.elapsedInFreq,
          durationPerFreq: stats.durationCurrentSec
        };
      } catch (_) {}

      this._renderedRaf = requestAnimationFrame(loop);
    };

    this._renderedRaf = requestAnimationFrame(loop);
  }


  _stopRenderedTrack() {
    try { if (this._renderAbort) this._renderAbort.abort(); } catch(_) {}
    try {
      const el = this._iosRenderedEl;
      if (el) { el.pause(); el.removeAttribute("src"); el.load(); }
    } catch(_) {}
    try { if (this._rendered && this._rendered.url) URL.revokeObjectURL(this._rendered.url); } catch(_) {}
    try { if (this._renderedRaf) cancelAnimationFrame(this._renderedRaf); } catch(_) {}
    this._renderedRaf = null;
    this._rendered = { active:false };
  }



  cacheUI() {
    this.ui = {
      screens: {
        home: document.getElementById('page-home'),
        catalog: document.getElementById('page-catalog'),
        playlist: document.getElementById('page-playlist'),
        protocols: document.getElementById('page-protocols'),
        favorites: document.getElementById('page-favorites'),
        mysymptoms: document.getElementById('page-mysymptoms'),
        ai: document.getElementById('page-ai'),
        help: document.getElementById('page-help'),
        workbench: document.getElementById('page-workbench'),
        system: document.getElementById('page-system'),
        sessions: document.getElementById('page-sessions'),
        settings: document.getElementById('page-settings'),
        admin: document.getElementById('page-admin'),
        studio: document.getElementById('page-studio')
      },
      catalogOblastGrid: document.getElementById('catalog-oblast-grid'),
      catalogList: document.getElementById('catalog-list'),
      userSymptomsList: document.getElementById('user-symptoms-list'),
      userSymptomForm: document.getElementById('user-symptom-form'),
      protocolsList: document.getElementById('protocols-list'),
      protocolEditor: document.getElementById('protocol-editor'),
      protocolEditorBox: document.getElementById('protocol-editor-box'),
      aiPrompt: document.getElementById('ai_prompt'),
      main: document.querySelector('main'),
      playerDock: document.getElementById('player-dock'),
      pTitle: document.getElementById('p-title'),
      pTimerNow: document.getElementById('p-timer-now'),
      pTimerElapsed: document.getElementById('p-timer-elapsed'),
      pTimerTotal: document.getElementById('p-timer-total'),
      pProtocolTimer: document.getElementById('p-protocol-timer'),
      pStatus: document.getElementById('p-status'),
      btnPlayPause: document.getElementById('btn-pp'),
      btnNowList: document.getElementById('btn-nowlist'),
      nowPlayingPanel: document.getElementById('nowplaying-panel'),
      nowPlayingList: document.getElementById('nowplaying-list'),
      pActiveHz: document.getElementById('p-active-hz'),
      pActiveName: document.getElementById('p-active-name'),
      pActiveDesc: document.getElementById('p-active-desc'),
      pActiveSrc: document.getElementById('p-active-src'),
      pActiveDur: document.getElementById('p-active-dur')
    };
  }

  _syncDockPadding(forceHide = false) {
    // iOS / mobile: ensure main content is not hidden behind the fixed player dock.
    try {
      const main = this.ui?.main || document.querySelector('main');
      if (!main) return;
      const dock = this.ui?.playerDock || document.getElementById('player-dock');

      let h = 0;
      const visible = !!dock && !forceHide && (dock.style.display !== 'none');
      if (visible) {
        h = Math.round((dock.getBoundingClientRect?.().height || dock.offsetHeight || 0));
        // fallback for first frame
        if (h < 40) h = 190;
      }
      document.documentElement.style.setProperty('--dock-h', `${h}px`);
    } catch (_) {}
  }



  /* ===================== v15.7.1.5 — Loop / Repeat (UI) ===================== */

  _readRepeatSettings(ctx = "playlist") {
    // ctx: "playlist" | "modal" | "dock"
    const isModal = (ctx === "modal");
    const isDock = (ctx === "dock");
    const idCount = isModal ? "m-repeat-count" : (isDock ? "p-repeat-count" : "pl-repeat-count");
    const idInf = isModal ? "m-repeat-infinite" : (isDock ? "p-repeat-infinite" : "pl-repeat-infinite");
    const nameScope = isModal ? "m-repeat-scope" : (isDock ? "p-repeat-scope" : "pl-repeat-scope");

    let count = 1;
    let infinite = false;
    let scope = REPEAT_SCOPE_ITEM;

    try {
      const elC = document.getElementById(idCount);
      count = Math.max(1, Math.min(200, Number(elC?.value || 1) || 1));
    } catch(_) { count = 1; }

    try { infinite = !!document.getElementById(idInf)?.checked; } catch(_) { infinite = false; }

    try {
      const el = document.querySelector(`input[name="${nameScope}"]:checked`);
      const v = (el?.value || "").toString();
      if (v === REPEAT_SCOPE_QUEUE) scope = REPEAT_SCOPE_QUEUE;
      else scope = REPEAT_SCOPE_ITEM;
    } catch(_) { scope = REPEAT_SCOPE_ITEM; }

    // Safety: if infinite + scope item + playlist mode and playlist has >1 items, user probably meant queue.
    // But do NOT override automatically — just warn subtly in status line.
    return { count, infinite, scope };
  }

  _applyRepeatSettings(s) {
    const count = Math.max(1, Number(s?.count) || 1);
    const infinite = !!s?.infinite;
    const scope = (s?.scope === REPEAT_SCOPE_QUEUE) ? REPEAT_SCOPE_QUEUE : REPEAT_SCOPE_ITEM;

    this.repeat.scope = scope;
    this.repeat.infinite = infinite;
    this.repeat.count = count;
    this.repeat.totalCycles = infinite ? Infinity : count;
    this.repeat.remaining = infinite ? Infinity : count;
    this.repeat.cycle = 1;

    try { this._renderRepeatStatus(); } catch(_) {}
  }

  _renderRepeatStatus(extra = "") {
    if (!this.ui?.pStatus) return;

    const scope = (this.repeat.scope === REPEAT_SCOPE_QUEUE) ? REPEAT_SCOPE_QUEUE : REPEAT_SCOPE_ITEM;
    const nLbl = (this.repeat.infinite) ? '∞' : String(this.repeat.count || 1);

    let cycLbl = '';
    if (scope === REPEAT_SCOPE_QUEUE) {
      const q = Math.max(1, Number(this._repeatRt?.queueCycle) || 1);
      cycLbl = this.repeat.infinite ? `Queue ciklus ${q}` : `Queue ciklus ${q}/${this.repeat.count || 1}`;
    } else {
      const ic = Math.max(1, Number(this._repeatRt?.itemCycle) || 1);
      cycLbl = this.repeat.infinite ? `Simptom ciklus ${ic}` : `Simptom ciklus ${ic}/${this.repeat.count || 1}`;
    }

    const msg = `🔁 ${cycLbl} (N=${nLbl})${extra ? ' • ' + extra : ''}`;
    this.ui.pStatus.innerText = msg;
  }

  /* ===================== v15.7.1.5 — UX overlay (“⏳ PRIPREMAM…”) ===================== */

  _showStartOverlay(text="⏳ PRIPREMAM…", sub="Sačekaj 5 sekundi…") {
    const ov = document.getElementById("start-overlay");
    const t = document.getElementById("start-overlay-text");
    const s = document.getElementById("start-overlay-sub");
    const bar = document.getElementById("start-overlay-bar");
    if (!ov) return;
    if (t) t.innerText = text;
    if (s) s.innerText = sub;
    if (bar) bar.style.width = "0%";
    ov.style.display = "flex";

    // pseudo-progress (indeterminate)
    const start = Date.now();
    const tick = () => {
      if (ov.style.display === "none") return;
      const dt = Date.now() - start;
      const pct = Math.min(92, Math.max(5, Math.floor((dt / 5200) * 92)));
      if (bar) bar.style.width = pct + "%";
      requestAnimationFrame(tick);
    };
    requestAnimationFrame(tick);
  }

  _hideStartOverlay() {
    const ov = document.getElementById("start-overlay");
    const bar = document.getElementById("start-overlay-bar");
    if (bar) bar.style.width = "100%";
    if (ov) ov.style.display = "none";
  }

  _setStartButtonsEnabled(enabled) {
    try {
      const b = document.querySelector('button.btn-full[onclick="startProtocol()"]');
      if (b) { b.disabled = !enabled; b.style.opacity = enabled ? "1" : "0.65"; }
    } catch(_) {}
    try {
      const b2 = document.getElementById("m-play-btn");
      if (b2) { b2.disabled = !enabled; b2.style.opacity = enabled ? "1" : "0.65"; }
    } catch(_) {}
  }

  /* ===================== v15.7.1.5 — Preporuka block ===================== */

  _getRecommendation(item) {
    const p = item?.preporuka || item?.preporuke || item?.recommendation || null;

    const perFreqMin = Number(
      p?.trajanje_po_frekv_min ?? p?.trajanjePoFrekvMin ?? p?.trajanjePoFrekvencijiMin ??
      item?.trajanjePoFrekvencijiMin ?? item?.trajanje_po_frekv_min ?? 5
    ) || 5;

    const loopN = Number(p?.loop_uzastopno ?? p?.loopN ?? p?.ponovi ?? 1) || 1;
    const daysN = Number(p?.dnevno_dana ?? p?.dnevnoDana ?? p?.dnevno ?? 1) || 1;
    const pauseDays = Number(p?.pauza_dana ?? p?.pauzaDana ?? p?.pauza ?? 0) || 0;

    const segMin = Number(p?.segment_min ?? p?.segmentMin ?? DEFAULT_SEGMENT_MIN) || DEFAULT_SEGMENT_MIN;

    return {
      perFreqMin: Math.max(1, perFreqMin),
      loopN: Math.max(1, loopN),
      daysN: Math.max(1, daysN),
      pauseDays: Math.max(0, pauseDays),
      segMin: Math.max(5, segMin)
    };
  }

  _renderRecommendationToModal(item) {
    const r = this._getRecommendation(item);
    try { const el = document.getElementById("m-reco-dur"); if (el) el.innerText = `${r.perFreqMin} min`; } catch(_) {}
    try { const el = document.getElementById("m-reco-loop"); if (el) el.innerText = `${r.loopN}×`; } catch(_) {}
    try { const el = document.getElementById("m-reco-days"); if (el) el.innerText = `${r.daysN}`; } catch(_) {}
    try { const el = document.getElementById("m-reco-pause"); if (el) el.innerText = `${r.pauseDays}`; } catch(_) {}
    try { const el = document.getElementById("m-reco-seg"); if (el) el.innerText = `${r.segMin}`; } catch(_) {}
  }

  _renderRecommendationToPlayer(item) {
    const box = document.getElementById("p-reco-box");
    if (!box) return;
    const r = this._getRecommendation(item);
    try { const el = document.getElementById("p-reco-dur"); if (el) el.innerText = `${r.perFreqMin} min`; } catch(_) {}
    try { const el = document.getElementById("p-reco-loop"); if (el) el.innerText = `${r.loopN}×`; } catch(_) {}
    try { const el = document.getElementById("p-reco-days"); if (el) el.innerText = `${r.daysN}`; } catch(_) {}
    try { const el = document.getElementById("p-reco-pause"); if (el) el.innerText = `${r.pauseDays}`; } catch(_) {}
    try { const el = document.getElementById("p-reco-seg"); if (el) el.innerText = `${r.segMin}`; } catch(_) {}
    box.style.display = "block";
  }

  async logEx(cat, action, details="", extra=null) {
    try { await this.db?.logAction?.(cat, action, details, extra || null); } catch(e) {}
  }

  async log(cat, action, details="") {
    return this.logEx(cat, action, details, null);
  }

  /* ===================== DATA ===================== */
  
async loadCatalogData(force = false) {
  const loader = document.getElementById('loader');
  const loaderText = document.getElementById('loader-text') || loader;
  const loaderRetry = document.getElementById('loader-retry');
  const loaderDetail = document.getElementById('loader-detail');

  // Track state for iOS diag / UX
  try {
    this._catalogLoad = this._catalogLoad || { state: "init", err: null, count: 0, url: null, ts: 0 };
    this._catalogLoad.state = "loading";
    this._catalogLoad.err = null;
    this._catalogLoad.count = 0;
    this._catalogLoad.url = null;
    this._catalogLoad.ts = Date.now();
  } catch(_) {}

  if (loaderRetry) loaderRetry.style.display = "none";
  if (loaderDetail) loaderDetail.style.display = "none";
  if (loaderText) loaderText.innerText = "⏳ Učitavam katalog iz /data/...";

  // Prefer canonical STL if present, fallback to runtime catalog
  const candidates = [
    'data/SINET_STL.json',
    'data/SINET_CATALOG.json'
  ];

  let lastErr = null;
  for (const url of candidates) {
    try {
      // cache-bust on force
      const u = force ? (url + (url.includes('?') ? '&' : '?') + 't=' + Date.now()) : url;
      const res = await fetch(u, { cache: "no-store" });
      if (!res.ok) throw new Error(`${url} nije pronađen (${res.status})`);
      const raw = await res.json();

      try {
        const fc = raw?.meta?.freq_catalog;
        if (fc && window.SINET_FrequencySchema?.setFreqCatalog) window.SINET_FrequencySchema.setFreqCatalog(fc);
      } catch(_) {}

      const norm = normalizeCatalogPayload(raw);
      this.catalogItems = Array.isArray(norm.items) ? norm.items : [];

      // v15.3 — keep canonical core list (without user additions/overrides)
      this.catalogCoreItems = this.catalogItems.slice();

      if (loader) loader.style.display = 'none';

      try {
        this._catalogLoad.state = "ok";
        this._catalogLoad.err = null;
        this._catalogLoad.count = this.catalogItems.length;
        this._catalogLoad.url = url;
        this._catalogLoad.ts = Date.now();
      } catch(_) {}

      this.log("DATA", "Catalog Loaded", `${this.catalogItems.length} • ${url}`);
      return;
    } catch (e) {
      lastErr = e;
    }
  }

  // All failed
  const msg = "GREŠKA: " + (lastErr?.message || "Catalog load failed");
  if (loaderText) loaderText.innerText = msg;
  if (loaderRetry) loaderRetry.style.display = "inline-flex";
  if (loaderDetail) {
    loaderDetail.style.display = "block";
    loaderDetail.innerText = "Saveti: 1) osveži stranicu 2) otvori /index-nosw.html 3) proveri da li fajlovi postoje u /data/.";
  }

  try {
    this._catalogLoad.state = "err";
    this._catalogLoad.err = String(lastErr?.message || lastErr || "unknown");
    this._catalogLoad.count = 0;
    this._catalogLoad.url = null;
    this._catalogLoad.ts = Date.now();
  } catch(_) {}

  console.error(lastErr);
  this.log("ERROR", "Catalog Load Failed", lastErr?.message || "");
}

async loadAcupressureRegistry() {
  // Optional offline registry (images/licensing). Must NEVER break init.
  const url = 'data/media/acupressure/registry.json';
  try {
    const res = await fetch(url, { cache: "no-store" });
    if (!res.ok) throw new Error(`${url} nije pronađen (${res.status})`);
    this.acupressureRegistry = await res.json();
    this.log("DATA", "Acupressure Registry Loaded", url);
  } catch (e) {
    this.acupressureRegistry = null;
    console.warn("Acupressure registry not loaded:", e?.message || e);
    this.log("DATA", "Acupressure Registry Missing", e?.message || "unknown");
  }
}

  // Manual retry (useful on iOS / SW-cache issues)
  async retryCatalog() {
    this.showToast("🔄 Pokušavam ponovo da učitam katalog...", { timeoutMs: 3500 });
    await this.loadCatalogData(true);
    try { this.renderSystemPresets(); } catch(_) {}
    try { if (this.currentPageId === 'catalog') this.showCatalogHome(); } catch(_) {}
    try { if (this.catalogItems && this.catalogItems.length) this.showToast(`✅ Katalog učitan (${this.catalogItems.length}).`, { timeoutMs: 3500 }); } catch(_) {}
  }


_captureNavContext(pageId) {
  let y = 0;
  try {
    if (this.ui?.main && Number.isFinite(this.ui.main.scrollTop)) y = this.ui.main.scrollTop || 0;
    else if (typeof window !== 'undefined' && Number.isFinite(window.scrollY)) y = window.scrollY || 0;
  } catch (_) {}
  return {
    pageId: pageId || 'home',
    scrollTop: Number(y) || 0
  };
}

_restoreNavContext(ctx) {
  if (!ctx || typeof ctx !== 'object') return;
  const y = Math.max(0, Number(ctx.scrollTop) || 0);
  const apply = () => {
    try { if (this.ui?.main) this.ui.main.scrollTop = y; } catch (_) {}
    try { if (typeof window !== 'undefined' && typeof window.scrollTo === 'function') window.scrollTo({ top: y, behavior: 'auto' }); } catch (_) {}
  };
  try {
    requestAnimationFrame(() => {
      apply();
      setTimeout(apply, 0);
      setTimeout(apply, 120);
    });
  } catch (_) {
    apply();
  }
}

  _ensureSpaPageTools() {
    const pages = ['catalog','settings','system','studio','favorites','playlist','protocols','mysymptoms','ai','admin','workbench','sessions','help'];
    const mkBtn = (label, cls, handler) => {
      const b = document.createElement('button');
      b.type = 'button';
      b.className = cls;
      b.textContent = label;
      b.addEventListener('click', handler);
      return b;
    };
    pages.forEach((pageId) => {
      const page = document.getElementById('page-' + pageId);
      if (!page || page.querySelector('[data-sinet-spa-nav="1"]')) return;
      const bar = document.createElement('div');
      bar.setAttribute('data-sinet-spa-nav', '1');
      bar.style.cssText = 'display:flex;gap:8px;flex-wrap:wrap;align-items:center;margin:0 0 12px 0;padding:10px;border:1px solid rgba(15,118,110,.14);border-radius:12px;background:#fff;box-shadow:0 4px 14px rgba(15,23,42,.04);';
      const back = mkBtn('⬅ Nazad','btn-mini', () => this.goBack());
      const home = mkBtn('🏠 Početak','btn-mini', () => this.nav('home'));
      back.style.fontWeight = '900';
      home.style.fontWeight = '900';
      bar.appendChild(back);
      bar.appendChild(home);
      if (page.firstChild) page.insertBefore(bar, page.firstChild); else page.appendChild(bar);
    });
  }

  _ensureMenuPriorityOrder() {
    const root = document.getElementById('nav-overlay');
    if (!root) return;
    if (!document.getElementById('menu-priority-shortcuts')) {
      const anchor = document.getElementById('menu-my-shortcuts') || document.getElementById('nav-menu');
      if (anchor && anchor.parentElement) {
        const box = document.createElement('div');
        box.id = 'menu-priority-shortcuts';
        box.style.cssText = 'display:grid;grid-template-columns:1fr;gap:8px;margin:0 0 12px 0;';
        box.innerHTML = [
          '<button class="btn-full" style="margin:0;padding:13px;background:#2563eb;" onclick="nav(\'catalog\'); closeMenu();">📚 OTVORI KATALOG</button>',
          '<button class="btn-full" style="margin:0;padding:13px;background:#2ecc71;" onclick="nav(\'mysymptoms\'); closeMenu();">🧬 MOJI SIMPTOMI</button>',
          '<button class="btn-full" style="margin:0;padding:13px;background:#f1c40f;color:#2c3e50;" onclick="nav(\'favorites\'); closeMenu();">⭐ MOJI FAVORITI</button>',
          '<button class="btn-full" style="margin:0;padding:13px;background:#1abc9c;" onclick="nav(\'protocols\'); closeMenu();">📑 MOJI PROTOKOLI</button>',
          '<button class="btn-full" style="margin:0;padding:13px;background:#0f766e;" onclick="nav(\'playlist\'); closeMenu();">📋 LISTA</button>'
        ].join('');
        anchor.parentElement.insertBefore(box, anchor);
      }
    }

    const navMenu = document.getElementById('nav-menu');
    if (!navMenu || navMenu.getAttribute('data-sinet-ordered') === '1') return;
    const findLi = (patterns) => {
      const items = Array.from(navMenu.children).filter(el => el.tagName === 'LI');
      for (const li of items) {
        const txt = (li.textContent || '').toLowerCase();
        if (patterns.every(p => txt.includes(p))) return li;
      }
      return null;
    };
    const order = [
      ['početna'],
      ['katalog'],
      ['moji protokoli'],
      ['queue'],
      ['sesije']
    ];
    let insertBefore = navMenu.firstElementChild;
    order.forEach(parts => {
      const li = findLi(parts);
      if (li) navMenu.insertBefore(li, insertBefore);
      if (!insertBefore) insertBefore = li;
    });
    navMenu.setAttribute('data-sinet-ordered','1');
  }


  /* ===================== NAV ===================== */
  nav(pageId, opts = {}) {
    try {
      if (window.matchMedia && window.matchMedia('(max-width: 900px)').matches) {
        const ae = document.activeElement;
        if (ae && typeof ae.blur === 'function' && /^(INPUT|TEXTAREA|SELECT)$/i.test(ae.tagName || '')) ae.blur();
      }
    } catch (_) {}
    const prev = this.currentPageId || 'home';
    const replace = opts.replace === true;
    const stack = opts.stack !== false;
    const restoreContext = opts.restoreContext && typeof opts.restoreContext === 'object' ? opts.restoreContext : null;

    if (!replace && stack && prev && prev !== pageId) {
      this.navStack.push(this._captureNavContext(prev));
      if (this.navStack.length > 40) this.navStack.shift();
    }

    this.currentPageId = pageId;

    Object.values(this.ui.screens).forEach(s => s?.classList?.remove('active'));
    document.getElementById('page-' + pageId)?.classList?.add('active');

    const ov = document.getElementById('nav-overlay');
    if (ov) ov.style.display='none';

    if (!restoreContext) {
      try {
        if (this.ui?.main) this.ui.main.scrollTop = 0;
        window.scrollTo({ top: 0, behavior: 'auto' });
      } catch (_) {}
    }

    this.log("UI", "Nav", pageId);
    try { this._ensureSpaPageTools(); } catch(_) {}
    try { this._ensureMenuPriorityOrder(); } catch(_) {}

    // Page-specific refresh
    // HOTFIX v15.7.9.6: touch keyboard blur after nav (Huawei/Android + iPhone)
    try {
      if ((window.matchMedia && window.matchMedia('(pointer: coarse)').matches) || window.innerWidth <= 900) {
        setTimeout(() => {
          ['menu-search','search-input'].forEach(id => {
            const el = document.getElementById(id);
            if (el && document.activeElement === el) el.blur();
          });
        }, 60);
      }
    } catch(_) {}

    if (pageId === 'favorites') this.updateFavorites();
    if (pageId === 'catalog') this.showCatalogHome();
    if (pageId === 'playlist') this.renderPlaylistUI();
    if (pageId === 'protocols') this.renderProtocolsUI();
    if (pageId === 'mysymptoms') this.renderUserSymptomsUI();
    if (pageId === 'ai') this.prepareAIUI();
    if (pageId === 'workbench') this.renderWorkbenchPicker();
    if (pageId === 'settings') this.renderAuditPreview();
    if (pageId === 'sessions') this.renderSessionsUI();
    if (pageId === 'admin') this.renderAdminUI();
    if (pageId === 'system' && typeof this.renderSystemUI === 'function') this.renderSystemUI();
    if (pageId === 'studio') this.renderStudioUI();
    if (pageId === 'home') {
      this.renderHomeResumePanel();
      // Always return to ENTRY (only two big buttons) when navigating to Home from anywhere.
      // User can then choose PRVA POMOĆ or GLAVNI MENI.
      try { if (!(opts && opts.keepHomeEntryMode)) this.setHomeEntryMode(null); } catch(_) {}
      try { this.applyHomeEntryMode(); } catch(_) {}
      try { if (typeof window.scrollTo === 'function') window.scrollTo({ top: 0, behavior: 'auto' }); } catch(_) {}
    }

    if (restoreContext) this._restoreNavContext(restoreContext);
  }

  goBack() {
    const prev = this.navStack.pop();
    if (prev && typeof prev === 'object' && prev.pageId) {
      this.nav(prev.pageId, { replace: true, stack: false, restoreContext: prev });
      return;
    }
    this.nav(prev || 'home', { replace: true, stack: false });
  }

  showToast(msg, opts = {}) {
    const el = document.getElementById('toast') || this._createToast();
    if (!el) return;

    const actionLabel = (opts.actionLabel || '').toString();
    const actionNav = (opts.actionNav || '').toString();

    const action2Label = (opts.action2Label || '').toString();
    const action2Onclick = (opts.action2Onclick || '').toString();

    el.innerHTML = `
      <div class="toast-msg">${this.escapeHtml(String(msg||''))}</div>
      ${actionLabel && actionNav ? `<button class="toast-btn" onclick="nav('${actionNav}'); window.app && window.app.hideToast && window.app.hideToast()">${this.escapeHtml(actionLabel)}</button>` : ''}
      ${action2Label && action2Onclick ? `<button class="toast-btn" onclick="${action2Onclick}; window.app && window.app.hideToast && window.app.hideToast()">${this.escapeHtml(action2Label)}</button>` : ''}
      <button class="toast-x" onclick="window.app && window.app.hideToast && window.app.hideToast()" aria-label="Zatvori">✕</button>
    `;

    el.style.display = 'flex';

    if (this._toastTimer) clearTimeout(this._toastTimer);
    this._toastTimer = setTimeout(() => this.hideToast(), Number(opts.timeoutMs) || 3800);
  }

  hideToast() {
    const el = document.getElementById('toast');
    if (el) el.style.display = 'none';
    if (this._toastTimer) {
      clearTimeout(this._toastTimer);
      this._toastTimer = null;
    }
  }

  openQuickHelpModal() {
    const ov = document.getElementById("quick-help-modal");
    if (!ov) {
      this.showToast("ℹ️ Help modal nije učitan.");
      return;
    }
    ov.style.display = "flex";
  }

  closeQuickHelpModal() {
    const ov = document.getElementById("quick-help-modal");
    if (!ov) return;
    ov.style.display = "none";
  }

  _scrollPlayerDockIntoViewSafe() {
    try {
      const dock = this.ui && this.ui.playerDock;
      if (!dock) return;
      const mainEl = document.querySelector('main');
      const headerEl = document.querySelector('header');
      const quickbarEl = document.getElementById('sinet-quickbar');
      const stickyTop = (quickbarEl ? quickbarEl.offsetHeight : 0) + (headerEl ? headerEl.offsetHeight : 0) + 10;
      const dockRect = dock.getBoundingClientRect();
      if (mainEl && getComputedStyle(mainEl).overflowY !== 'visible') {
        const mainRect = mainEl.getBoundingClientRect();
        const target = mainEl.scrollTop + (dockRect.top - mainRect.top) - stickyTop;
        mainEl.scrollTo({ top: Math.max(0, target), behavior: 'smooth' });
      } else {
        const target = window.scrollY + dockRect.top - stickyTop;
        window.scrollTo({ top: Math.max(0, target), behavior: 'smooth' });
      }
    } catch (_) {}
  }

  _createToast() {
    const d = document.createElement('div');
    d.id = 'toast';
    d.className = 'toast';
    d.style.display = 'none';
    document.body.appendChild(d);
    return d;
  }


  /* ===================== PLAYLIST (DB) ===================== */
  async loadPlaylistFromDB() {
    try {
      this.playlist = await this.db?.getMainPlaylist?.() || [];
      if (!Array.isArray(this.playlist)) this.playlist = [];
      this.log("DATA", "Playlist Loaded", String(this.playlist.length));
    } catch(e) {
      this.playlist = [];
      this.log("ERROR", "Playlist Load Failed", e.message);
    }
  }

  async savePlaylistToDB() {
    try { await this.db?.saveMainPlaylist?.(this.playlist); } catch(e) {}
  }

  /* ===================== AUDIO UI ===================== */

  onFreqChange(freqObj, stats) {
    this._lastStats = stats;
    this._updateMediaSessionMetadata(freqObj, stats);

    const hz = Number(freqObj?.value ?? freqObj?.hz) || 0;
    const idx = (stats?.currentIndex ?? 0) + 1;
    const total = stats?.totalItems ?? 0;

    const isSub = hz > 0 && hz < 50;
    const carrierNote = isSub ? ` <span class="muted">(noseći 200 Hz)</span>` : "";

        const naziv = (freqObj?.naziv || "").toString().trim();
    const opis = (freqObj?.opis || "").toString().trim();
    const funkcija = (freqObj?.funkcija || freqObj?.svrha || "").toString().trim();

    const shortLabel = (funkcija || naziv || "").toString().trim();
    // Backward compatibility: some runtime lists only have svrha/desc
    const legacyLabel = (freqObj?.svrha || freqObj?.desc || "").toString().trim();
    const desc = (shortLabel || legacyLabel || "").toString().trim();

    const srcObj = (freqObj?.izvor_obj || freqObj?.izvor || freqObj?.src || null);
    const srcText = this._srcToText(srcObj);

    const recMinRaw = (freqObj?.trajanje_min ?? freqObj?.preporuceno_min ?? freqObj?.preporucenoMin ?? null);
    const recMin = (recMinRaw === null || recMinRaw === undefined || recMinRaw === "") ? null : Math.max(0, Number(recMinRaw) || 0);

    const curDurSec = (stats && Number.isFinite(Number(stats.durationCurrentSec))) ? Number(stats.durationCurrentSec) : (Number(stats?.durationPerFreq) || 0);
    const currentMin = curDurSec / 60;
    const fmtMin = (v) => {
      const n = Math.max(0, Number(v) || 0);
      if (!n) return "0";
      // show .5 if needed
      const r = Math.round(n * 2) / 2;
      return (r % 1 === 0) ? String(Math.round(r)) : String(r);
    };

    // === Player active meta (STL) ===
    if (this.ui.pActiveHz) this.ui.pActiveHz.innerText = `${hz} Hz${isSub ? " (noseći 200 Hz)" : ""}`;
    if (this.ui.pActiveName) this.ui.pActiveName.innerText = desc ? `— ${desc}` : "";
    if (this.ui.pActiveDesc) this.ui.pActiveDesc.textContent = opis ? `ℹ️ ${opis}` : "ℹ️ Opis frekvencije nije popunjen (još).";

    // Source as link if possible
    let srcHtml = "";
    if (srcObj && typeof srcObj === "object") {
      const url = (srcObj.url || srcObj.URL || srcObj.link || "").toString().trim();
      if (url) srcHtml = `📎 Izvor: <a href="${this.escapeAttr(url)}" target="_blank" rel="noopener noreferrer">${this.escapeHtml(url)}</a>`;
    }
    if (!srcHtml && srcText) {
      const looksUrl = /^https?:\/\//i.test(srcText);
      srcHtml = looksUrl
        ? `📎 Izvor: <a href="${this.escapeAttr(srcText)}" target="_blank" rel="noopener noreferrer">${this.escapeHtml(srcText)}</a>`
        : `📎 Izvor: ${this.escapeHtml(srcText)}`;
    }
    if (this.ui.pActiveSrc) this.ui.pActiveSrc.innerHTML = srcHtml || "📎 Izvor: —";

    const durLine = recMin && recMin > 0
      ? `⏱ Preporučeno: ${fmtMin(recMin)} min • Trenutno: ${fmtMin(currentMin)} min`
      : `⏱ Trenutno: ${fmtMin(currentMin)} min • Preporučeno: nije popunjeno`;
    if (this.ui.pActiveDur) this.ui.pActiveDur.textContent = durLine;

    const nextObj = (this.audio.currentSequence || [])[stats.currentIndex + 1];
    const nextHz = nextObj ? (Number(nextObj.value ?? nextObj.hz) || 0) : null;
    const nextIsSub = nextHz !== null && nextHz > 0 && nextHz < 50;

    const nextDesc = nextObj ? (nextObj?.svrha || nextObj?.funkcija || nextObj?.desc || nextObj?.naziv || "").toString().trim() : "";
    const nextLine = nextHz !== null
      ? `⏭ Sledeće: <b>${nextHz} Hz</b>${nextIsSub ? ` <span class="muted">(noseći 200 Hz)</span>` : ""}${nextDesc ? ` <span class="muted">— ${this.escapeHtml(nextDesc)}</span>` : ""}`
      : `⏭ Sledeće: kraj`;

    // Alternative = naredne 2-3 frekvencije posle sledeće
    const alt = [];
    const seq = this.audio.currentSequence || [];
    for (let j = (stats?.currentIndex ?? 0) + 2; j < Math.min(seq.length, (stats?.currentIndex ?? 0) + 5); j++) {
      const o = seq[j] || {};
      const hz2 = Number(o.value ?? o.hz) || 0;
      if (!hz2) continue;
      alt.push(`${hz2} Hz`);
    }

    // Symptom meta (uniformno za Listu / Favorite / Katalog)
    const it = this.selectedItem || null;
    const sName = (it?.simptom || "").toString().trim();
    const sDesc = (it?.opis || it?.desc || it?.description || "").toString().trim();
    const mkbCode = this.getMKB10Code(it?.mkb10);
    const mkbDesc = this.getMKB10Desc(it?.mkb10);
    const mkbUrl = this.getMKB10Url(it?.mkb10);
    const mkbSys = (it?.mkb10?.izvor?.sistem || it?.mkb10?.izvor?.system || "").toString().trim();

    const mkbMain = (mkbCode || mkbDesc)
      ? `${this.escapeHtml(mkbCode || "")}${(mkbCode && mkbDesc) ? " — " : ""}${this.escapeHtml(mkbDesc || "")}${mkbSys ? ` <span class="muted">(${this.escapeHtml(mkbSys)})</span>` : ""}`
      : `<span class="muted">nije popunjeno</span>`;

    const mkbLink = mkbUrl
      ? `<span class="line muted">🔗 Izvor MKB-10: <a href="${this.escapeAttr(mkbUrl)}" target="_blank" rel="noopener noreferrer">otvori</a></span>`
      : "";

    const descLine = `<span class="line muted">ℹ️ ${this.escapeHtml(desc || "Opis frekvencije nije unet.")}</span>`;
    const srcLine  = srcText ? `<span class="line muted">📎 ${this.escapeHtml(srcText)}</span>` : "";
    const altLine  = alt.length ? `<span class="line muted">🔁 Alternative: ${this.escapeHtml(alt.join(", "))}</span>` : "";

    const symLine  = sName ? `<span class="line">🩺 Simptom: <b>${this.escapeHtml(sName)}</b></span>` : "";
    const symDescLine = sName ? `<span class="line muted">📝 ${this.escapeHtml(sDesc || "Opis simptoma nije popunjen.")}</span>` : "";
    const mkbLine  = sName ? `<span class="line muted">🏷️ ${mkbMain}</span>` : "";

    if (this.ui.pStatus) {
      this.ui.pStatus.innerHTML =
        `<span class="line">🎵 Sada: <b>${hz} Hz</b>${carrierNote} <small>(${idx}/${total})</small></span>` +
        `${descLine}${srcLine}` +
        `<span class="line">${nextLine}</span>` +
        `${altLine}` +
        `${symLine}${symDescLine}${mkbLine}` +
        `${mkbLink}`;
    }

    // Keep Now-Playing list in sync while running
    this.renderNowPlayingList(stats);
  }


  onAudioTick(stats) {
    this._lastStats = stats;
    const seq = (this.audio && this.audio.currentSequence) ? this.audio.currentSequence : [];
    const curObj = seq && seq.length ? (seq[Number(stats?.currentIndex||0)] || {}) : {};
    this._updateMediaSessionMetadata(curObj, stats);

    const hasPer = !!stats?.hasPerFreqDurations;
    const totalTrack = hasPer ? (Number(stats.totalTrackSec)||0) : (Number(stats.totalItems)||0) * (Number(stats.durationPerFreq)||0);

    // robustno: ako elapsedInFreq ne "radi" u nekim browserima / audio režimima,
    // izračunaj ga iz ukupnog vremena
    const per = Number(stats.durationPerFreq) || 0;
    const curDur = hasPer ? (Number(stats.durationCurrentSec)||0) : per;

    const elapsedTrack = hasPer ? (Number(stats.elapsedTrackSec)||0) : (Number(stats.currentIndex)||0) * per + (Number(stats.elapsedInFreq)||0);

    let elapsedInFreq = Number(stats.elapsedInFreq);
    if (!Number.isFinite(elapsedInFreq) || elapsedInFreq < 0) elapsedInFreq = 0;
    if (!elapsedInFreq && !hasPer) {
      const approx = elapsedTrack - (Number(stats.currentIndex) || 0) * per;
      elapsedInFreq = Math.max(0, Math.min(per, approx));
    }
    if (hasPer) {
      // clamp to current duration
      elapsedInFreq = Math.max(0, Math.min(curDur, elapsedInFreq));
    }

// UI: levo = u frekvenciji, sredina = ukupno, desno = ukupno trajanje
    if (this.ui.pTimerNow) this.ui.pTimerNow.innerText = this.formatTime(elapsedInFreq);
    if (this.ui.pTimerElapsed) this.ui.pTimerElapsed.innerText = this.formatTime(elapsedTrack);
    if (this.ui.pTimerTotal) this.ui.pTimerTotal.innerText = this.formatTime(totalTrack);

    if (this.isPlaylistActive && this._protocolTimerEnabled) {
      const currentTotal = this.protocolBaseElapsedSec + elapsedTrack;
      if (this.ui.pProtocolTimer) {
        this.ui.pProtocolTimer.innerText = `UKUPNO: ${this.formatTime(currentTotal)} / ${this.formatTime(this.protocolTotalTimeSec)}`;
        this.ui.pProtocolTimer.style.display = 'block';
      }
    } else {
      if (this.ui.pProtocolTimer) this.ui.pProtocolTimer.style.display = 'none';
    }

    this.saveResumeState(false);
  }
  async onAudioComplete() {
    this.log('PLAYER', 'Complete', this.selectedItem?.id || '');

    if (!this.isPlaylistActive) {
      try { await this.db?.upsertSession?.({ id: this._sessionId || (await this.db.getPlayerState())?.sessionId, status: 'completed', updatedAt: Date.now() }); } catch(_) {}
      try {
        const sid = String(this._sessionId || (await this.db.getPlayerState())?.sessionId || '');
        if (sid) {
          const st = await this.db?.getPlayerState?.();
          const a = st?.audit || null;
          this._sessionLastEvent = 'completed';
          await this.logEx('SESSION', 'Completed', this._sessionTitle || (st?.sessionTitle||'Sesija'), { entityType:'session', entityId:sid, source:'player', meta:{ audit: a||null } });
        }
      } catch(_) {}
      this.stopPlayer(true);
      alert('Terapija završena.');
      return;
    }

    this._repeatRt = this._repeatRt || { baseProtocolSec: 0, queueCycle: 1, itemCycle: 1 };

    const scope = this.repeat?.scope || REPEAT_SCOPE_ITEM;
    const N = (this.repeat?.infinite) ? Infinity : Math.max(1, Number(this.repeat?.count) || 1);

    const idx = Number(this.currentPlaylistIndex) || 0;
    const isLastItem = (idx >= (this.playlist.length - 1));

    if (scope === REPEAT_SCOPE_ITEM) {
      const canRepeatItem = (this.repeat?.infinite) || (this._repeatRt.itemCycle < N);

      if (canRepeatItem) {
        this._repeatRt.itemCycle = (Number(this._repeatRt.itemCycle) || 1) + 1;
        try { this._renderRepeatStatus(); } catch (_) {}
        setTimeout(() => this.playPlaylistItem(idx), 250);
        return;
      }

      this._repeatRt.itemCycle = 1;
      try { this._renderRepeatStatus(); } catch (_) {}

      if (!isLastItem) {
        const nextIdx = this._nextPlaylistIndex(idx);
        setTimeout(() => this.playPlaylistItem(nextIdx), 250);
        return;
      }

      this.stopPlayer(true);
      alert('Protokol završen!');
      return;
    }

    if (scope === REPEAT_SCOPE_QUEUE) {
      if (!isLastItem) {
        const nextIdx = this._nextPlaylistIndex(idx);
        setTimeout(() => this.playPlaylistItem(nextIdx), 250);
        return;
      }

      const canRepeatQueue = (this.repeat?.infinite) || (this._repeatRt.queueCycle < N);
      if (canRepeatQueue) {
        this._repeatRt.queueCycle = (Number(this._repeatRt.queueCycle) || 1) + 1;
        try { this._renderRepeatStatus(); } catch (_) {}
        const nextIdx = this._nextPlaylistIndex(idx, true);
        setTimeout(() => this.playPlaylistItem(nextIdx), 250);
        return;
      }

      this.stopPlayer(true);
      alert('Protokol završen!');
      return;
    }

    const nextIdx = this._nextPlaylistIndex(idx);
    setTimeout(() => this.playPlaylistItem(nextIdx), 250);
  }
  _nextPlaylistIndex(currentIdx, fromQueueRestart=false) {
    const n = Array.isArray(this.playlist) ? this.playlist.length : 0;
    if (n <= 0) return 0;
    const mode = (this.playMode || PLAY_MODE_SEQ);
    if (mode === PLAY_MODE_RANDOM) {
      if (n === 1) return 0;
      // avoid repeating the same item if possible
      let r = currentIdx;
      let guard = 0;
      while (r === currentIdx && guard < 10) {
        r = Math.floor(Math.random() * n);
        guard++;
      }
      return r;
    }
    // Sequential: next in line, or wrap on queue restart
    if (fromQueueRestart) return 0;
    const next = (Number(currentIdx) || 0) + 1;
    return (next >= n) ? (n - 1) : next;
  }




  togglePlayPause() {
    // iOS PRO (Rendered WAV): use HTMLAudioElement controls
    if (this._isIOS && this.isIosBgRenderedEnabled() && this._rendered && this._rendered.active) {
      const el = this._iosRenderedEl;
      if (!el) return;
      try {
        if (!el.paused && !el.ended) {
          el.pause();
          if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = "▶";
          this.log("PLAYER", "Pause (iOS PRO Rendered)", this.selectedItem?.id || "");
          this.saveResumeState(false);
        } else {
          const p = el.play();
          if (p && typeof p.catch === "function") p.catch(() => {});
          if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = "⏸";
          this.log("PLAYER", "Play (iOS PRO Rendered)", this.selectedItem?.id || "");
        }
      } catch(_) {}
      return;
    }

    if (this.audio.isPlaying) {
      const st = this.audio.pause();
      if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = "▶";
      this._lastStats = {
        currentIndex: st.currentIndex,
        totalItems: st.totalItems,
        elapsedInFreq: st.elapsedInFreq,
        durationPerFreq: st.durationPerFreq
      };
      this.log("PLAYER", "Pause", `${this.selectedItem?.id||""} @${Math.round(st.elapsedInFreq)}s`);
      this._teardownPlaybackSession("pause");
      this.saveResumeState(false);
    } else {
      this._ensurePlaybackSession();
      this.audio.play();
      if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = "⏸";
      this.log("PLAYER", "Play", this.selectedItem?.id || "");
    }
  }


  async stopPlayer(clearResume=false) {
    this._teardownPlaybackSession("stop");
    // iOS PRO: stop rendered track if active
    try { if (this._rendered && this._rendered.active) this._stopRenderedTrack(); } catch(_) {}
    this.log("PLAYER", "Stop", this.selectedItem?.id || "");
    // Session audit log (stop event)
    try {
      const sid = String(this._sessionId || (await this.db?.getPlayerState?.())?.sessionId || '');
      if (sid) {
        const st = await this.db?.getPlayerState?.();
        const a = st?.audit || null;
        const detail = a ? `elapsed=${Math.floor(Number(a.elapsedSec||0))}s total=${Math.floor(Number(a.totalSec||0))}s remaining=${Math.floor(Number(a.remainingSec||0))}s` : (this.selectedItem?.id || '');
        this._sessionLastEvent = 'stop';
        await this.logEx('SESSION', 'Stop', detail, { entityType:'session', entityId: sid, source:'player', meta:{ audit: a||null } });
      }
    } catch(_) {}
    await this.saveResumeState(true);
    this.audio.stop();
    if (this.ui.playerDock) { this.ui.playerDock.style.display = 'none'; this._syncDockPadding(true); }
    this.isPlaylistActive = false;
    if (this.ui.pStatus) this.ui.pStatus.innerText = "";
    if (clearResume) await this.db?.clearPlayerState?.();
    
    // If we were playing only selected items, restore full list
    try {
      if (this._playlistTempActive && Array.isArray(this._playlistTempBackup)) {
        this.playlist = this._playlistTempBackup.slice();
      }
    } catch(_) {}
    this._playlistTempActive = false;
    this._playlistTempBackup = null;
this.renderPlaylistUI();
    await this.renderResumeHint();
    try { this._loadResumePref(); this._syncResumeButtonUI(); } catch(_) {}
    try { await this.renderHomeResumePanel(); } catch(_) {}
    try { await this.maybeToastResumeOnStartup(); } catch(_) {}

  }

  async saveResumeState(isStopEvent) {
    if (!this.isResumeEnabled()) return;
    if (!this.db || !this.selectedItem || !this._lastStats) return;

    // Ensure we have a session identity for multi-session resume/history.
    this._ensureSessionContext();

    const payload = {
      sessionId: this._sessionId,
      sessionTitle: this._sessionTitle,
      sessionSource: this._sessionSource,
      sessionStartedAt: this._sessionStartedAt,

      activePlaylistIndex: this.currentPlaylistIndex,
      activePresetId: this.selectedItem.id,
      freqIndex: this._lastStats.currentIndex || 0,
      elapsedInFreqSec: this._lastStats.elapsedInFreq || 0,
      isPlaylistActive: !!this.isPlaylistActive,
      updatedAt: Date.now(),
      reason: isStopEvent ? "stop" : "tick"
    };


    // Batch audit summary (total/elapsed/remaining + meta)
    let _audit = null;
    try { _audit = this._computeSessionAudit(payload); } catch(_) { _audit = null; }
    if (_audit) {
      payload.audit = _audit;
      payload.sessionGeneratedBy = _audit.generatedBy;
      payload.sessionNote = _audit.note;
    }

    // Persist "last state" (quick resume)
    try { await this.db.savePlayerState(payload); } catch(e) {}

    // Persist session log (multi resume)
    try {
      // throttle session store writes (every ~5s) unless stop event
      const now = Date.now();
      if (isStopEvent || (now - (this._sessionLastPersistAt || 0) > 5000)) {
        this._sessionLastPersistAt = now;
        await this.db.upsertSession({
          id: this._sessionId,
          title: this._sessionTitle,
          source: this._sessionSource,
          generatedBy: (this._sessionGeneratedBy || payload.sessionGeneratedBy || 'manual'),
          note: (typeof this._sessionNote === 'string' ? this._sessionNote : (payload.sessionNote || '')),
          startedAt: this._sessionStartedAt,
          updatedAt: payload.updatedAt,
          status: isStopEvent ? "interrupted" : "active",
          audit: payload.audit || null,
          // also keep state for exact resume
          state: payload
        });
      }
    } catch(_) {}
  }

  async renderResumeHint() {
    const hint = document.getElementById("resume-hint");
    if (!hint || !this.db) return;
    const st = await this.db.getPlayerState();
    if (!st || !st.activePresetId) { hint.style.display="none"; return; }
    hint.style.display="block";
    hint.innerText = `RESUME dostupno: ${st.activePresetId} • frekvencija #${(st.freqIndex||0)+1} • ${Math.floor(st.elapsedInFreqSec||0)}s`;
  }

  async resumeLastSession() {
    const st = await this.db?.getPlayerState?.();
    if (!st) return alert("Nema sačuvanog stanja.");

    // Always refresh sources
    await this.loadPlaylistFromDB();
    await this.loadProtocolsFromDB();
    await this.loadAcupressureRegistry();

    const id = (st.activePresetId || "").toString();
    // Session audit log (resume)
    try {
      const sid = String(st.sessionId || this._sessionId || '');
      if (sid) {
        this._sessionId = sid;
        this._sessionLastEvent = 'resume';
        await this.logEx('SESSION', 'Resume', String(id||''), { entityType:'session', entityId: sid, source:'player', meta:{ source: st.sessionSource||'', generatedBy: st.sessionGeneratedBy||'', audit: st.audit||null } });
      }
    } catch(_) {}


    // 1) Resume user protocol
    if (id.startsWith("PROTO_")) {
      await this.playUserProtocolById(id, {
        freqIndex: Number(st.freqIndex)||0,
        elapsedInFreqSec: Number(st.elapsedInFreqSec)||0
      });
      this.log("PLAYER", "Resume Protocol", id);
      return;
    }

    // 2) Resume playlist protocol (queue of symptoms)
    if (st.isPlaylistActive) {
      if (!this.playlist.length) return alert("Lista je prazna.");
      const plIndex = Math.max(0, Number(st.activePlaylistIndex) || 0);
      this.isPlaylistActive = true;
      this.playPlaylistItem(plIndex, {
        freqIndex: Number(st.freqIndex)||0,
        elapsedInFreqSec: Number(st.elapsedInFreqSec)||0
      });
      this.log("PLAYER", "Resume Playlist", id);
      return;
    }

    // 3) Resume single symptom play-now
    const item = this.catalogItems.find(x => x.id === id);
    if (!item) return alert("Ne mogu da pronađem terapiju za RESUME.");

    const freqs = (item.frekvencije || []).filter(f => f.enabled !== false);
    const perFreqMin = Math.max(1, Number(item.trajanjePoFrekvencijiMin) || 5);
    const totalDurSec = freqs.length * perFreqMin * 60;

        try {
      let o = originTag ? String(originTag) : '';
      if (!o) {
        const p = (this.currentPageId || '').toString();
        if (p === 'favorites') o = 'favorites';
        else if (p === 'mysymptoms') o = 'mysymptoms';
        else if (p === 'playlist') o = 'playlist';
        else o = 'catalog';
      }
      this._playOrigin = o;
    } catch(_) {}

this.selectedItem = item;
    this.isPlaylistActive = false;

    // iOS PRO (Rendered WAV): resume via rendered WAV track
    if (this._isIOS && this.isIosBgRenderedEnabled()) {
      const startIndex = Number(st.freqIndex)||0;
      const elapsedInFreq = Number(st.elapsedInFreqSec)||0;
      const perStepSec = (freqs.length ? (totalDurSec / freqs.length) : 0);
      const resumeTrackSec = Math.max(0, (startIndex * perStepSec) + elapsedInFreq);

      const r = await this._startRenderedTrack({
        sequence: freqs,
        durationsSec: null,
        totalSec: totalDurSec,
        resumeTrackSec,
        titleText: `[1/1] ${item.simptom}`,
        loopAll: false
      });
      if (r && r.fallback === false) {
        if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = "⏸";
        this.log("PLAYER", "Resume Item (iOS PRO Rendered)", id);
        return;
      }
      // else: fallback to live WebAudio
    }

    this.audio.loadSequence(freqs, totalDurSec, Number(st.freqIndex)||0, Number(st.elapsedInFreqSec)||0);

    if (this.ui.playerDock) { this.ui.playerDock.style.display = 'block'; requestAnimationFrame(() => this._syncDockPadding(false)); }
    if (this.ui.nowPlayingPanel) this.ui.nowPlayingPanel.style.display = 'block';
    const btnNowList = document.getElementById('btn-nowlist');
    if (btnNowList) btnNowList.innerHTML = '▾ FREKVENCIJE';
    if (this.ui.pTitle) this.ui.pTitle.innerText = `[1/1] ${item.simptom}`;
    if (this.ui.pProtocolTimer) this.ui.pProtocolTimer.style.display = 'none';

    this._ensurePlaybackSession();
    this.audio.play();
    if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = "⏸";
    this.log("PLAYER", "Resume Item", id);
  }

  /* ===================== Sessions / Resume UX ===================== */

  _loadResumePref() {
    try {
      const v = localStorage.getItem('sinet_resume_enabled');
      if (v === null || v === undefined || v === '') this._resumeEnabled = true;
      else this._resumeEnabled = (String(v) !== '0' && String(v).toLowerCase() !== 'false');
    } catch(_) { this._resumeEnabled = true; }
  }

  isResumeEnabled() {
    return this._resumeEnabled !== false;
  }

  toggleResumeEnabled() {
    this._resumeEnabled = !this.isResumeEnabled();
    try { localStorage.setItem('sinet_resume_enabled', this._resumeEnabled ? '1' : '0'); } catch(_) {}
    this._syncResumeButtonUI();
    try { this.db?.logAction?.('PREF', 'Resume', this._resumeEnabled ? 'ON' : 'OFF', { enabled: this._resumeEnabled }); } catch(_) {}
    this.showToast(this._resumeEnabled ? '💾 Resume uključen (pamti prekinute sesije).' : '💾 Resume isključen (ne pamti sesije).');
  }

  _syncResumeButtonUI() {
    try {
      const b = document.getElementById('p-resume');
      if (!b) return;
      b.innerText = this.isResumeEnabled() ? '💾 Resume: ON' : '💾 Resume: OFF';
      b.style.opacity = this.isResumeEnabled() ? '1' : '0.7';
    } catch(_) {}
  }

  _makeSessionId() {
    const now = Date.now();
    const rand = Math.random().toString(36).slice(2, 6);
    return 'S' + now.toString(36) + rand;
  }

  _guessSessionTitle() {
    try {
      if (this.selectedItem) {
        return (this.selectedItem.simptom || this.selectedItem.naziv || this.selectedItem.name || this.selectedItem.id || 'Sesija');
      }
    } catch(_) {}
    return 'Sesija';
  }

  _guessSessionSource() {
    // Prefer explicit origin tags if available (more accurate for audit).
    try {
      const o = (this._playOrigin || "").toString().trim();
      if (o) return o;
      if (this.selectedItem && String(this.selectedItem.id||'').startsWith('PROTO_')) return 'protocol';
      if (this.isPlaylistActive) return 'playlist';
    } catch(_) {}
    return 'unknown';
  }

  _guessSessionGeneratedBy() {
    // user | ai | upitnik | manual
    try {
      const o = (this._playOrigin || "").toString();
      if (o.includes('ai')) return 'ai';
      if (o.includes('upitnik')) return 'upitnik';

      const it = this.selectedItem || {};
      const id = String(it.id || '');
      if (id.startsWith('usr-')) return 'user';
      if (id.startsWith('PROTO_')) {
        // Try to infer from protocol meta
        const p = (this.protocols || []).find(x => x && x.id === id);
        const meta = (p && (p._meta || p.meta)) ? (p._meta || p.meta) : {};
        const cf = String(meta.createdFrom || meta.source || meta.generator || '').toLowerCase();
        if (cf.includes('ai') || cf.includes('ds') || cf.includes('generator')) return 'ai';
        if (cf.includes('upitnik')) return 'upitnik';
        return 'manual';
      }
      // catalog/default
      return 'manual';
    } catch(_) {}
    return 'manual';
  }

  _ensureSessionContext() {
    if (this._sessionId) return;
    const now = Date.now();
    this._sessionId = this._makeSessionId();
    this._sessionStartedAt = now;
    this._sessionTitle = this._guessSessionTitle();
    this._sessionSource = this._guessSessionSource();
    this._sessionLastPersistAt = 0;
    this._sessionStartedLogged = false;
    this._sessionLastEvent = '';
    this._sessionGeneratedBy = this._guessSessionGeneratedBy();
    if (typeof this._sessionNote !== 'string') this._sessionNote = '';
  }

  async maybeToastResumeOnStartup() {
    try {
      if (!this.isResumeEnabled() || !this.db) return;
      const st = await this.db.getPlayerState();
      if (!st || !st.activePresetId) return;
      this.showToast('⏸ Prekinuta sesija je pronađena. Želiš da nastaviš?', {
        actionLabel: '🕘 Sesije',
        actionNav: 'sessions',
        action2Label: '▶ Nastavi',
        action2Onclick: 'resumeLastSession()'
      });
    } catch(_) {}
  }

  _formatTs(ts) {
    try {
      const d = new Date(Number(ts||0));
      const pad=(n)=>String(n).padStart(2,'0');
      return `${pad(d.getDate())}.${pad(d.getMonth()+1)}.${d.getFullYear()} ${pad(d.getHours())}:${pad(d.getMinutes())}`;
    } catch(_) { return ''; }
  }


  _fmtDur(sec) {
    try {
      if (sec === Infinity) return '∞';
      const s = Math.max(0, Math.floor(Number(sec || 0)));
      const h = Math.floor(s / 3600);
      const m = Math.floor((s % 3600) / 60);
      const ss = s % 60;
      if (h > 0) return `${h}h ${m}m`;
      if (m > 0) return `${m}m ${ss}s`;
      return `${ss}s`;
    } catch (_) {
      return '';
    }
  }

  _labelSource(src) {
    const s = (src || '').toString();
    if (s === 'favorites') return '⭐ Favoriti';
    if (s === 'mysymptoms') return '🧬 Moji simptomi';
    if (s === 'playlist') return '📋 Lista';
    if (s === 'protocol') return '📑 Protokol';
    if (s === 'catalog') return '📚 Katalog';
    if (s === 'ai_upitnik') return '🤖 AI upitnik';
    if (!s || s === 'unknown') return '…';
    return s;
  }

  _labelGeneratedBy(gen) {
    const g = (gen || '').toString();
    if (g === 'user') return '👤 user';
    if (g === 'ai') return '🤖 AI';
    if (g === 'upitnik') return '🧾 upitnik';
    if (!g || g === 'manual') return '✍️ manual';
    return g;
  }

  _computeSessionAudit(payload) {
    try {
      const isPl = !!payload?.isPlaylistActive;
      const item = this.selectedItem || {};
      const freqs = Array.isArray(item.frekvencije) ? item.frekvencije.filter(f => f && f.enabled !== false) : [];
      const perMin = Math.max(1, Number(item.userPerFreqMin) || Number(item.trajanjePoFrekvencijiMin) || 5);
      const perStepSec = perMin * 60;

      const itemTotalSec = freqs.length * perStepSec;
      const itemElapsedSec = Math.max(0, (Number(payload.freqIndex) || 0) * perStepSec + (Number(payload.elapsedInFreqSec) || 0));
      const itemRemainingSec = Math.max(0, itemTotalSec - itemElapsedSec);

      let batchTotalSec = itemTotalSec;
      let batchElapsedSec = itemElapsedSec;
      if (isPl) {
        batchTotalSec = (typeof this.protocolTotalTimeSec !== 'undefined') ? this.protocolTotalTimeSec : itemTotalSec;
        batchElapsedSec = (Number(this.protocolBaseElapsedSec) || 0) + itemElapsedSec;
      }

      let batchRemainingSec = null;
      if (batchTotalSec === Infinity) batchRemainingSec = Infinity;
      else if (Number.isFinite(batchTotalSec)) batchRemainingSec = Math.max(0, Number(batchTotalSec) - Number(batchElapsedSec));

      const baseItemsTotal = isPl ? ((this.playlist || []).length) : 1;
      let baseFreqTotal = 0;
      if (isPl) {
        for (const it of (this.playlist || [])) {
          const f = Array.isArray(it?.frekvencije) ? it.frekvencije.filter(x => x && x.enabled !== false).length : 0;
          baseFreqTotal += f;
        }
      } else {
        baseFreqTotal = freqs.length;
      }

      const rep = this.repeat || {};
      const repInfinite = !!rep.infinite;
      const repCount = repInfinite ? Infinity : Math.max(1, Number(rep.count) || 1);
      const repScope = (rep.scope || (isPl ? REPEAT_SCOPE_ITEM : ''));

      // Planned totals (best-effort; exact repeats vary by scope, but this matches UI expectation for N)
      let plannedItems = baseItemsTotal;
      let plannedFreq = baseFreqTotal;
      if (isPl) {
        if (repCount !== Infinity) {
          plannedItems = baseItemsTotal * repCount;
          plannedFreq = baseFreqTotal * repCount;
        } else {
          plannedItems = null;
          plannedFreq = null;
        }
      }

      const gen = (this._sessionGeneratedBy || this._guessSessionGeneratedBy() || 'manual').toString();
      const src = (this._sessionSource || this._guessSessionSource() || 'unknown').toString();

      return {
        version: 1,
        source: src,
        generatedBy: gen,
        note: (typeof this._sessionNote === 'string') ? this._sessionNote : '',
        repeat: { scope: repScope, count: repCount, infinite: repInfinite },
        itemsTotal: plannedItems,
        freqTotal: plannedFreq,
        itemFreqTotal: freqs.length,
        perFreqMin: perMin,
        totalSec: batchTotalSec,
        elapsedSec: batchElapsedSec,
        remainingSec: batchRemainingSec,
        itemTotalSec,
        itemElapsedSec,
        itemRemainingSec
      };
    } catch (_) {
      return null;
    }
  }

  async setSessionNote(sessionId) {
    if (!this.db || !sessionId) return;
    try {
      const sessions = await (this.db.getSessions ? this.db.getSessions() : []);
      const s = (Array.isArray(sessions) ? sessions : []).find(x => x && x.id === sessionId) || null;
      const prev = (s && typeof s.note === 'string') ? s.note : '';
      const note = prompt('📝 Napomena za ovu sesiju (audit)', prev || '');
      if (note === null) return;
      const safe = String(note || '').slice(0, 800);
      await this.db.upsertSession({ id: sessionId, note: safe, updatedAt: Date.now() });
      try { await this.logEx('SESSION', 'Note', safe, { entityType:'session', entityId:String(sessionId||''), source:'ui' }); } catch(_) {}
      if (this._sessionId === sessionId) this._sessionNote = safe;
      this.showToast('✅ Napomena sačuvana.', { timeoutMs: 3000 });
      try { await this.renderSessionsUI(); } catch(_) {}
      try { await this.renderHomeResumePanel(); } catch(_) {}
    } catch (_) {
      this.showToast('⚠️ Ne mogu da sačuvam napomenu.', { timeoutMs: 4000 });
    }
  }

  _resolvePresetTitleById(id) {
    try {
      if (!id) return '';
      if (String(id).startsWith('PROTO_')) {
        const p = (this.protocols||[]).find(x=>x.id===id);
        return p ? (p.name||p.id) : String(id);
      }
      const c = (this.catalogItems||[]).find(x=>x.id===id);
      if (c) return (c.simptom || c.naziv || c.id);
      const u = (this.userSymptoms||[]).find(x=>x.id===id);
      if (u) return (u.simptom || u.naziv || u.id);
      return String(id);
    } catch(_) { return String(id||''); }
  }

  async renderHomeResumePanel() {
    const panel = document.getElementById('home-resume-panel');
    const body = document.getElementById('home-resume-body');
    if (!panel || !body || !this.db) return;

    if (!this.isResumeEnabled()) {
      panel.style.display = 'none';
      return;
    }

    const st = await this.db.getPlayerState();
    const sessions = await (this.db.getSessions ? this.db.getSessions() : []);
    const activeSessions = (Array.isArray(sessions) ? sessions : []).filter(s => s && (s.status === 'active' || s.status === 'interrupted'));
    activeSessions.sort((a,b)=> Number(b.updatedAt||0) - Number(a.updatedAt||0));
    const top = activeSessions[0] || null;

    if (!st || !st.activePresetId) {
      // still show recent sessions if any
      if (!top) { panel.style.display='none'; return; }
    }

    const title = this._resolvePresetTitleById((top?.state?.activePresetId) || st?.activePresetId || top?.title || '');
    const hzIndex = Number((top?.state?.freqIndex) ?? st?.freqIndex ?? 0) + 1;
    const sec = Math.floor(Number((top?.state?.elapsedInFreqSec) ?? st?.elapsedInFreqSec ?? 0) || 0);
    const updated = this._formatTs(Number((top?.updatedAt) ?? st?.updatedAt ?? 0));

    const sid = (top && top.id) ? top.id : (st && st.sessionId ? st.sessionId : '');

    const audit = (top && top.audit) ? top.audit : (st && st.audit) ? st.audit : null;
    const aSrc = this._labelSource((audit && audit.source) ? audit.source : (top?.source || st?.sessionSource || ''));
    const aGen = this._labelGeneratedBy((audit && audit.generatedBy) ? audit.generatedBy : (top?.generatedBy || ''));
    const aTot = (audit && typeof audit.totalSec !== 'undefined') ? this._fmtDur(audit.totalSec) : '';
    const aEl = (audit && typeof audit.elapsedSec !== 'undefined') ? this._fmtDur(audit.elapsedSec) : '';
    const aRem = (audit && typeof audit.remainingSec !== 'undefined') ? this._fmtDur(audit.remainingSec) : '';
    const aNote = (audit && typeof audit.note === 'string' && audit.note.trim()) ? audit.note.trim() : ((top && typeof top.note === 'string' && top.note.trim()) ? top.note.trim() : '');
    let recentHtml = '';
    try {
      const rows = await this.db.getAuditLog({ limit: 60 });
      const pick = (rows || []).filter(r => {
        const c = String(r.category||'').toUpperCase();
        return (c === 'USER' || c === 'PLAYER' || c === 'SESSION');
      }).slice(-6).reverse();
      if (pick.length) {
        recentHtml = `<div style="margin-top:14px; border-top:1px solid #eef2f7; padding-top:10px;">`
          + `<div style="font-weight:900; margin-bottom:6px;">🧭 Preporučeni sledeći korak</div>`
          + pick.map(r => {
              const t = String(r.timestamp||'').replace('T',' ').replace('Z','').slice(0,16);
              const act = String(r.action||'');
              const det = String(r.details||'');
              return `<div style="font-size:0.9rem; color:#444; margin:4px 0;">`
                + `<b>${this.escapeHtml(t)}</b> • ${this.escapeHtml(act)}${det ? ` — <span style="color:#666;">${this.escapeHtml(det).slice(0,120)}</span>` : ''}`
                + `</div>`;
            }).join('')
          + `</div>`;
      }
    } catch(_) {}


    body.innerHTML = `
      <div style="margin-bottom:10px;">
        <div style="font-weight:900; font-size:1.05rem;">⏸ Prekinuta sesija</div>
        <div style="margin-top:6px;">📌 <b>${this.escapeHtml(title || 'Sesija')}</b></div>
        <div style="margin-top:4px; color:#666;">${this.escapeHtml(aSrc)}${aGen ? ` • ${this.escapeHtml(aGen)}` : ''}</div>
        <div style="margin-top:4px; color:#666;">Frekvencija #${hzIndex} • ${sec}s • ${this.escapeHtml(updated)}${(aEl && aTot) ? ` • ${this.escapeHtml(aEl)}/${this.escapeHtml(aTot)}${aRem ? ` • ostalo ${this.escapeHtml(aRem)}` : ''}` : ''}</div>
        ${aNote ? `<div style="margin-top:6px; color:#444; background:#f8fafc; border:1px solid #eef2f7; padding:8px 10px; border-radius:10px;">📝 ${this.escapeHtml(aNote)}</div>` : ''}
      </div>
      <div style="display:flex; gap:10px; flex-wrap:wrap;">
        <button class="btn-full" style="margin:0; padding:10px 12px; background:#2ecc71;" onclick="resumeLastSession()">▶ Nastavi</button>
        <button class="btn-full" style="margin:0; padding:10px 12px; background:#2563eb;" onclick="nav('sessions')">🕘 Evidencija</button>
        ${sid ? `<button class="btn-full" style="margin:0; padding:10px 12px; background:#e74c3c;" onclick="deleteSession('${sid}')">🗑 Obriši</button>` : ''}
      </div>
      ${recentHtml}
    `;

    panel.style.display = 'block';
  }

  async renderSessionsUI() {
    const box = document.getElementById('sessions-content');
    if (!box || !this.db) return;

    // Load / init filter state
    if (!this._sessionsFilter) {
      try {
        this._sessionsFilter = JSON.parse(localStorage.getItem('sinet_sessions_filter_v1') || 'null') || {};
      } catch(_) { this._sessionsFilter = {}; }
    }
    const f = this._sessionsFilter || {};
    const q = String(f.q || '').trim().toLowerCase();
    const statusFilter = String(f.status || '').trim(); // '', 'active','interrupted','completed'
    const srcFilter = String(f.source || '').trim(); // '', 'playlist','favorites','mysymptoms','protocol','catalog','ai_upitnik'
    const genFilter = String(f.generatedBy || '').trim(); // '', 'user','ai','upitnik','manual'
    const showDone = (typeof f.showDone === 'undefined') ? true : !!f.showDone;

    const sessions = await (this.db.getSessions ? this.db.getSessions() : []);
    const listAll = Array.isArray(sessions) ? sessions.slice() : [];
    listAll.sort((a,b)=> Number(b.updatedAt||0) - Number(a.updatedAt||0));

    const getAudit = (s) => (s && s.audit) ? s.audit : (s && s.state && s.state.audit) ? s.state.audit : null;
    const getSrc = (s) => {
      const a = getAudit(s);
      return String((a && a.source) ? a.source : (s?.source || s?.state?.sessionSource || '') || '').trim();
    };
    const getGen = (s) => {
      const a = getAudit(s);
      return String((a && a.generatedBy) ? a.generatedBy : (s?.generatedBy || s?.state?.sessionGeneratedBy || '') || '').trim();
    };
    const getTitle = (s) => {
      const st = s?.state || {};
      return this._resolvePresetTitleById(st.activePresetId) || s?.title || s?.id || '';
    };
    const getNote = (s) => {
      const a = getAudit(s);
      const an = (a && typeof a.note === 'string') ? a.note.trim() : '';
      const sn = (s && typeof s.note === 'string') ? s.note.trim() : '';
      return an || sn || '';
    };

    // Filter
    let list = listAll;
    if (statusFilter) list = list.filter(s => String(s?.status||'') === statusFilter);
    if (srcFilter) list = list.filter(s => getSrc(s) === srcFilter);
    if (genFilter) list = list.filter(s => getGen(s) === genFilter);
    if (q) {
      list = list.filter(s => {
        const blob = [
          getTitle(s),
          getNote(s),
          this._labelSource(getSrc(s)),
          this._labelGeneratedBy(getGen(s)),
          String(s?.id || '')
        ].map(x => String(x||'').toLowerCase()).join(' | ');
        return blob.includes(q);
      });
    }

    const active = list.filter(s => s && (s.status === 'active' || s.status === 'interrupted'));
    const done = list.filter(s => s && (s.status === 'completed'));

    // Build filter options from all sessions (not filtered), for stable UX
    const uniq = (arr) => Array.from(new Set(arr.filter(Boolean)));
    const srcOptions = uniq(listAll.map(getSrc));
    const genOptions = uniq(listAll.map(getGen));

    const filterBar = `
      <div style="display:flex; gap:10px; flex-wrap:wrap; align-items:center; margin:10px 0 14px 0;">
        <input id="sessions-filter-q" placeholder="🔎 Pretraga (naziv / napomena)" value="${this.escapeHtml(String(f.q||''))}"
          oninput="window.app && window.app.setSessionsFilter && window.app.setSessionsFilter('q', this.value)"
          style="flex:1 1 240px; min-width:220px; padding:10px 12px; border:1px solid #e5e7eb; border-radius:12px; font-weight:700;">
        <select id="sessions-filter-status" onchange="window.app && window.app.setSessionsFilter && window.app.setSessionsFilter('status', this.value)"
          style="padding:10px 12px; border:1px solid #e5e7eb; border-radius:12px; font-weight:800;">
          <option value="" ${!statusFilter?'selected':''}>Status: sve</option>
          <option value="active" ${statusFilter==='active'?'selected':''}>▶ aktivno</option>
          <option value="interrupted" ${statusFilter==='interrupted'?'selected':''}>⏸ prekinuto</option>
          <option value="completed" ${statusFilter==='completed'?'selected':''}>✅ završeno</option>
        </select>
        <select id="sessions-filter-source" onchange="window.app && window.app.setSessionsFilter && window.app.setSessionsFilter('source', this.value)"
          style="padding:10px 12px; border:1px solid #e5e7eb; border-radius:12px; font-weight:800;">
          <option value="" ${!srcFilter?'selected':''}>Izvor: sve</option>
          ${srcOptions.map(s => `<option value="${this.escapeHtml(String(s))}" ${srcFilter===String(s)?'selected':''}>${this.escapeHtml(this._labelSource(s))}</option>`).join('')}
        </select>
        <select id="sessions-filter-gen" onchange="window.app && window.app.setSessionsFilter && window.app.setSessionsFilter('generatedBy', this.value)"
          style="padding:10px 12px; border:1px solid #e5e7eb; border-radius:12px; font-weight:800;">
          <option value="" ${!genFilter?'selected':''}>Generisao: sve</option>
          ${genOptions.map(g => `<option value="${this.escapeHtml(String(g))}" ${genFilter===String(g)?'selected':''}>${this.escapeHtml(this._labelGeneratedBy(g))}</option>`).join('')}
        </select>
        <label style="display:flex; gap:8px; align-items:center; padding:8px 10px; border:1px solid #e5e7eb; border-radius:12px; background:white; font-weight:800;">
          <input type="checkbox" ${showDone?'checked':''}
            onchange="window.app && window.app.setSessionsFilter && window.app.setSessionsFilter('showDone', this.checked)">
          Završene
        </label>
        <button onclick="window.app && window.app.resetSessionsFilter && window.app.resetSessionsFilter()"
          style="padding:10px 12px; border-radius:12px; border:1px solid #e5e7eb; background:#f8fafc; font-weight:900;">
          ♻ Reset
        </button>
      </div>
      <div style="color:#666; font-size:0.9rem; margin-bottom:6px;">
        Prikaz: <b>${list.length}</b> / ${listAll.length} • Nedovršene: <b>${active.length}</b>${showDone ? ` • Završene: <b>${done.length}</b>` : ``}
      </div>
    `;

    const row = (s) => {
      const st = s.state || {};
      const t = getTitle(s);
      const hzIndex = Number(st.freqIndex||0)+1;
      const sec = Math.floor(Number(st.elapsedInFreqSec||0) || 0);
      const upd = this._formatTs(Number(s.updatedAt||st.updatedAt||0));

      const audit = getAudit(s);
      const aSrc = this._labelSource(getSrc(s));
      const aGen = this._labelGeneratedBy(getGen(s));
      const aTot = (audit && typeof audit.totalSec !== 'undefined') ? this._fmtDur(audit.totalSec) : '';
      const aEl = (audit && typeof audit.elapsedSec !== 'undefined') ? this._fmtDur(audit.elapsedSec) : '';
      const aRem = (audit && typeof audit.remainingSec !== 'undefined') ? this._fmtDur(audit.remainingSec) : '';
      const aFreq = (audit && typeof audit.freqTotal !== 'undefined' && audit.freqTotal !== null) ? audit.freqTotal : ((audit && typeof audit.itemFreqTotal !== 'undefined') ? audit.itemFreqTotal : null);
      const aItems = (audit && typeof audit.itemsTotal !== 'undefined' && audit.itemsTotal !== null) ? audit.itemsTotal : null;
      const aNote = getNote(s);

      const status = (s && s.status) ? String(s.status) : '';
      const statusLbl = (status === 'active') ? '▶ aktivno' : (status === 'interrupted' ? '⏸ prekinuto' : (status === 'completed' ? '✅ završeno' : ''));

      const counts = [];
      if (aItems !== null && typeof aItems !== 'undefined') counts.push(`📦 ${aItems}`);
      if (aFreq !== null && typeof aFreq !== 'undefined') counts.push(`🎵 ${aFreq}`);
      const countsLine = counts.length ? (counts.join(' • ') + ' • ') : '';

      const progressLine = (aEl && aTot) ? `⏱ ${this.escapeHtml(aEl)}/${this.escapeHtml(aTot)}${aRem ? ` • ostalo ${this.escapeHtml(aRem)}` : ''}` : `frekv. #${hzIndex} • ${sec}s`;

      return `
        <div style="border:1px solid #eee; border-radius:12px; padding:12px; margin:10px 0;">
          <div style="font-weight:900;">${this.escapeHtml(t)}</div>
          <div style="color:#666; margin-top:4px;">${this.escapeHtml(aSrc)}${aGen ? ` • ${this.escapeHtml(aGen)}` : ''}${statusLbl ? ` • ${this.escapeHtml(statusLbl)}` : ''} • ${this.escapeHtml(upd)}</div>
          <div style="color:#666; margin-top:4px;">${this.escapeHtml(countsLine)}${progressLine}</div>
          ${aNote ? `<div style="margin-top:8px; color:#444; background:#f8fafc; border:1px solid #eef2f7; padding:8px 10px; border-radius:10px;">📝 ${this.escapeHtml(aNote)}</div>` : ``}
          <div style="display:flex; gap:8px; flex-wrap:wrap; margin-top:10px;">
            <button class="btn-full" style="margin:0; padding:10px 12px; background:#2ecc71;" onclick="resumeSession('${this.escapeJsString(s.id)}')">▶ Nastavi</button>
            <button class="btn-full" style="margin:0; padding:10px 12px; background:#2563eb;" onclick="openSessionAudit('${this.escapeJsString(s.id)}')">📜 Audit</button>
            <button class="btn-full" style="margin:0; padding:10px 12px; background:#64748b;" onclick="setSessionNote('${this.escapeJsString(s.id)}')">📝 Napomena</button>
            <button class="btn-full" style="margin:0; padding:10px 12px; background:#e74c3c;" onclick="deleteSession('${this.escapeJsString(s.id)}')">🗑 Obriši</button>
          </div>
        </div>
      `;
    };

    box.innerHTML = `
      ${filterBar}
      <div style="margin-top:10px;">
        <h3 style="margin:0 0 10px 0; color:var(--primary);">⏸ Nedovršene sesije (${active.length})</h3>
        ${active.length ? active.map(row).join('') : `<div style="color:#666;">Nema nedovršenih sesija.</div>`}
      </div>
      ${showDone ? `
      <div style="margin-top:18px;">
        <h3 style="margin:0 0 10px 0; color:var(--primary);">✅ Završene sesije (${done.length})</h3>
        ${done.length ? done.slice(0,40).map(s => {
          const st = s.state || {};
          const t = getTitle(s);
          const upd = this._formatTs(Number(s.updatedAt||0));
          const audit = getAudit(s);
          const aSrc = this._labelSource(getSrc(s));
          const aGen = this._labelGeneratedBy(getGen(s));
          const aTot = (audit && typeof audit.totalSec !== 'undefined') ? this._fmtDur(audit.totalSec) : '';
          const aEl = (audit && typeof audit.elapsedSec !== 'undefined') ? this._fmtDur(audit.elapsedSec) : '';
          const aRem = (audit && typeof audit.remainingSec !== 'undefined') ? this._fmtDur(audit.remainingSec) : '';
          const aFreq = (audit && typeof audit.freqTotal !== 'undefined' && audit.freqTotal !== null) ? audit.freqTotal : null;
          const aItems = (audit && typeof audit.itemsTotal !== 'undefined' && audit.itemsTotal !== null) ? audit.itemsTotal : null;
          const aNote = getNote(s);

          const counts = [];
          if (aItems !== null) counts.push(`📦 ${aItems}`);
          if (aFreq !== null) counts.push(`🎵 ${aFreq}`);
          const countsLine = counts.length ? counts.join(' • ') : '';
          const progressLine = (aEl && aTot) ? `⏱ ${this.escapeHtml(aEl)}/${this.escapeHtml(aTot)}${aRem ? ` • ostalo ${this.escapeHtml(aRem)}` : ''}` : '';

          return `<div style="border:1px solid #f3f3f3; border-radius:12px; padding:10px; margin:8px 0; color:#444;">
            <div style="font-weight:900;">${this.escapeHtml(t)}</div>
            <div style="color:#666; margin-top:4px;">${this.escapeHtml(aSrc)}${aGen ? ` • ${this.escapeHtml(aGen)}` : ''} • ${this.escapeHtml(upd)}</div>
            ${countsLine || progressLine ? `<div style="color:#666; margin-top:4px;">${this.escapeHtml(countsLine)}${countsLine && progressLine ? ' • ' : ''}${progressLine}</div>` : ``}
            ${aNote ? `<div style="margin-top:8px; color:#444; background:#f8fafc; border:1px solid #eef2f7; padding:8px 10px; border-radius:10px;">📝 ${this.escapeHtml(aNote)}</div>` : ``}
            <div style="display:flex; gap:8px; flex-wrap:wrap; margin-top:8px;">
              <button class="btn-full" style="margin:0; padding:8px 10px; background:#2563eb;" onclick="openSessionAudit('${this.escapeJsString(s.id)}')">📜 Audit</button>
              <button class="btn-full" style="margin:0; padding:8px 10px; background:#64748b;" onclick="setSessionNote('${this.escapeJsString(s.id)}')">📝 Napomena</button>
            </div>
          </div>`;
        }).join('') : `<div style="color:#666;">Još nema završenih sesija.</div>`}
      </div>
      ` : ``}
    `;
  }


  setSessionsFilter(key, value) {
    try {
      this._sessionsFilter = this._sessionsFilter || {};
      if (key === 'showDone') this._sessionsFilter.showDone = !!value;
      else this._sessionsFilter[String(key||'')] = (typeof value === 'string') ? value : value;
      localStorage.setItem('sinet_sessions_filter_v1', JSON.stringify(this._sessionsFilter));
    } catch(_) {}
    try { this.renderSessionsUI(); } catch(_) {}
  }

  resetSessionsFilter() {
    try {
      this._sessionsFilter = { q:'', status:'', source:'', generatedBy:'', showDone:true };
      localStorage.setItem('sinet_sessions_filter_v1', JSON.stringify(this._sessionsFilter));
    } catch(_) {}
    try { this.renderSessionsUI(); } catch(_) {}
  }

  async openSessionAudit(sessionId) {
    const id = String(sessionId || '');
    if (!id || !this.db) return;
    const modal = this._ensureAuditModal();
    const box = document.getElementById("audit-table");
    if (!modal || !box) return;

    const rows = await this.db.getAuditLog({ limit: 2500, includeArchive: true });
    const rel = rows.filter(r => String(r.entityId || '') === id).slice(-500).reverse();

    if (!rel.length) {
      box.innerHTML = '<div style="font-size:0.95rem; color:#667085;">(Nema audit zapisa za ovu sesiju.)</div>';
      modal.style.display = "flex";
      return;
    }

    box.innerHTML = '<div style="font-weight:900; margin-bottom:10px;">📜 Audit — sesija ' + this.escapeHtml(id) + '</div>'
      + rel.map(r => {
        const t = this.escapeHtml(String(r.timestamp || '').replace('T', ' ').replace('Z', ''));
        const sev = String(r.severity || '').toUpperCase() || 'INFO';
        const cat = this.escapeHtml(String(r.category || '').toUpperCase());
        const act = this.escapeHtml(String(r.action || ''));
        const det = this.escapeHtml(String(r.details || ''));
        const badge = (sev==='ERROR') ? 'background:#ffecec; border:1px solid #ffd2d2;'
                    : (sev==='WARN') ? 'background:#fff6e6; border:1px solid #ffe0b2;'
                    : 'background:#f6f7f9; border:1px solid #eceff3;';
        return '<div style="border:1px solid #eee; border-radius:12px; padding:10px; margin-bottom:8px; background:white;">'
          + '<div style="display:flex; gap:8px; align-items:center; flex-wrap:wrap;">'
          + '<span style="font-weight:900;">' + t + '</span>'
          + '<span style="padding:4px 8px; border-radius:999px; font-weight:900; font-size:0.8rem; ' + badge + '">' + this.escapeHtml(sev) + '</span>'
          + '<span style="font-weight:900;">' + cat + '</span>'
          + '<span>' + act + '</span>'
          + '</div>'
          + (det ? '<div style="margin-top:6px; white-space:pre-wrap; font-size:0.9rem; color:#111;">' + det + '</div>' : '')
          + '</div>';
      }).join('');

    modal.style.display = "flex";
  }
  async resumeSavedSession(sessionId) {
    if (!this.db || !sessionId) return;
    const sessions = await (this.db.getSessions ? this.db.getSessions() : []);
    const s = (Array.isArray(sessions) ? sessions : []).find(x => x && x.id === sessionId);
    if (!s || !s.state) return alert('Ne mogu da nađem sesiju.');
    try { await this.db.savePlayerState(s.state); } catch(_) {}
    // carry session id forward
    this._sessionId = s.id;
    this._sessionStartedAt = Number(s.startedAt||Date.now());
    this._sessionTitle = String(s.title||'Sesija');
    this._sessionSource = String(s.source||'unknown');
    this._sessionGeneratedBy = String(s.generatedBy || 'manual');
    this._sessionNote = String(s.note || '');
    return this.resumeLastSession();
  }

  async deleteSavedSession(sessionId) {
    if (!this.db || !sessionId) return;
    try { await this.db.deleteSession(sessionId); } catch(_) {}

    // if it was the current resume pointer, clear it
    try {
      const st = await this.db.getPlayerState();
      if (st && st.sessionId === sessionId) await this.db.clearPlayerState();
    } catch(_) {}

    try { await this.renderSessionsUI(); } catch(_) {}
    try { await this.renderHomeResumePanel(); } catch(_) {}
    try {
      await this.logEx('SESSION', 'Delete', String(sessionId||''), { entityType:'session', entityId:String(sessionId||''), source:'ui' });
    } catch(_) {}
    this.showToast('🗑 Sesija obrisana.');
  }


  /* ===================== v15.7.1.5 — Repeat helpers ===================== */

  _getPlaylistItemTotalSec(item) {
    const m = Number(item?.userTotalMin ?? 0) || 0;
    if (m > 0) return Math.max(1, m) * 60;
    const freqs = (item?.frekvencije || []).filter(f => f?.enabled !== false);
    const perFreqMin = Math.max(1, Number(item?.userPerFreqMin) || Number(item?.trajanjePoFrekvencijiMin) || 5);
    return Math.max(1, freqs.length * perFreqMin) * 60;
  }

  _computeBaseProtocolSec() {
    try {
      return (this.playlist || []).reduce((acc, it) => acc + this._getPlaylistItemTotalSec(it), 0);
    } catch (_) {
      return 0;
    }
  }

  _computeProtocolBaseElapsedSec(index) {
    const baseTotal = Number(this._repeatRt?.baseProtocolSec) || this._computeBaseProtocolSec();

    if (this.repeat?.scope === REPEAT_SCOPE_QUEUE) {
      const q = Math.max(1, Number(this._repeatRt?.queueCycle) || 1);
      let before = 0;
      for (let i = 0; i < index; i++) before += this._getPlaylistItemTotalSec(this.playlist[i]);
      return Math.max(0, (q - 1) * baseTotal + before);
    }

    // default: REPEAT_SCOPE_ITEM (A)
    let beforeItems = 0;
    for (let i = 0; i < index; i++) beforeItems += this._getPlaylistItemTotalSec(this.playlist[i]);
    const itemTotal = this._getPlaylistItemTotalSec(this.playlist[index]);
    const ic = Math.max(1, Number(this._repeatRt?.itemCycle) || 1);
    const N = (this.repeat?.infinite) ? Infinity : Math.max(1, Number(this.repeat?.count) || 1);
    const mult = Number.isFinite(N) ? N : 1;
    return Math.max(0, beforeItems * mult + itemTotal * (ic - 1));
  }  /* ===================== PROTOCOL ===================== */
  startProtocol() {
    if (!this.playlist.length) return;

    try { if (!this._playOrigin) this._playOrigin = 'playlist'; } catch(_) {}
    // Session context (multi-resume + audit)
    try { this._ensureSessionContext(); } catch(_) {}
    try {
      if (!this._sessionStartedLogged) {
        this._sessionStartedLogged = true;
        this._sessionLastEvent = 'start';
        this.logEx('SESSION', 'Start', `${this._sessionTitle || 'Sesija'} • items=${this.playlist.length}`, {
          entityType: 'session', entityId: String(this._sessionId||''), source: 'player',
          meta: { source: this._sessionSource||'', generatedBy: this._sessionGeneratedBy||'manual', playOrigin: this._playOrigin||'', repeat: this.repeat||null }
        });
      }
    } catch(_) {}


    // Repeat settings (Playlist UI)
    try {
      const s = this._readRepeatSettings('playlist');
      this._applyRepeatSettings(s);
    } catch (_) {}

    // Play mode (Queue/List)
    this._setPlayModeFromControl('pl-play-mode');

    this.isPlaylistActive = true;
    this._protocolTimerEnabled = true;
    this.currentPlaylistIndex = 0;

    // Repeat runtime
    this._repeatRt = this._repeatRt || { baseProtocolSec: 0, queueCycle: 1, itemCycle: 1 };
    this._repeatRt.queueCycle = 1;
    this._repeatRt.itemCycle = 1;
    this._repeatRt.baseProtocolSec = this._computeBaseProtocolSec();

    // Total protocol time (informational)
    if (this.repeat?.infinite) this.protocolTotalTimeSec = Infinity;
    else this.protocolTotalTimeSec = this._repeatRt.baseProtocolSec * Math.max(1, Number(this.repeat?.count) || 1);
    this.protocolBaseElapsedSec = 0;

    try { this._renderRepeatStatus(); } catch (_) {}

    // UX overlay
    try {
      this._showStartOverlay('⏳ PRIPREMAM…', 'Render/Start može potrajati (iPhone).');
      this._setStartButtonsEnabled(false);
    } catch (_) {}

    try {
      const p = this.playPlaylistItem(0);
      if (p && typeof p.catch === 'function') {
        p.catch(err => {
          console.warn('startProtocol failed', err);
          const msg = (err && (err.name || err.message)) ? `${err.name || ''} ${err.message || ''}`.trim() : 'unknown';
          this._hideStartOverlay?.();
          this._setStartButtonsEnabled?.(true);
          this.showToast(`⚠️ iPhone: start playback failed (${msg}). Pokušaj još jednom (tap ▶).`, {
            timeoutMs: 6500,
            action2Label: '▶',
            action2Onclick: 'window.app && window.app.startProtocol && window.app.startProtocol()'
          });
        });
      }
    } catch (err) {
      console.warn('startProtocol failed (sync)', err);
      const msg = (err && (err.name || err.message)) ? `${err.name || ''} ${err.message || ''}`.trim() : 'unknown';
      this._hideStartOverlay?.();
      this._setStartButtonsEnabled?.(true);
      this.showToast(`⚠️ iPhone: start playback failed (${msg}). Pokušaj još jednom (tap ▶).`, {
        timeoutMs: 6500,
        action2Label: '▶',
        action2Onclick: 'window.app && window.app.startProtocol && window.app.startProtocol()'
      });
    }

    this.log('PLAYER', 'Start Protocol', `${this.playlist.length} • repeat=${this.repeat?.scope||'item'} N=${this.repeat?.infinite?'∞':(this.repeat?.count||1)}`);
  }

  async playPlaylistItem(index, resume=null) {
    if (index >= this.playlist.length) {
      try { await this.db?.upsertSession?.({ id: this._sessionId || (await this.db.getPlayerState())?.sessionId, status: 'completed', updatedAt: Date.now() }); } catch(_) {}
      this.stopPlayer(true);
      alert('Protokol završen!');
      return;
    }

    const item = this.playlist[index];
    this.selectedItem = item;

    try {
      const r = this._getRecommendation(item);
      if (r?.segMin) this._renderSeg.segMin = Number(r.segMin) || DEFAULT_SEGMENT_MIN;
    } catch (_) {}

    try { this._renderRecommendationToPlayer(item); } catch(_) {}

    this.currentPlaylistIndex = index;

    this.protocolBaseElapsedSec = this._computeProtocolBaseElapsedSec(index);

    const freqs = (item.frekvencije || []).filter(f => f.enabled !== false);
    const perFreqMin = Math.max(1, Number(item.userPerFreqMin) || Number(item.trajanjePoFrekvencijiMin) || 5);
    const totalDurSec = freqs.length * perFreqMin * 60;

    const startIndex = resume?.freqIndex || 0;
    const elapsedInFreq = resume?.elapsedInFreqSec || 0;

    if (this._isIOS && this.isIosBgRenderedEnabled()) {
      const perStepSec = (freqs.length ? (totalDurSec / freqs.length) : 0);
      const resumeTrackSec = Math.max(0, (startIndex * perStepSec) + elapsedInFreq);
      const segMin = Number(this._renderSeg?.segMin) || DEFAULT_SEGMENT_MIN;

      const r = await this._startRenderedTrack({
        sequence: freqs,
        durationsSec: null,
        totalSec: totalDurSec,
        resumeTrackSec,
        titleText: `[${index+1}/${this.playlist.length}] ${item.simptom}`,
        loopAll: false,
        segmentMin: segMin
      });

      if (r && r.fallback === false) {
        if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = '⏸';
        this.renderPlaylistUI();
        return;
      }
    }

    this.audio.loadSequence(freqs, totalDurSec, startIndex, elapsedInFreq);

    if (this.ui.playerDock) { this.ui.playerDock.style.display = 'block'; requestAnimationFrame(() => this._syncDockPadding(false)); }
    if (this.ui.nowPlayingPanel) this.ui.nowPlayingPanel.style.display = 'block';
    const btnNowList = document.getElementById('btn-nowlist');
    if (btnNowList) btnNowList.innerHTML = '▾ FREKVENCIJE';
    if (this.ui.pTitle) this.ui.pTitle.innerText = `[${index+1}/${this.playlist.length}] ${item.simptom}`;

    this._ensurePlaybackSession();
    this.audio.play();
    if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = '⏸';

    try { this._hideStartOverlay(); this._setStartButtonsEnabled(true); } catch (_) {}

    this.renderPlaylistUI();
  }


  /* ===================== MODAL (details) ===================== */
  openModal(id) {
    this.currentModalId = id;
    const item = this.catalogItems.find(i => i.id === id);
    if (!item) return;
    if (window.SINET_FrequencySchema?.normalizeFrequencies) item.frekvencije = window.SINET_FrequencySchema.normalizeFrequencies(item.frekvencije || []);

    this._modalItem = item;

    document.getElementById('m-title').innerText = item.simptom;

    // ⭐ Favorites status (async)
    this.refreshModalFavoriteUI();

    // ===== SUMMARY (OPIS + MKB-10) — uvek prikaži (sa placeholder-ima) =====
    const descBox = document.getElementById('m-desc-box');
    const descEl = document.getElementById('m-desc');
    const opis = (item.opis || item.description || item.desc || "").toString().trim();
    if (descEl) descEl.textContent = opis || "Opis nije popunjen (još).";
    if (descBox) descBox.style.display = 'block';

    const mkbBox = document.getElementById('m-mkb-box');
    const mkbEl = document.getElementById('m-mkb');
    const mkbHtml = this.formatMKB10Html(item.mkb10, { includeLink: true, showEmpty: true });
    if (mkbEl) mkbEl.innerHTML = mkbHtml;
    if (mkbBox) mkbBox.style.display = 'block';
    if (descBox) {
      let metaHost = document.getElementById('m-meta-ribbon');
      if (!metaHost) {
        metaHost = document.createElement('div');
        metaHost.id = 'm-meta-ribbon';
        descBox.appendChild(metaHost);
      }
      metaHost.innerHTML = window.SINET_ItemShell?.renderMetaRibbon ? window.SINET_ItemShell.renderMetaRibbon(item, { mkbText: this._mkbToText(item.mkb10 || '') }) : '';
    }

// init duration slider
    const slider = document.getElementById('m-slider');
    const perMinInit = Math.max(1, Number(item.trajanjePoFrekvencijiMin) || 5);
    if (slider) slider.value = String(perMinInit);
    const durLbl = document.getElementById('m-dur-lbl');
    if (durLbl) durLbl.innerText = `${perMinInit} min`;

    // v15.7.1.5 — Preporuka block + default loop controls
    try { this._renderRecommendationToModal(item); } catch(_) {}
    try {
      const r = this._getRecommendation(item);
      const sel = document.getElementById("m-repeat-count");
      if (sel) sel.value = String(Math.max(1, r.loopN || 1));
      const cb = document.getElementById("m-repeat-infinite");
      if (cb) cb.checked = false;

      // scope default (A) — item
      const ra = document.querySelector('input[name="m-repeat-scope"][value="item"]');
      if (ra) ra.checked = true;
    } catch(_) {}

    // show frequencies list with canonical schema
    const freqsAll = window.SINET_FrequencySchema?.normalizeFrequencies ? window.SINET_FrequencySchema.normalizeFrequencies(item.frekvencije || []) : (item.frekvencije || []);
    item.frekvencije = freqsAll;
    const freqs = freqsAll.filter(f=>f.enabled!==false);
    const perMin = Math.max(1, Number(item.trajanjePoFrekvencijiMin)||5);
    const freqBox = document.getElementById('m-freqs');
    if (freqBox) {
      const totalMin = freqs.length * perMin;
      if (window.SINET_ItemShell?.renderFrequencyCollection) {
        freqBox.innerHTML = window.SINET_ItemShell.renderFrequencyCollection(freqsAll, {
          title: `Frekvencije (${freqs.length} aktivnih / ${freqsAll.length} ukupno) - Procena: ${totalMin} min`,
          perMin,
          rowPrefix: (f, i) => `<input class="freq-toggle" type="checkbox" ${f.enabled === false ? "" : "checked"} onchange="window.app.setModalFreqEnabled(${i}, this.checked)" style="margin-right:10px;">`,
          emptyText: 'Nema frekvencija za prikaz.'
        });
      } else {
        freqBox.innerHTML = `<div style="font-weight:900; margin-top:6px;">Frekvencije (${freqs.length}) - Procena: ${totalMin} min</div>`;
      }
    }

// holistic (supports legacy + STL-normalized)
const h = item.holisticki || item.holistika || {};
let html = "";

const addBox = (label, color, body, source=null) => {
  if (!body) return;
  html += `<div class="holistic-box" style="border-left:4px solid ${color}">
    <small style="color:${color}; font-weight:bold;">${label}</small><br>
    ${body}
    ${source ? `<div style="margin-top:6px; font-size:0.75rem; color:#666;">Izvor: ${source}</div>` : ""}
  </div>`;
};

addBox("PSIHOSOMATIKA – UZROK", "#8e44ad", h.psihosomatika?.uzrok || "");
addBox("PSIHOSOMATIKA – LEK", "#8e44ad", h.psihosomatika?.lek || "");

addBox("AFIRMACIJA", "#16a085",
  h.afirmacija?.tekst ? `${h.afirmacija.tekst}${h.afirmacija.autor ? `<div style="margin-top:6px;"><b>Autor:</b> ${h.afirmacija.autor}</div>` : ""}` : "",
  h.afirmacija?.izvor || ""
);

// molitva / duhovnost
addBox("MOLITVA", "#f39c12", h.molitva?.tekst || h.duhovnost?.tekst || "", h.molitva?.izvor || h.duhovnost?.izvor || "");

// narodni lek
addBox("NARODNI LEK", "#27ae60", h.narodni_lek?.opis || h.saveti?.narodno || "");

document.getElementById('m-holistic').innerHTML = html;

// akupresura
this.renderAcupressureInModal(item);

    // v15.3 — advanced JSON / local override
    this.renderModalAdvanced(item);

    this.updateModalDuration();

    document.getElementById('modal').style.display = 'flex';
    this.log("UI", "Open Modal", id);
  }

updateModalDuration() {
  const slider = document.getElementById('m-slider');
  const perMin = Math.max(1, Number(slider?.value || "5"));
  const lbl = document.getElementById('m-dur-lbl');
  if (lbl) lbl.innerText = `${perMin} min`;

  if (this._modalItem) this._modalItem.freq_duration_min = perMin;

  const item = this._modalItem || this.catalogItems.find(i => i.id === this.currentModalId);
  if (!item) return;

  const freqsAll = window.SINET_FrequencySchema?.normalizeFrequencies ? window.SINET_FrequencySchema.normalizeFrequencies(item.frekvencije || []) : (item.frekvencije || []);
  item.frekvencije = freqsAll;
  const freqs = freqsAll.filter(f => f.enabled !== false);
  const freqBox = document.getElementById('m-freqs');
  if (!freqBox) return;

  const totalMin = freqs.length * perMin;
  if (window.SINET_ItemShell?.renderFrequencyCollection) {
    freqBox.innerHTML = window.SINET_ItemShell.renderFrequencyCollection(freqsAll, {
      title: `Frekvencije (${freqs.length} aktivnih / ${freqsAll.length} ukupno) - Procena: ${totalMin} min`,
      perMin,
      rowPrefix: (f, i) => `<input class="freq-toggle" type="checkbox" ${f.enabled === false ? "" : "checked"} onchange="window.app.setModalFreqEnabled(${i}, this.checked)" style="margin-right:10px;">`,
      emptyText: 'Nema frekvencija za prikaz.'
    });
  }

  const proto = document.getElementById('m-proto');
  if (proto) {
    const enabledCount = freqs.length;
    const loops = Math.max(1, Number(item.freq_loops) || 1);
    const estMin = enabledCount * perMin;
    proto.textContent = `${enabledCount} frekv. - ${perMin} min x ${loops} = ~${estMin * loops} min`;
  }
}

_extractAcupPoints(item){
  const a = item?.akupresura;
  if (!a) return [];
  if (Array.isArray(a)) return a;
  if (Array.isArray(a.points)) return a.points;
  if (Array.isArray(a.tacke)) return a.tacke;
  return [];
}

renderAcupressureInModal(item){
  const box = document.getElementById('m-acupressure');
  if (!box) return;

  const pts = this._extractAcupPoints(item);
  if (!pts.length){
    box.innerHTML = `<div class="muted" style="color:#777; font-style:italic;">Nema akupresure za ovaj simptom (još). 🙂</div>`;
    return;
  }

  const regPoints = this.acupressureRegistry?.points || {};

  const cards = pts.map((p, i) => {
    const code = (p.tacka || p.tačka || p.code || "").toString().trim();
    const name = (p.naziv || p.name || "").toString().trim();
    const loc  = (p.lokacija || "").toString().trim();
    const how  = (p.uputstvo || p.uputstva || "").toString().trim();
    const src  = (p.izvor?.url || p.izvor || "").toString().trim();

    const reg = code ? regPoints[code] : null;
    const img = (p.slika || reg?.default || (Array.isArray(reg?.files) && reg.files[0]) || "").toString().trim();
    const hasImg = !!img;

    return `
      <div class="acup-card">
        <div class="acup-head">
          <div class="acup-code">${code || "TAČKA"}</div>
          <div class="acup-name">${name || ""}</div>
        </div>
        ${loc ? `<div class="acup-loc">📍 ${loc}</div>` : ""}
        ${how ? `<div class="acup-how">👉 ${how}</div>` : ""}
        ${src ? `<div class="muted" style="margin-top:6px; font-size:0.8rem; color:#666;">Izvor: ${src}</div>` : ""}
        <div class="acup-actions">
          <button class="acup-btn" onclick="openAcupressureViewer('${item.id}', ${i})">${hasImg ? "👁 Prikaži" : "👁 Prikaži (bez slike)"}</button>
        </div>
      </div>`;
  }).join("");

  box.innerHTML = `<div class="acup-grid">${cards}</div>`;
}

openAcupressureViewer(itemId, idx){
  const modal = document.getElementById('acupressure-modal');
  if (!modal) return;

  const item = this.catalogItems.find(i => i.id === itemId);
  if (!item) return;

  const pts = this._extractAcupPoints(item);
  const p = pts[Number(idx)] || null;
  if (!p) return;

  const code = (p.tacka || p.tačka || p.code || "").toString().trim();
  const name = (p.naziv || p.name || "").toString().trim();
  const loc  = (p.lokacija || "").toString().trim();
  const how  = (p.uputstvo || p.uputstva || "").toString().trim();
  const src  = (p.izvor?.url || p.izvor || "").toString().trim();

  // Title
  const t = document.getElementById('acup-title');
  if (t) t.innerText = `${code || "Akupresura"}${name ? " — " + name : ""}`;

  // Image resolve: point.slika > registry.points[code].default > first file
  const reg = (code && this.acupressureRegistry?.points) ? this.acupressureRegistry.points[code] : null;
  const imgPath = (p.slika || reg?.default || (Array.isArray(reg?.files) && reg.files[0]) || "").toString().trim();

  const img = document.getElementById('acup-img');
  const noimg = document.getElementById('acup-noimg');

  if (imgPath) {
    if (img) { img.src = imgPath; img.style.display = "block"; }
    if (noimg) noimg.style.display = "none";
  } else {
    if (img) { img.removeAttribute("src"); img.style.display = "none"; }
    if (noimg) noimg.style.display = "block";
  }

  // Marker (0..1 coords)
  const marker = document.getElementById('acup-marker');
  const mx = Number(p.marker?.x);
  const my = Number(p.marker?.y);
  if (marker && Number.isFinite(mx) && Number.isFinite(my)) {
    marker.style.display = "block";
    marker.style.left = `${Math.max(0, Math.min(1, mx)) * 100}%`;
    marker.style.top  = `${Math.max(0, Math.min(1, my)) * 100}%`;
  } else if (marker) {
    marker.style.display = "none";
  }

  // Text
  const elLoc = document.getElementById('acup-loc');
  const elHow = document.getElementById('acup-how');
  const elSrc = document.getElementById('acup-src');

  if (elLoc) elLoc.innerText = loc ? `📍 ${loc}` : "";
  if (elHow) elHow.innerText = how || "";
  if (elSrc) elSrc.innerText = src ? `Izvor: ${src}` : "";

  modal.style.display = "flex";
  this.log("UI", "Acupressure Viewer Open", `${itemId}:${idx}`);
}

closeAcupressureViewer(){
  const modal = document.getElementById('acupressure-modal');
  if (modal) modal.style.display = "none";
}


  closeModal() {
    document.getElementById('modal').style.display = 'none';
    this.log("UI", "Close Modal", this.currentModalId||"");
  }
  playFromModal() {
    const item = this.catalogItems.find(i => i.id === this.currentModalId);
    if (!item) return;

    try {
      const s = this._readRepeatSettings('modal');
      if (s && s.scope === REPEAT_SCOPE_QUEUE) s.scope = REPEAT_SCOPE_ITEM;
      this._applyRepeatSettings(s);
    } catch (_) {}

    const perFreqMin = Number(document.getElementById('m-slider')?.value || '5');
    const freqsAll = (item.frekvencije || []);
    const freqs = freqsAll.filter(f => f.enabled !== false);
    const totalMin = Math.max(1, freqs.length * perFreqMin);

    this.isPlaylistActive = true;
    this._protocolTimerEnabled = false;
    this.currentPlaylistIndex = 0;

    this.playlist = [{
      ...item,
      uid: Date.now(),
      userPerFreqMin: perFreqMin,
      userTotalMin: totalMin
    }];
    this.savePlaylistToDB();

    this._repeatRt = this._repeatRt || { baseProtocolSec: 0, queueCycle: 1, itemCycle: 1 };
    this._repeatRt.queueCycle = 1;
    this._repeatRt.itemCycle = 1;
    this._repeatRt.baseProtocolSec = this._computeBaseProtocolSec();

    if (this.repeat?.infinite) this.protocolTotalTimeSec = Infinity;
    else this.protocolTotalTimeSec = this._repeatRt.baseProtocolSec * Math.max(1, Number(this.repeat?.count) || 1);
    this.protocolBaseElapsedSec = 0;

    try { this._renderRepeatStatus(); } catch (_) {}

    try {
      this._showStartOverlay('⏳ PRIPREMAM…', 'Render/Start može potrajati (iPhone).');
      this._setStartButtonsEnabled(false);
    } catch (_) {}

    try {
      const p = this.playPlaylistItem(0);
      if (p && typeof p.catch === 'function') {
        p.catch(err => {
          console.error('playFromModal error', err);
          const msg = (err && (err.name || err.message)) ? `${err.name || ''} ${err.message || ''}`.trim() : 'unknown';
          this._hideStartOverlay?.();
          this._setStartButtonsEnabled?.(true);
          this.showToast(`⚠️ iPhone: start playback failed (${msg}). Pokušaj još jednom (tap ▶).`, {
            timeoutMs: 6500,
            action2Label: '▶',
            action2Onclick: 'window.app && window.app.playPlaylistItem && window.app.playPlaylistItem(0)'
          });
        });
      }
    } catch (err) {
      console.error('playFromModal error (sync)', err);
      const msg = (err && (err.name || err.message)) ? `${err.name || ''} ${err.message || ''}`.trim() : 'unknown';
      this._hideStartOverlay?.();
      this._setStartButtonsEnabled?.(true);
      this.showToast(`⚠️ iPhone: start playback failed (${msg}). Pokušaj još jednom (tap ▶).`, {
        timeoutMs: 6500,
        action2Label: '▶',
        action2Onclick: 'window.app && window.app.playPlaylistItem && window.app.playPlaylistItem(0)'
      });
    }

    if (this.ui.pProtocolTimer) this.ui.pProtocolTimer.style.display = 'none';

    this.closeModal();
    this.nav('playlist');

    try { requestAnimationFrame(() => this._scrollPlayerDockIntoViewSafe()); } catch (_) {}
  }

  async addToPlaylistFromModal() {
    const item = this.catalogItems.find(i => i.id === this.currentModalId);
    if (!item) return;

    const perFreqMin = Number(document.getElementById('m-slider')?.value || "5");
    const freqsAll = (item.frekvencije || []);
    const freqs = freqsAll.filter(f=>f.enabled!==false);
    const totalMin = Math.max(1, freqs.length * perFreqMin);

    this.playlist.push({
      ...item,
      uid: Date.now(),
      userPerFreqMin: perFreqMin,
      userTotalMin: totalMin
    });

    await this.savePlaylistToDB();
    this.renderPlaylistUI();
    this.closeModal();
    this.log("USER", "Add To Playlist", item.id);
    this.showToast('✅ Dodato u listu.', {
      actionLabel: '🎵 Otvori listu',
      actionNav: 'playlist',
      action2Label: '▶ Pusti odmah',
      action2Onclick: `window.app && window.app.playItemNow && window.app.playItemNow('${item.id}')`
    });
  }

  addToMySymptomsFromModal() {
    if (!this.currentModalId) return;
    this.quickAddToMySymptoms(this.currentModalId);
    try { this.log("USER","Add To MySymptoms From Modal", this.currentModalId); } catch(_) {}
  }

  createProtocolFromModal() {
    if (!this.currentModalId) return;
    this.quickCreateProtocolFromItem(this.currentModalId);
    try { this.log("USER","Create Protocol From Modal", this.currentModalId); } catch(_) {}
  }


  async quickAddToMySymptoms(id) {
    const item = (this.catalogItems||[]).find(i => i.id === id);
    if (!item) return;
    // If already user item, just open it
    if (String(item.id||"").startsWith("usr-")) {
      this.nav("mysymptoms");
      this.showUserSymptomForm(item.id);
      return;
    }
    const existing = (this.userSymptoms||[]).find(x => x && x.sourceId === id);
    if (existing) {
      this.showToast("ℹ️ Već postoji u Mojim simptomima.", { actionLabel:"🧬 Otvori", actionNav:"mysymptoms" });
      return;
    }
    const copy = this._clone(item);
    copy.sourceId = id;
    copy.id = "usr-" + Date.now();
    copy.oblast = "MOJI SIMPTOMI";
    copy.simptom = (copy.simptom || "Stavka").toString();
    copy._createdAt = Date.now();
    (this.userSymptoms||[]).push(copy);
    await this.saveUserSymptoms();
    this.applyUserDataToCatalog();
    try { this.renderUserSymptomsUI(); } catch(_) {}
    this.showToast("✅ Dodato u Moje simptome.", {
      actionLabel: "🧬 Otvori",
      actionNav: "mysymptoms",
      action2Label: "✏️ Izmeni",
      action2Onclick: `window.app && window.app.editUserSymptom && window.app.editUserSymptom('${copy.id}')`
    });
  }

  async quickCreateProtocolFromItem(id) {
    const item = (this.catalogItems||[]).find(i => i.id === id);
    if (!item || !this.db?.putProtocol) return;
    const perMin = Math.max(1, Number(item.trajanjePoFrekvencijiMin)||5);
    const freqs = (item.frekvencije||[]).filter(f=>f.enabled!==false);
    const steps = freqs.map(f => {
      const hz = Number(f.hz || f.freq || f.value || 0);
      return { minutes: perMin, freq: { hz, name: f.name||f.naziv||"", desc: f.desc||f.opis||"", source: f.source||"" } };
    }).filter(s=>s.freq && s.freq.hz);
    const proto = {
      id: this._newProtoId(),
      name: `Protokol: ${(item.simptom||"Stavka").toString().slice(0,60)}`,
      loopCount: 1,
      steps,
      _meta: { createdFrom: "catalogItem", symptomOpis: (item.opis||"").toString().slice(0,300), mkb10: this._mkbToText(item.mkb10||"") }
    };
    await this.db.putProtocol(proto);
    this.protocols = await this.db.getProtocols();
    this.showToast("✅ Protokol sačuvan.", { actionLabel:"📑 Otvori protokole", actionNav:"protocols" });
  }


  async quickAddToPlaylist(id) {
    const item = this.catalogItems.find(i => i.id === id);
    if (!item) return;

    this.playlist.push(this._makePlaylistEntry(item));
    await this.savePlaylistToDB();
    this.renderPlaylistUI();
    this.log("USER", "Quick Add Playlist", item.id);

    this.showToast('✅ Dodato u listu.', {
      actionLabel: '🎵 Otvori listu',
      actionNav: 'playlist',
      action2Label: '▶ Pusti odmah',
      action2Onclick: `window.app && window.app.playItemNow && window.app.playItemNow('${id}')`
    });
  }

  // v15.4.4 — Play selected symptom immediately (without destroying user's playlist)
  async playItemNow(id, originTag = null) {
    const item = this.catalogItems.find(i => i.id === id);
    if (!item) return;

    const freqs = (item.frekvencije || []).filter(f => f.enabled !== false);
    const perFreqMin = Math.max(1, Number(item.trajanjePoFrekvencijiMin) || 5);
    const totalDurSec = freqs.length * perFreqMin * 60;

    this.selectedItem = item;
    this.isPlaylistActive = false;

    // iOS PRO (Rendered WAV): render protocol to WAV-in-RAM and play via <audio> (better chance to continue in background)
    if (this._isIOS && this.isIosBgRenderedEnabled()) {
      const r = await this._startRenderedTrack({
        sequence: freqs,
        durationsSec: null,
        totalSec: totalDurSec,
        resumeTrackSec: 0,
        titleText: `[1/1] ${item.simptom}`,
        loopAll: false
      });
      if (r && r.fallback === false) {
        if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = "⏸";
        this.log("PLAYER", "Play Now (iOS PRO Rendered)", item.id);
        return;
      }
      // else: fallback to live WebAudio
    }

    this.audio.loadSequence(freqs, totalDurSec, 0, 0);

    if (this.ui.playerDock) { this.ui.playerDock.style.display = 'block'; requestAnimationFrame(() => this._syncDockPadding(false)); }
    if (this.ui.nowPlayingPanel) this.ui.nowPlayingPanel.style.display = 'block';
    const btnNowList = document.getElementById('btn-nowlist');
    if (btnNowList) btnNowList.innerHTML = '▾ FREKVENCIJE';
    if (this.ui.pTitle) this.ui.pTitle.innerText = `[1/1] ${item.simptom}`;
    if (this.ui.pProtocolTimer) this.ui.pProtocolTimer.style.display = 'none';

    this._ensurePlaybackSession();
    this.audio.play();
    if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = "⏸";
    this.log("PLAYER", "Play Now", item.id);
  }

  applyAudioPrefs() {
    try {
      const p = this.audioPrefs || {};
      if (p.wave) this.audio.setOscType(p.wave);
      if (typeof p.master !== "undefined") this.audio.setMasterVolume(Number(p.master) || 0.55);
      this.audio.setBoostEnabled(!!p.boost);
    } catch(_) {}
  }

  saveAudioPrefs() {
    try { localStorage.setItem("sinet_audio_prefs_v1", JSON.stringify(this.audioPrefs||{})); } catch(_) {}
  }

  setMasterVolumeUI(val01) {
    const v = Math.min(1, Math.max(0, Number(val01)));
    this.audioPrefs.master = v;
    try { this.audio.setMasterVolume(v); } catch(_) {}
    this.saveAudioPrefs();
  }

  toggleAudioBoost() {
    const on = !this.audioPrefs.boost;
    this.audioPrefs.boost = on;
    try { this.audio.setBoostEnabled(on); } catch(_) {}
    this.saveAudioPrefs();
    try {
      const btn = document.getElementById("p-boost");
      if (btn) btn.innerText = `⚡ Boost: ${on ? "ON" : "OFF"}`;
    } catch(_) {}
  }

  setWaveform(type) {
    this.audioPrefs.wave = (type||"sine").toString();
    try { this.audio.setOscType(this.audioPrefs.wave); } catch(_) {}
    this.saveAudioPrefs();
  }

  _makePlaylistEntry(item, uidSeed = null) {
    if (!item) return null;
    const normalized = this._clone(item);
    const freqsAll = (normalized.frekvencije || []).filter(f => f && f.enabled !== false);
    const perFreqMin = Math.max(1, Number(normalized.userPerFreqMin) || Number(normalized.trajanjePoFrekvencijiMin) || 5);
    const totalMin = Math.max(1, Number(normalized.userTotalMin) || (freqsAll.length * perFreqMin) || perFreqMin);
    normalized.uid = uidSeed || normalized.uid || (Date.now() + Math.floor(Math.random() * 1000));
    normalized.userPerFreqMin = perFreqMin;
    normalized.userTotalMin = totalMin;
    return normalized;
  }

  _setPlayModeFromControl(controlId) {
    try {
      const pm = (document.getElementById(controlId)?.value || PLAY_MODE_SEQ).toString();
      this.playMode = (pm === PLAY_MODE_RANDOM) ? PLAY_MODE_RANDOM : PLAY_MODE_SEQ;
      try { localStorage.setItem('SINET_PLAYMODE', this.playMode); } catch(_) {}
    } catch(_) {
      this.playMode = PLAY_MODE_SEQ;
    }
  }

  _setControlValueIfExists(id, value) {
    const el = document.getElementById(id);
    if (el) el.value = value;
  }

  _startTemporaryPlaylist(items, controlId = 'pl-play-mode', originTag = null) {
    const picked = (Array.isArray(items) ? items : []).filter(Boolean).map((it, idx) => this._makePlaylistEntry(it, `${Date.now()}_${idx}`)).filter(Boolean);
    if (!picked.length) return false;
    try { if (originTag) this._playOrigin = String(originTag); } catch(_) {}
    this._playlistTempBackup = Array.isArray(this.playlist) ? this.playlist.slice() : [];
    this._playlistTempActive = true;
    this.playlist = picked;
    this.currentPlaylistIndex = 0;
    this.isPlaylistActive = true;
    this._setPlayModeFromControl(controlId);
    this.startProtocol();
    return true;
  }

  _createProtocolFromItems(items, prefix = 'Izabrano') {
    const picked = (Array.isArray(items) ? items : []).filter(Boolean).map(it => this._makePlaylistEntry(it)).filter(Boolean);
    if (!picked.length) return false;
    const steps = [];
    for (const it of picked) steps.push(...this._expandSymptomToSteps(it, Number(it.userPerFreqMin)||null, `🎵 ${it.simptom}`));
    this._protoEdit = { id: this._newProtoId(), name: `${prefix} • ${new Date().toISOString().slice(0,10)}`, loopCount: 1, steps };
    this.nav('protocols');
    this.renderProtocolsUI();
    return true;
  }

  renderPlaylistUI() {
    const container = document.getElementById('playlist-container');
    if (!container) return;

    if (!this.playlist.length) {
      container.innerHTML = "<p style='text-align:center; padding:20px; color:#999'>Lista je prazna.</p>";
      const a = document.getElementById('pl-actions');
      if (a) a.style.display='none';
      const b = document.getElementById('badge-pl');
      if (b) b.style.display='none';
      return;
    }

    const a = document.getElementById('pl-actions');
    if (a) a.style.display='block';
    const b = document.getElementById('badge-pl');
    if (b) { b.innerText = this.playlist.length; b.style.display='inline-block'; }

    const totalMin = this.playlist.reduce((acc, it)=> acc + (Number(it.userTotalMin)||0), 0);
    const td = document.getElementById('pl-total-duration');
    if (td) td.innerText = `${totalMin} min`;

    container.innerHTML = this.playlist.map((item, index) => {
      const active = (this.isPlaylistActive && this.currentPlaylistIndex===index) ? "active" : "";
      const mkbCode = this.getMKB10Code(item.mkb10);
      const mkbDesc = this.getMKB10Desc(item.mkb10);
      const mkbBadge = mkbCode ? `<span class="mkb-badge" style="background:#eef3ff; font-size:0.75rem; padding:2px 6px; border-radius:7px; margin-left:6px;">${this.escapeHtml(mkbCode)}</span>` : "";
      const mkbLine = (mkbCode || mkbDesc)
        ? `<small style="color:#777;">🏷️ ${this.escapeHtml(mkbCode || "")}${(mkbCode && mkbDesc) ? " — " : ""}${this.escapeHtml((mkbDesc || "").slice(0,80))}</small>`
        : `<small style="color:#999;">🏷️ MKB-10: nije popunjeno</small>`;

      const checked = (this.playlistSelected && this.playlistSelected.has(String(item.uid))) ? "checked" : "";

      return `
        <div class="playlist-item ${active}">
          <div style="margin-right:10px; display:flex; align-items:center;">
            <input type="checkbox" class="pl-sel" ${checked}
              onclick="event.stopPropagation(); window.app && window.app.togglePlaylistSelect && window.app.togglePlaylistSelect('${item.uid}', this.checked)">
          </div>
          <div style="flex:1">
            <strong style="display:block;">${index+1}. ${this.escapeHtml(item.simptom)} ${mkbBadge}</strong>
            <small style="color:#777;">${item.userPerFreqMin||5} min/freq • ukupno: ${item.userTotalMin||0} min</small>
            ${mkbLine}
          </div>
          <button onclick="app.removeFromPlaylist('${item.uid}')" style="background:none; border:none; color:#C0392B; font-weight:bold; font-size:1.2rem;">✕</button>
        </div>
      `;
    }).join("");

    try { this._renderPlaylistSelectionHint(); } catch(_) {}
  }
  /* ===================== PLAYLIST SELECTION (Play Selected) ===================== */

  _ensurePlaylistSelection() {
    if (!this.playlistSelected || !(this.playlistSelected instanceof Set)) this.playlistSelected = new Set();
  }

  togglePlaylistSelect(uid, checked) {
    this._ensurePlaylistSelection();
    const key = String(uid || "");
    if (!key) return;
    if (checked) this.playlistSelected.add(key);
    else this.playlistSelected.delete(key);
    try { this._renderPlaylistSelectionHint?.(); } catch(_) {}
  }

  selectAllPlaylist() {
    this._ensurePlaylistSelection();
    for (const it of (this.playlist || [])) this.playlistSelected.add(String(it.uid));
    this.renderPlaylistUI();
    try { this._renderPlaylistSelectionHint?.(); } catch(_) {}
  }

  clearPlaylistSelection() {
    this._ensurePlaylistSelection();
    this.playlistSelected.clear();
    this.renderPlaylistUI();
    try { this._renderPlaylistSelectionHint?.(); } catch(_) {}
  }

  _getSelectedPlaylistItems() {
    this._ensurePlaylistSelection();
    const sel = this.playlistSelected;
    const items = Array.isArray(this.playlist) ? this.playlist : [];
    const picked = items.filter(it => sel.has(String(it.uid)));
    return picked;
  }

  _renderPlaylistSelectionHint() {
    const el = document.getElementById('pl-selected-count');
    if (!el) return;
    const n = this.playlistSelected ? this.playlistSelected.size : 0;
    el.innerText = n ? `Izabrano: ${n}` : '';
  }

  startProtocolSelected() {
    const picked = this._getSelectedPlaylistItems();
    if (!picked.length) {
      this.showToast("ℹ️ Nisi izabrao nijednu stavku u listi.", { timeoutMs: 4500 });
      return;
    }
    this._startTemporaryPlaylist(picked, 'pl-play-mode', 'playlist');
  }

  createProtocolFromSelected() {
    const picked = this._getSelectedPlaylistItems();
    if (!picked.length) {
      this.showToast("ℹ️ Nisi izabrao nijednu stavku u listi.", { timeoutMs: 4500 });
      return;
    }
    this._createProtocolFromItems(picked, 'Izabrano');
  }





  async removeFromPlaylist(uid) {
    this.playlist = this.playlist.filter(i => i.uid != uid);
    await this.savePlaylistToDB();
    this.renderPlaylistUI();
    this.log("USER", "Remove From Playlist", String(uid));
  }

  async clearPlaylist() {
    if (!confirm("Obrisati listu?")) return;
    this.playlist = [];
    await this.savePlaylistToDB();
    this.renderPlaylistUI();
    this.log("USER", "Clear Playlist", "");
  }


  /* ===================== MY PROTOCOLS (frequency sequences) ===================== */

  async loadProtocolsFromDB() {
    if (!this.db || !this.db.getProtocols) { this.protocols = []; return; }
    try {
      this.protocols = await this.db.getProtocols();
    } catch(e) {
      this.protocols = [];
    }
  }

  _newProtoId() {
    const r = Math.random().toString(36).slice(2,8);
    return `PROTO_${Date.now()}_${r}`;
  }

  _clone(obj) {
    try { return JSON.parse(JSON.stringify(obj)); } catch(e) { return obj; }
  }

  _expandSymptomToSteps(item, perFreqMinOverride=null, metaLabel="") {
    if (!item) return [];
    const freqs = (item.frekvencije || []).filter(f => f && f.enabled !== false);
    const perMin = Math.max(0.5, Number(perFreqMinOverride ?? item.userPerFreqMin ?? item.trajanjePoFrekvencijiMin ?? 5) || 5);
    const steps = [];
    for (const f of freqs) {
      const hz = Number(f.value ?? f.hz) || 0;
      if (!hz) continue;
      const step = this._clone(f);
      step.value = hz; // audio engine uses .value
      step.hz = hz;
      step._from = { symptomId: item.id, symptom: item.simptom };
      if (metaLabel) step.note = metaLabel;
      // keep catalog descriptive fields; ensure string safety
      steps.push({ freq: step, minutes: perMin });
    }
    return steps;
  }

  _stepsToSequence(steps) {
    const seq = [];
    const durs = [];
    for (const s of (Array.isArray(steps) ? steps : [])) {
      const f = (s && typeof s === "object") ? (s.freq || s) : null;
      if (!f) continue;
      const hz = Number(f.value ?? f.hz) || 0;
      if (!hz) continue;

      const obj = this._clone(f);
      obj.value = hz;
      obj.hz = hz;

      // Make custom label visible in player
      if (!obj.funkcija && !obj.svrha && obj.note) obj.funkcija = obj.note;

      seq.push(obj);
      const min = Math.max(0, Number(s.minutes ?? s.min ?? s.trajanje_min ?? 0) || 0);
      durs.push(Math.max(0, min * 60));
    }
    return { seq, durs, totalSec: durs.reduce((a,b)=>a+b,0) };
  }


_sanitizeLoopCount(n) {
  const v = Math.floor(Number(n) || 1);
  if (!Number.isFinite(v)) return 1;
  return Math.max(1, Math.min(20, v));
}

_repeatSteps(steps, loops) {
  const L = this._sanitizeLoopCount(loops);
  if (L <= 1) return Array.isArray(steps) ? steps : [];
  const src = Array.isArray(steps) ? steps : [];
  const out = [];
  for (let i=0;i<L;i++) {
    for (const s of src) out.push(this._clone(s));
  }
  return out;
}

  renderProtocolsUI() {
    const listEl = this.ui.protocolsList || document.getElementById("protocols-list");
    const editorEl = this.ui.protocolEditor || document.getElementById("protocol-editor");
    if (!listEl || !editorEl) return;

    // Render list
    const protos = Array.isArray(this.protocols) ? this.protocols : [];
    if (!protos.length) {
      listEl.innerHTML = `<p style="text-align:center; padding:20px; color:#999">Nema sačuvanih protokola.\n<br>Napravite novi protokol ili generišite iz Favorita / Liste.</p>`;
    } else {
      listEl.innerHTML = protos.map(p => {
        const name = (p.name || p.naziv || "Protokol").toString();
        const steps = Array.isArray(p.steps) ? p.steps : [];
        const baseMin = steps.reduce((acc,s)=> acc + (Number(s.minutes||0) || 0), 0);
        const loops = this._sanitizeLoopCount(p.loopCount ?? p.loops ?? 1);
        const totalMin = baseMin * loops;
        const updated = p.updatedAt ? new Date(p.updatedAt).toLocaleString() : "";
        const meta = (p && (p._meta || p.meta)) ? (p._meta || p.meta) : {};
        const subParts = [];
        try {
          if (meta && typeof meta === 'object') {
            if (meta.symptomOpis) subParts.push(String(meta.symptomOpis));
            if (meta.mkb10) subParts.push(String(meta.mkb10));
          }
        } catch(_) {}
        const sub = subParts.filter(Boolean).join(' • ');
        const subShort = sub.length > 160 ? (sub.slice(0, 157) + '…') : sub;
        return `
          <div class="proto-card">
            <div class="proto-head">
              <div style="flex:1;">
                <div class="proto-title">🧩 ${this.escapeHtml(name)}</div>
                <div class="proto-meta">Koraka: <b>${steps.length}</b> • Trajanje: <b>${Math.round(totalMin*10)/10} min</b>${loops>1 ? ` • Loop: <b>${loops}×</b>` : ''}${updated ? ` • <span class="muted">${this.escapeHtml(updated)}</span>` : ""}</div>
                ${subShort ? `<div class="proto-submeta">${this.escapeHtml(subShort)}</div>` : ``}
              </div>
              <div class="proto-actions">
                <button class="btn-mini" onclick="window.app.playUserProtocolById('${this.escapeJsString(p.id)}')">▶ Pusti</button>
                <button class="btn-mini" onclick="window.app.openProtocolEditor('${this.escapeJsString(p.id)}')">✏️ Izmeni</button>
                <button class="btn-mini" onclick="window.app.exportSingleProtocol('${this.escapeJsString(p.id)}')">⬆ Export</button>
                ${(p && p._guideHtml) ? `<button class="btn-mini" onclick="window.app.openProtocolGuideById('${this.escapeJsString(p.id)}')">🧾 Vodič</button>` : ``}
                <button class="btn-mini danger" onclick="window.app.deleteProtocolById('${this.escapeJsString(p.id)}')">🗑</button>
              </div>
            </div>
          </div>
        `;
      }).join("");
    }

    // Render editor
    if (!this._protoEdit) {
      editorEl.innerHTML = "";
      return;
    }

    const p = this._protoEdit;
    const name = (p.name || "Novi protokol").toString();
    const steps = Array.isArray(p.steps) ? p.steps : [];
    const baseMin = steps.reduce((acc,s)=> acc + (Number(s.minutes||0) || 0), 0);
    const loops = this._sanitizeLoopCount(p.loopCount ?? p.loops ?? 1);
    const totalMin = baseMin * loops;

    const stepRows = steps.map((s, idx) => {
      const f = s.freq || {};
      const hz = Number(f.value ?? f.hz) || 0;
      const label = (f.funkcija || f.svrha || f.desc || f.note || f.naziv || "").toString();
      const min = Math.max(0, Number(s.minutes || 0) || 0);
      return `
        <div class="proto-step-row">
          <div class="proto-step-main">
            <div><b>${hz} Hz</b> ${label ? `<span class="muted">— ${this.escapeHtml(label)}</span>` : ""}</div>
            <div class="muted" style="font-size:0.82rem;">⏱ ${min} min</div>
          </div>
          <div class="proto-step-actions">
            <button class="btn-mini" onclick="window.app.protoMoveStep(${idx}, -1)">↑</button>
            <button class="btn-mini" onclick="window.app.protoMoveStep(${idx}, 1)">↓</button>
            <button class="btn-mini danger" onclick="window.app.protoRemoveStep(${idx})">✖</button>
          </div>
        </div>
      `;
    }).join("");

    editorEl.innerHTML = `
      <div id="protocol-editor-box" class="proto-editor">
        <div style="display:flex; gap:10px; align-items:center; justify-content:space-between; flex-wrap:wrap;">
          <h3 style="margin:0;">🧩 Uređivanje protokola</h3>
          <div style="display:flex; gap:8px; flex-wrap:wrap;">
            <button class="btn-mini" onclick="window.app.protoAppendFromFavorites()">⭐ Dodaj iz Favorita</button>
            <button class="btn-mini" onclick="window.app.protoAppendFromQueue()">🎵 Dodaj iz Liste</button>
            <button class="btn-mini" onclick="window.app.protoSave()">💾 Sačuvaj</button>
            <button class="btn-mini danger" onclick="window.app.protoCancel()">✖ Zatvori</button>
          </div>
        </div>

        <div style="margin-top:10px; display:flex; gap:10px; flex-wrap:wrap; align-items:center;">
          <label style="flex:1; min-width:200px;">
            <div class="muted" style="font-size:0.85rem; margin-bottom:4px;">Naziv</div>
            <input class="input" value="${this.escapeAttr(name)}" oninput="window.app.protoSetName(this.value)" placeholder="Naziv protokola">
          
          </label>

          <div style="min-width:220px;">
            <div class="muted" style="font-size:0.85rem; margin-bottom:4px;">Loop protokola</div>
            <div style="display:flex; gap:8px; align-items:center; flex-wrap:wrap;">
              <label style="display:flex; gap:6px; align-items:center; user-select:none;">
                <input type="checkbox" ${loops>1 ? "checked" : ""} onchange="window.app.protoSetLoopEnabled(this.checked)">
                <span>Uključi</span>
              </label>
              <input class="input" type="number" min="1" max="20" step="1" value="${loops}" oninput="window.app.protoSetLoopCount(this.value)" style="width:92px;" ${loops>1 ? "" : "disabled"}>
              <span class="muted" style="font-size:0.85rem;">× (1–20)</span>
            </div>
          </div>

          <div style="min-width:180px; text-align:right;">

            <div class="muted" style="font-size:0.85rem;">Ukupno</div>
            <div style="font-weight:900;">${Math.round(totalMin*10)/10} min</div>
          </div>
        </div>

        <div style="margin-top:12px;" class="proto-add-grid">
          <div class="proto-add-card">
            <div style="font-weight:900; margin-bottom:6px;">➕ Dodaj ručno</div>
            <div style="display:flex; gap:8px; flex-wrap:wrap;">
              <input id="proto_hz" class="input" type="number" step="0.01" placeholder="Hz (npr 7.83)" style="width:140px;">
              <input id="proto_min" class="input" type="number" step="0.5" placeholder="min (npr 5)" style="width:120px;">
              <input id="proto_lbl" class="input" type="text" placeholder="opis (opciono)" style="flex:1; min-width:180px;">
              <button class="btn-mini" onclick="window.app.protoAddManual()">Dodaj</button>
            </div>
          </div>

          <div class="proto-add-card">
            <div style="font-weight:900; margin-bottom:6px;">🔎 Dodaj iz kataloga</div>
            <div style="display:flex; gap:8px; flex-wrap:wrap; align-items:center;">
              <input id="proto_search" class="input" type="text" placeholder="pretraga (simptom ili Hz)" style="flex:1; min-width:180px;">
              <button class="btn-mini" onclick="window.app.protoSearch()">Traži</button>
            </div>
            <div id="proto_search_results" style="margin-top:8px;"></div>
          </div>
        </div>

        <div style="margin-top:12px;">
          <div style="font-weight:900; margin-bottom:6px;">Koraci</div>
          <div class="proto-steps">${stepRows || "<div class='muted'>Nema koraka (još).</div>"}</div>
        </div>
      </div>
    `;
  }

  openProtocolEditor(id=null) {
    if (!id) {
      this._protoEdit = { id: this._newProtoId(), name: "Novi protokol", loopCount: 1, steps: [] };
      this.nav("protocols");
      this.renderProtocolsUI();
      return;
    }
    const p = (this.protocols || []).find(x => x.id === id);
    if (!p) return;
    this._protoEdit = this._clone(p);
    if (!Array.isArray(this._protoEdit.steps)) this._protoEdit.steps = [];
    if (!this._protoEdit.loopCount) this._protoEdit.loopCount = 1;
    this.nav("protocols");
    this.renderProtocolsUI();
  }

  protoCancel() {
    this._protoEdit = null;
    this.renderProtocolsUI();
  }

  protoSetName(v) {
    if (!this._protoEdit) return;
    this._protoEdit.name = (v || "").toString().slice(0, 120);
  }

  // v15.7.1.5 — iOS: keep playback start inside user gesture (no Promise microtask)
  protoSetLoopEnabled(isEnabled) {
    if (!this._protoEdit) return;
    const enabled = !!isEnabled;
    const current = this._sanitizeLoopCount(this._protoEdit.loopCount ?? this._protoEdit.loops ?? 1);

    if (enabled) {
      const remembered = this._sanitizeLoopCount(this._protoEdit._lastLoopCount ?? current);
      this._protoEdit.loopCount = Math.max(2, remembered);
    } else {
      if (current > 1) this._protoEdit._lastLoopCount = current;
      this._protoEdit.loopCount = 1;
    }

    this.renderProtocolsUI();
  }

  protoSetLoopCount(v) {
    if (!this._protoEdit) return;
    let n = this._sanitizeLoopCount(v);

    // If user is editing loop count (input is enabled), keep it >= 2.
    const currentlyEnabled = this._sanitizeLoopCount(this._protoEdit.loopCount ?? 1) > 1;
    if (currentlyEnabled) n = Math.max(2, n);

    this._protoEdit.loopCount = n;
    if (n > 1) this._protoEdit._lastLoopCount = n;
    this.renderProtocolsUI();
  }

  async protoSave() {
    if (!this._protoEdit || !this.db?.putProtocol) return;
    const p = this._protoEdit;
    if (!p.name || !p.name.trim()) p.name = "Protokol";
    if (!Array.isArray(p.steps)) p.steps = [];
    p.loopCount = this._sanitizeLoopCount(p.loopCount ?? p.loops ?? 1);
    // prune invalid steps
    p.steps = p.steps.filter(s => {
      const f = s?.freq || {};
      const hz = Number(f.value ?? f.hz) || 0;
      const min = Number(s.minutes||0) || 0;
      return hz > 0 && min > 0;
    });
    try {
      await this.db.putProtocol(p);
      await this.loadProtocolsFromDB();
      this._protoEdit = null;
      this.renderProtocolsUI();
      this.showToast("✅ Protokol sačuvan.", { actionLabel: "Protokoli", actionNav: "protocols" });
    } catch(e) {
      alert("Ne mogu da sačuvam protokol: " + (e?.message || e));
    }
  }

  protoMoveStep(idx, delta) {
    if (!this._protoEdit) return;
    const steps = this._protoEdit.steps || [];
    const i = Number(idx);
    const d = Number(delta);
    if (!Number.isFinite(i) || !Number.isFinite(d)) return;
    const j = i + d;
    if (j < 0 || j >= steps.length) return;
    const tmp = steps[i];
    steps[i] = steps[j];
    steps[j] = tmp;
    this._protoEdit.steps = steps;
    this.renderProtocolsUI();
  }

  protoRemoveStep(idx) {
    if (!this._protoEdit) return;
    const i = Number(idx);
    if (!Number.isFinite(i)) return;
    this._protoEdit.steps = (this._protoEdit.steps || []).filter((_,k)=>k!==i);
    this.renderProtocolsUI();
  }

  protoAddManual() {
    if (!this._protoEdit) return;
    const hz = Number(document.getElementById("proto_hz")?.value || 0);
    const min = Number(document.getElementById("proto_min")?.value || 0);
    const lbl = (document.getElementById("proto_lbl")?.value || "").toString().trim();

    if (!hz || hz <= 0) return alert("Unesi Hz.");
    if (!min || min <= 0) return alert("Unesi trajanje (min).");

    const freq = { value: hz, hz, funkcija: lbl || "Custom", opis: "", izvor: "user", note: lbl || "" };
    this._protoEdit.steps = this._protoEdit.steps || [];
    this._protoEdit.steps.push({ minutes: min, freq });

    // clear inputs
    try { document.getElementById("proto_hz").value=""; } catch(_) {}
    try { document.getElementById("proto_min").value=""; } catch(_) {}
    try { document.getElementById("proto_lbl").value=""; } catch(_) {}
    this.renderProtocolsUI();
  }

  protoAppendFromFavorites() {
    if (!this._protoEdit) return;
    const ids = Array.from(this._favSet || []);
    const items = ids.map(id => this.catalogItems.find(x => x.id === id)).filter(Boolean);
    const steps = [];
    for (const it of items) steps.push(...this._expandSymptomToSteps(it, null, `⭐ ${it.simptom}`));
    this._protoEdit.steps = (this._protoEdit.steps || []).concat(steps);
    this.renderProtocolsUI();
  }

  protoAppendFromQueue() {
    if (!this._protoEdit) return;
    const items = Array.isArray(this.playlist) ? this.playlist : [];
    const steps = [];
    for (const it of items) steps.push(...this._expandSymptomToSteps(it, Number(it.userPerFreqMin)||null, `🎵 ${it.simptom}`));
    this._protoEdit.steps = (this._protoEdit.steps || []).concat(steps);
    this.renderProtocolsUI();
  }

  protoSearch() {
    if (!this._protoEdit) return;
    const q = (document.getElementById("proto_search")?.value || "").toString().trim().toLowerCase();
    const box = document.getElementById("proto_search_results");
    if (!box) return;

    if (!q) { box.innerHTML = "<div class='muted'>Unesi pojam.</div>"; return; }

    // Search symptoms first (by name)
    const hitsItems = (this.catalogItems || []).filter(it => (it.simptom||"").toString().toLowerCase().includes(q)).slice(0, 6);

    // Search individual frequencies by label or Hz exact/partial
    const hitsFreq = [];
    const hzQ = Number(q);
    for (const it of (this.catalogItems || [])) {
      const freqs = (it.frekvencije || []).filter(f=>f && f.enabled!==false);
      for (const f of freqs) {
        const hz = Number(f.value ?? f.hz) || 0;
        const label = (f.svrha || f.funkcija || f.desc || f.naziv || "").toString().toLowerCase();
        if (Number.isFinite(hzQ) && hzQ>0 && Math.abs(hz - hzQ) < 0.001) {
          hitsFreq.push({ it, f, hz });
        } else if (label && label.includes(q)) {
          hitsFreq.push({ it, f, hz });
        }
        if (hitsFreq.length >= 10) break;
      }
      if (hitsFreq.length >= 10) break;
    }

    const htmlItems = hitsItems.map(it => {
      const perMin = Math.max(1, Number(it.trajanjePoFrekvencijiMin)||5);
      return `<div class="proto-hit">
        <div style="flex:1;">
          <div><b>${this.escapeHtml(it.simptom)}</b></div>
          <div class="muted">Dodaje sve frekvencije (${(it.frekvencije||[]).filter(f=>f&&f.enabled!==false).length}) • ${perMin} min / frekv.</div>
        </div>
        <button class="btn-mini" onclick="window.app.protoAddSymptom('${this.escapeJsString(it.id)}')">➕ Dodaj</button>
      </div>`;
    }).join("");

    const htmlFreq = hitsFreq.map(h => {
      const label = (h.f.svrha || h.f.funkcija || h.f.desc || h.f.naziv || "").toString();
      return `<div class="proto-hit">
        <div style="flex:1;">
          <div><b>${h.hz} Hz</b> <span class="muted">— ${this.escapeHtml(label||'')}</span></div>
          <div class="muted">(${this.escapeHtml(h.it.simptom)})</div>
        </div>
        <button class="btn-mini" onclick="window.app.protoAddFreq('${this.escapeJsString(h.it.id)}','${this.escapeJsString(String(h.hz))}')">➕ Dodaj</button>
      </div>`;
    }).join("");

    box.innerHTML = `
      ${htmlItems ? `<div style="font-weight:900; margin:6px 0;">Simptomi</div>${htmlItems}` : ""}
      ${htmlFreq ? `<div style="font-weight:900; margin:10px 0 6px;">Pojedinačne frekvencije</div>${htmlFreq}` : ""}
      ${(!htmlItems && !htmlFreq) ? "<div class='muted'>Nema rezultata.</div>" : ""}
    `;
  }

  protoAddSymptom(itemId) {
    if (!this._protoEdit) return;
    const it = (this.catalogItems || []).find(x => x.id === itemId);
    if (!it) return;
    const steps = this._expandSymptomToSteps(it, null, `📚 ${it.simptom}`);
    this._protoEdit.steps = (this._protoEdit.steps || []).concat(steps);
    this.renderProtocolsUI();
  }

  protoAddFreq(itemId, hzStr) {
    if (!this._protoEdit) return;
    const it = (this.catalogItems || []).find(x => x.id === itemId);
    if (!it) return;
    const hzQ = Number(hzStr) || 0;
    if (!hzQ) return;
    const f0 = (it.frekvencije || []).find(f => Math.abs((Number(f.value ?? f.hz)||0) - hzQ) < 0.001);
    if (!f0) return;

    const perMin = Math.max(1, Number(it.trajanjePoFrekvencijiMin)||5);
    const step = this._clone(f0);
    step.value = hzQ;
    step.hz = hzQ;
    step.note = `(${it.simptom})`;
    this._protoEdit.steps = (this._protoEdit.steps || []).concat([{ minutes: perMin, freq: step }]);
    this.renderProtocolsUI();
  }

  async deleteProtocolById(id) {
    if (!id) return;
    if (!confirm("Obrisati protokol?")) return;
    try {
      await this.db?.deleteProtocol?.(id);
      await this.loadProtocolsFromDB();
      this.renderProtocolsUI();
      this.showToast("🗑 Protokol obrisan.", { actionLabel: "Protokoli", actionNav: "protocols" });
    } catch(e) {
      alert("Ne mogu da obrišem protokol: " + (e?.message || e));
    }
  }

  async exportProtocols() {
    try {
      await this.loadProtocolsFromDB();
      const payload = { format: "SINET_PROTOCOLS_v1", exportedAt: new Date().toISOString(), protocols: this.protocols || [] };
      const blob = new Blob([JSON.stringify(payload, null, 2)], { type: "application/json" });
      const a = document.createElement("a");
      a.href = URL.createObjectURL(blob);
      a.download = `SINET_PROTOCOLS_${new Date().toISOString().slice(0,10)}.json`;
      a.click();
      URL.revokeObjectURL(a.href);
      this.log("USER", "Protocols Export", "");
    } catch(e) {
      alert("Export nije uspeo: " + (e?.message || e));
    }
  }

  exportSingleProtocol(id) {
    const p = (this.protocols || []).find(x => x.id === id);
    if (!p) return;
    const payload = { format: "SINET_PROTOCOL_v1", exportedAt: new Date().toISOString(), protocol: p };
    const blob = new Blob([JSON.stringify(payload, null, 2)], { type: "application/json" });
    const a = document.createElement("a");
    a.href = URL.createObjectURL(blob);
    a.download = `SINET_PROTOCOL_${id}.json`;
    a.click();
    URL.revokeObjectURL(a.href);
    this.log("USER", "Protocol Export", id);
  }

  openProtocolGuideById(id) {
    const p = (this.protocols || []).find(x => x.id === id);
    if (!p || !p._guideHtml) {
      alert("Nema vodiča (HTML) za ovaj protokol.");
      return;
    }
    try {
      const blob = new Blob([String(p._guideHtml)], { type: "text/html;charset=utf-8" });
      const url = URL.createObjectURL(blob);
      window.open(url, "_blank", "noopener");
      setTimeout(() => { try { URL.revokeObjectURL(url); } catch(_) {} }, 60_000);
      this.log("USER", "Protocol Guide Open", id);
    } catch(e) {
      console.warn("openProtocolGuideById failed", e);
      alert("Ne mogu da otvorim vodič.");
    }
  }

  importProtocolsPrompt() {
    const inp = document.createElement("input");
    inp.type = "file";
    inp.accept = "application/json";
    inp.onchange = async () => {
      const file = inp.files?.[0];
      if (!file) return;
      try {
        const text = await file.text();
        const payload = JSON.parse(text);
        const arr = payload?.protocols ? payload.protocols : (payload?.protocol ? [payload.protocol] : []);
        if (!Array.isArray(arr) || !arr.length) throw new Error("Nema protokola u fajlu.");

        for (const p0 of arr) {
          if (!p0 || typeof p0 !== "object") continue;
          const p = this._clone(p0);
          if (!p.id) p.id = this._newProtoId();
          if (!Array.isArray(p.steps)) p.steps = [];
          // avoid id clashes by suffix
          const exists = (this.protocols || []).some(x => x.id === p.id);
          if (exists) p.id = this._newProtoId();
          await this.db.putProtocol(p);
        }

        await this.loadProtocolsFromDB();
        this.renderProtocolsUI();
        this.showToast("✅ Uvoz protokola uspešan.", { actionLabel: "Protokoli", actionNav: "protocols" });
        this.log("USER", "Protocols Import", "");
      } catch(e) {
        alert("Uvoz nije uspeo: " + (e?.message || e));
      } finally {
        try { inp.value = ""; } catch(_) {}
      }
    };
    inp.click();
  }

  async createProtocolFromFavorites() {
    const ids = Array.from(this._favSet || []);
    const items = ids.map(id => this.catalogItems.find(x => x.id === id)).filter(Boolean);
    const steps = [];
    for (const it of items) steps.push(...this._expandSymptomToSteps(it, null, `⭐ ${it.simptom}`));
    this._protoEdit = { id: this._newProtoId(), name: `Favoriti • ${new Date().toISOString().slice(0,10)}`, loopCount: 1, steps };
    this.nav("protocols");
    this.renderProtocolsUI();
  }

  async createProtocolFromQueue() {
    const items = Array.isArray(this.playlist) ? this.playlist : [];
    const steps = [];
    for (const it of items) steps.push(...this._expandSymptomToSteps(it, Number(it.userPerFreqMin)||null, `🎵 ${it.simptom}`));
    this._protoEdit = { id: this._newProtoId(), name: `Lista • ${new Date().toISOString().slice(0,10)}`, loopCount: 1, steps };
    this.nav("protocols");
    this.renderProtocolsUI();
  }

  async playUserProtocolById(id, resume=null) {
    const p = (this.protocols || []).find(x => x.id === id) || await this.db?.getProtocol?.(id);
    if (!p) return alert("Ne mogu da nađem protokol.");
    const steps0 = Array.isArray(p.steps) ? p.steps : [];
    const loops = this._sanitizeLoopCount(p.loopCount ?? p.loops ?? 1);
    const steps = this._repeatSteps(steps0, loops);
    const { seq, durs, totalSec } = this._stepsToSequence(steps);
    if (!seq.length) return alert("Protokol nema koraka.");

    const startIndex = resume?.freqIndex || 0;
    const elapsedInFreq = resume?.elapsedInFreqSec || 0;

    this.selectedItem = { id: p.id, simptom: p.name || "Protokol" };
    this.isPlaylistActive = false;
    try { this._playOrigin = 'protocol'; } catch(_) {}

    // iOS PRO (Rendered WAV): render protocol to WAV-in-RAM and play via <audio>
    if (this._isIOS && this.isIosBgRenderedEnabled()) {
      let resumeTrackSec = 0;
      for (let i=0;i<startIndex;i++) resumeTrackSec += (Number(durs[i])||0);
      resumeTrackSec += Math.max(0, Number(elapsedInFreq) || 0);

      const r = await this._startRenderedTrack({
        sequence: seq,
        durationsSec: durs,
        totalSec,
        resumeTrackSec,
        titleText: `[PROTO${loops>1 ? ' x'+loops : ''}] ${(p.name || 'Protokol').toString()}`,
        loopAll: false
      });
      if (r && r.fallback === false) {
        if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = "⏸";
        this.log("PLAYER", "Play Protocol (iOS PRO Rendered)", p.id);
        return;
      }
      // else: fallback to live WebAudio
    }

    this.audio.loadSequence(seq, totalSec, startIndex, elapsedInFreq, durs);

    if (this.ui.playerDock) { this.ui.playerDock.style.display = 'block'; requestAnimationFrame(() => this._syncDockPadding(false)); }
    if (this.ui.nowPlayingPanel) this.ui.nowPlayingPanel.style.display = 'block';
    const btnNowList = document.getElementById('btn-nowlist');
    if (btnNowList) btnNowList.innerHTML = '▾ FREKVENCIJE';

    if (this.ui.pTitle) this.ui.pTitle.innerText = `[PROTO${loops>1 ? ' x'+loops : ''}] ${(p.name || 'Protokol').toString()}`;
    if (this.ui.pProtocolTimer) this.ui.pProtocolTimer.style.display = 'none';

    this._ensurePlaybackSession();
    this.audio.play();
    if (this.ui.btnPlayPause) this.ui.btnPlayPause.innerText = "⏸";
    this.log("PLAYER", "Play Protocol", p.id);
  }


  /* ===================== FAVORITES ===================== */

  // v15.4.7 — fast favorite lookup
  async refreshFavoritesSet() {
    if (!this.db) { this._favSet = new Set(); return; }
    try {
      const favs = await this.db.getFavorites();
      const ids = (Array.isArray(favs) ? favs : []).map(f => (f?.id || f)).filter(Boolean);
      this._favSet = new Set(ids);
    } catch (e) {
      this._favSet = new Set();
    }
  }

  isFavoriteId(id) {
    return !!(this._favSet && this._favSet.has(id));
  }

  // Toggle favorite from any list button (catalog, playlist, etc.)
  async toggleFavoriteQuick(id, btnEl = null) {
    if (!this.db || !id) return;
    try {
      const nowFav = await this.db.toggleFavorite(id);
      if (nowFav) this._favSet.add(id); else this._favSet.delete(id);

      // Update button label immediately
      if (btnEl) {
        btnEl.innerHTML = nowFav ? "⭐ <span class=\"btn-label\">Favorit</span>" : "☆ <span class=\"btn-label\">Favorit</span>";
        btnEl.setAttribute("aria-pressed", nowFav ? "true" : "false");
        btnEl.title = nowFav ? "Ukloni iz favorita" : "Dodaj u favorite";
      }

      // If modal is open for the same id, sync its state
      if (this.currentModalId === id) await this.refreshModalFavoriteUI();

      // If Favorites page is visible, refresh list
      if (this.currentPageId === 'favorites') this.updateFavorites();

      this.showToast(nowFav ? "Dodato u favorite ⭐" : "Uklonjeno iz favorita", { actionLabel: "Favoriti", actionNav: "favorites" });
      this.log("USER", "Toggle Favorite Quick", id);
    } catch (e) {
      this.showToast("Greška: ne mogu da sačuvam favorit.", {});
      this.log("ERROR", "Toggle Favorite Quick Failed", e?.message || "unknown");
    }
  }

  async refreshModalFavoriteUI() {
    const btn = document.getElementById('btn-qs-fav');
    if (!btn || !this.db || !this.currentModalId) return;

    // Prefer cached set; refresh if empty
    try {
      if (!this._favSet || this._favSet.size === 0) await this.refreshFavoritesSet();
    } catch (_) {}

    const isFav = this.isFavoriteId(this.currentModalId);

    btn.dataset.fav = isFav ? "1" : "0";
    btn.innerText = isFav ? "⭐ FAVORIT" : "☆ FAVORIT";
    btn.setAttribute("aria-pressed", isFav ? "true" : "false");
    btn.title = isFav ? "Ukloni iz favorita" : "Dodaj u favorite";
  }

  async toggleFavoriteFromModal() {
    if (!this.db || !this.currentModalId) return;
    try {
      const nowFav = await this.db.toggleFavorite(this.currentModalId);
      if (nowFav) this._favSet.add(this.currentModalId); else this._favSet.delete(this.currentModalId);
      await this.refreshModalFavoriteUI();
      this.showToast(nowFav ? "Dodato u favorite ⭐" : "Uklonjeno iz favorita", { actionLabel: "Favoriti", actionNav: "favorites" });
      this.log("USER", "Toggle Favorite", this.currentModalId);
    } catch (e) {
      this.showToast("Greška: ne mogu da sačuvam favorit.", {});
      this.log("ERROR", "Toggle Favorite Failed", e?.message || "unknown");
    }
  }

  // Backward compatibility (older UI calls)
  async toggleFavoriteCurrent() {
    return this.toggleFavoriteFromModal();
  }

  /* ===================== FAVORITES SELECTION ===================== */

  _ensureFavoritesSelection() {
    if (!this.favoritesSelected || !(this.favoritesSelected instanceof Set)) this.favoritesSelected = new Set();
  }

  toggleFavoriteSelect(id, checked) {
    this._ensureFavoritesSelection();
    const key = String(id || '');
    if (!key) return;
    if (checked) this.favoritesSelected.add(key);
    else this.favoritesSelected.delete(key);
    this._renderFavoritesSelectionHint();
  }

  _getFavoriteItems() {
    this._ensureFavoritesSelection();
    const favIds = Array.from(this._favSet || []);
    return favIds.map(fid => this.catalogItems.find(x => x.id === fid)).filter(Boolean);
  }

  _getSelectedFavoriteItems() {
    this._ensureFavoritesSelection();
    const sel = this.favoritesSelected;
    return this._getFavoriteItems().filter(it => sel.has(String(it.id)));
  }

  selectAllFavorites() {
    this._ensureFavoritesSelection();
    for (const it of this._getFavoriteItems()) this.favoritesSelected.add(String(it.id));
    this.updateFavorites();
  }

  clearFavoriteSelection() {
    this._ensureFavoritesSelection();
    this.favoritesSelected.clear();
    this.updateFavorites();
  }

  _renderFavoritesSelectionHint() {
    const el = document.getElementById('fav-selected-count');
    if (!el) return;
    const n = this._getSelectedFavoriteItems().length;
    el.innerText = n ? `Izabrano: ${n}` : '';
    const box = document.getElementById('fav-actions');
    if (box) box.style.display = this._getFavoriteItems().length ? 'block' : 'none';
    this._setControlValueIfExists('fav-play-mode', this.playMode || PLAY_MODE_SEQ);
  }

  startFavoritesSelected() {
    const picked = this._getSelectedFavoriteItems();
    if (!picked.length) {
      this.showToast('ℹ️ Nisi izabrao nijedan favorit.', { timeoutMs: 4500 });
      return;
    }
    this._startTemporaryPlaylist(picked, 'fav-play-mode', 'favorites');
  }

  createProtocolFromFavoriteSelected() {
    const picked = this._getSelectedFavoriteItems();
    if (!picked.length) {
      this.showToast('ℹ️ Nisi izabrao nijedan favorit.', { timeoutMs: 4500 });
      return;
    }
    this._createProtocolFromItems(picked, 'Favoriti');
  }

  async updateFavorites() {
    const cont = document.getElementById('favorites-list');
    if (!cont) return;
    cont.innerHTML = "";

    try {
      if (!this._favSet || this._favSet.size === 0) await this.refreshFavoritesSet();
      this._ensureFavoritesSelection();
      const favItems = this._getFavoriteItems();

      if (!favItems.length) {
        const box = document.getElementById('fav-actions');
        if (box) box.style.display = 'none';
        const count = document.getElementById('fav-selected-count');
        if (count) count.innerText = '';
        cont.innerHTML = "<p style='text-align:center'>Nema favorita.</p>";
        return;
      }

      const validIds = new Set(favItems.map(it => String(it.id)));
      this.favoritesSelected = new Set(Array.from(this.favoritesSelected).filter(id => validIds.has(String(id))));
      this._setControlValueIfExists('fav-play-mode', this.playMode || PLAY_MODE_SEQ);

      cont.innerHTML = favItems.map(it => {
        const mkbCode = this.getMKB10Code(it.mkb10);
        const mkbDesc = this.getMKB10Desc(it.mkb10);
        const mkbBadge = mkbCode ? `<span class="mkb-badge" style="background:#eef3ff; font-size:0.75rem; padding:2px 6px; border-radius:7px; margin-left:6px;">${this.escapeHtml(mkbCode)}</span>` : "";
        const short = (it.opis || it.desc || "").toString().trim().slice(0, 90);
        const checked = this.favoritesSelected.has(String(it.id)) ? 'checked' : '';
        return `
          <div class="cat-item">
            <div style="margin-right:10px; display:flex; align-items:center;">
              <input type="checkbox" class="pl-sel" ${checked}
                onclick="event.stopPropagation(); window.app && window.app.toggleFavoriteSelect && window.app.toggleFavoriteSelect('${this.escapeJsString(it.id)}', this.checked)">
            </div>
            <div class="cat-main" onclick="app.openModal('${this.escapeJsString(it.id)}')">
              <div class="cat-title">⭐ ${this.escapeHtml(it.simptom)} ${mkbBadge}</div>
              <div class="cat-sub">${this.escapeHtml(short || "Klikni za detalje i frekvencije.")}</div>
              <div class="cat-sub" style="margin-top:3px;">🏷️ MKB-10: ${mkbCode || mkbDesc ? `${this.escapeHtml(mkbCode || "")}${(mkbCode && mkbDesc) ? " — " : ""}${this.escapeHtml((mkbDesc || "").slice(0,80))}` : `<span class="muted">nije popunjeno</span>`}</div>
            </div>
            <div class="cat-actions">
              <button class="cat-mini-btn" title="Pusti odmah" onclick="event.stopPropagation(); app.playItemNow('${this.escapeJsString(it.id)}')">▶ <span class="btn-label">Pusti</span></button>
              <button class="btn-add-playlist" title="Dodaj u listu" onclick="event.stopPropagation(); app.quickAddToPlaylist('${this.escapeJsString(it.id)}')">➕ <span class="btn-label">U listu</span></button>
              <button class="cat-mini-btn" title="Dodaj u Moje simptome" onclick="event.stopPropagation(); app.quickAddToMySymptoms('${this.escapeJsString(it.id)}')">🧬 <span class="btn-label">Moji</span></button>
              <button class="cat-mini-btn" title="Sačuvaj kao protokol" onclick="event.stopPropagation(); app.quickCreateProtocolFromItem('${this.escapeJsString(it.id)}')">📑 <span class="btn-label">Protokol</span></button>
              <button class="cat-mini-btn" title="Ukloni iz favorita" onclick="event.stopPropagation(); app.toggleFavoriteQuick('${this.escapeJsString(it.id)}', this)">✖ <span class="btn-label">Ukloni</span></button>
            </div>
          </div>
        `;
      }).join('');

      this._renderFavoritesSelectionHint();
      this.log("DATA", "Favorites Render", String(favItems.length));
    } catch(e) {
      cont.innerHTML = "<p style='text-align:center; color:#c0392b'>Greška učitavanja favorita.</p>";
      console.error(e);
      this.log("ERROR", "Favorites Failed", e.message);
    }
  }



  /* ===================== BACKUP / RESTORE ===================== */

// Backward-compatible UI hooks (index.html expects exportData/importData)
async exportData() { return this.exportBackup(); }

async importData(fileInput) {
  try {
    const file = fileInput?.files?.[0];
    if (!file) return;
    const text = await file.text();
    const payload = JSON.parse(text);

    // 1) DB restore
    await this.db.importAll(payload);

	    // 2) LocalStorage restore (sve što je korisnik popunjavao)
	    try {
	      const loc = payload?.local || {};
	      if (typeof loc.userSymptoms === "string") localStorage.setItem("sinet_user_symptoms_v1", loc.userSymptoms);
	      if (typeof loc.overrides === "string") localStorage.setItem("sinet_overrides_v1", loc.overrides);
	      if (typeof loc.iosBgExperimental === "string") localStorage.setItem("sinet_ios_bg_experimental", loc.iosBgExperimental);
	
	      // New: bulk localStorage restore (prefix-based)
	      const kv = payload?.localStorage || loc?.kv || loc?.localStorage || {};
	      if (kv && typeof kv === 'object') {
	        for (const [k, v] of Object.entries(kv)) {
	          if (!k) continue;
	          try { localStorage.setItem(String(k), String(v ?? '')); } catch(_) {}
	        }
	      }
	    } catch(_) {}

    // 3) Hardening: after restore/import, do ONE clean reload to avoid mixed in-memory state (and SW cache edge cases)
    try { localStorage.setItem("sinet_restore_completed","1"); } catch(_) {}
    alert("Uvoz uspešan ✅\nAplikacija će se sada osvežiti radi stabilnosti.");
    this.log("USER", "Backup Import", "OK");
    try { location.reload(); } catch(_) {}
    return;
  } catch(e) {
    alert("Uvoz nije uspeo: " + (e?.message || e));
    this.log("ERROR", "Backup Import Failed", e?.message || "unknown");
  } finally {
    try { if (fileInput) fileInput.value = ""; } catch(_) {}
  }

  }

  async exportBackup() {
  try {
    const payload = await this.db.exportAll();

	    // Include local-only user data (not in IndexedDB)
	    payload.local = {};
	    try { payload.local.userSymptoms = localStorage.getItem("sinet_user_symptoms_v1") || ""; } catch(_) {}
	    try { payload.local.overrides = localStorage.getItem("sinet_overrides_v1") || ""; } catch(_) {}
	    try { payload.local.iosBgExperimental = localStorage.getItem("sinet_ios_bg_experimental") || "0"; } catch(_) {}

	    // New: save ALL relevant localStorage keys (da backup stvarno bude "sve")
	    payload.localStorage = {};
	    try {
	      const keep = (k) => {
	        if (!k) return false;
	        if (k === 'sinetNotes' || k === 'sinetHistory') return true;
	        return /^(sinet_|SINET_|anamneza_|ANAMNEZA_|integrative_|INTEGRATIVE_|ds_|DS_)/.test(k);
	      };
	      for (let i=0; i<localStorage.length; i++) {
	        const k = localStorage.key(i);
	        if (!keep(k)) continue;
	        payload.localStorage[k] = localStorage.getItem(k) || '';
	      }
	    } catch(_) {}

    const blob = new Blob([JSON.stringify(payload, null, 2)], { type: "application/json" });
    const a = document.createElement("a");
    a.href = URL.createObjectURL(blob);
    a.download = `SINET_BACKUP_${new Date().toISOString().slice(0,10)}.json`;
    a.click();
    URL.revokeObjectURL(a.href);
    this.log("USER", "Backup Export", "");
  } catch(e) {
    alert("Backup nije uspeo: " + e.message);
    this.log("ERROR", "Backup Export Failed", e.message);
  }
  }

  importBackupPrompt() {
    const inp = document.createElement("input");
    inp.type = "file";
    inp.accept = "application/json";
        inp.onchange = async () => {
      const file = inp.files?.[0];
      if (!file) return;
      const text = await file.text();
      try {
        const payload = JSON.parse(text);
        await this.db.importAll(payload);

        // LocalStorage restore (compatible with exportBackup payload)
        try {
          const loc = payload?.local || {};
          if (typeof loc.userSymptoms === "string") localStorage.setItem("sinet_user_symptoms_v1", loc.userSymptoms);
          if (typeof loc.overrides === "string") localStorage.setItem("sinet_overrides_v1", loc.overrides);
          if (typeof loc.iosBgExperimental === "string") localStorage.setItem("sinet_ios_bg_experimental", loc.iosBgExperimental);
          const kv = payload?.localStorage || loc?.kv || loc?.localStorage || {};
          if (kv && typeof kv === "object") {
            for (const [k, v] of Object.entries(kv)) {
              if (!k) continue;
              try { localStorage.setItem(String(k), String(v ?? "")); } catch(_) {}
            }
          }
        } catch(_) {}

        // Hardening: reload once after restore
        try { localStorage.setItem("sinet_restore_completed","1"); } catch(_) {}
        alert("Restore uspešan ✅\nAplikacija će se sada osvežiti radi stabilnosti.");
        try { location.reload(); } catch(_) {}
        return;
      } catch(e) {
        alert("Restore nije uspeo: " + e.message);
      }
    };
    inp.click();
  }



  async loadFrequencyCatalog() {
    try {
      const res = await fetch('data/freq_catalog.json', { cache: 'no-store' });
      if (!res.ok) throw new Error('freq_catalog.json missing');
      const catalog = await res.json();
      if (window.SINET_FrequencySchema && typeof window.SINET_FrequencySchema.setFreqCatalog === 'function') {
        window.SINET_FrequencySchema.setFreqCatalog(catalog);
      }
      this.log('DATA', 'Freq Catalog Loaded', String(Object.keys(catalog||{}).length));
    } catch (e) {
      this.log('DATA', 'Freq Catalog Load Failed', e?.message || String(e));
    }
  }

  _installGlobalErrorHooks() {
    if (this.__errHooksInstalled) return;
    this.__errHooksInstalled = true;

    window.addEventListener('error', (ev) => {
      try {
        const msg = ev && ev.message ? ev.message : 'Runtime error';
        const src = ev && ev.filename ? (String(ev.filename) + ':' + (ev.lineno||0) + ':' + (ev.colno||0)) : '';
        if (this.db && this.db.logError) this.db.logError('window.error', new Error(msg), { source: src, meta: { stack: ev && ev.error ? (ev.error.stack||'') : '' } });
      } catch(_) {}
    });

    window.addEventListener('unhandledrejection', (ev) => {
      try {
        const r = ev ? ev.reason : null;
        const err = (r instanceof Error) ? r : new Error(typeof r === 'string' ? r : JSON.stringify(r));
        if (this.db && this.db.logError) this.db.logError('unhandledrejection', err, { source: 'promise' });
      } catch(_) {}
    });

    try {
      const orig = console.error;
      console.error = (...args) => {
        try {
          const text = args.map(a => (a && a.message) ? a.message : String(a)).join(' | ');
          if (this.db && this.db.logAction) this.db.logAction('ERROR', 'console.error', text, { severity: 'ERROR', source: 'console' });
        } catch(_) {}
        return orig.apply(console, args);
      };
    } catch(_) {}
  }
  async renderAuditPreview() {
    const box = document.getElementById('audit-preview');
    const stats = document.getElementById('audit-stats');
    if (!box || !this.db) return;

    const q = String((document.getElementById('audit-filter-text') || {}).value || '').trim().toLowerCase();
    const lvl = String((document.getElementById('audit-filter-level') || {}).value || '').trim().toUpperCase();
    const catSel = String((document.getElementById('audit-filter-cat') || {}).value || '').trim().toUpperCase();

    const rows = await this.db.getAuditLog({ limit: 2000 });
    const errCount = rows.filter(r => String(r.severity||'').toUpperCase()==='ERROR' || String(r.category||'').toUpperCase()==='ERROR').length;

    let filtered = rows;
    if (lvl) filtered = filtered.filter(r => String(r.severity||'').toUpperCase()===lvl);
    if (catSel) filtered = filtered.filter(r => String(r.category||'').toUpperCase()===catSel);
    if (q) {
      filtered = filtered.filter(r => {
        const blob = [
          r.timestamp, r.severity, r.category, r.action, r.details,
          r.entityType, r.entityId
        ].map(x => String(x||'').toLowerCase()).join(' | ');
        return blob.includes(q);
      });
    }

    const last = filtered.slice(-120).reverse();

    if (stats) {
      stats.innerHTML = '<div style="display:flex; gap:10px; flex-wrap:wrap; font-size:0.85rem;">'
        + '<div><b>Zapisa:</b> ' + rows.length + '</div>'
        + '<div><b>Greške:</b> ' + errCount + '</div>'
        + '<div><b>Prikaz:</b> ' + last.length + ' / ' + filtered.length + '</div>'
        + '<div><b>Retention:</b> ' + this.escapeHtml(this._formatAuditRetentionLabel()) + '</div>'
        + '</div>';
    }

    if (!last.length) {
      box.innerHTML = '<div style="font-size:0.9rem; color:#666;">(Nema zapisa za izabrani filter.)</div>';
      return;
    }

    // Card view: one entry per row (no horizontal scroll)
    box.innerHTML = last.map(r => {
      const t = String(r.timestamp||'').replace('T',' ').replace('Z','');
      const sev = String(r.severity||'').toUpperCase();
      const cat = String(r.category||'').toUpperCase();
      const act = String(r.action||'');
      const det = String(r.details||'');
      const eid = String(r.entityId||'');
      const et = String(r.entityType||'');
      const badge = (sev==='ERROR') ? 'background:#ffecec; border:1px solid #ffd2d2;'
                  : (sev==='WARN') ? 'background:#fff6e6; border:1px solid #ffe0b2;'
                  : 'background:#f6f7f9; border:1px solid #eceff3;';
      return '<div style="border:1px solid #eee; border-radius:12px; padding:10px; margin-bottom:8px; background:white;">'
        + '<div style="display:flex; gap:8px; align-items:center; flex-wrap:wrap;">'
        + '<span style="font-weight:900;">' + this.escapeHtml(t) + '</span>'
        + '<span style="padding:4px 8px; border-radius:999px; font-weight:900; font-size:0.8rem; ' + badge + '">'
        + this.escapeHtml(sev || 'INFO') + '</span>'
        + '<span style="font-weight:900;">' + this.escapeHtml(cat) + '</span>'
        + '<span style="color:#333;">' + this.escapeHtml(act) + '</span>'
        + '</div>'
        + (eid || et ? '<div style="margin-top:6px; font-size:0.85rem; color:#666;"><b>' + this.escapeHtml(et) + ':</b> ' + this.escapeHtml(eid) + '</div>' : '')
        + (det ? '<div style="margin-top:6px; white-space:pre-wrap; font-size:0.9rem; color:#111;">' + this.escapeHtml(det) + '</div>' : '')
        + '</div>';
    }).join('');
  }

  /* ---------- SINET Admin (Audit retention + archives + KTIP) ---------- */

  getAuditRetentionSettings() {
    const mode = String(localStorage.getItem('sinet_audit_retention_mode') || 'count').toLowerCase();
    const keepCount = Number(localStorage.getItem('sinet_audit_retention_count') || 1000);
    const keepDays = Number(localStorage.getItem('sinet_audit_retention_days') || 7);

    // sanitize
    const m = (mode === 'days' || mode === 'hybrid' || mode === 'count') ? mode : 'count';
    const c = (Number.isFinite(keepCount) && keepCount > 0) ? keepCount : 1000;
    const d = (Number.isFinite(keepDays) && keepDays > 0) ? keepDays : 7;

    return { mode: m, keepCount: c, keepDays: d, policyTag: m };
  }

  setAuditRetentionSettings(s) {
    const mode = String(s?.mode || 'count').toLowerCase();
    const keepCount = Number(s?.keepCount || 1000);
    const keepDays = Number(s?.keepDays || 7);
    localStorage.setItem('sinet_audit_retention_mode', (mode === 'days' || mode === 'hybrid' || mode === 'count') ? mode : 'count');
    localStorage.setItem('sinet_audit_retention_count', String((Number.isFinite(keepCount) && keepCount > 0) ? keepCount : 1000));
    localStorage.setItem('sinet_audit_retention_days', String((Number.isFinite(keepDays) && keepDays > 0) ? keepDays : 7));
  }

  _formatAuditRetentionLabel() {
    const s = this.getAuditRetentionSettings();
    if (s.mode === 'days') return `${s.keepDays} dana`;
    if (s.mode === 'hybrid') return `HYBRID: ${s.keepCount} + ${s.keepDays}d`;
    return `${s.keepCount} zapisa`;
  }

  _adminToggleRetentionRows(mode) {
    const rowCount = document.getElementById('admin-audit-retention-count-row');
    const rowDays = document.getElementById('admin-audit-retention-days-row');
    if (!rowCount || !rowDays) return;
    if (mode === 'days') {
      rowDays.style.display = '';
      rowCount.style.display = 'none';
    } else if (mode === 'count') {
      rowCount.style.display = '';
      rowDays.style.display = 'none';
    } else {
      rowCount.style.display = '';
      rowDays.style.display = '';
    }
  }

  async renderAdminUI() {
    const modeSel = document.getElementById('admin-audit-retention-mode');
    const countSel = document.getElementById('admin-audit-retention-count');
    const daysSel = document.getElementById('admin-audit-retention-days');

    const statsBox = document.getElementById('admin-audit-stats');
    const listBox = document.getElementById('admin-audit-archives');
    const ktipBox = document.getElementById('admin-ktip-results');

    const btnSave = document.getElementById('admin-audit-save-btn');
    const btnRotate = document.getElementById('admin-audit-rotate-btn');
    const btnKTIP = document.getElementById('admin-ktip-run-btn');

    if (!modeSel || !countSel || !daysSel || !statsBox || !listBox) return;

    const s = this.getAuditRetentionSettings();
    modeSel.value = s.mode;
    countSel.value = String(s.keepCount);
    daysSel.value = String(s.keepDays);

    this._adminToggleRetentionRows(s.mode);

    modeSel.onchange = () => this._adminToggleRetentionRows(String(modeSel.value || 'count'));

    const readSettingsFromUI = () => {
      const mode = String(modeSel.value || 'count').toLowerCase();
      const keepCount = Number(countSel.value || 1000);
      const keepDays = Number(daysSel.value || 7);
      return { mode, keepCount, keepDays, policyTag: mode };
    };

    if (btnSave) btnSave.onclick = async () => {
      try {
        const ns = readSettingsFromUI();
        this.setAuditRetentionSettings(ns);
        this.showToast('Podešavanje sačuvano ✅', { timeoutMs: 3000 });
        try { await this.renderAuditPreview(); } catch(_) {}
        await this.renderAdminUI();
      } catch (e) {
        this.showToast('Greška pri čuvanju podešavanja', { timeoutMs: 4000 });
      }
    };

    if (btnRotate) btnRotate.onclick = async () => {
      try {
        const ns = readSettingsFromUI();
        this.setAuditRetentionSettings(ns);
        const res = await this.db.rotateAudit(ns);
        if (res && res.moved) this.showToast(`Arhivirano ${res.moved} zapisa ✅`, { timeoutMs: 4000 });
        else this.showToast('Nema zapisa za arhiviranje', { timeoutMs: 3000 });
        try { await this.renderAuditPreview(); } catch(_) {}
        await this.renderAdminUI();
      } catch (e) {
        this.showToast('Greška pri arhiviranju', { timeoutMs: 4000 });
      }
    };

    if (btnKTIP) btnKTIP.onclick = async () => {
      await this.runKTIPChecks();
    };

    // Stats
    try {
      const st = await this.db.getAuditStats();
      const last = st?.lastBatch;
      const lastLine = last
        ? `<div><b>Poslednja arhiva:</b> ${this.escapeHtml(String(last.range_from||'').replace('T',' ').replace('Z',''))} — ${this.escapeHtml(String(last.range_to||'').replace('T',' ').replace('Z',''))} <span class="badge">${Number(last.count||0)} zapisa</span></div>`
        : `<div><b>Poslednja arhiva:</b> (nema)</div>`;

      statsBox.innerHTML =
        `<div class="admin-stats">`
        + `<div><b>Hot log:</b> ${Number(st?.hotCount||0)}</div>`
        + `<div><b>Arhiva (events):</b> ${Number(st?.archiveCount||0)}</div>`
        + `<div><b>Arhive (batch):</b> ${Number(st?.batchCount||0)}</div>`
        + `<div><b>Retention:</b> ${this.escapeHtml(this._formatAuditRetentionLabel())}</div>`
        + `</div>`
        + `<div style="margin-top:8px; font-size:0.92rem;">${lastLine}</div>`;
    } catch (e) {
      statsBox.innerHTML = `<div class="muted">(Nije moguće učitati statistiku.)</div>`;
    }

    // Archive list
    try {
      const batches = await this.db.getAuditBatches({ limit: 60 });
      if (!batches || !batches.length) {
        listBox.innerHTML = `<div class="muted">(Nema arhiviranih audit paketa.)</div>`;
      } else {
        listBox.innerHTML = batches.map(b => {
          const from = this.escapeHtml(String(b.range_from||'').replace('T',' ').replace('Z',''));
          const to = this.escapeHtml(String(b.range_to||'').replace('T',' ').replace('Z',''));
          const cnt = Number(b.count||0);
          const tag = this.escapeHtml(String(b.policy_tag||''));
          const id = this.escapeHtml(String(b.id||''));
          return `
            <div class="admin-archive-row">
              <div class="admin-archive-meta">
                <div class="admin-archive-range">${from} — ${to}</div>
                <div class="admin-archive-sub"><span class="badge">${cnt} zapisa</span> <span class="badge badge-soft">${tag}</span></div>
                <div class="admin-archive-id">${id}</div>
              </div>
              <div class="admin-archive-actions">
                <button class="btn" onclick="openAuditBatch('${id}')">Pregled</button>
              </div>
            </div>
          `;
        }).join('');
      }
    } catch (e) {
      listBox.innerHTML = `<div class="muted">(Greška pri učitavanju arhiva.)</div>`;
    }

    if (ktipBox && !ktipBox.innerHTML.trim()) {
      ktipBox.innerHTML = `<div class="muted">(KTIP još nije pokrenut.)</div>`;
    }
  }

  /* ===================== SYSTEM (SINETADMIN HUB ENTRY) ===================== */

  renderSystemUI() {
    // System page is mostly static HTML; this hook exists to avoid TypeError and to allow future dynamic badges.
    try {
      // no-op for now
    } catch(_) {}
  }

  /* ===================== CONTENT STUDIO (SINETSTUDIO v1 LITE) ===================== */

  _studioLoadState() {
    try {
      const raw = localStorage.getItem("sinet_studio_state_v1");
      this.studioState = raw ? JSON.parse(raw) : {};
      if (!this.studioState || typeof this.studioState !== "object") this.studioState = {};
    } catch(e) {
      this.studioState = {};
    }
  }

  _studioSaveState() {
    try { localStorage.setItem("sinet_studio_state_v1", JSON.stringify(this.studioState || {})); } catch(e) {}
  }

  _studioToday() {
    try { return new Date().toISOString().slice(0,10); } catch(_) { return "date"; }
  }

  _studioMakePrompt(batchIndex, batchTotal, batchSize, topic) {
    const t = (topic || "").trim();
    const oblastHint = t ? `Oblast/Tema: "${t}"` : "Oblast/Tema: (izaberi smisleno, kratko)";
    return [
      "TI SI 'SINET Symptom Studio' generator.",
      "ZADATAK: Generiši TAČNO " + batchSize + " stavki simptoma kao NDJSON (svaki red 1 JSON objekat).",
      "BEZ dodatnog teksta pre/posle. BEZ markdown-a. BEZ numerisanja.",
      "",
      "BATCH: " + batchIndex + "/" + batchTotal,
      oblastHint,
      "",
      "ŠEMA (svaki JSON objekat):",
      "{",
      '  "oblast": "npr. 0100. Respiratorno",',
      '  "simptom": "Naziv simptoma (kratko)",',
      '  "opis": "1 rečenica (neutralno, bez dijagnoze)",',
      '  "mkb10": "" ,',
      '  "frekvencije": []',
      "}",
      "",
      "PRAVILA:",
      "- Ne dupliraj simptome unutar batch-a.",
      "- Ne piši dijagnoze ni terapije; samo simptome.",
      "- Koristi srpski (latinica).",
      "- Polje 'mkb10' ostavi prazno ako nisi siguran.",
      "- 'frekvencije' ostavi prazno ([]) ako nemaš pouzdane vrednosti."
    ].join("\n");
  }

  async renderStudioUI() {
    this._studioLoadState();
    const topicEl = document.getElementById("studio-topic");
    const ndEl = document.getElementById("studio-ndjson");
    const promptsEl = document.getElementById("studio-prompts");
    const reportEl = document.getElementById("studio-report");

    if (topicEl && typeof this.studioState.topic === "string") topicEl.value = this.studioState.topic;
    if (ndEl && typeof this.studioState.ndjson === "string") ndEl.value = this.studioState.ndjson;

    // render prompts
    const prompts = Array.isArray(this.studioState.prompts) ? this.studioState.prompts : [];
    if (promptsEl) {
      promptsEl.innerHTML = "";
      if (prompts.length === 0) {
        const p = document.createElement("div");
        p.className = "muted";
        p.style.opacity = "0.8";
        p.style.marginTop = "6px";
        p.textContent = "Generiši prompt (1×100 ili 10×100), kopiraj u AI, pa nalepi NDJSON rezultat ispod.";
        promptsEl.appendChild(p);
      } else {
        prompts.forEach((txt, i) => {
          const wrap = document.createElement("div");
          wrap.style.marginTop = "10px";
          wrap.style.border = "1px solid #e5e7eb";
          wrap.style.borderRadius = "10px";
          wrap.style.overflow = "hidden";
          wrap.style.background = "white";

          const head = document.createElement("div");
          head.style.display = "flex";
          head.style.alignItems = "center";
          head.style.justifyContent = "space-between";
          head.style.padding = "10px 12px";
          head.style.background = "#f8fafc";
          head.style.borderBottom = "1px solid #e5e7eb";
          const title = document.createElement("div");
          title.style.fontWeight = "700";
          title.textContent = `Prompt ${i+1} / ${prompts.length}`;
          const btn = document.createElement("button");
          btn.className = "btn-full";
          btn.style.padding = "10px 12px";
          btn.style.width = "auto";
          btn.style.background = "#0ea5e9";
          btn.style.fontWeight = "700";
          btn.textContent = "📋 Kopiraj";
          btn.onclick = async () => {
            try {
              if (navigator.clipboard && navigator.clipboard.writeText) await navigator.clipboard.writeText(txt);
              else {
                const ta = document.createElement("textarea");
                ta.value = txt; document.body.appendChild(ta); ta.select(); document.execCommand("copy"); ta.remove();
              }
              alert("Prompt kopiran ✅");
            } catch(e) { alert("Kopiranje nije uspelo."); }
          };
          head.appendChild(title);
          head.appendChild(btn);

          const pre = document.createElement("pre");
          pre.style.margin = "0";
          pre.style.padding = "12px";
          pre.style.whiteSpace = "pre-wrap";
          pre.style.wordBreak = "break-word";
          pre.style.fontSize = "12px";
          pre.textContent = txt;

          wrap.appendChild(head);
          wrap.appendChild(pre);
          promptsEl.appendChild(wrap);
        });
      }
    }

    // render validation report (if any)
    if (reportEl) {
      reportEl.innerHTML = "";
      const rep = this.studioState.lastReport;
      if (rep && typeof rep === "object") {
        const box = document.createElement("div");
        box.style.border = "1px solid #e5e7eb";
        box.style.borderRadius = "10px";
        box.style.padding = "12px";
        box.style.background = "#fff";
        box.innerHTML = `
          <div style="font-weight:800; margin-bottom:6px;">📊 Rezime</div>
          <div>Ukupno linija: <b>${rep.total||0}</b> • Validnih: <b>${rep.valid||0}</b> • Grešaka: <b>${rep.invalid||0}</b> • Duplikata: <b>${rep.dup||0}</b></div>
          <div class="muted" style="margin-top:6px;">${rep.note||""}</div>
        `;
        if (Array.isArray(rep.errors) && rep.errors.length) {
          const ul = document.createElement("div");
          ul.style.marginTop = "10px";
          ul.innerHTML = `<div style="font-weight:700; margin-bottom:6px;">Greške (prvih ${Math.min(10,rep.errors.length)})</div>`;
          const list = document.createElement("ul");
          list.style.margin = "0 0 0 18px";
          rep.errors.slice(0,10).forEach(e => {
            const li = document.createElement("li");
            li.textContent = e;
            list.appendChild(li);
          });
          ul.appendChild(list);
          box.appendChild(ul);
        }
        reportEl.appendChild(box);
      } else {
        const p = document.createElement("div");
        p.className = "muted";
        p.style.opacity = "0.8";
        p.textContent = "Nalepi NDJSON i klikni ✅ Validiraj.";
        reportEl.appendChild(p);
      }
    }

    // live-save textarea
    if (ndEl && !ndEl._sinetBound) {
      ndEl._sinetBound = true;
      ndEl.addEventListener("input", () => {
        this.studioState.ndjson = ndEl.value || "";
        this._studioSaveState();
      });
    }
    if (topicEl && !topicEl._sinetBound) {
      topicEl._sinetBound = true;
      topicEl.addEventListener("input", () => {
        this.studioState.topic = topicEl.value || "";
        this._studioSaveState();
      });
    }
  }

  async studioGeneratePrompts(batches = 1, batchSize = 100) {
    this._studioLoadState();
    const topic = (document.getElementById("studio-topic")?.value || this.studioState.topic || "").trim();
    const B = Math.max(1, Math.min(50, Number(batches)||1));
    const N = Math.max(10, Math.min(500, Number(batchSize)||100));
    const prompts = [];
    for (let i=1;i<=B;i++) prompts.push(this._studioMakePrompt(i, B, N, topic));
    this.studioState.topic = topic;
    this.studioState.prompts = prompts;
    this._studioSaveState();
    try { await this.db?.logAction("STUDIO","STUDIO_PROMPT_GEN",`Batches=${B} size=${N}`,{ entityType:"studio", entityId:"symptom", meta:{batches:B,size:N,topic} }); } catch(_) {}
    await this.renderStudioUI();
  }

  _studioParseNDJSON(text) {
    const lines = String(text||"").split(/\r?\n/).map(x=>x.trim()).filter(Boolean);
    const out = [];
    const errors = [];
    for (let i=0;i<lines.length;i++) {
      const ln = lines[i];
      try {
        const obj = JSON.parse(ln);
        out.push(obj);
      } catch(e) {
        errors.push(`Linija ${i+1}: nevažeći JSON`);
      }
    }
    return { lines, objects: out, errors };
  }

  _studioNormalizeToUserItem(obj) {
    const simptom = (obj?.simptom || obj?.naziv || obj?.title || "").toString().trim();
    const oblast = (obj?.oblast || obj?.category || "MOJI SIMPTOMI").toString().trim() || "MOJI SIMPTOMI";
    const opis = (obj?.opis || obj?.description || "").toString();
    const mkb10 = (obj?.mkb10 || obj?.mkb10_sifra || obj?.icd10 || "").toString().trim();
    const izvor = (obj?.izvor || obj?.source || "AI").toString().trim();

    const frekvencijeRaw = Array.isArray(obj?.frekvencije) ? obj.frekvencije : (Array.isArray(obj?.freqs) ? obj.freqs : []);
    const freqs = [];
    if (Array.isArray(frekvencijeRaw)) {
      frekvencijeRaw.forEach((f,i) => {
        if (f == null) return;
        if (typeof f === "number" || typeof f === "string") {
          const v = Number(f);
          if (!Number.isFinite(v)) return;
          freqs.push({ value: v, svrha: "", izvor: izvor || "AI", enabled: true });
          return;
        }
        // object
        const v = Number(f.value ?? f.hz ?? f.freq ?? "");
        if (!Number.isFinite(v)) return;
        freqs.push({
          value: v,
          svrha: (f.svrha || f.funkcija || f.purpose || "").toString(),
          izvor: (f.izvor || izvor || "AI").toString(),
          enabled: (f.enabled !== false)
        });
      });
    }

    return { simptom, oblast, opis, mkb10, izvor, frekvencije: freqs };
  }

  async studioValidate() {
    this._studioLoadState();
    const nd = (document.getElementById("studio-ndjson")?.value ?? this.studioState.ndjson ?? "");
    this.studioState.ndjson = nd;
    const parsed = this._studioParseNDJSON(nd);
    const objects = parsed.objects || [];
    const errors = (parsed.errors || []).slice();

    // validate required fields
    const norm = [];
    const seenKey = new Set();
    let dup = 0;
    for (let i=0;i<objects.length;i++) {
      const obj = objects[i];
      const n = this._studioNormalizeToUserItem(obj);
      if (!n.simptom) { errors.push(`Linija ${i+1}: nedostaje 'simptom'/'naziv'`); continue; }
      const key = this._slugify(n.simptom);
      if (seenKey.has(key)) { dup++; continue; }
      seenKey.add(key);
      norm.push(n);
    }

    const rep = {
      total: (parsed.lines||[]).length,
      valid: norm.length,
      invalid: errors.length,
      dup,
      errors,
      note: "Validacija proverava JSON + obavezno polje naziv simptoma + duplikate."
    };

    this.studioState.lastReport = rep;
    this.studioState.lastValid = norm;
    this._studioSaveState();

    try { await this.db?.logAction("STUDIO","STUDIO_VALIDATE",`Valid=${rep.valid} Errors=${rep.invalid} Dup=${rep.dup}`,{ entityType:"studio", entityId:"symptom", meta:rep }); } catch(_) {}

    await this.renderStudioUI();
    if (rep.invalid > 0) alert("Ima grešaka u NDJSON (vidi rezime).");
    else alert("Validacija OK ✅");
  }

  async studioCommitToUserSymptoms() {
    this._studioLoadState();
    const norm = Array.isArray(this.studioState.lastValid) ? this.studioState.lastValid : [];
    if (!norm.length) return alert("Nema validnih stavki. Prvo klikni ✅ Validiraj.");
    const list = Array.isArray(this.userSymptoms) ? this.userSymptoms : [];
    const used = new Set(list.map(x => String(x.id||"")));

    let added = 0;
    const ids = [];
    for (const n of norm) {
      const base = "usr-" + this._slugify(n.simptom);
      let id = base;
      if (used.has(id)) {
        let k = 2;
        while (used.has(`${id}-${k}`)) k++;
        id = `${id}-${k}`;
      }
      used.add(id);

      const item = window.SINET_FrequencySchema?.normalizeItem({
        id,
        simptom: n.simptom,
        oblast: n.oblast || "MOJI SIMPTOMI",
        opis: n.opis || "",
        mkb10: n.mkb10 || "",
        izvor: n.izvor || "AI",
        frekvencije: n.frekvencije || []
      }) || {
        id,
        simptom: n.simptom,
        oblast: n.oblast || "MOJI SIMPTOMI",
        opis: n.opis || "",
        mkb10: n.mkb10 || "",
        izvor: n.izvor || "AI",
        frekvencije: n.frekvencije || []
      };

      list.unshift(item);
      ids.push(id);
      added++;
    }

    this.userSymptoms = list;
    await this.saveUserSymptoms();
    this.applyUserDataToCatalog();

    try { await this.db?.logAction("STUDIO","STUDIO_COMMIT",`Added=${added}`,{ entityType:"studio_commit", entityId:"symptom", meta:{added, ids: ids.slice(0,50)} }); } catch(_) {}
    alert(`Dodato u Moje simptome: ${added} ✅`);
  }

  async studioExportPatch() {
    this._studioLoadState();
    const norm = Array.isArray(this.studioState.lastValid) ? this.studioState.lastValid : [];
    if (!norm.length) return alert("Nema validnih stavki. Prvo klikni ✅ Validiraj.");
    const today = this._studioToday();
    // export in a lightweight NDJSON schema (portable)
    const ndjson = norm.map(o => JSON.stringify({
      oblast: o.oblast || "MOJI SIMPTOMI",
      simptom: o.simptom,
      opis: o.opis || "",
      mkb10: o.mkb10 || "",
      frekvencije: Array.isArray(o.frekvencije) ? o.frekvencije : []
    })).join("\n") + "\n";

    const rep = this.studioState.lastReport || {};
    const reportMd = [
      `# Symptom Studio Export Report`,
      ``,
      `Datum: ${today}`,
      ``,
      `- Ukupno linija: ${rep.total||0}`,
      `- Validnih: ${rep.valid||norm.length}`,
      `- Grešaka: ${rep.invalid||0}`,
      `- Duplikata: ${rep.dup||0}`,
      ``,
      `Napomena: Ovo je 'patch' u portable formatu (oblast/simptom/opis/mkb10/frekvencije).`,
      ``
    ].join("\n");

    const download = (filename, content, mime) => {
      const blob = new Blob([content], { type: mime || "text/plain" });
      const a = document.createElement("a");
      a.href = URL.createObjectURL(blob);
      a.download = filename;
      a.click();
      setTimeout(()=>{ try { URL.revokeObjectURL(a.href); } catch(_) {} }, 1000);
    };

    download(`SYMPTOMS_PATCH_${today}.ndjson`, ndjson, "application/x-ndjson");
    download(`SYMPTOMS_REPORT_${today}.md`, reportMd, "text/markdown");

    try { await this.db?.logAction("STUDIO","STUDIO_EXPORT",`Exported=${norm.length}`,{ entityType:"studio_export", entityId:"symptom", meta:{count:norm.length} }); } catch(_) {}
  }

  async studioClear() {
    this._studioLoadState();
    this.studioState.ndjson = "";
    this.studioState.lastValid = [];
    this.studioState.lastReport = null;
    this._studioSaveState();
    const ndEl = document.getElementById("studio-ndjson");
    if (ndEl) ndEl.value = "";
    await this.renderStudioUI();
  }



  async openAuditBatch(batchId) {
    const modal = this._ensureAuditModal();
    const box = document.getElementById("audit-table");
    if (!modal || !box || !this.db) return;

    const rows = (await this.db.getAuditArchiveBatch(String(batchId||''), { limit: 5000 })).slice().reverse();

    if (!rows.length) {
      box.innerHTML = '<div style="font-size:0.95rem; color:#667085;">(Nema audit zapisa u ovoj arhivi.)</div>';
      modal.style.display = "flex";
      return;
    }

    box.innerHTML = rows.map(r => {
      const t = this.escapeHtml(String(r.timestamp || '').replace('T', ' ').replace('Z', ''));
      const sev = String(r.severity || '').toUpperCase() || 'INFO';
      const cat = this.escapeHtml(String(r.category || '').toUpperCase());
      const act = this.escapeHtml(String(r.action || ''));
      const det = this.escapeHtml(String(r.details || ''));
      const et = this.escapeHtml(String(r.entityType || ''));
      const eid = this.escapeHtml(String(r.entityId || ''));
      const badge = (sev === 'ERROR') ? '#ffe8e8' : (sev === 'WARN' ? '#fff4df' : '#eef4ff');
      return ''
        + '<div style="border:1px solid #e6e8ee; border-radius:12px; padding:10px; margin:8px 0; background:#fff;">'
        + '  <div style="display:flex; justify-content:space-between; gap:10px; flex-wrap:wrap;">'
        + `    <div style="font-weight:700;">${act}</div>`
        + `    <div style="font-size:0.85rem; color:#667085;">${t}</div>`
        + '  </div>'
        + '  <div style="display:flex; gap:8px; flex-wrap:wrap; margin-top:6px;">'
        + `    <span style="background:${badge}; padding:2px 8px; border-radius:999px; font-size:0.75rem; font-weight:700;">${this.escapeHtml(sev)}</span>`
        + (cat ? `    <span style="background:#f2f4f7; padding:2px 8px; border-radius:999px; font-size:0.75rem;">${cat}</span>` : '')
        + (et ? `    <span style="background:#f2f4f7; padding:2px 8px; border-radius:999px; font-size:0.75rem;">${et}${eid ? ':'+eid : ''}</span>` : '')
        + '  </div>'
        + (det ? `  <div style="margin-top:8px; font-size:0.92rem; color:#111827; white-space:pre-wrap;">${det}</div>` : '')
        + '</div>';
    }).join('');

    modal.style.display = "flex";
  }

  async runKTIPChecks() {
    const out = [];
    const push = (id, severity, message) => out.push({ id, severity, message });

    // 1) DB ready
    try {
      if (this.db && this.db.isReady) push('DB_READY', 'OK', 'Baza je spremna (IndexedDB).');
      else push('DB_READY', 'FAIL', 'Baza nije spremna.');
    } catch(_) { push('DB_READY', 'FAIL', 'Baza nije spremna.'); }

    // 2) Audit stats available
    let st = null;
    try {
      st = await this.db.getAuditStats();
      push('AUDIT_STATS', 'OK', `Hot=${st.hotCount}, ArchiveEvents=${st.archiveCount}, Batches=${st.batchCount}`);
    } catch (e) {
      push('AUDIT_STATS', 'FAIL', 'Ne mogu da učitam audit statistiku.');
    }

    // 3) Retention config sane
    try {
      const s = this.getAuditRetentionSettings();
      if (!s.mode) push('RETENTION_CFG', 'FAIL', 'Retention mode nije podešen.');
      else push('RETENTION_CFG', 'OK', `Mode=${s.mode}, Count=${s.keepCount}, Days=${s.keepDays}`);
    } catch (e) {
      push('RETENTION_CFG', 'FAIL', 'Greška u retention podešavanju.');
    }

    // 4) Minimal audit read (hot)
    try {
      const rows = await this.db.getAuditLog({ limit: 3 });
      push('AUDIT_READ', 'OK', `Učitano ${rows.length} zapisa iz hot log-a.`);
    } catch (e) {
      push('AUDIT_READ', 'FAIL', 'Ne mogu da učitam audit hot log.');
    }

    // 5) Service Worker status (informativno)
    try {
      const sw = (navigator && navigator.serviceWorker) ? 'OK' : 'N/A';
      push('SERVICE_WORKER', 'INFO', `serviceWorker: ${sw}`);
    } catch(_) { push('SERVICE_WORKER', 'INFO', 'serviceWorker: N/A'); }

    // Render
    const box = document.getElementById('admin-ktip-results');
    if (box) {
      box.innerHTML = out.map(r => {
        const sev = String(r.severity||'INFO').toUpperCase();
        const cls = (sev==='FAIL') ? 'badge-danger' : (sev==='OK' ? 'badge-ok' : 'badge-soft');
        return `<div class="ktip-row">
          <div><span class="badge ${cls}">${this.escapeHtml(sev)}</span> <b>${this.escapeHtml(r.id)}</b></div>
          <div class="ktip-msg">${this.escapeHtml(r.message)}</div>
        </div>`;
      }).join('');
    }

    // Log to audit
    try {
      await this.db.logAction('SYSTEM', 'DIAGNOSTIC_RUN', 'KTIP checks', {
        entityType: 'ktip',
        entityId: 'ktip_v1',
        source: 'admin',
        meta: { results: out, stats: st }
      });
    } catch(_) {}

    this.showToast('KTIP završen ✅', { timeoutMs: 2500 });
  }


  async exportAuditCsv() {
    const rows = await this.db.getAuditLog({ limit: 5000 });
    const csv = this.db.auditToCsv(rows);
    const blob = new Blob([csv], { type: 'text/csv;charset=utf-8' });
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = 'SINET_AUDIT_' + new Date().toISOString().slice(0,10) + '.csv';
    a.click();
    URL.revokeObjectURL(a.href);
    this.log('USER', 'Audit Export CSV', '');
  }

  async exportAuditJson() {
    await this.downloadAuditLog();
  }

  /* ===================== WORKBENCH v1 ===================== */

  workbenchSetMode(mode) {
    this._wbMode = (mode === 'edit') ? 'edit' : 'view';
    const ta = document.getElementById('wb-json');
    if (ta) ta.readOnly = (this._wbMode !== 'edit');

    if (!this._wbCurrentId && this._wbSelectedId) {
      this.workbenchLoadSelected({ mode: this._wbMode });
      return;
    }
    if (!this._wbCurrentId) {
      this._wbMsg('Prvo otvori ili učitaj stavku iz liste.', 'err');
      return;
    }

    this._wbMsg('Mode: ' + this._wbMode.toUpperCase(), '');

    try {
      const cur = ta && ta.value ? JSON.parse(ta.value) : (this._wbFormItem || null);
      if (cur) this.workbenchPopulateForm(cur);
      if (this._wbMode === 'edit') document.getElementById('wb-f-naziv')?.focus();
    } catch(_) {}
  }


  workbenchPopulateForm(item) {
    const mode = (this._wbMode === 'edit') ? 'edit' : 'view';

    const fNaziv = document.getElementById('wb-f-naziv');
    const fOblast = document.getElementById('wb-f-oblast');
    const fMkb = document.getElementById('wb-f-mkb10');
    const fOpis = document.getElementById('wb-f-opis');
    const fNap = document.getElementById('wb-f-napomena');

    if (fNaziv) fNaziv.value = String(item?.naziv || item?.title || '');
    if (fOblast) fOblast.value = String(item?.oblast || '');
    if (fMkb) fMkb.value = this.getMKB10Code?.(item?.mkb10) || (typeof item?.mkb10 === 'string' ? item.mkb10 : '');
    if (fOpis) fOpis.value = String(item?.opis || '');
    if (fNap) fNap.value = String(item?.napomena || item?.note || '');

    const ro = (mode !== 'edit');
    for (const el of [fNaziv, fOblast, fMkb, fOpis, fNap]) {
      try { if (el) el.readOnly = ro; } catch(_) {}
    }

    this._wbFormItem = JSON.parse(JSON.stringify(item || {}));
    this.workbenchRenderFreqList();
  }

  workbenchFormChanged() {
    try {
      if (!this._wbFormItem) return;
      const fNaziv = document.getElementById('wb-f-naziv');
      const fOblast = document.getElementById('wb-f-oblast');
      const fMkb = document.getElementById('wb-f-mkb10');
      const fOpis = document.getElementById('wb-f-opis');
      const fNap = document.getElementById('wb-f-napomena');

      if (fNaziv) this._wbFormItem.naziv = String(fNaziv.value || '').trim();
      if (fOblast) this._wbFormItem.oblast = String(fOblast.value || '').trim();
      if (fMkb) this._wbFormItem.mkb10 = String(fMkb.value || '').trim();
      if (fOpis) this._wbFormItem.opis = String(fOpis.value || '').trim();
      if (fNap) this._wbFormItem.napomena = String(fNap.value || '').trim();

      const ta = document.getElementById('wb-json');
      if (ta) ta.value = JSON.stringify(this._wbFormItem, null, 2);
    } catch(_) {}
  }

  workbenchRenderFreqList() {
    const box = document.getElementById('wb-freq-list');
    if (!box) return;
    const arr = (this._wbFormItem && Array.isArray(this._wbFormItem.frekvencije)) ? this._wbFormItem.frekvencije : [];
    if (!arr.length) {
      box.innerHTML = '<div style="font-size:0.85rem; color:#666;">(Nema frekvencija u ovoj stavci.)</div>';
      return;
    }
    box.innerHTML = arr.slice(0, 120).map((f, i) => {
      const hz = String(f?.hz ?? f?.frekvencija ?? '');
      const meta = (this.freqSchema && this.freqSchema.getMeta) ? this.freqSchema.getMeta(hz) : null;
      const title = meta?.naziv || f?.naziv || '';
      const desc = meta?.opis || f?.opis || '';
      const src = meta?.izvor || f?.izvor || '';
      return '<div class="wb-freq-item">'
        + '<div class="wb-freq-top">'
        + '<div class="wb-freq-hz">🎧 ' + this.escapeHtml(hz) + ' Hz' + (title ? ' — ' + this.escapeHtml(title) : '') + '</div>'
        + '<button class="wb-btn wb-btn-secondary wb-btn-inline" onclick="app.workbenchRemoveFreq(' + i + ')">🗑 Ukloni</button>'
        + '</div>'
        + (desc ? '<div class="wb-freq-desc">' + this.escapeHtml(String(desc).slice(0,220)) + '</div>' : '')
        + (src ? '<div class="wb-freq-desc"><b>Izvor:</b> ' + this.escapeHtml(String(src).slice(0,180)) + '</div>' : '')
        + '</div>';
    }).join('');
  }

  workbenchRemoveFreq(idx) {
    try {
      if (!this._wbFormItem || !Array.isArray(this._wbFormItem.frekvencije)) return;
      this._wbFormItem.frekvencije.splice(Number(idx), 1);
      this.workbenchFormChanged();
      this.workbenchRenderFreqList();
      this.log('UI', 'Workbench Remove Freq', String(this._wbCurrentId||''));
      if (this.db && this.db.logAction) this.db.logAction('UI', 'Workbench Remove Freq', String(this._wbCurrentId||''), { entityType:'ITEM', entityId:String(this._wbCurrentId||''), source:'workbench' });
    } catch(_) {}
  }

  workbenchOpenFreqPicker() {
    try {
      const q = prompt('Dodaj frekvenciju: unesi Hz (npr. 528) ili naziv (npr. theta):', '');
      if (q === null) return;
      const query = String(q||'').trim();
      if (!query) return;

      const catalog = (this.freqSchema && this.freqSchema._catalog) ? this.freqSchema._catalog : null;
      let hz = '';
      if (/^\d+(\.\d+)?$/.test(query)) hz = query;
      else if (catalog) {
        const norm = query.toLowerCase();
        const hit = Object.keys(catalog).find(k => String(catalog[k]?.naziv||'').toLowerCase().includes(norm));
        if (hit) hz = hit;
      }
      if (!hz) {
        this.showToast('Nije pronađena frekvencija u katalogu. Pokušaj da uneseš tačan Hz.', { timeoutMs: 5000 });
        return;
      }

      if (!this._wbFormItem) this._wbFormItem = {};
      if (!Array.isArray(this._wbFormItem.frekvencije)) this._wbFormItem.frekvencije = [];
      this._wbFormItem.frekvencije.push({ hz: hz });

      this.workbenchFormChanged();
      this.workbenchRenderFreqList();
      this.log('UI', 'Workbench Add Freq', hz);
      if (this.db && this.db.logAction) this.db.logAction('UI', 'Workbench Add Freq', hz, { entityType:'FREQ', entityId:hz, source:'workbench' });
    } catch(_) {}
  }

  _wbMsg(text, kind) {
    const el = document.getElementById('wb-msg');
    if (!el) return;
    el.textContent = text || '';
    el.style.color = (kind === 'err') ? '#c0392b' : '#2c3e50';
  }

  _normWorkbenchText(value) {
    return String(value || '')
      .toLowerCase()
      .normalize('NFKD')
      .replace(/[̀-ͯ]/g, '')
      .trim();
  }

  _getWorkbenchCatalog(query = '') {
    const all = Array.isArray(this.catalogItems) ? this.catalogItems.slice() : [];
    const q = String(query || '').toLowerCase();
    const wantsUser = q.includes('usr-') || q.includes('moji simptom') || q.includes('lični simptom');
    if (!wantsUser) return all.filter(it => !String(it?.id || '').startsWith('usr-'));
    return all;
  }

  _findWorkbenchMatches(query, limit = 250) {
    const q = this._normWorkbenchText(query);
    const all = this._getWorkbenchCatalog(query);
    if (!q) {
      return all.slice()
        .sort((a,b) => String(a.naziv||a.id||'').localeCompare(String(b.naziv||b.id||''), 'sr'))
        .slice(0, Math.max(1, limit));
    }

    const exactId = all.find(it => this._normWorkbenchText(it.id) === q);
    if (exactId) return [exactId];

    const exactName = all.find(it => this._normWorkbenchText(it.naziv) === q || this._normWorkbenchText(it.title) === q);
    if (exactName) return [exactName];

    const starts = [];
    const contains = [];
    for (const it of all) {
      const id = this._normWorkbenchText(it.id);
      const name = this._normWorkbenchText(it.naziv || it.title);
      const oblast = this._normWorkbenchText(it.oblast);
      const mkb = this._normWorkbenchText(this.getMKB10Code?.(it.mkb10) || (typeof it.mkb10 === 'string' ? it.mkb10 : ''));
      const hay = [id, name, oblast, mkb].filter(Boolean).join(' | ');
      if (!hay) continue;
      if (id.startsWith(q) || name.startsWith(q)) starts.push(it);
      else if (hay.includes(q)) contains.push(it);
      if (starts.length + contains.length >= limit) break;
    }
    return starts.concat(contains).slice(0, Math.max(1, limit));
  }
  renderWorkbenchPicker(filterValue) {
    const box = document.getElementById('wb-list');
    if (!box) return;
    const filter = (filterValue === undefined || filterValue === null)
      ? String((document.getElementById('wb-filter') || {}).value || (document.getElementById('wb-id') || {}).value || '')
      : String(filterValue || '');

    const rows = this._findWorkbenchMatches(filter, filter ? 250 : 180);
    const activeId = String(this._wbSelectedId || this._wbCurrentId || '');

    box.innerHTML = rows.map(it => {
      const id = String(it.id || '');
      const naziv = String(it.naziv || it.title || id || '');
      const mkb = this.getMKB10Code?.(it.mkb10) || (typeof it.mkb10 === 'string' ? it.mkb10 : '');
      const oblast = String(it.oblast || '');
      const sub = [id, mkb, oblast].filter(Boolean).join(' • ');
      const cls = (id && id === activeId) ? 'wb-item active' : 'wb-item';
      return '<div class="' + cls + '" onclick="app.workbenchPick(\'' + this.escapeHtml(id) + '\')">'
        + '<div class="wb-item-title">' + this.escapeHtml(naziv) + '</div>'
        + '<div class="wb-item-sub">' + this.escapeHtml(sub) + '</div>'
        + '</div>';
    }).join('');

    if (!this._wbSelectedId && rows[0] && rows[0].id) {
      this._wbSelectedId = String(rows[0].id);
    }
  }
  onWorkbenchLookupInput(value) {
    try {
      const filter = document.getElementById('wb-filter');
      if (filter && !String(filter.value || '').trim()) filter.value = String(value || '');
    } catch (_) {}
    this.renderWorkbenchPicker(value);
  }

  workbenchPick(id) {
    try {
      this._wbSelectedId = String(id || '').trim();
      const inp = document.getElementById('wb-id');
      if (inp && this._wbSelectedId) inp.value = this._wbSelectedId;
      const f = String((document.getElementById('wb-filter') || {}).value || '');
      this.renderWorkbenchPicker(f);
      this._wbMsg('Izabrana stavka: ' + this._wbSelectedId, '');
    } catch(_) {}
  }

  _workbenchLoadResolvedItem(item, opts = {}) {
    const mode = (opts.mode === 'edit') ? 'edit' : 'view';
    const id = String(item?.id || '').trim();
    if (!id) return false;
    this._wbCurrentId = id;
    this._wbSelectedId = id;
    const merged = (this.overrides && this.overrides[id]) ? this._deepMerge(item, this.overrides[id]) : item;
    const ta = document.getElementById('wb-json');
    if (ta) ta.value = JSON.stringify(merged, null, 2);
    try { this.workbenchPopulateForm(merged); } catch(_) {}
    const inp = document.getElementById('wb-id');
    if (inp) inp.value = id;
    this.workbenchSetMode(mode);
    return true;
  }

  workbenchLoadSelected(opts = {}) {
    const id = String(this._wbSelectedId || '').trim();
    if (!id) { this._wbMsg('Prvo izaberi stavku iz liste.', 'err'); return false; }
    const item = this._findItemByLookup(id);
    if (!item) { this._wbMsg('Izabrana stavka više nije dostupna.', 'err'); return false; }
    const ok = this._workbenchLoadResolvedItem(item, opts);
    if (ok) {
      const modeLabel = (opts.mode === 'edit') ? 'Edit' : 'View';
      this._wbMsg('Učitano iz liste: ' + id + ' (' + modeLabel + ')', '');
      this.log('UI', 'Workbench Load From List', id);
      if (this.db && this.db.logAction) this.db.logAction('UI', 'Workbench Load From List', id, { entityType:'ITEM', entityId:id, source:'workbench' });
    }
    return ok;
  }

  workbenchAdoptSelected() {
    const inp = document.getElementById('wb-id');
    const id = String(this._wbSelectedId || '').trim();
    if (inp && id) inp.value = id;
    if (!id) { this._wbMsg('Prvo izaberi stavku iz liste.', 'err'); return; }
    this.workbenchLoadSelected({ mode: 'view' });
  }

  async workbenchOpenSelected() {
    const ok = this.workbenchLoadSelected({ mode: 'view' });
    if (!ok) return;
    try { await this.renderAuditPreview(); } catch(_) {}
  }

  _findItemByLookup(query) {
    const matches = this._findWorkbenchMatches(query, 20);
    return matches[0] || null;
  }

  async workbenchOpenById() {
    const inp = document.getElementById('wb-id');
    const query = inp ? String(inp.value || '').trim() : '';
    if (!query) { this._wbMsg('Unesi ID ili naziv stavke.', 'err'); return; }

    const matches = this._findWorkbenchMatches(query, 20);
    if (!matches.length) {
      this.renderWorkbenchPicker(query);
      this._wbMsg('Stavka nije pronađena. Probaj naziv ili izaberi iz liste ispod.', 'err');
      return;
    }
    if (matches.length > 1 && this._normWorkbenchText(matches[0].id) !== this._normWorkbenchText(query) && this._normWorkbenchText(matches[0].naziv || matches[0].title) !== this._normWorkbenchText(query)) {
      this.renderWorkbenchPicker(query);
      this._wbMsg('Pronađeno je više poklapanja — izaberi stavku iz liste pa klikni „Otvori izbor”.', 'err');
      return;
    }

    const item = matches[0];
    const id = String(item.id || '').trim();
    if (!id) { this._wbMsg('Stavka nema validan ID.', 'err'); return; }

    this._workbenchLoadResolvedItem(item, { mode: 'view' });
    if (inp) inp.value = id;
    this.log('UI', 'Workbench Open', id);
    if (this.db && this.db.logAction) this.db.logAction('UI', 'Workbench Open', id, { entityType:'ITEM', entityId:id, source:'workbench' });
    try { await this.renderAuditPreview(); } catch(_) {}
  }

  _bumpEditCounter(entityId, context) {
    try {
      const raw = localStorage.getItem('sinet_edit_counter_v1');
      const map = raw ? JSON.parse(raw) : {};
      const id = String(entityId||'');
      if (!id) return 0;
      const cur = map[id] || { count: 0, lastTs: 0 };
      cur.count = Number(cur.count||0) + 1;
      cur.lastTs = Date.now();
      map[id] = cur;
      localStorage.setItem('sinet_edit_counter_v1', JSON.stringify(map));
      if (cur.count >= 5) {
        this.showToast('⚠️ Primetno: već ' + cur.count + '× menjaš isti zapis. Ako se radi o simptomima koji traju ili se pogoršavaju, razmotri pregled kod izabranog lekara.', { timeoutMs: 9000 });
        this.log('SAFETY', 'Repeat Edit Warning', id + ' | ' + (context||''));
      }
      return cur.count;
    } catch (_) {
      return 0;
    }
  }

  async workbenchSaveOverride() {
    const id = String(this._wbCurrentId || '').trim();
    if (!id) { this._wbMsg('Prvo otvori stavku.', 'err'); return; }

    let obj;
    try {
      obj = this._wbFormItem ? JSON.parse(JSON.stringify(this._wbFormItem)) : null;
      if ((!obj || typeof obj !== 'object') && document.getElementById('wb-json')) {
        obj = JSON.parse(String(document.getElementById('wb-json').value || ''));
      }
    } catch(e) { this._wbMsg('JSON greška: ' + e.message, 'err'); return; }
    if (!obj || typeof obj !== 'object') { this._wbMsg('Nema podataka za čuvanje.', 'err'); return; }

    obj.id = id;
    if (!this.overrides || typeof this.overrides !== 'object') this.overrides = {};
    this.overrides[id] = obj;

    await this.saveOverrides();
    this.applyUserDataToCatalog();

    this.log('USER', 'Save Override', id);
    if (this.db && this.db.logAction) this.db.logAction('USER', 'Save Override', '', { entityType: 'ITEM', entityId: id, source: 'workbench' });
    this._bumpEditCounter(id, 'override');

    this._wbMsg('Override sačuvan ✅', '');
    try { await this.renderAuditPreview(); } catch(_) {}
  }

  async workbenchCloneToMySymptoms() {
    const id = String(this._wbCurrentId || '').trim();
    if (!id) { this._wbMsg('Prvo otvori stavku.', 'err'); return; }

    let obj;
    try {
      obj = this._wbFormItem ? JSON.parse(JSON.stringify(this._wbFormItem)) : null;
      if ((!obj || typeof obj !== 'object') && document.getElementById('wb-json')) {
        obj = JSON.parse(String(document.getElementById('wb-json').value || ''));
      }
    } catch(e) { this._wbMsg('JSON greška: ' + e.message, 'err'); return; }
    if (!obj || typeof obj !== 'object') { this._wbMsg('Nema podataka za kloniranje.', 'err'); return; }

    const newId = 'usr-' + Date.now();
    obj.id = newId;
    obj.oblast = obj.oblast || 'MOJI SIMPTOMI';

    if (!Array.isArray(this.userSymptoms)) this.userSymptoms = [];
    this.userSymptoms.push(obj);

    await this.saveUserSymptoms();
    this.applyUserDataToCatalog();

    this.log('USER', 'Clone To MySymptoms', newId);
    if (this.db && this.db.logAction) this.db.logAction('USER', 'Clone To MySymptoms', '', { entityType: 'ITEM', entityId: newId, source: 'workbench', meta: { from: id } });
    this._bumpEditCounter(id, 'clone');

    const inp = document.getElementById('wb-id');
    if (inp) inp.value = newId;
    this._wbCurrentId = newId;
    this._wbSelectedId = newId;
    this._wbFormItem = JSON.parse(JSON.stringify(obj));
    const ta = document.getElementById('wb-json');
    if (ta) ta.value = JSON.stringify(obj, null, 2);
    try { this.workbenchPopulateForm(obj); } catch(_) {}

    this._wbMsg('Klonirano u Moji simptomi ✅ (' + newId + ')', '');
    this.showToast('✅ Stavka je klonirana u “Moji simptomi”.', { timeoutMs: 3500, actionLabel:'Otvori', actionNav:'mysymptoms' });
    try { await this.renderAuditPreview(); } catch(_) {}
  }

  async workbenchResetOverride() {
    const id = String(this._wbCurrentId || '').trim();
    if (!id) { this._wbMsg('Prvo otvori stavku.', 'err'); return; }

    if (this.overrides && this.overrides[id]) {
      delete this.overrides[id];
      await this.saveOverrides();
      this.applyUserDataToCatalog();
      this.log('USER', 'Reset Override', id);
      if (this.db && this.db.logAction) this.db.logAction('USER', 'Reset Override', '', { entityType: 'ITEM', entityId: id, source: 'workbench' });
      this._wbMsg('Override resetovan ♻️', '');

      const item = this._findItemById(id);
      const ta = document.getElementById('wb-json');
      if (ta && item) ta.value = JSON.stringify(item, null, 2);
      try { await this.renderAuditPreview(); } catch(_) {}
      return;
    }

    this._wbMsg('Nema override-a za ovu stavku.', '');
  }

  /* ===================== AUDIT LOG UI ===================== */
  _ensureAuditModal() {
    let modal = document.getElementById("audit-modal");
    if (modal) return modal;
    modal = document.createElement('div');
    modal.id = 'audit-modal';
    modal.style.cssText = 'position:fixed; inset:0; background:rgba(17,24,39,0.55); display:none; align-items:center; justify-content:center; padding:16px; z-index:10050;';
    modal.innerHTML = ''
      + '<div style="width:min(980px, 100%); max-height:88vh; background:white; border-radius:18px; box-shadow:0 20px 60px rgba(0,0,0,0.25); overflow:hidden; display:flex; flex-direction:column;">'
      + '  <div style="display:flex; align-items:center; justify-content:space-between; gap:12px; padding:14px 16px; border-bottom:1px solid #eef2f7;">'
      + '    <div><div style="font-weight:900; font-size:1.05rem;">🧾 Sistemski Audit Log</div><div style="font-size:0.85rem; color:#667085;">Prikaz poslednjih zapisa bez horizontalnog skrola.</div></div>'
      + '    <button type="button" onclick="app.closeAuditLog()" style="padding:10px 12px; border:1px solid #ddd; border-radius:12px; background:white; font-weight:900; cursor:pointer;">✖ Zatvori</button>'
      + '  </div>'
      + '  <div id="audit-table" style="padding:14px 16px; overflow:auto;"></div>'
      + '</div>';
    modal.addEventListener('click', (ev) => { if (ev.target === modal) this.closeAuditLog(); });
    document.body.appendChild(modal);
    return modal;
  }

  async openAuditLog() {
    const modal = this._ensureAuditModal();
    const box = document.getElementById("audit-table");
    if (!modal || !box || !this.db) return;
    const rows = (await this.db.getAuditLog({ limit: 500, includeArchive: true })).slice().reverse();

    if (!rows.length) {
      box.innerHTML = '<div style="font-size:0.95rem; color:#667085;">(Nema audit zapisa.)</div>';
      modal.style.display = "flex";
      return;
    }

    box.innerHTML = rows.map(r => {
      const t = this.escapeHtml(String(r.timestamp || '').replace('T', ' ').replace('Z', ''));
      const sev = String(r.severity || '').toUpperCase() || 'INFO';
      const cat = this.escapeHtml(String(r.category || '').toUpperCase());
      const act = this.escapeHtml(String(r.action || ''));
      const det = this.escapeHtml(String(r.details || ''));
      const et = this.escapeHtml(String(r.entityType || ''));
      const eid = this.escapeHtml(String(r.entityId || ''));
      const badge = (sev === 'ERROR') ? '#ffe8e8' : (sev === 'WARN' ? '#fff4df' : '#eef4ff');
      return ''
        + '<div style="border:1px solid #edf1f5; border-radius:14px; padding:12px; margin-bottom:10px; background:#fff;">'
        + '  <div style="display:flex; gap:8px; align-items:center; flex-wrap:wrap;">'
        + '    <span style="font-weight:900;">' + t + '</span>'
        + '    <span style="padding:4px 8px; border-radius:999px; background:' + badge + '; font-size:0.8rem; font-weight:900;">' + this.escapeHtml(sev) + '</span>'
        + '    <span style="font-weight:900;">' + cat + '</span>'
        + '    <span>' + act + '</span>'
        + '  </div>'
        + (eid || et ? '  <div style="margin-top:6px; color:#667085; font-size:0.88rem;"><b>' + et + ':</b> ' + eid + '</div>' : '')
        + (det ? '  <div style="margin-top:8px; white-space:pre-wrap; font-size:0.92rem;">' + det + '</div>' : '')
        + '</div>';
    }).join('');
    modal.style.display = "flex";
    this.log("UI", "Open Audit", "");
  }

  closeAuditLog() {
    const modal = document.getElementById("audit-modal");
    if (modal) modal.style.display = "none";
  }

  async downloadAuditLog() {
    const rows = await this.db.getAuditLog(5000);
    const blob = new Blob([JSON.stringify(rows, null, 2)], { type: "application/json" });
    const a = document.createElement("a");
    a.href = URL.createObjectURL(blob);
    a.download = `SINET_AUDIT_${new Date().toISOString().slice(0,10)}.json`;
    a.click();
    URL.revokeObjectURL(a.href);
    this.log("USER", "Audit Export", "");
  }

  
  /* ===================== v15.3 — PRESETS (Seniors + Hitno) ===================== */

  async loadSeniorPresets() {
    const url = 'data/presets/senior_presets.json';
    try {
      const res = await fetch(url, { cache: "no-store" });
      if (!res.ok) throw new Error(`${url} nije pronađen (${res.status})`);
      this.seniorPresets = await res.json();
      this.log("DATA", "Senior Presets Loaded", url);
    } catch (e) {
      this.seniorPresets = null;
      console.warn("Senior presets not loaded:", e?.message || e);
      this.log("DATA", "Senior Presets Missing", e?.message || "unknown");
    }
  }

  _norm(str) {
    return (str || "")
      .toString()
      .toLowerCase()
      .normalize('NFKD')
      .replace(/[\u0300-\u036f]/g, '')
      .replace(/[^a-z0-9čćšđž\s-]/g, ' ')
      .replace(/\s+/g, ' ')
      .trim();
  }

  
  // Home entry mode:
  // - null: show only two tabs (no content)
  // - 'firstaid': show ONLY 🧓 STARIJI — Najčešće (FULL)
  // - 'main': show full home cards (without senior list)
  setHomeEntryMode(mode) {
    this.homeEntryMode = mode || null;

    const firstaidBtn = document.getElementById('home-entry-firstaid');
    const mainBtn = document.getElementById('home-entry-main');
    const firstaidBox = document.getElementById('home-firstaid-only');
    const mainBox = document.getElementById('home-main-content');

    if (firstaidBtn) firstaidBtn.classList.toggle('active', this.homeEntryMode === 'firstaid');
    if (mainBtn) mainBtn.classList.toggle('active', this.homeEntryMode === 'main');

    if (firstaidBox) firstaidBox.style.display = (this.homeEntryMode === 'firstaid') ? 'block' : 'none';
    if (mainBox) mainBox.style.display = (this.homeEntryMode === 'main') ? 'block' : 'none';

    // Body classes (for styling entry screen vs selected mode)
    try {
      document.body.classList.toggle('home-mode-entry', !this.homeEntryMode);
      document.body.classList.toggle('home-mode-firstaid', this.homeEntryMode === 'firstaid');
      document.body.classList.toggle('home-mode-main', this.homeEntryMode === 'main');
    } catch(_) {}

    // Render senior-only list immediately when first aid is selected
    if (this.homeEntryMode === 'firstaid') {
      try { this.seniorShowAll = true; } catch(_) {}
      try { this.renderSystemPresets({ onlySenior: true, forceFull: true, titleSuffix: " (FULL)" }); } catch(_) {}
      try { if (typeof window.scrollTo === 'function') window.scrollTo({ top: 0, behavior: 'auto' }); } catch(_) {}
    }
  }

  applyHomeEntryMode() {
    // Ensure initial state is "only two tabs" (no content) until user chooses
    if (this.homeEntryMode !== 'firstaid' && this.homeEntryMode !== 'main') {
      this.setHomeEntryMode(null);
      return;
    }
    this.setHomeEntryMode(this.homeEntryMode);
  }


  renderSystemPresets(opts = {}) {
    const container = document.getElementById('presets-container');
    if (!container) return;

    let onlySenior = !!opts.onlySenior;
    let forceFull = !!opts.forceFull;
    let titleSuffix = (typeof opts.titleSuffix === 'string') ? opts.titleSuffix : '';

    // If Home is in first-aid mode, force senior-only FULL (regardless of who called this method)
    if (this.homeEntryMode === 'firstaid') {
      onlySenior = true;
      forceFull = true;
      titleSuffix = ' (FULL)';
    }

    let html = "";

    // 1) Seniors (always above HITNO)
    const g0 = this.seniorPresets?.groups?.find(g => g.id === "senior") || this.seniorPresets?.groups?.[0];
    if (g0 && Array.isArray(g0.entries) && g0.entries.length) {
      const limit = forceFull ? g0.entries.length : (this.seniorShowAll ? g0.entries.length : (Number(g0.defaultLimit) || 30));
      html += `<div class="preset-category-title">${(g0.title || "🧓 STARIJI — Najčešće") + titleSuffix}</div>`;
      html += `<div class="preset-grid">`;
      g0.entries.slice(0, limit).forEach((e, idx) => {
        const sub = (e.terms || []).slice(0, 2).join(" • ");
        html += `
          <div class="preset-chip" onclick="app.openSeniorPreset(${idx})" role="button" aria-label="Otvori ${this.escapeHtml(e.label)}">
            <div class="ico">⚡</div>
            <div class="txt">
              <div class="t">${this.escapeHtml(e.label)}</div>
              <div class="s">${this.escapeHtml(sub || "klikni za detalje")}</div>
            </div>
          </div>
        `;
      });
      html += `</div>`;

      if (!onlySenior && !forceFull && g0.entries.length > (Number(g0.defaultLimit) || 30)) {
        html += `
          <div class="preset-morebar">
            <button onclick="app.toggleSeniorShowAll()">${this.seniorShowAll ? "Prikaži manje" : `Prikaži sve (${g0.entries.length})`}</button>
            <button onclick="nav('catalog')">Otvori katalog</button>
          </div>
        `;
      }
    }

        if (!onlySenior) {

    // 2) HITNO (akutno) — derive from catalog
    const hitnoItems = (this.catalogItems || []).filter(it => {
      const g = (it.quickGroup || "").toLowerCase();
      const o = (it.oblast || "").toLowerCase();
      const id = String(it.id || "");
      return id.startsWith("sys-hitno") || g.includes("prva pomoć") || g.includes("prva pomoc") || o.includes("hitno") || g.includes("akut");
    }).slice(0, 10);

    if (hitnoItems.length) {
      html += `<div class="preset-category-title">🚨 HITNO (akutno)</div>`;
      hitnoItems.forEach(it => {
        const short = (it.opis || "").toString().trim().slice(0, 85);
        html += `
          <div class="preset-item" onclick="app.openModal('${this.escapeJsString(it.id)}')">
            <div class="preset-info">
              <div style="font-weight:900; color:#333;">⚡ ${this.escapeHtml(it.simptom)}</div>
              <div style="font-size:0.82rem; color:#777;">${this.escapeHtml(short)}</div>
            </div>
            <div class="play-icon">▶</div>
          </div>
        `;
      });
    }

        }

    container.innerHTML = html;
  }

  toggleSeniorShowAll() {
    this.seniorShowAll = !this.seniorShowAll;
    this.renderSystemPresets();
  }

  openSeniorPreset(idx) {
    const g0 = this.seniorPresets?.groups?.find(g => g.id === "senior") || this.seniorPresets?.groups?.[0];
    const entry = g0?.entries?.[Number(idx)];
    if (!entry) return;

// If catalog isn't loaded, preset resolution cannot work (common on iOS when SW-cache breaks /data fetch)
if (!Array.isArray(this.catalogItems) || this.catalogItems.length === 0) {
  const st = (this._catalogLoad && this._catalogLoad.state) ? this._catalogLoad.state : "unknown";
  this.showToast(`⚠️ Katalog nije spreman (state: ${st}). Sačekaj 5–10s ili tapni 🔄 Retry.`, {
    timeoutMs: 12000,
    action2Label: "🔄 Retry",
    action2Onclick: "window.app && window.app.retryCatalog && window.app.retryCatalog()"
  });
  return;
}

    const found = this.resolvePresetEntry(entry);
    if (found) return this.openModal(found.id);

    // Fallback: AI Upitnik (prefill)
    this.prefillAI(entry.label, "", "Nije pronađeno u katalogu — koristi AI upitnik 🙂");
    this.nav('ai');
  }

  resolvePresetEntry(entry) {
    const label = entry?.label || "";
    const lab = this._norm(label);

    // 1) Explicit preferred IDs (best)
    const preferIds = Array.isArray(entry?.preferIds) ? entry.preferIds : [];
    for (const pid of preferIds) {
      const it = this.catalogItems.find(x => x.id === pid);
      if (it) return it;
    }

    // 2) Special-case: "Zubobolja & infekcija" can incorrectly match to UTI.
    if (lab.includes("zubobolja")) {
      const z = (this.catalogItems || []).find(it =>
        this._norm(it.simptom || "").includes("zubobolja") &&
        (it.seniorQuick === true || String(it.quickGroup || "").toLowerCase().includes("zubi") || it.favoritesDefault === true)
      );
      if (z) return z;
    }

    const terms = Array.isArray(entry?.terms) ? entry.terms : [];
    const want = terms.filter(Boolean).map(t => this._norm(t)).filter(Boolean);

    // Primary term heuristic: first term OR first segment before '&' / '+' / '/'
    const primaryFromLabel = this._norm((label || "").split(/[&+\/]/)[0] || "");
    const primary = (want[0] || primaryFromLabel || "").trim();

    const allItems = (this.catalogItems || []);
    let candidates = allItems.filter(it => {
      const hay = this._norm(`${it.simptom || ""} ${it.id || ""} ${it.oblast || ""}`);
      if (want.length && want.some(t => hay.includes(t))) return true;
      if (lab && hay.includes(lab)) return true;
      if (primary && hay.includes(primary)) return true;
      return false;
    });

    // If primary exists, restrict to primary-matching candidates (prevents wrong matches)
    if (primary) {
      const strict = candidates.filter(it => {
        const hay = this._norm(`${it.simptom || ""} ${it.id || ""} ${it.oblast || ""}`);
        return hay.includes(primary);
      });
      if (strict.length) candidates = strict;
    }

    if (!candidates.length) return null;

    // Score by #term hits first, then seniorQuick/favDefault, then label hit.
    let best = null, bestScore = -1;
    for (const it of candidates) {
      const hay = this._norm(`${it.simptom || ""} ${it.id || ""} ${it.oblast || ""}`);
      let hitCount = 0;
      for (const t of want) if (t && hay.includes(t)) hitCount += 1;

      let score = hitCount * 10;
      if (primary && hay.includes(primary)) score += 6;
      if (lab && hay.includes(lab)) score += 3;

      if (it.seniorQuick === true) score += 5;
      if (it.favoritesDefault === true) score += 3;
      if (!String(it.id || "").startsWith("usr-")) score += 1;

      // Tie-breaker: longer symptom name often more specific
      score += Math.min(3, ((it.simptom || "").length / 50));

      if (score > bestScore) { bestScore = score; best = it; }
    }
    return best;
  }


  /* ===================== v15.3 — CATALOG (Oblasti-first, elder-friendly) ===================== */

  showCatalogHome() {
    this.activeOblast = null;
    const inp = document.getElementById('search-input');
    if (inp) {
      inp.value = "";
      try { if (document.activeElement === inp) inp.blur(); } catch(_) {}
    }

    // FIX v15.4.4: if user previously opened an oblast, the grid may stay hidden.
    const grid = this.ui.catalogOblastGrid || document.getElementById('catalog-oblast-grid');
    if (grid) grid.style.display = 'grid';

    this.renderCatalogOblastGrid();
    if (this.ui.catalogList) this.ui.catalogList.innerHTML = "";
  }

  renderCatalogOblastGrid() {
    const grid = this.ui.catalogOblastGrid || document.getElementById('catalog-oblast-grid');
    if (!grid) return;

    // Ensure visible in case it was hidden by an opened oblast view.
    grid.style.display = 'grid';

    const items = this.catalogItems || [];
    const counts = new Map();
    for (const it of items) {
      const o = (it.oblast || "Ostalo").toString();
      counts.set(o, (counts.get(o) || 0) + 1);
    }

    const oblasts = Array.from(counts.entries())
      .sort((a,b)=> b[1]-a[1]) // most populated first
      .slice(0, 60);

    grid.innerHTML = oblasts.map(([oblast, n]) => `
      <div class="oblast-card" onclick="app.openOblast('${this.escapeAttr(oblast)}')">
        <div class="oblast-title">📌 ${this.escapeHtml(oblast)}</div>
        <div class="oblast-count">${n} stavki</div>
        <div class="oblast-cta">👉 Klikni za listu</div>
      </div>
    `).join("");
  }

  openOblast(oblast) {
    this.activeOblast = oblast;
    const inp = document.getElementById('search-input');
    if (inp) inp.value = "";

    const filtered = (this.catalogItems || []).filter(it => (it.oblast || "Ostalo") === oblast);
    this.renderCatalogList(filtered, `Oblast: ${oblast}`, true);
  }

  renderCatalogList(items, title = "", showBack = false) {
    const container = this.ui.catalogList || document.getElementById('catalog-list');
    if (!container) return;

    // Remember last rendered list (for TXT export)
    this.lastCatalogViewItems = Array.isArray(items) ? items.slice() : [];
    this.lastCatalogViewTitle = title || "";

    const grid = this.ui.catalogOblastGrid || document.getElementById('catalog-oblast-grid');
    if (grid) grid.style.display = showBack ? "none" : "grid";

    // Backbar
    let backHtml = "";
    if (showBack) {
      backHtml = `
        <div class="catalog-backbar">
          <button class="backbtn" onclick="app.showCatalogHome()">← Oblasti</button>
          <div class="label">${this.escapeHtml(title)}</div>
        </div>
      `;
    }

    if (!items || !items.length) {
      container.innerHTML = backHtml + "<p style='text-align:center; padding:20px;'>Nema rezultata.</p>";
      return;
    }

    // Flat list cards
    const html = items.map(it => {
      const short = (it.opis || it.desc || "").toString().trim().slice(0, 110);

      const mkbCode = this.getMKB10Code(it.mkb10);
      const mkbDesc = this.getMKB10Desc(it.mkb10);
      const mkbInline = (mkbCode || mkbDesc)
        ? `🏷️ MKB-10: ${this.escapeHtml(mkbCode || "")}${(mkbCode && mkbDesc) ? " — " : ""}${this.escapeHtml((mkbDesc || "").slice(0, 80))}`
        : `🏷️ MKB-10: <span class="muted">nije popunjeno</span>`;

      const mkbBadge = mkbCode
        ? `<span class="mkb-badge" style="background:#eef3ff; font-size:0.75rem; padding:2px 6px; border-radius:7px; margin-left:6px;">${this.escapeHtml(mkbCode)}</span>`
        : '';

      const isUser = String(it.id||"").startsWith("usr-");
      const isFav = this.isFavoriteId(it.id);

      return `
        <div class="cat-item">
          <div class="cat-main" onclick="app.openModal('${this.escapeJsString(it.id)}')">
            <div class="cat-title">${isUser ? "🧩 " : ""}${this.escapeHtml(it.simptom)} ${mkbBadge}</div>
            <div class="cat-sub">${this.escapeHtml(short || "Klikni za detalje i frekvencije.")}</div>
            <div class="cat-sub" style="margin-top:3px;">${mkbInline}</div>
          </div>
          <div class="cat-actions">
            <button class="btn-add-playlist" title="Dodaj u listu" aria-label="Dodaj u listu" onclick="event.stopPropagation(); app.quickAddToPlaylist('${this.escapeJsString(it.id)}')">➕ <span class="btn-label">U listu</span></button>
            <button class="cat-mini-btn btn-fav" title="${isFav ? "Ukloni iz favorita" : "Dodaj u favorite"}" aria-label="Favorit" aria-pressed="${isFav ? "true" : "false"}" onclick="event.stopPropagation(); app.toggleFavoriteQuick('${this.escapeJsString(it.id)}', this)">${isFav ? "⭐" : "☆"} <span class="btn-label">Favorit</span></button>
            <button class="cat-mini-btn" title="Dodaj u Moje simptome" onclick="event.stopPropagation(); app.quickAddToMySymptoms('${this.escapeJsString(it.id)}')">🧬 <span class="btn-label">Moje</span></button>
            <button class="cat-mini-btn" title="Napravi protokol" onclick="event.stopPropagation(); app.quickCreateProtocolFromItem('${this.escapeJsString(it.id)}')">📑 <span class="btn-label">Protokol</span></button>
            ${isUser ? `<button class="cat-mini-btn" title="Izmeni" onclick="event.stopPropagation(); app.editUserSymptom('${this.escapeJsString(it.id)}')">✎ <span class="btn-label">Edit</span></button>` : `<button class="cat-mini-btn" title="AI upitnik" onclick="event.stopPropagation(); app.prefillAI('${this.escapeJsString(it.simptom)}', '${this.escapeJsString(short)}'); nav('ai');">🤖 <span class="btn-label">AI</span></button>`}
          </div>
        </div>
      `;
    }).join("");

    container.innerHTML = backHtml + html;
  }


  filterCatalog(query = null) {
    const q = (query !== null ? query : (document.getElementById('search-input')?.value || "")).toString().trim();
    const nq = this._norm(q);

    if (!nq) {
      if (this.activeOblast) return this.openOblast(this.activeOblast);
      return this.showCatalogHome();
    }

    const items = (this.catalogItems || []).filter(it => {
      const mkb = `${this.getMKB10Code(it.mkb10)} ${this.getMKB10Desc(it.mkb10)} ${this.getMKB10Url(it.mkb10)}`;
      const hay = this._norm(`${it.simptom || ""} ${it.oblast || ""} ${it.opis || ""} ${mkb}`);
      return hay.includes(nq);
    });

    this.renderCatalogList(items, `Rezultati: ${q}`, true);
  }


  /* ===================== v15.4.5 — CATALOG TXT EXPORT ===================== */

  exportCatalogTxt(mode = "view") {
    try {
      const items = this._getCatalogExportItems(mode);
      if (!items || !items.length) return alert("Nema stavki za izvoz.");

      const now = new Date();
      const date = now.toISOString().slice(0, 10);

      const scopeLabel = (mode === "all")
        ? "CELOKUPAN KATALOG"
        : (this.lastCatalogViewTitle || this.activeOblast ? ("PRIKAZ: " + (this.lastCatalogViewTitle || this.activeOblast)) : "PRIKAZ");

      const txt = this._formatCatalogTxt(items, {
        date,
        scopeLabel
      });

      const fnameSafe = (mode === "all" ? "SVE" : "PRIKAZ");
      const filename = `SINET_KATALOG_${fnameSafe}_${date}.txt`;
      this._downloadText(txt, filename);

      this.log("USER", "Catalog TXT Export", `${mode} • ${items.length}`);
    } catch (e) {
      alert("Izvoz TXT nije uspeo: " + (e?.message || e));
      this.log("ERROR", "Catalog TXT Export Failed", e?.message || "unknown");
    }
  }

  _getCatalogExportItems(mode = "view") {
    const all = Array.isArray(this.catalogItems) ? this.catalogItems.slice() : [];

    if (mode === "all") return all;

    // Prefer exact current view items (oblast list or search results)
    if (Array.isArray(this.lastCatalogViewItems) && this.lastCatalogViewItems.length) {
      return this.lastCatalogViewItems.slice();
    }

    // Fallback: active oblast or search input
    const q = (document.getElementById('search-input')?.value || "").toString().trim();
    const nq = this._norm(q);

    if (nq) {
      return all.filter(it => {
        const hay = this._norm(`${it.simptom || ""} ${it.oblast || ""} ${it.opis || ""} ${this.getMKB10Code(it.mkb10) || ""} ${this.getMKB10Desc(it.mkb10) || ""}`);
        return hay.includes(nq);
      });
    }

    if (this.activeOblast) return all.filter(it => (it.oblast || "Ostalo") === this.activeOblast);

    return all;
  }

  _formatCatalogTxt(items, ctx = {}) {
    const date = ctx.date || new Date().toISOString().slice(0,10);
    const scopeLabel = ctx.scopeLabel || "KATALOG";

    // Group by oblast
    const groups = new Map();
    for (const it of items) {
      const oblast = (it.oblast || "Ostalo").toString();
      if (!groups.has(oblast)) groups.set(oblast, []);
      groups.get(oblast).push(it);
    }

    const oblastNames = Array.from(groups.keys()).sort((a,b)=> a.localeCompare(b, 'sr', { sensitivity:'base' }));

    let out = "";
    out += "==================================================\n";
    out += "SINET AUDIO LEKAR - EXPORT KATALOGA\n";
    out += "==================================================\n";
    out += `Datum: ${date}\n`;
    out += `Opseg: ${scopeLabel}\n`;
    out += `Stavki: ${items.length}\n`;
    out += "==================================================\n\n";

    for (const oblast of oblastNames) {
      const arr = groups.get(oblast) || [];
      arr.sort((a,b)=> (a.simptom||"").localeCompare(b.simptom||"", 'sr', { sensitivity:'base' }));

      out += "##################################################\n";
      out += `OBLAST: ${oblast}\n`;
      out += "##################################################\n\n";

      let n = 1;
      for (const it of arr) {
        const naziv = (it.simptom || it.naziv || "").toString().trim();
        const opis = (it.opis || it.desc || "").toString().trim();
        const id = (it.id || "").toString().trim();

        const mkbCode = this.getMKB10Code(it.mkb10);
        const mkbDesc = this.getMKB10Desc(it.mkb10);
        const mkbUrl  = this.getMKB10Url ? this.getMKB10Url(it.mkb10) : "";

        const h = it.holisticki || {};
        const psi = h.psihosomatika || {};
        const af  = h.afirmacija || {};
        const mol = h.molitva || h.duhovnost || {};
        const narOpis = (h.narodni_lek && (h.narodni_lek.opis || h.narodni_lek.tekst)) ? (h.narodni_lek.opis || h.narodni_lek.tekst) : (h.saveti?.narodno || "");

        const mkbLine = `MKB-10: code="${(mkbCode||"").toString().replace(/"/g,"'")}" | opis="${(mkbDesc||"").toString().replace(/"/g,"'")}" | url="${(mkbUrl||"").toString().replace(/"/g,"'")}"`;

        // Frequencies — full fields (even if empty)
        const freqsIn = Array.isArray(it.frekvencije) ? it.frekvencije : [];
        const freqLines = freqsIn
          .map(f => {
            const hz = Number(f?.hz ?? f?.value ?? 0) || 0;
            if (!hz) return null;
            const nazivF = (f?.naziv || "").toString().trim().replace(/"/g,"'");
            const opisF  = (f?.opis || "").toString().trim().replace(/"/g,"'");
            const funF   = (f?.funkcija || f?.svrha || f?.desc || "").toString().trim().replace(/"/g,"'");
            const srcF   = (f?.izvor || f?.src || "").toString().trim().replace(/"/g,"'");
            return `   - ${hz} Hz | naziv="${nazivF}" | opis="${opisF}" | funkcija="${funF}" | izvor="${srcF}"`;
          })
          .filter(Boolean);

        out += `${n}. ${naziv || "(bez naziva)"}\n`;
        out += "   ----------------------------------------------\n";
        if (id) out += `   ID: ${id}\n`;
        out += `   OPIS: ${(opis || "").toString().replace(/\n/g," ")}\n`;
        out += `   ${mkbLine}\n`;

        out += `   PSIHOSOMATIKA: uzrok="${(psi.uzrok||"").toString().replace(/"/g,"'")}" | lek="${(psi.lek||"").toString().replace(/"/g,"'")}"\n`;
        out += `   AFIRMACIJA: tekst="${(af.tekst||"").toString().replace(/"/g,"'")}" | autor="${(af.autor||"").toString().replace(/"/g,"'")}" | izvor="${(af.izvor||"").toString().replace(/"/g,"'")}"\n`;
        out += `   MOLITVA: tekst="${(mol.tekst||"").toString().replace(/"/g,"'")}" | izvor="${(mol.izvor||"").toString().replace(/"/g,"'")}"\n`;
        out += `   NARODNI_LEK: opis="${(narOpis||"").toString().replace(/"/g,"'")}"\n`;

        if (freqLines.length) {
          out += "   FREKVENCIJE:\n";
          out += freqLines.join("\n") + "\n";
        } else {
          out += "   FREKVENCIJE: (nema)\n";
        }

        out += "   ----------------------------------------------\n\n";
        n++;
      }

      out += "\n";
    }

    return out;
  }

  _wrapTxtLine(line, maxLen = 120, indent = "   ") {
    // Keeps words/segments split on ', ' where possible
    if (!line || line.length <= maxLen) return line + "\n";
    const parts = line.split(", ");
    let out = "";
    let cur = "";
    for (let i=0; i<parts.length; i++) {
      const seg = (i===0) ? parts[i] : (", " + parts[i]);
      if ((cur + seg).length <= maxLen) {
        cur += seg;
      } else {
        out += cur + "\n";
        cur = indent + parts[i];
      }
    }
    if (cur) out += cur + "\n";
    return out;
  }

  _downloadText(text, filename) {
    const blob = new Blob([text], { type: "text/plain;charset=utf-8" });
    const a = document.createElement("a");
    a.href = URL.createObjectURL(blob);
    a.download = filename;
    a.click();
    URL.revokeObjectURL(a.href);
  }




  /* ===================== v15.4.7.4 — STL v1.1 JSON EXPORT ===================== */

  exportCatalogSTL(mode = "view") {
    try {
      const items = this._getCatalogExportItems(mode);
      if (!items || !items.length) return alert("Nema stavki za izvoz.");

      const now = new Date();
      const date = now.toISOString().slice(0, 10);

      const scopeLabel = (mode === "all")
        ? "CELOKUPAN KATALOG"
        : (this.lastCatalogViewTitle || this.activeOblast ? ("PRIKAZ: " + (this.lastCatalogViewTitle || this.activeOblast)) : "PRIKAZ");

      const stl = this._buildSTLPayload(items, { mode, scopeLabel });

      const fnameSafe = (mode === "all" ? "SVE" : "PRIKAZ");
      const filename = `SINET_STL_${fnameSafe}_${date}.json`;

      this._downloadJson(stl, filename);
      this.log("USER", "Catalog STL Export", `${mode} • ${items.length}`);
    } catch (e) {
      alert("Izvoz STL JSON nije uspeo: " + (e?.message || e));
      this.log("ERROR", "Catalog STL Export Failed", e?.message || "unknown");
    }
  }


  /* ===================== v15.7.6.7 — STL export for user-generated content ===================== */

  exportUserSymptomsSTL() {
    try {
      const items = Array.isArray(this.userSymptoms) ? this.userSymptoms : [];
      if (!items.length) return alert("Nema stavki u 'Moji simptomi'.");

      const now = new Date();
      const date = now.toISOString().slice(0, 10);
      const stl = this._buildSTLPayload(items, {
        mode: "user",
        scopeLabel: "MOJI SIMPTOMI (user-generated)",
        sourceFile: "SINET_USER_SYMPTOMS"
      });

      const filename = `SINET_USER_SYMPTOMS_STL_${date}.json`;
      this._downloadJson(stl, filename);
      this.showToast("⬇️ Izvezen STL (Moji simptomi)", { actionLabel: "Moji simptomi", actionNav: "mysymptoms" });
      this.log("USER", "UserSymptoms STL Export", String(items.length));
    } catch (e) {
      alert("Izvoz nije uspeo: " + (e?.message || e));
      this.log("ERROR", "UserSymptoms STL Export Failed", e?.message || "unknown");
    }
  }

  exportCurrentAsSTL() {
    try {
      const id = this.currentModalId;
      if (!id) return alert("Nije izabrana stavka.");
      const it = (this.catalogItems || []).find(x => String(x?.id) === String(id));
      if (!it) return alert("Ne mogu da nađem stavku.");

      const now = new Date();
      const date = now.toISOString().slice(0, 10);
      const title = (it.simptom || it.naziv || "Stavka").toString().trim();
      const stl = this._buildSTLPayload([it], {
        mode: "single",
        scopeLabel: `SINGLE • ${title}`,
        sourceFile: "SINET_SINGLE_ITEM"
      });

      const safeId = String(it.id || "item").replace(/[^a-z0-9\-_.]/gi, "_");
      const filename = `SINET_ITEM_${safeId}_${date}.json`;
      this._downloadJson(stl, filename);
      this.showToast("💾 Preuzet STL (standard)", { actionLabel: "Moji simptomi", actionNav: "mysymptoms" });
      this.log("USER", "Single Item STL Export", safeId);
    } catch (e) {
      alert("Izvoz nije uspeo: " + (e?.message || e));
      this.log("ERROR", "Single Item STL Export Failed", e?.message || "unknown");
    }
  }

  _downloadJson(obj, filename) {
    const text = JSON.stringify(obj, null, 2);
    const blob = new Blob([text], { type: "application/json;charset=utf-8" });
    const a = document.createElement("a");
    a.href = URL.createObjectURL(blob);
    a.download = filename;
    a.click();
    URL.revokeObjectURL(a.href);
  }

  _buildSTLPayload(items, ctx = {}) {
    const now = new Date();
    const ts = now.toISOString();

    // Summary stats
    let freqTotal = 0;
    for (const it of items) {
      const freqs = Array.isArray(it?.frekvencije) ? it.frekvencije : [];
      freqTotal += freqs.filter(f => Number(f?.value ?? f?.hz ?? 0) > 0).length;
    }

    const simptomi = items.map(it => this._runtimeItemToSTL(it)).filter(Boolean);

	    const meta = {
      schema: "SINET_STL",
      version: "1.1",
      generatedAt: ts,
	      sourceFile: ctx.sourceFile || ((ctx.mode === "all") ? "SINET_CATALOG (ALL)" : "SINET_CATALOG (VIEW)"),
      notes: ctx.scopeLabel || "",
      audit: [{
        ts,
        tool: "SINET Audio Lekar",
	        toolVersion: SINET_APP_VERSION,
        mode: "manual",
        providers: [],
        summary: { simptomi: simptomi.length, freqTotal }
      }]
    };

    return { meta, simptomi };
  }

  _runtimeItemToSTL(it) {
    if (!it || !it.id) return null;

    const id = String(it.id).trim();
    const naziv = (it.simptom || it.naziv || "").toString();
    const opis = (it.opis || it.desc || "").toString();

    // Prefer rich object if present
    const mkbObj = it.mkb10_obj || it.mkb10 || null;

    // Holistics (runtime uses holisticki.*)
    const h = it.holisticki || {};
    const psi = h.psihosomatika || {};
    const af  = h.afirmacija || {};
    const mol = h.molitva || h.duhovnost || {};
    const nar = (h.narodni_lek?.opis !== undefined)
      ? h.narodni_lek
      : { opis: (h.saveti?.narodno || "") };

    // Frekvencije
    const perMin = Number(it.freq_duration_min ?? it.trajanjePoFrekvencijiMin ?? it.trajanje_po_frekvenciji_min ?? 0) || null;
    const freqsIn = Array.isArray(it.frekvencije) ? it.frekvencije : [];
    const frekvencije = freqsIn
      .map(f => this._runtimeFreqToSTL(f, perMin))
      .filter(Boolean);

    // Ensure stable full shape (even if empty)
    const stl = {
      id,
      uid: (it.uid !== undefined) ? it.uid : null,
      naziv: naziv || "",
      opis: opis || "",
      mkb10: this._mkb10ToCanonical(mkbObj),

      psihosomatika: {
        uzrok: (psi.uzrok || "").toString(),
        lek: (psi.lek || "").toString()
      },

      afirmacija: {
        tekst: (af.tekst || "").toString(),
        autor: (af.autor || "").toString(),
        izvor: (af.izvor || "").toString()
      },

      molitva: {
        tekst: (mol.tekst || "").toString(),
        izvor: (mol.izvor || "").toString()
      },

      narodni_lek: {
        opis: (nar.opis || nar.tekst || "").toString()
      },

      akupresura: (it.akupresura !== undefined ? it.akupresura : null),

      frekvencije,

      extensions: {
        player: {
          trajanje_po_frekvenciji_min: perMin,
          ukupno_trajanje_min: Number(it.ukupnoTrajanjeMin ?? 0) || null
        },
        runtime: {
          oblast: (it.oblast || "").toString(),
          podOblast: (it.podOblast || "").toString()
        }
      }
    };

    // If item was derived from STL earlier, preserve richer blocks if present,
    // but keep canonical keys stable (do NOT remove required canonical keys).
    if (it._stl && typeof it._stl === "object") {
      // Preserve akupresura if it exists in _stl and current is null/empty
      if ((stl.akupresura === null || stl.akupresura === undefined) && it._stl.akupresura !== undefined) {
        stl.akupresura = it._stl.akupresura;
      }
    }

    return stl;
  }

  _mkb10ToCanonical(mkb) {
    // Canonical target:
    // { code: "", opis: "", izvor?: { sistem,url,autor,delo,licenca } }
    if (!mkb) return { code: "", opis: "", izvor: { sistem: "ICD-10 / MKB-10", url: "", autor: "", delo: "", licenca: "" } };

    if (typeof mkb === "string") {
      return { code: mkb.trim(), opis: "", izvor: { sistem: "ICD-10 / MKB-10", url: "", autor: "", delo: "", licenca: "" } };
    }

    if (typeof mkb !== "object") {
      return { code: String(mkb), opis: "", izvor: { sistem: "ICD-10 / MKB-10", url: "", autor: "", delo: "", licenca: "" } };
    }

    const code = (mkb.code || mkb.sifra || mkb.sifra_mkb || mkb.icd10 || "").toString().trim();
    const opis = (mkb.opis || mkb.naziv || mkb.desc || "").toString().trim();
    const iz = mkb.izvor || {};
    const izvor = {
      sistem: (iz.sistem || iz.system || "ICD-10 / MKB-10").toString(),
      url: (iz.url || iz.URL || iz.link || "").toString(),
      autor: (iz.autor || "").toString(),
      delo: (iz.delo || "").toString(),
      licenca: (iz.licenca || "").toString()
    };
    return { code, opis, izvor };
  }

  _runtimeFreqToSTL(f, perMin = null) {
    if (!f) return null;
    const hz = Number(f.hz ?? f.value ?? 0);
    if (!Number.isFinite(hz) || hz <= 0) return null;

    const funkcija = (f.funkcija || f.svrha || f.desc || f.naziv || "").toString().trim();
    const opis = (f.opis || "").toString().trim();
    const naziv = (f.naziv || "").toString().trim();

    const izObj = f.izvor_obj || f.izvor || f.src || null;
    const izvor = this._sourceToObj(izObj);

    const out = {
      hz,
      naziv: naziv || "",
      opis: opis || "",
      funkcija: funkcija || "",
      izvor,
      enabled: f.enabled !== false
    };

    // Optional: keep per-frequency duration hint if known
    if (perMin !== null) out.trajanje_min = perMin;

    return out;
  }

  _sourceToObj(src) {
    const blank = { sistem: "", url: "", autor: "", delo: "", licenca: "" };
    if (!src) return { ...blank };

    if (typeof src === "string") {
      const s = src.trim();
      if (!s) return { ...blank };
      if (/^https?:\/\//i.test(s)) return { ...blank, url: s };
      return { ...blank, sistem: s };
    }

    if (typeof src !== "object") return { ...blank, sistem: String(src) };

    return {
      sistem: (src.sistem || src.system || src.tradicija || "").toString(),
      url: (src.url || src.URL || src.link || "").toString(),
      autor: (src.autor || "").toString(),
      delo: (src.delo || "").toString(),
      licenca: (src.licenca || "").toString()
    };
  }


  /* ===================== v15.3 — USER DATA (My Symptoms + Overrides) ===================== */

  async loadUserSymptoms() {
    try {
      const raw = localStorage.getItem("sinet_user_symptoms_v1");
      this.userSymptoms = raw ? JSON.parse(raw) : [];
      if (!Array.isArray(this.userSymptoms)) this.userSymptoms = [];
    } catch (e) {
      this.userSymptoms = [];
    }
  }

  async saveUserSymptoms() {
    try {
      localStorage.setItem("sinet_user_symptoms_v1", JSON.stringify(this.userSymptoms || []));
    } catch (e) {}
  }

  async loadOverrides() {
    try {
      const raw = localStorage.getItem("sinet_overrides_v1");
      this.overrides = raw ? JSON.parse(raw) : {};
      if (!this.overrides || typeof this.overrides !== "object") this.overrides = {};
    } catch (e) {
      this.overrides = {};
    }
  }

  async saveOverrides() {
    try { localStorage.setItem("sinet_overrides_v1", JSON.stringify(this.overrides || {})); } catch (e) {}
  }

  _deepMerge(base, patch) {
    if (Array.isArray(base) || Array.isArray(patch)) return patch; // replace arrays
    if (typeof base !== "object" || base === null) return patch;
    if (typeof patch !== "object" || patch === null) return patch;

    const out = { ...base };
    for (const k of Object.keys(patch)) {
      if (k in base) out[k] = this._deepMerge(base[k], patch[k]);
      else out[k] = patch[k];
    }
    return out;
  }

  applyUserDataToCatalog() {
    const core = Array.isArray(this.catalogCoreItems) ? this.catalogCoreItems.slice() : (this.catalogItems || []).slice();

    // Apply overrides to core
    let merged = core.map(it => {
      const ov = this.overrides?.[it.id];
      return ov ? this._deepMerge(it, ov) : it;
    });

    // Remove existing user items, then append latest
    merged = merged.filter(it => !String(it.id||"").startsWith("usr-"));
    const us = Array.isArray(this.userSymptoms) ? this.userSymptoms : [];
    merged = merged.concat(us);

    // De-duplicate by id
    const seen = new Set();
    const out = [];
    for (const it of merged) {
      const id = String(it?.id || "");
      if (!id) continue;
      if (seen.has(id)) continue;
      seen.add(id);
      out.push(it);
    }

    this.catalogItems = out;
    try { this.renderWorkbenchPicker((document.getElementById('wb-filter')||{}).value || ''); } catch(_) {}
  }

  /* ===================== MY SYMPTOMS SELECTION ===================== */

  _ensureUserSymptomsSelection() {
    if (!this.userSymptomsSelected || !(this.userSymptomsSelected instanceof Set)) this.userSymptomsSelected = new Set();
  }

  toggleUserSymptomSelect(id, checked) {
    this._ensureUserSymptomsSelection();
    const key = String(id || '');
    if (!key) return;
    if (checked) this.userSymptomsSelected.add(key);
    else this.userSymptomsSelected.delete(key);
    this._renderUserSymptomsSelectionHint();
  }

  _getUserSymptomsItems() {
    return Array.isArray(this.userSymptoms) ? this.userSymptoms.map(it => window.SINET_FrequencySchema?.normalizeItem(it) || it).filter(Boolean) : [];
  }

  _getSelectedUserSymptomsItems() {
    this._ensureUserSymptomsSelection();
    const sel = this.userSymptomsSelected;
    return this._getUserSymptomsItems().filter(it => sel.has(String(it.id)));
  }

  selectAllUserSymptoms() {
    this._ensureUserSymptomsSelection();
    for (const it of this._getUserSymptomsItems()) this.userSymptomsSelected.add(String(it.id));
    this.renderUserSymptomsList();
  }

  clearUserSymptomsSelection() {
    this._ensureUserSymptomsSelection();
    this.userSymptomsSelected.clear();
    this.renderUserSymptomsList();
  }

  _renderUserSymptomsSelectionHint() {
    const el = document.getElementById('mys-selected-count');
    if (!el) return;
    const n = this._getSelectedUserSymptomsItems().length;
    el.innerText = n ? `Izabrano: ${n}` : '';
    const box = document.getElementById('mys-actions');
    if (box) box.style.display = this._getUserSymptomsItems().length ? 'block' : 'none';
    this._setControlValueIfExists('mys-play-mode', this.playMode || PLAY_MODE_SEQ);
  }

  startUserSymptomsSelected() {
    const picked = this._getSelectedUserSymptomsItems();
    if (!picked.length) {
      this.showToast('ℹ️ Nisi izabrao nijedan lični simptom.', { timeoutMs: 4500 });
      return;
    }
    this._startTemporaryPlaylist(picked, 'mys-play-mode', 'mysymptoms');
  }

  createProtocolFromUserSymptomsSelected() {
    const picked = this._getSelectedUserSymptomsItems();
    if (!picked.length) {
      this.showToast('ℹ️ Nisi izabrao nijedan lični simptom.', { timeoutMs: 4500 });
      return;
    }
    this._createProtocolFromItems(picked, 'Moji simptomi');
  }


  renderUserSymptomsUI() {
    // refresh from storage in case it changed
    this.loadUserSymptoms().then(() => {
      this.applyUserDataToCatalog();
      this.renderUserSymptomsList();
      this._renderUserSymptomsSelectionHint();
    });
  }

  renderUserSymptomsList() {
    const box = this.ui.userSymptomsList || document.getElementById('user-symptoms-list');
    if (!box) return;

    this._ensureUserSymptomsSelection();
    const list = this._getUserSymptomsItems();
    if (!list.length) {
      const actions = document.getElementById('mys-actions');
      if (actions) actions.style.display = 'none';
      const count = document.getElementById('mys-selected-count');
      if (count) count.innerText = '';
      box.innerHTML = "<p style='text-align:center; color:#777;'>Još nema ličnih simptoma. Kliknite na dugme iznad da dodate. 🙂</p>";
      return;
    }

    const validIds = new Set(list.map(it => String(it.id)));
    this.userSymptomsSelected = new Set(Array.from(this.userSymptomsSelected).filter(id => validIds.has(String(id))));
    this._setControlValueIfExists('mys-play-mode', this.playMode || PLAY_MODE_SEQ);

    box.innerHTML = list.map(it => {
      const normalized = window.SINET_FrequencySchema?.normalizeItem(it) || it;
      const short = (normalized.opis || "").toString().slice(0, 120);
      const mkb = this.getMKB10Code?.(normalized.mkb10) || (typeof normalized.mkb10 === 'string' ? normalized.mkb10 : '');
      const freqCount = Array.isArray(normalized.frekvencije) ? normalized.frekvencije.length : 0;
      const meta = [mkb ? `🏷 ${mkb}` : '', freqCount ? `🎵 ${freqCount} frekv.` : ''].filter(Boolean).join(' • ');
      const checked = this.userSymptomsSelected.has(String(normalized.id)) ? 'checked' : '';
      return `
        <div class="cat-item">
          <div style="margin-right:10px; display:flex; align-items:center;">
            <input type="checkbox" class="pl-sel" ${checked}
              onclick="event.stopPropagation(); window.app && window.app.toggleUserSymptomSelect && window.app.toggleUserSymptomSelect('${this.escapeJsString(normalized.id)}', this.checked)">
          </div>
          <div class="cat-main" onclick="app.openModal('${this.escapeJsString(normalized.id)}')">
            <div class="cat-title">🧩 ${this.escapeHtml(normalized.simptom || normalized.naziv || '')}</div>
            <div class="cat-sub">${this.escapeHtml(short || "Klikni za detalje.")}</div>
            ${meta ? `<div class="cat-sub" style="margin-top:4px; color:#54708a;">${this.escapeHtml(meta)}</div>` : ''}
          </div>
          <div class="cat-actions">
            <button class="cat-mini-btn" title="Pusti odmah" onclick="event.stopPropagation(); app.playItemNow('${this.escapeJsString(normalized.id)}')">▶ <span class="btn-label">Pusti</span></button>
            <button class="btn-add-playlist" title="Dodaj u listu" onclick="event.stopPropagation(); app.quickAddToPlaylist('${this.escapeJsString(normalized.id)}')">➕ <span class="btn-label">U listu</span></button>
            <button class="cat-mini-btn btn-fav" title="${app.isFavoriteId && app.isFavoriteId(normalized.id) ? "Ukloni iz favorita" : "Dodaj u favorite"}" aria-label="Favorit" aria-pressed="${app.isFavoriteId && app.isFavoriteId(normalized.id) ? "true":"false"}" onclick="event.stopPropagation(); app.toggleFavoriteQuick('${this.escapeJsString(normalized.id)}', this)">${app.isFavoriteId && app.isFavoriteId(normalized.id) ? "⭐":"☆"} <span class="btn-label">Favorit</span></button>
            <button class="cat-mini-btn" title="Sačuvaj kao protokol" onclick="event.stopPropagation(); app.quickCreateProtocolFromItem('${this.escapeJsString(normalized.id)}')">📑 <span class="btn-label">Protokol</span></button>
            <button class="cat-mini-btn" title="Izmeni" onclick="event.stopPropagation(); app.editUserSymptom('${this.escapeJsString(normalized.id)}')">✎ <span class="btn-label">Edit</span></button>
            <button class="cat-mini-btn" title="Obriši" onclick="event.stopPropagation(); app.deleteUserSymptom('${this.escapeJsString(normalized.id)}')">🗑</button>
          </div>
        </div>
      `;
    }).join("");

    this._renderUserSymptomsSelectionHint();
  }

  showUserSymptomForm(editId = null) {
    const host = this.ui.userSymptomForm || document.getElementById('user-symptom-form');
    if (!host) return;
    host.style.display = "block";

    const existing = editId ? (this.userSymptoms || []).find(x => x.id === editId) : null;
    const normalizedExisting = window.SINET_FrequencySchema?.normalizeItem(existing || {}) || existing || {};

    const oblasts = Array.from(new Set((this.catalogItems || []).map(i => i.oblast).filter(Boolean))).sort();
    const oblastOptions = ['MOJI SIMPTOMI', ...oblasts].slice(0, 80)
      .map(o => `<option value="${this.escapeAttr(o)}"${(normalizedExisting?.oblast||"MOJI SIMPTOMI")===o ? " selected":""}>${this.escapeHtml(o)}</option>`).join("");

    const freqs = Array.isArray(normalizedExisting?.frekvencije) && normalizedExisting.frekvencije.length ? normalizedExisting.frekvencije : [{}];
    const freqRows = window.SINET_ItemShell?.renderFrequencyEditorRows ? window.SINET_ItemShell.renderFrequencyEditorRows(freqs) : '';
    const mkbText = typeof normalizedExisting?.mkb10 === 'string' ? normalizedExisting.mkb10 : this._mkbToText(normalizedExisting?.mkb10 || '');
    const metaRibbon = window.SINET_ItemShell?.renderMetaRibbon ? window.SINET_ItemShell.renderMetaRibbon({ oblast: normalizedExisting?.oblast || 'MOJI SIMPTOMI', frekvencije: freqs }, { mkbText }) : '';

    host.innerHTML = `
      <div style="background:#fff; border:1px solid #eee; border-radius:12px; padding:12px; margin-top:10px;">
        <div style="font-weight:900; margin-bottom:8px;">${existing ? "✎ Izmeni simptom" : "➕ Novi simptom"}</div>
        ${metaRibbon}

        <label style="font-weight:900;">Naziv</label>
        <input id="us_name" value="${this.escapeAttr(normalizedExisting?.simptom || normalizedExisting?.naziv || "")}" placeholder="npr. Reumatoidni artritis" style="width:100%; padding:12px; border:1px solid #ddd; border-radius:8px; margin:6px 0 10px 0;">

        <label style="font-weight:900;">Oblast</label>
        <select id="us_oblast" style="width:100%; padding:12px; border:1px solid #ddd; border-radius:8px; margin:6px 0 10px 0;">
          ${oblastOptions}
        </select>

        <label style="font-weight:900;">Opis</label>
        <textarea id="us_desc" rows="3" placeholder="Kratko objašnjenje..." style="width:100%; padding:12px; border:1px solid #ddd; border-radius:8px; margin:6px 0 10px 0;">${this.escapeHtml(normalizedExisting?.opis || "")}</textarea>

        <button class="btn-full" style="margin-top:-2px; background:#3498db;" onclick="app.userSymptomToAI()">🤖 Dopuni preko AI (prompt)</button>
        <p style="margin:8px 0 12px 0; color:#777; font-size:0.82rem;">
          AI će napraviti kompletan STL JSON. Posle kopiranja prompta, zalepi AI odgovor u AI Upitnik i klikni „Uvezi u Moji simptomi“.
        </p>

        <div style="display:flex; gap:10px;">
          <div style="flex:1;">
            <label style="font-weight:900;">MKB-10 (opciono)</label>
            <input id="us_mkb" value="${this.escapeAttr(mkbText || "")}" placeholder="npr. M06.9" style="width:100%; padding:12px; border:1px solid #ddd; border-radius:8px; margin:6px 0 10px 0;">
          </div>
          <div style="flex:1;">
            <label style="font-weight:900;">Izvor (opciono)</label>
            <input id="us_src" value="${this.escapeAttr(normalizedExisting?.izvor || "")}" placeholder="link ili tekst" style="width:100%; padding:12px; border:1px solid #ddd; border-radius:8px; margin:6px 0 10px 0;">
          </div>
        </div>

        <div style="font-weight:900; margin:8px 0;">Frekvencije</div>
        <div style="color:#5f778c; font-size:0.88rem; margin:-2px 0 10px 0;">Za svaku frekvenciju sada unosimo: Hz, naziv, opis/funkciju i izvor.</div>
        <div id="us_freqs">
          ${freqRows}
        </div>

        <div style="display:flex; gap:10px; margin-top:10px;">
          <button class="btn-full" style="flex:1; margin-top:0; background:#2ecc71;" onclick="app.addUserFreqRow()">➕ Dodaj frekvenciju</button>
          <button class="btn-full" style="flex:1; margin-top:0; background:#8e44ad;" onclick="app.saveUserSymptom('${existing?.id || ""}')">💾 Sačuvaj</button>
        </div>
        <button class="btn-full" style="margin-top:10px; background:#eee; color:#333;" onclick="app.cancelUserSymptomForm()">Zatvori</button>
      </div>
    `;
    this._userFormEditingId = existing?.id || null;
  }

  addUserFreqRow() {
    const box = document.getElementById('us_freqs');
    if (!box) return;
    const i = box.querySelectorAll('[data-fhz]').length;
    const row = document.createElement('div');
    row.innerHTML = window.SINET_ItemShell?.renderFrequencyEditorRows
      ? window.SINET_ItemShell.renderFrequencyEditorRows([{ hz:'', naziv:'', funkcija:'', izvor:'' }])
          .replaceAll('data-fhz="0"', `data-fhz="${i}"`)
          .replaceAll('data-fnm="0"', `data-fnm="${i}"`)
          .replaceAll('data-fdc="0"', `data-fdc="${i}"`)
          .replaceAll('data-fsrc="0"', `data-fsrc="${i}"`)
      : `
        <div style="display:flex; gap:8px; margin:6px 0;">
          <input data-fhz="${i}" placeholder="Hz" value="" style="width:90px; padding:10px; border:1px solid #ddd; border-radius:8px;">
          <input data-fnm="${i}" placeholder="Naziv frekvencije" value="" style="flex:1; padding:10px; border:1px solid #ddd; border-radius:8px;">
        </div>
        <textarea data-fdc="${i}" placeholder="Opis / funkcija" style="width:100%; padding:10px; border:1px solid #ddd; border-radius:8px; margin:6px 0;"></textarea>
        <input data-fsrc="${i}" placeholder="Izvor (link ili tekst)" value="" style="width:100%; padding:10px; border:1px solid #ddd; border-radius:8px; margin-bottom:6px;">
      `;
    box.appendChild(row);
  }

  cancelUserSymptomForm() {
    const host = this.ui.userSymptomForm || document.getElementById('user-symptom-form');
    if (host) { host.style.display = "none"; host.innerHTML = ""; }
    this._userFormEditingId = null;
  }

  _slugify(name) {
    return this._norm(name).replace(/\s+/g, '-').replace(/-+/g,'-').replace(/^-|-$/g,'') || "symptom";
  }

  async saveUserSymptom(existingId = "") {
    try {
      const name = document.getElementById('us_name')?.value?.trim();
      if (!name) return alert("Unesite naziv simptoma.");
      const oblast = document.getElementById('us_oblast')?.value || "MOJI SIMPTOMI";
      const desc = document.getElementById('us_desc')?.value || "";
      const mkb10 = document.getElementById('us_mkb')?.value?.trim() || "";
      const src = document.getElementById('us_src')?.value?.trim() || "";

      // collect freqs
      const hzInputs = Array.from(document.querySelectorAll('[data-fhz]'));
      const freqs = hzInputs.map(inp => {
        const i = inp.getAttribute('data-fhz');
        const hz = inp.value?.trim();
        const naziv = document.querySelector(`[data-fnm="${i}"]`)?.value?.trim() || "";
        const funkcija = document.querySelector(`[data-fdc="${i}"]`)?.value?.trim() || "";
        const izvor = document.querySelector(`[data-fsrc="${i}"]`)?.value?.trim() || "";
        if (!hz && !naziv && !funkcija && !izvor) return null;
        return window.SINET_FrequencySchema?.normalizeFrequency({
          hz: hz ? Number(hz) : "",
          naziv,
          opis: "",
          funkcija,
          svrha: funkcija,
          izvor
        }, Number(i) || 0) || { hz: hz ? Number(hz) : "", naziv, opis: "", funkcija, svrha: funkcija, izvor };
      }).filter(Boolean);

      let id = existingId || ("usr-" + this._slugify(name));
      if (!existingId) {
        // ensure unique
        const used = new Set((this.userSymptoms || []).map(x => x.id));
        if (used.has(id)) {
          let k = 2;
          while (used.has(`${id}-${k}`)) k++;
          id = `${id}-${k}`;
        }
      }

      const item = window.SINET_FrequencySchema?.normalizeItem({
        id,
        simptom: name,
        oblast,
        opis: desc,
        mkb10,
        izvor: src,
        frekvencije: freqs
      }) || {
        id,
        simptom: name,
        oblast,
        opis: desc,
        mkb10,
        izvor: src,
        frekvencije: freqs
      };

      const list = Array.isArray(this.userSymptoms) ? this.userSymptoms : [];
      const ix = list.findIndex(x => x.id === id || (existingId && x.id === existingId));
      if (ix >= 0) list[ix] = item;
      else list.unshift(item);
      this.userSymptoms = list;

      await this.saveUserSymptoms();
      this.applyUserDataToCatalog();
      this.renderUserSymptomsList();
      this.cancelUserSymptomForm();
      alert("Sačuvano ✅");

      // refresh presets/catalog if needed
      this.renderSystemPresets();
      if (document.getElementById('page-catalog')?.classList?.contains('active')) this.showCatalogHome();
    } catch (e) {
      alert("Greška: " + (e?.message || e));
    }
  }

  editUserSymptom(id) {
    this.nav('mysymptoms');
    this.showUserSymptomForm(id);
  }

  async deleteUserSymptom(id) {
    if (!confirm("Obrisati ovaj simptom?")) return;
    this.userSymptoms = (this.userSymptoms || []).filter(x => x.id !== id);
    await this.saveUserSymptoms();
    this.applyUserDataToCatalog();
    this.renderUserSymptomsList();
    this.renderSystemPresets();
    if (document.getElementById('page-catalog')?.classList?.contains('active')) this.showCatalogHome();
  }

  /* ===================== v15.3 — MODAL ADVANCED (JSON / overrides) ===================== */

  renderModalAdvanced(item) {
    const host = document.getElementById('m-advanced');
    if (!host) return;
    const base = item || this._modalItem;
    if (!base) { host.innerHTML = ""; return; }

    const existing = this.overrides?.[base.id];
    const json = JSON.stringify(existing ? this._deepMerge(base, existing) : base, null, 2);

    host.innerHTML = `
      <div style="background:#fff; border:1px solid #eee; border-radius:12px; padding:12px;">
        <div style="color:#666; margin-bottom:8px;">
          Ovde vidiš ceo slog (JSON). Možeš da napraviš lokalnu izmenu (override) bez menjanja kataloga.
        </div>
        <textarea id="m-adv-json" rows="8" style="width:100%; padding:10px; border:1px solid #ddd; border-radius:10px;">${this.escapeHtml(json)}</textarea>

        <div style="display:flex; gap:10px; margin-top:10px;">
          <button class="btn-full" style="flex:1; margin-top:0; background:#2ecc71;" onclick="app.copyModalJSON()">📋 Kopiraj</button>
          <button class="btn-full" style="flex:1; margin-top:0; background:#8e44ad;" onclick="app.saveModalOverride()">💾 Sačuvaj override</button>
        </div>
        <button class="btn-full" style="margin-top:10px; background:#eee; color:#333;" onclick="app.clearModalOverride()">♻ Vrati original</button>
      </div>
    `;
  }

  copyModalJSON() {
    const ta = document.getElementById('m-adv-json');
    if (!ta) return;
    ta.select();
    document.execCommand('copy');
    alert("Kopirano ✅");
  }

  async saveModalOverride() {
    try {
      const ta = document.getElementById('m-adv-json');
      if (!ta || !this._modalItem?.id) return;
      const patch = JSON.parse(ta.value);
      this.overrides = this.overrides || {};
      this.overrides[this._modalItem.id] = patch;
      await this.saveOverrides();
      this.applyUserDataToCatalog();
      alert("Override sačuvan ✅ (lokalno)");
    } catch (e) {
      alert("Neispravan JSON: " + (e?.message || e));
    }
  }

  async clearModalOverride() {
    if (!this._modalItem?.id) return;
    if (!confirm("Obrisati lokalni override i vratiti original?")) return;
    if (this.overrides) delete this.overrides[this._modalItem.id];
    await this.saveOverrides();
    this.applyUserDataToCatalog();
    this.renderModalAdvanced(this._modalItem);
    alert("Vraćeno ✅");
  }

  /* ===================== v15.3 — AI UPITNIK (prompt generator) ===================== */

  prepareAIUI() {
    try{
      const box = document.getElementById('ai-active-profile-banner');
      if(box){
        box.innerHTML = (window.SINET_ProfilesCore && window.SINET_ProfilesCore.getProfileHeaderHtml)
          ? window.SINET_ProfilesCore.getProfileHeaderHtml(window.SINET_ProfilesCore.getActiveProfile())
          : '';
      }
      this.prefillAI();
    }catch(_){}
  }

  prefillAI(symptom = "", desc = "", note = "") {
    try {
      const s = document.getElementById('ai_symptom');
      const d = document.getElementById('ai_desc');
      const ageEl = document.getElementById('ai_age');
      const sexEl = document.getElementById('ai_sex');
      const medEl = document.getElementById('ai_med');
      const goalEl = document.getElementById('ai_goal');
      const mkbEl = document.getElementById('ai_mkb');
      if (s && symptom) s.value = symptom;
      if (d && desc) d.value = desc;
      const p = (window.SINET_ProfilesCore && window.SINET_ProfilesCore.getActiveProfile) ? window.SINET_ProfilesCore.getActiveProfile() : null;
      if (p) {
        const approxAge = p.approxAge ?? (window.SINET_ProfilesCore && window.SINET_ProfilesCore.snapshot ? (window.SINET_ProfilesCore.snapshot(p)||{}).approxAge : '');
        if (ageEl && !String(ageEl.value || '').trim() && approxAge != null && String(approxAge).trim()) ageEl.value = String(approxAge);
        if (sexEl && (!String(sexEl.value || '').trim() || (sexEl.value === 'nebitno' && p.sex))) sexEl.value = p.sex || sexEl.value;
        const meds = [p.primaryDiagnosis, p.meds, p.allergies ? `Alergije: ${p.allergies}` : '', p.chronic ? `Hronično: ${p.chronic}` : ''].filter(Boolean).join(' · ');
        if (medEl && !String(medEl.value || '').trim() && meds) medEl.value = meds;
        if (goalEl && !String(goalEl.value || '').trim() && p.reason) goalEl.value = p.reason;
        const icd = String(p.primaryIcd || p.mkb10 || p.mkb || '').trim();
        if (mkbEl && !String(mkbEl.value || '').trim() && icd) mkbEl.value = icd;
      }
      try {
        window.SINET_SharedContext && window.SINET_SharedContext.refreshSharedContext && window.SINET_SharedContext.refreshSharedContext('ai_upitnik', { currentState:{ source:'prefill_ai', symptom: s?.value || '', notes: d?.value || '' } });
      } catch (_) {}
      if (note) console.log("AI note:", note);
    } catch (_) {}
  }

  userSymptomToAI() {
    const name = (document.getElementById('us_name')?.value || "").trim();
    const desc = (document.getElementById('us_desc')?.value || "").trim();
    if (!name) return alert("Prvo upiši naziv simptoma.");
    this.prefillAI(name, desc);
    this.nav('ai');
    // Auto-generate prompt for convenience
    setTimeout(() => this.generateAIPrompt(), 50);
  }

  generateAIPrompt() {
    const symptom = (document.getElementById('ai_symptom')?.value || "").trim();
    const desc = (document.getElementById('ai_desc')?.value || "").trim();
    const age = (document.getElementById('ai_age')?.value || "").trim();
    const sex = (document.getElementById('ai_sex')?.value || "nebitno").trim();
    const med = (document.getElementById('ai_med')?.value || "").trim();
    const goal = (document.getElementById('ai_goal')?.value || "").trim();
    const mkb = (document.getElementById('ai_mkb')?.value || "").trim();
    try { window.SINET_SharedContext && window.SINET_SharedContext.refreshSharedContext && window.SINET_SharedContext.refreshSharedContext('ai_upitnik', { currentState:{ source:'generate_ai_prompt', symptom, notes: desc, goal } }); } catch (_) {}

    if (!symptom) return alert("Upiši simptom / problem.");

    const prompt = `
TI SI medicinsko-informativni asistent za SINET Audio Lekar.
Tvoja uloga je da napraviš JEDAN slog simptoma u SINET STL v1.1 formatu (offline katalog), sa proverljivim izvorima.
NE DIJAGNOSTIKUJ; NE DAVAJ hitne medicinske savete. Ako uočiš alarmantne simptome, u JSON dodaj kratku napomenu u polju "napomena" da korisnik potraži lekara.

Korisnički unos:
- Simptom: "${symptom}"
- Opis: "${desc}"
- Godine: "${age}"
- Pol: "${sex}"
- MKB-10 (ako poznato): "${mkb}"
- Dijagnoze/terapije/napomene: "${med}"
- Cilj: "${goal}"

ZADATAK:
1) Vrati VALIDAN JSON (bez markdown-a, bez dodatnog teksta).
2) Popuni sledeća polja (što detaljnije, ali kratko i jasno za starije osobe):
   - id (slug, latinica, bez razmaka)
   - uid: null
   - naziv
   - opis
   - mkb10: { sifra, naziv/opis, izvor: { sistem, url } }  (ako nema URL — navedi naziv sistema)
   - psihosomatika: { uzrok, lek, izvor: { autor, delo, url } }
   - afirmacija: { tekst, autor, izvor: { autor, delo, url, napomena } }
   - molitva: { tekst, izvor: { tradicija, url } }
   - narodni_lek: { opis/tekst, napomena, izvor: { autor, delo, url } }
   - akupresura: [ { tacka, naziv, lokacija, uputstvo, izvor: { sistem, url } } ]  (3–6 tačaka)
   - frekvencije: [ { hz, naziv, opis, funkcija, izvor: { sistem, autor, delo, url, licenca } } ] (8–15 frekvencija)
3) SVAKO polje koje ima "izvor" mora imati proverljiv izvor (URL ili autor+delo).
4) Izlazni JSON format:
{
  "stl_version": "1.1",
  "napomena": "...(opciono)...",
  "simptom": {
    ... polja iznad ...
  }
}

VAŽNO: samo JSON.`;

    const out = document.getElementById('ai_prompt');
    if (out) out.value = prompt.trim();
  }

  copyAIPrompt() {
    const out = document.getElementById('ai_prompt');
    if (!out) return;
    out.select();
    document.execCommand('copy');
    alert("Prompt kopiran ✅");
  }



  /* ===================== v15.7.7.7 — Text modal + AI result helpers ===================== */

  openTextModal(title, text, linksHtml = "") {
    const ov = document.getElementById('text-modal');
    const tt = document.getElementById('tm-title');
    const ta = document.getElementById('tm-text');
    const links = document.getElementById('tm-links');
    if (!ov || !tt || !ta) {
      try { this.showToast("ℹ️ Tekst modal nije učitan."); } catch(_) {}
      return;
    }
    tt.textContent = (title || 'Tekst').toString();
    ta.value = (text ?? '').toString();
    if (links) links.innerHTML = linksHtml || '';
    ov.style.display = 'flex';
    try { ta.scrollTop = 0; } catch(_) {}
  }

  closeTextModal() {
    const ov = document.getElementById('text-modal');
    if (ov) ov.style.display = 'none';
  }


  /* ===================== DOC VIEWER (Help/Protokoli bez prekida zvuka) ===================== */

  openDoc(href, title = "Dokument") {
    try {
      // Resolve relative URL against current location (supports ./docs/...).
      const url = new URL(String(href || ""), window.location.href).toString();
      const ov = document.getElementById('doc-modal');
      const fr = document.getElementById('doc-modal-frame');
      const tt = document.getElementById('doc-modal-title');
      if (!ov || !fr) {
        // Fallback: open in new tab if modal missing
        window.open(url, '_blank', 'noopener');
        return false;
      }

      // Store last doc for "Novi tab"
      this._lastDocUrl = url;

      if (tt) tt.textContent = String(title || "Dokument");
      // Important: set src AFTER showing to reduce iOS quirks
      ov.style.display = 'flex';
      fr.src = url;
      // IMPORTANT (iOS): do NOT start the silent keep-alive here.
      // Starting another HTMLAudioElement can steal audio focus and pause the main playback.
      // If iOS paused playback due to UI interaction, we only try to resume the SAME element.
      try {
        if (this._isIOS) {
          const el = this._iosRenderedEl;
          if (this._rendered && this._rendered.active && el && el.paused && !el.ended) {
            const pr = el.play();
            if (pr && pr.catch) pr.catch(() => {});
          } else if (this.audio && this.audio.isPlaying && this.audio.audioContext && this.audio.audioContext.resume) {
            this.audio.audioContext.resume().catch(() => {});
          }
        }
      } catch(_) {}

      return false; // prevent default navigation
    } catch (e) {
      try { window.open(String(href || ""), '_blank', 'noopener'); } catch(_) {}
      return false;
    }
  }

  closeDocModal() {
    const ov = document.getElementById('doc-modal');
    const fr = document.getElementById('doc-modal-frame');
    if (fr) fr.src = 'about:blank';
    if (ov) ov.style.display = 'none';
  }

  openDocInNewTab() {
    const url = this._lastDocUrl;
    if (!url) return;
    try { window.open(url, '_blank', 'noopener'); } catch(_) {}
  }

  copyTextModal() {
    const ta = document.getElementById('tm-text');
    if (!ta) return;
    const txt = (ta.value || '').toString();
    if (!txt) return alert('Nema teksta za kopiranje.');

    // Prefer modern clipboard
    try {
      navigator.clipboard.writeText(txt).then(() => alert('Kopirano ✅'));
      return;
    } catch (_) {}

    // Fallback
    try {
      ta.focus();
      ta.select();
      document.execCommand('copy');
      alert('Kopirano ✅');
    } catch (e) {
      alert('Ne mogu da kopiram (dozvole).');
    }
  }

  copyAIResult() {
    const ta = document.getElementById('ai_result');
    if (!ta) return;
    const txt = (ta.value || '').trim();
    if (!txt) return alert('Nema AI odgovora za kopiranje.');
    try {
      navigator.clipboard.writeText(txt).then(() => alert('AI odgovor kopiran ✅'));
    } catch (_) {
      try {
        ta.focus();
        ta.select();
        document.execCommand('copy');
        alert('AI odgovor kopiran ✅');
      } catch (e) {
        alert('Ne mogu da kopiram (dozvole).');
      }
    }
  }

  showAIResultModal() {
    const raw = (document.getElementById('ai_result')?.value || '').trim();
    if (!raw) return alert('Zalepi AI odgovor (JSON) u polje ispod.');

    let pretty = raw;
    try {
      const parsed = this._parseAiJsonLoose(raw);
      if (parsed && parsed.obj) pretty = JSON.stringify(parsed.obj, null, 2);
    } catch (_) {}

    const linksHtml = `
      <div style="display:flex; gap:10px; flex-wrap:wrap; margin-top:12px;">
        <button class="btn-full" style="margin-top:0; padding:10px; background:#6f4e37;" onclick="window.app && window.app.exportAIReportHTML && window.app.exportAIReportHTML()">📄 AI izveštaj (HTML)</button>
        <button class="btn-full" style="margin-top:0; padding:10px; background:#95a5a6;" onclick="window.app && window.app.closeTextModal && window.app.closeTextModal()">Zatvori</button>
      </div>
      <div style="margin-top:10px; font-size:0.9rem; color:#666;">
        Savet: AI izveštaj pravi čitljiv plan u <b>Template v2</b> formatu.
      </div>
    `;

    this.openTextModal('🤖 AI odgovor (JSON)', pretty, linksHtml);
  }

  exportAIReportHTML() {
    const raw = (document.getElementById('ai_result')?.value || '').trim();
    if (!raw) return alert('Nema AI odgovora (JSON).');

    let parsed;
    try { parsed = this._parseAiJsonLoose(raw); }
    catch (e) { parsed = null; }
    if (!parsed || !parsed.obj) {
      this._showAiJsonFixModal((parsed && parsed.parseSource) || raw, 'Ne mogu da pročitam JSON. Proveri navodnike, markdown linkove ili dodatni tekst.', parsed);
      return;
    }
    const obj = parsed.obj;
    this._applyAiJsonCleanupToTextarea(parsed);

    const html = this._buildAIReportHtml(obj);
    const w = window.open('', '_blank');
    if (!w) return alert('Pop-up je blokiran. Dozvoli otvaranje novih tabova.');
    w.document.open();
    w.document.write(html);
    w.document.close();
  }

  _buildAIReportHtml(obj) {
    const esc = (s) => this.escapeHtml(String(s ?? ''));

    const s = obj?.simptom || obj?.symptom || obj;
    const naziv = (s?.naziv || s?.name || s?.simptom || 'AI rezultat').toString().trim();
    const opis = (s?.opis || '').toString().trim();
    const mkb = this._mkbToText(s?.mkb10 || s?.mkb || '');

    const psih = s?.psihosomatika || null;
    const afr = s?.afirmacija || null;
    const mol = s?.molitva || null;
    const nar = s?.narodni_lek || null;

    let ak = s?.akupresura;
    if (ak && !Array.isArray(ak) && typeof ak === 'object') ak = [ak];
    if (!Array.isArray(ak)) ak = [];

    const freqs = window.SINET_FrequencySchema?.normalizeFrequencies ? window.SINET_FrequencySchema.normalizeFrequencies(Array.isArray(s?.frekvencije) ? s.frekvencije : []) : (Array.isArray(s?.frekvencije) ? s.frekvencije : []);

    const freqRows = freqs.map(f => {
      const hz = Number(f?.hz ?? f?.value ?? f?.freq ?? 0) || 0;
      if (!hz) return '';
      const name = (f?.naziv || f?.name || '').toString().trim();
      const desc = (f?.funkcija || f?.svrha || f?.opis || '').toString().trim();
      const src = this._srcToText(f?.izvor || f?.source || f?.izvor_obj || '');
      return `<tr>
        <td style="padding:8px; border-bottom:1px solid #e8dfd3;"><b>${hz} Hz</b>${name ? `<div class="small">${esc(name)}</div>` : ''}</td>
        <td style="padding:8px; border-bottom:1px solid #e8dfd3;">${esc(desc) || '<span class="small">(nema opisa)</span>'}</td>
        <td style="padding:8px; border-bottom:1px solid #e8dfd3;" class="small">${esc(src)}</td>
      </tr>`;
    }).filter(Boolean).join('');

    const freqTable = freqRows ? `
      <h2>🎵 Frekvencije</h2>
      <div class="card">
        <table style="width:100%; border-collapse:collapse;">
          <thead>
            <tr>
              <th style="text-align:left; padding:8px; border-bottom:1px solid #e8dfd3;">Hz</th>
              <th style="text-align:left; padding:8px; border-bottom:1px solid #e8dfd3;">Opis / funkcija</th>
              <th style="text-align:left; padding:8px; border-bottom:1px solid #e8dfd3;">Izvor</th>
            </tr>
          </thead>
          <tbody>${freqRows}</tbody>
        </table>
      </div>
    ` : '';

    const akHtml = ak.length ? `
      <h2>👉 Akupresura</h2>
      <div class="card">
        ${ak.map((x, i) => {
          const t = (x.tacka || x.point || `Tačka ${i+1}`).toString();
          const nm = (x.naziv || '').toString();
          const lok = (x.lokacija || '').toString();
          const how = (x.uputstvo || x.how || '').toString();
          const src = this._srcToText(x.izvor || x.source || '');
          return `
            <div style="margin-bottom:14px;">
              <div><b>${esc(t)}</b>${nm ? ` — ${esc(nm)}` : ''}</div>
              ${lok ? `<div class="small">📍 ${esc(lok)}</div>` : ''}
              ${how ? `<div>${esc(how)}</div>` : ''}
              ${src ? `<div class="small">📎 ${esc(src)}</div>` : ''}
            </div>`;
        }).join('')}
      </div>
    ` : '';

    const blocks = [];

    blocks.push(`<div class="card">
      ${mkb ? `<div><b>📌 MKB-10:</b> ${esc(mkb)}</div>` : ''}
      ${opis ? `<div style="margin-top:8px;"><b>ℹ️ Opis:</b> ${esc(opis)}</div>` : ''}
      <div class="small" style="margin-top:10px;">Izveštaj je informativan i ne postavlja dijagnozu.</div>
    </div>`);

    if (psih && typeof psih === 'object') {
      blocks.push(`<h2>🧠 Psihosomatika</h2><div class="card">
        ${psih.uzrok ? `<div><b>Uzrok:</b> ${esc(psih.uzrok)}</div>` : ''}
        ${psih.lek ? `<div style="margin-top:8px;"><b>Preporuka:</b> ${esc(psih.lek)}</div>` : ''}
        ${(psih.izvor || psih.source) ? `<div class="small" style="margin-top:10px;">📎 ${esc(this._srcToText(psih.izvor || psih.source))}</div>` : ''}
      </div>`);
    }

    if (afr && typeof afr === 'object') {
      blocks.push(`<h2>✨ Afirmacija</h2><div class="card">
        ${afr.tekst ? `<div style="font-size:1.1rem;"><b>“${esc(afr.tekst)}”</b></div>` : ''}
        ${afr.autor ? `<div class="small" style="margin-top:8px;">Autor: ${esc(afr.autor)}</div>` : ''}
        ${(afr.izvor || afr.source) ? `<div class="small" style="margin-top:10px;">📎 ${esc(this._srcToText(afr.izvor || afr.source))}</div>` : ''}
      </div>`);
    }

    if (mol && typeof mol === 'object') {
      blocks.push(`<h2>🙏 Molitva</h2><div class="card">
        ${mol.tekst ? `<div style="font-style:italic;">${esc(mol.tekst)}</div>` : ''}
        ${(mol.izvor || mol.source) ? `<div class="small" style="margin-top:10px;">📎 ${esc(this._srcToText(mol.izvor || mol.source))}</div>` : ''}
      </div>`);
    }

    if (nar && typeof nar === 'object') {
      const txt = (nar.opis || nar.tekst || '').toString();
      blocks.push(`<h2>🌿 Narodni lek</h2><div class="card">
        ${txt ? `<div>${esc(txt)}</div>` : ''}
        ${nar.napomena ? `<div class="small" style="margin-top:8px;">Napomena: ${esc(nar.napomena)}</div>` : ''}
        ${(nar.izvor || nar.source) ? `<div class="small" style="margin-top:10px;">📎 ${esc(this._srcToText(nar.izvor || nar.source))}</div>` : ''}
      </div>`);
    }

    if (akHtml) blocks.push(akHtml);
    if (freqTable) blocks.push(freqTable);

    // RAW JSON in details
    try {
      blocks.push(`<details class="card" style="margin-top:18px;"><summary style="cursor:pointer; font-weight:900;">🧾 RAW JSON (AI odgovor)</summary><pre>${esc(JSON.stringify(obj, null, 2))}</pre></details>`);
    } catch(_) {}

    const contentHtml = blocks.join('\n');

    const title = `AI izveštaj · ${naziv}`.trim();
    const subtitle = 'AI odgovor pretvoren u čitljiv plan (Template v2).';
    const warningsHtml = `<div class="warning-box">⚠️ <strong>Upozorenje:</strong> Ovo je informativan sadržaj i nije zamena za lekarski pregled. Kod naglog pogoršanja, jakog bola, gušenja, slabosti jedne strane tela, krvarenja, visoke temperature ili sumnje na hitno stanje — odmah potraži urgentnu pomoć.</div>`;

    if (window.SINET_TemplateV2 && typeof window.SINET_TemplateV2.buildDoc === 'function') {
      const activeProfile = (window.SINET_ProfilesCore && window.SINET_ProfilesCore.getActiveProfile) ? window.SINET_ProfilesCore.getActiveProfile() : null;
      const patientLine = activeProfile ? `<strong>Profil:</strong> ${esc(activeProfile.displayName || [activeProfile.firstName, activeProfile.lastName].filter(Boolean).join(' '))}` : '';
      return window.SINET_TemplateV2.buildDoc({
        title,
        subtitle,
        templateName: 'SINET_TEMPLATE_v2',
        coauthor: 'miuchins (Svetozar Miučin) + SINET AI',
        backUrl: (location.href || '').split('#')[0] || null,
        patientLine,
        profileSnapshotHtml: (window.SINET_SharedContext && window.SINET_SharedContext.getProfileSnapshotHtml ? window.SINET_SharedContext.getProfileSnapshotHtml() : ''),
        warningsHtml,
        contentHtml,
        badgesHtml: `<span class="badge">AI Upitnik</span>${mkb ? `<span class="badge">MKB: ${esc(mkb.split('—')[0].trim())}</span>` : ''}`
      });
    }

    // fallback minimal
    return `<!doctype html><html lang="sr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(title)}</title></head><body>${warningsHtml}${contentHtml}</body></html>`;
  }

  /* ===== v15.4 — AI JSON import into My Symptoms ===== */

  _mkbToText(mkb) {
    if (!mkb) return "";
    if (typeof mkb === 'string') return this._cleanupAiJsonText(mkb);
    const code = (mkb.sifra || mkb.code || "").toString().trim();
    const name = (mkb.naziv || mkb.opis || mkb.naziv_opis || "").toString().trim();
    if (code && name) return `${code} — ${this._cleanupAiJsonText(name)}`;
    return code || this._cleanupAiJsonText(name) || "";
  }

  _srcToText(src) {
    if (!src) return "";
    if (typeof src === 'string') return this._cleanupAiJsonText(src);
    const url = this._cleanupAiJsonText((src.url || "").toString().trim());
    if (url) return url;
    const delo = this._cleanupAiJsonText((src.delo || "").toString().trim());
    const autor = this._cleanupAiJsonText((src.autor || "").toString().trim());
    if (delo && autor) return `${delo} — ${autor}`;
    return delo || autor || this._cleanupAiJsonText((src.sistem || "").toString()) || "";
  }

  _cleanupAiJsonText(str) {
    return String(str ?? '')
      .replace(/[“”]/g, '"')
      .replace(/[‘’]/g, "'")
      .replace(/\[([^\]]+)\]\((https?:\/\/[^\s)]+)\)/g, '$2')
      .replace(/\s+/g, ' ')
      .trim();
  }

  _extractLikelyJson(raw) {
    let text = String(raw ?? '').replace(/^\uFEFF/, '').trim();
    if (!text) return '';
    text = text.replace(/^```(?:json)?\s*/i, '').replace(/\s*```$/i, '').trim();
    const firstBrace = text.indexOf('{');
    const lastBrace = text.lastIndexOf('}');
    const firstBracket = text.indexOf('[');
    const lastBracket = text.lastIndexOf(']');
    if (firstBrace !== -1 && lastBrace !== -1 && lastBrace > firstBrace) {
      text = text.slice(firstBrace, lastBrace + 1);
    } else if (firstBracket !== -1 && lastBracket !== -1 && lastBracket > firstBracket) {
      text = text.slice(firstBracket, lastBracket + 1);
    }
    return text.trim();
  }

  _repairLooseQuotesInJson(text) {
    let out = '';
    let inString = false;
    let escaped = false;
    for (let i = 0; i < text.length; i++) {
      const ch = text[i];
      if (!inString) {
        out += ch;
        if (ch === '"') inString = true;
        continue;
      }
      if (escaped) {
        out += ch;
        escaped = false;
        continue;
      }
      if (ch === '\\') {
        out += ch;
        escaped = true;
        continue;
      }
      if (ch === '"') {
        let j = i + 1;
        while (j < text.length && /\s/.test(text[j])) j++;
        const next = text[j] || '';
        if (next === ',' || next === '}' || next === ']' || next === ':') {
          out += ch;
          inString = false;
        } else {
          out += "'";
        }
        continue;
      }
      out += ch;
    }
    return out;
  }

  _deepNormalizeAiObject(value, parentKey = '') {
    if (Array.isArray(value)) return value.map(v => this._deepNormalizeAiObject(v, parentKey));
    if (value && typeof value === 'object') {
      const out = {};
      for (const [k, v] of Object.entries(value)) out[k] = this._deepNormalizeAiObject(v, k);
      return out;
    }
    if (typeof value === 'string') {
      let s = this._cleanupAiJsonText(value);
      if (parentKey === 'url') {
        const m = s.match(/https?:\/\/\S+/);
        if (m) s = m[0].replace(/[)>.,]+$/, '');
      }
      return s;
    }
    return value;
  }

  _parseAiJsonLoose(raw) {
    const original = String(raw ?? '');
    const candidates = [];
    const seen = new Set();
    const push = (label, txt) => {
      txt = String(txt ?? '');
      if (!txt || seen.has(txt)) return;
      seen.add(txt);
      candidates.push({ label, text: txt });
    };

    push('raw', original.trim());
    const extracted = this._extractLikelyJson(original);
    push('extracted', extracted);
    push('normalized', extracted
      .replace(/[“”]/g, '"')
      .replace(/[‘’]/g, "'")
      .replace(/\[([^\]]+)\]\((https?:\/\/[^\s)]+)\)/g, '$2')
      .trim());
    push('repaired_quotes', this._repairLooseQuotesInJson(candidates[candidates.length - 1]?.text || extracted));

    let lastErr = null;
    let lastText = extracted || original;
    let lastMode = 'failed';
    for (const c of candidates) {
      try {
        const obj = JSON.parse(c.text);
        const normalized = this._deepNormalizeAiObject(obj);
        return { obj: normalized, cleanedText: JSON.stringify(normalized, null, 2), mode: c.label, parseSource: c.text, error: null };
      } catch (e) {
        lastErr = e;
        lastText = c.text;
        lastMode = c.label;
      }
    }
    return { obj: null, cleanedText: extracted || original, mode: lastMode, error: lastErr, parseSource: lastText };
  }

  _getJsonErrorLocation(text, error) {
    try {
      const msg = String(error?.message || '');
      let pos = null;
      let m = msg.match(/position\s+(\d+)/i);
      if (m) pos = parseInt(m[1], 10);

      let line = null;
      let col = null;
      m = msg.match(/line\s+(\d+)\s+column\s+(\d+)/i);
      if (m) {
        line = parseInt(m[1], 10);
        col = parseInt(m[2], 10);
      }

      if ((line == null || col == null) && Number.isFinite(pos)) {
        const before = String(text || '').slice(0, pos);
        const lines = before.split(/\n/);
        line = lines.length;
        col = (lines[lines.length - 1] || '').length + 1;
      }

      if (line == null || col == null) return null;
      const all = String(text || '').split(/\n/);
      const errLine = all[Math.max(0, line - 1)] || '';
      const pointerPad = ' '.repeat(Math.max(0, col - 1));
      const excerpt = errLine.length > 220 ? errLine.slice(0, 220) + '…' : errLine;
      return { line, col, excerpt, pointer: `${pointerPad}^` };
    } catch (_) {
      return null;
    }
  }

  _findLikelyQuoteIssue(text) {
    try {
      const s = String(text || '');
      let inString = false;
      let escaped = false;
      for (let i = 0; i < s.length; i++) {
        const ch = s[i];
        if (!inString) {
          if (ch === '"') inString = true;
          continue;
        }
        if (escaped) { escaped = false; continue; }
        if (ch === '\\') { escaped = true; continue; }
        if (ch === '"') {
          let j = i + 1;
          while (j < s.length && /\s/.test(s[j])) j++;
          const next = s[j] || '';
          if (next === ',' || next === '}' || next === ']' || next === ':') {
            inString = false;
          } else {
            const before = s.slice(0, i);
            const lines = before.split(/\n/);
            return { line: lines.length, col: (lines[lines.length - 1] || '').length + 1, reason: 'Mogući neispravan dvostruki navodnik unutar stringa' };
          }
        }
      }
      return null;
    } catch (_) {
      return null;
    }
  }

  _buildAiJsonFixSuggestions(raw, parseSource = '', error = null) {
    const text = String(parseSource || this._extractLikelyJson(raw) || raw || '');
    const suggestions = [];
    if (/```/m.test(raw)) suggestions.push('Ukloni code block oznake poput ```json i ```.');
    if (/\[[^\]]+\]\(https?:\/\//i.test(raw)) suggestions.push('Zameni markdown link [tekst](url) običnim URL stringom, npr. "https://example.com".');
    if (/"[^"\n]*"[^,:}\]\n]/.test(text)) suggestions.push('Unutar stringova koristi jednostruke navodnike ili escaped dvostruke navodnike: \\"tekst\\".');

    const quoteIssue = this._findLikelyQuoteIssue(text);
    if (quoteIssue) suggestions.push(`Proveri navodnike blizu linije ${quoteIssue.line}, kolone ${quoteIssue.col}.`);

    const loc = error ? this._getJsonErrorLocation(text, error) : null;
    if (loc) suggestions.push(`Parser puca blizu linije ${loc.line}, kolone ${loc.col}.`);

    suggestions.push('Svako polje "url" treba da bude čist string bez dodatnog teksta.');
    suggestions.push('JSON mora početi sa { i završiti se sa } bez objašnjenja pre ili posle.');
    suggestions.push('Ako AI vrati dodatni tekst, klikni "AUTO-CLEAN JSON" pa pokušaj ponovo.');
    return suggestions;
  }

  _showAiJsonFixModal(raw, prefix, parsed = null) {
    const src = (parsed && parsed.parseSource) || this._extractLikelyJson(raw) || String(raw || '');
    const err = parsed && parsed.error ? parsed.error : null;
    const loc = err ? this._getJsonErrorLocation(src, err) : null;
    const suggestions = this._buildAiJsonFixSuggestions(raw, src, err);

    let body = `${prefix || 'Ne mogu da pročitam JSON.'}\n\n`;
    if (loc) {
      body += `Greška je blizu: linija ${loc.line}, kolona ${loc.col}.\n\n`;
      if (loc.excerpt) body += `${loc.excerpt}\n${loc.pointer}\n\n`;
    } else {
      const q = this._findLikelyQuoteIssue(src);
      if (q) body += `Mogući problem sa navodnicima blizu: linija ${q.line}, kolona ${q.col}.\n\n`;
    }

    body += 'Predlog popravke:\n';
    suggestions.forEach((s, i) => { body += `${i + 1}. ${s}\n`; });
    body += `\nMini primer validnog URL polja:\n"url": "https://example.com"`;

    const footer = `
      <div style="display:flex; gap:10px; flex-wrap:wrap; margin-top:12px;">
        <button class="btn-full" style="margin-top:0; padding:10px; background:#8e44ad;" onclick="window.app && window.app.cleanAIResult && window.app.cleanAIResult()">🧹 AUTO-CLEAN JSON</button>
        <button class="btn-full" style="margin-top:0; padding:10px; background:#2ecc71;" onclick="window.app && window.app.copyAIResult && window.app.copyAIResult()">📋 Kopiraj sadržaj</button>
        <button class="btn-full" style="margin-top:0; padding:10px; background:#95a5a6;" onclick="window.app && window.app.closeTextModal && window.app.closeTextModal()">Zatvori</button>
      </div>
    `;
    this.openTextModal('🩺 Dijagnostika AI JSON greške', body, footer);
  }

  _formatAiJsonError(raw, prefix, error = null) {
    const text = this._extractLikelyJson(raw);
    let msg = prefix || 'Ne mogu da pročitam JSON.';
    const loc = error ? this._getJsonErrorLocation(text, error) : null;
    if (loc) {
      msg += `\n\nGreška je blizu: linija ${loc.line}, kolona ${loc.col}.`;
      if (loc.excerpt) msg += `\n\n${loc.excerpt}\n${loc.pointer}`;
    } else {
      const q = this._findLikelyQuoteIssue(text);
      if (q) msg += `\n\nMogući problem sa navodnicima blizu: linija ${q.line}, kolona ${q.col}.`;
    }
    if (/\[[^\]]+\]\(https?:\/\//i.test(raw)) msg += '\n\nPrimećen je markdown link tipa [tekst](url). U JSON-u koristi čist URL string.';
    if (/"[^"\n]*"[^,:}\]\n]/.test(text)) msg += '\n\nVerovatno postoje neispravni dvostruki navodnici unutar teksta. Unutar stringova koristi jednostruke navodnike ili escaped \\".';
    if (/```/m.test(raw)) msg += '\n\nPrimećen je code block (```json). Program pokušava auto-clean, ali proveri odgovor.';
    return msg;
  }

  _applyAiJsonCleanupToTextarea(parsed) {
    try {
      if (!parsed || !parsed.cleanedText) return;
      const ta = document.getElementById('ai_result');
      if (!ta) return;
      ta.value = parsed.cleanedText;
    } catch (_) {}
  }

  cleanAIResult() {
    const ta = document.getElementById('ai_result');
    const raw = (ta?.value || '').trim();
    if (!raw) return alert('Nema AI odgovora za čišćenje.');
    const parsed = this._parseAiJsonLoose(raw);
    if (!parsed || !parsed.obj) {
      this._showAiJsonFixModal((parsed && parsed.parseSource) || raw, 'Auto-clean nije uspeo.', parsed);
      return;
    }
    this._applyAiJsonCleanupToTextarea(parsed);
    alert(`AI JSON očišćen ✅ (${parsed.mode})`);
  }

  diagnoseAIJson() {
    const ta = document.getElementById('ai_result');
    const raw = (ta?.value || '').trim();
    if (!raw) return alert('Nema AI odgovora za dijagnostiku.');
    const parsed = this._parseAiJsonLoose(raw);
    if (parsed && parsed.obj) {
      this._applyAiJsonCleanupToTextarea(parsed);
      this.openTextModal(
        '✅ AI JSON izgleda ispravno',
        `JSON je validan ili je uspešno očišćen (${parsed.mode}).\n\nMožeš odmah da klikneš 'UVEZI U Moji simptomi'.`,
        `
          <div style="display:flex; gap:10px; flex-wrap:wrap; margin-top:12px;">
            <button class="btn-full" style="margin-top:0; padding:10px; background:#3498db;" onclick="window.app && window.app.importAIResult && window.app.importAIResult()">📥 Uvezi</button>
            <button class="btn-full" style="margin-top:0; padding:10px; background:#95a5a6;" onclick="window.app && window.app.closeTextModal && window.app.closeTextModal()">Zatvori</button>
          </div>
        `
      );
      return;
    }
    this._showAiJsonFixModal((parsed && parsed.parseSource) || raw, 'AI JSON ima problem i treba ga ispraviti.', parsed);
  }

  _normalizeSymptomNameForDedupe(name) {
    return String(name || '')
      .normalize('NFD')
      .replace(/[\u0300-\u036f]/g, '')
      .toLowerCase()
      .replace(/[\-_/.,;:()\[\]{}|]+/g, ' ')
      .replace(/[^a-z0-9\s]+/g, ' ')
      .replace(/\s+/g, ' ')
      .trim();
  }

  _splitJsonlInput(raw) {
    return String(raw || '')
      .replace(/^\uFEFF/, '')
      .split(/\r?\n/)
      .map((line, i) => ({ lineNo: i + 1, text: line }))
      .filter(x => {
        const t = (x.text || '').trim();
        if (!t) return false;
        if (/^```/.test(t)) return false;
        if (/^(CONTINUE\b|DONE\b)/i.test(t)) return false;
        return true;
      });
  }

  _buildUserSymptomItemFromAiObject(obj, ctx = {}) {
    const s = obj?.simptom || obj?.symptom || obj;
    if (!s || typeof s !== 'object') return { ok: false, reason: "JSON nema polje 'simptom'." };

    const naziv = this._cleanupAiJsonText((s.naziv || s.name || s.simptom || "").toString());
    if (!naziv) return { ok: false, reason: "Nedostaje 'naziv' u simptomu." };

    const opis = this._cleanupAiJsonText((s.opis || "").toString());
    const baseSlug = this._cleanupAiJsonText((s.id || this._slugify(naziv)).toString()).trim().replace(/^usr-/, '');
    if (!baseSlug) return { ok: false, reason: "Nedostaje validan 'id' / slug." };

    const usedIds = ctx.usedIds || new Set();
    const usedNames = ctx.usedNames || new Set();
    const allowSuffix = ctx.allowSuffix !== false;

    const nazivNorm = this._normalizeSymptomNameForDedupe(naziv);
    if (nazivNorm && usedNames.has(nazivNorm)) {
      return { ok: false, duplicate: true, reason: `Duplikat naziva: ${naziv}` };
    }

    let id = `usr-${baseSlug}`;
    const originalId = id;
    const warnings = [];
    if (usedIds.has(id)) {
      if (!allowSuffix) return { ok: false, duplicate: true, reason: `Duplikat ID: ${id}` };
      let k = 2;
      while (usedIds.has(`${id}-${k}`)) k++;
      id = `${id}-${k}`;
      warnings.push(`ID je već postojao; dodeljen novi ID: ${id}`);
    }

    const freqsIn = Array.isArray(s.frekvencije) ? s.frekvencije : [];
    const freqs = window.SINET_FrequencySchema?.normalizeFrequencies ? window.SINET_FrequencySchema.normalizeFrequencies(freqsIn.map((f) => ({
      hz: Number(f?.hz ?? f?.value ?? 0) || 0,
      naziv: this._cleanupAiJsonText((f?.naziv || "").toString()),
      opis: this._cleanupAiJsonText((f?.opis || f?.description || "").toString()),
      funkcija: this._cleanupAiJsonText((f?.funkcija || f?.svrha || f?.opis || f?.naziv || "").toString()),
      izvor: this._srcToText(f?.izvor),
      enabled: f?.enabled !== false
    }))) : freqsIn.map((f) => {
      const hz = Number(f?.hz ?? f?.value ?? 0) || 0;
      if (!hz) return null;
      const nazivF = this._cleanupAiJsonText((f?.naziv || "").toString());
      const opisF = this._cleanupAiJsonText((f?.opis || f?.description || "").toString());
      const svrha = this._cleanupAiJsonText((f?.funkcija || f?.svrha || f?.opis || f?.naziv || "").toString());
      const izvor = this._srcToText(f?.izvor);
      return { hz, value: hz, naziv: nazivF, opis: opisF, svrha, izvor, enabled: f?.enabled !== false };
    }).filter(Boolean);

    const mkb10 = this._mkbToText(s.mkb10);
    const holisticki = {
      psihosomatika: s.psihosomatika || null,
      afirmacija: s.afirmacija || null,
      molitva: s.molitva || null,
      narodni_lek: s.narodni_lek || null
    };

    let akup = s.akupresura;
    if (akup && !Array.isArray(akup) && typeof akup === 'object') akup = [akup];
    if (!Array.isArray(akup)) akup = [];
    akup = akup.map(p => ({
      tacka: p.tacka || p.point || "",
      naziv: p.naziv || "",
      lokacija: p.lokacija || "",
      uputstvo: p.uputstvo || p.how || "",
      slika: p.slika,
      marker: p.marker,
      izvor: this._srcToText(p.izvor)
    })).filter(p => p.tacka || p.naziv || p.lokacija || p.uputstvo);

    const item = { id, simptom: naziv, oblast: "MOJI SIMPTOMI", opis, mkb10, holisticki, akupresura: akup, frekvencije: freqs };
    return { ok: true, item, id, originalId, renamed: id !== originalId, naziv, nazivNorm, warnings };
  }

  _buildJsonlBatchReport(result) {
    const lines = [];
    lines.push(result.preview ? 'JSONL dedupe preview / pre-import' : 'JSONL batch dijagnostika / import');
    lines.push('');
    lines.push(`Ukupno redova: ${result.total}`);
    lines.push(`Validnih JSON redova: ${result.valid}`);
    if (result.preview) {
      lines.push(`Spremno za uvoz: ${result.importable}`);
      lines.push(`Biće renamovano: ${result.renamed}`);
      lines.push(`Spojiti / preskočiti: ${result.mergeCandidates}`);
    } else {
      lines.push(`Uvezenih: ${result.imported}`);
      lines.push(`Renamovano pri uvozu: ${result.renamed}`);
      lines.push(`Preskočeni duplikati / spajanje: ${result.duplicates}`);
    }
    lines.push(`Auto-clean uspeo: ${result.autoclean}`);
    lines.push(`Greške: ${result.errors}`);
    lines.push('');

    const renderList = (title, arr, mapper, max = 50) => {
      if (!arr || !arr.length) return;
      lines.push(title);
      arr.slice(0, max).forEach((x, i) => lines.push(`${i + 1}. ${mapper(x)}`));
      if (arr.length > max) lines.push(`... i još ${arr.length - max}`);
      lines.push('');
    };

    if (result.preview) {
      renderList('Spremno za uvoz:', result.items, (x) => `[red ${x.lineNo}] ${x.naziv} -> ${x.id}`);
      renderList('Biće renamovano:', result.renamedItems, (x) => `[red ${x.lineNo}] ${x.naziv} : ${x.oldId} -> ${x.id}`);
      renderList('Spojiti / preskočiti (kandidat duplikata):', result.mergeItems, (x) => `[red ${x.lineNo}] ${x.naziv} — ${x.reason}`);
    } else {
      renderList('Uvezeni simptomi:', result.items, (x) => `[red ${x.lineNo}] ${x.naziv} -> ${x.id}`);
      renderList('Renamovano pri uvozu:', result.renamedItems, (x) => `[red ${x.lineNo}] ${x.naziv} : ${x.oldId} -> ${x.id}`);
      renderList('Preskočeni duplikati / spajanje:', result.mergeItems, (x) => `[red ${x.lineNo}] ${x.naziv} — ${x.reason}`);
    }

    if (result.issues.length) {
      lines.push('Problemi / upozorenja:');
      result.issues.forEach((x, i) => {
        lines.push(`${i + 1}. [red ${x.lineNo}] ${x.kind}: ${x.message}`);
        if (x.loc) {
          lines.push(`   linija ${x.loc.line}, kolona ${x.loc.col}`);
          if (x.loc.excerpt) {
            lines.push(`   ${x.loc.excerpt}`);
            lines.push(`   ${x.loc.pointer}`);
          }
        }
      });
    } else {
      lines.push('Nema prijavljenih problema ✅');
    }
    return lines.join('\n');
  }

  async _runAiJsonlBatch(doImport = false) {
    const ta = document.getElementById('ai_batch_result');
    const raw = (ta?.value || '').trim();
    if (!raw) return alert('Nema JSONL batch sadržaja.');

    const rows = this._splitJsonlInput(raw);
    if (!rows.length) return alert('Nema validnih JSONL redova za obradu.');

    const existing = Array.isArray(this.userSymptoms) ? this.userSymptoms : [];
    const usedIds = new Set(existing.map(x => x.id));
    const usedNames = new Set(existing.map(x => this._normalizeSymptomNameForDedupe(x?.simptom || x?.naziv || '')));

    const result = {
      total: rows.length,
      valid: 0,
      imported: 0,
      importable: 0,
      renamed: 0,
      duplicates: 0,
      mergeCandidates: 0,
      autoclean: 0,
      errors: 0,
      items: [],
      renamedItems: [],
      mergeItems: [],
      issues: []
    };

    const toImport = [];

    for (const row of rows) {
      const parsed = this._parseAiJsonLoose(row.text);
      const rawSym = parsed?.obj?.simptom || parsed?.obj?.symptom || parsed?.obj || {};
      const rawName = this._cleanupAiJsonText((rawSym?.naziv || rawSym?.name || rawSym?.simptom || '(bez naziva)').toString());

      if (!parsed || !parsed.obj) {
        result.errors++;
        result.issues.push({
          lineNo: row.lineNo,
          kind: 'JSON',
          message: 'Neispravan JSON red.',
          loc: this._getJsonErrorLocation((parsed && parsed.parseSource) || row.text, parsed && parsed.error)
        });
        continue;
      }

      if (parsed.mode && parsed.mode !== 'raw') result.autoclean++;

      const prep = this._buildUserSymptomItemFromAiObject(parsed.obj, {
        usedIds,
        usedNames,
        allowSuffix: true
      });

      if (!prep.ok) {
        if (prep.duplicate) {
          result.duplicates++;
          result.mergeCandidates++;
          result.mergeItems.push({ lineNo: row.lineNo, naziv: rawName, reason: prep.reason });
          result.issues.push({ lineNo: row.lineNo, kind: 'DUPLIKAT', message: prep.reason });
        } else {
          result.errors++;
          result.issues.push({ lineNo: row.lineNo, kind: 'VALIDACIJA', message: prep.reason });
        }
        continue;
      }

      result.valid++;
      usedIds.add(prep.id);
      if (prep.nazivNorm) usedNames.add(prep.nazivNorm);

      if (prep.renamed) {
        result.renamed++;
        result.renamedItems.push({ lineNo: row.lineNo, naziv: prep.naziv, oldId: prep.originalId, id: prep.id });
      } else {
        result.importable++;
        result.items.push({ lineNo: row.lineNo, id: prep.id, naziv: prep.naziv });
      }

      if (doImport) toImport.push(prep.item);

      if (Array.isArray(prep.warnings)) {
        prep.warnings.forEach(w => result.issues.push({ lineNo: row.lineNo, kind: 'UPOZORENJE', message: w }));
      }
    }

    if (doImport && toImport.length) {
      this.userSymptoms = [...toImport, ...existing];
      await this.saveUserSymptoms();
      this.applyUserDataToCatalog();
      this.renderUserSymptomsList();
      this.renderSystemPresets();
      result.imported = toImport.length;
    }

    const report = this._buildJsonlBatchReport(result);
    this.lastAiJsonlBatchReport = {
      generatedAt: new Date().toISOString(),
      text: report,
      data: { ...result, mode: doImport ? 'import' : 'diagnose' }
    };
    const footer = `
      <div style="display:flex; gap:10px; flex-wrap:wrap; margin-top:12px;">
        ${doImport && toImport.length ? `<button class="btn-full" style="margin-top:0; padding:10px; background:#3498db;" onclick="window.app && window.app.nav && window.app.nav('mysymptoms')">📂 Otvori Moji simptomi</button>` : ``}
        ${!doImport ? `<button class="btn-full" style="margin-top:0; padding:10px; background:#1f8b4c;" onclick="window.app && window.app.importAIJsonlBatch && window.app.importAIJsonlBatch()">📦 Uvezi batch sada</button>` : ``}
        <button class="btn-full" style="margin-top:0; padding:10px; background:#8e44ad;" onclick="window.app && window.app.exportLastJsonlBatchReportMd && window.app.exportLastJsonlBatchReportMd()">💾 Export MD report</button>
        <button class="btn-full" style="margin-top:0; padding:10px; background:#16a085;" onclick="window.app && window.app.exportLastJsonlBatchReportJson && window.app.exportLastJsonlBatchReportJson()">💾 Export JSON report</button>
        <button class="btn-full" style="margin-top:0; padding:10px; background:#95a5a6;" onclick="window.app && window.app.closeTextModal && window.app.closeTextModal()">Zatvori</button>
      </div>
    `;
    this.openTextModal(doImport ? '📦 JSONL batch import — report' : '🩺 JSONL batch dijagnostika', report, footer);
  }

  async previewAIJsonlBatch() {
    const ta = document.getElementById('ai_batch_result');
    const raw = (ta?.value || '').trim();
    if (!raw) return alert('Nema JSONL batch sadržaja.');

    const rows = this._splitJsonlInput(raw);
    if (!rows.length) return alert('Nema validnih JSONL redova za obradu.');

    const existing = Array.isArray(this.userSymptoms) ? this.userSymptoms : [];
    const usedIds = new Set(existing.map(x => x.id));
    const usedNames = new Set(existing.map(x => this._normalizeSymptomNameForDedupe(x?.simptom || x?.naziv || '')));

    const result = {
      preview: true,
      total: rows.length,
      valid: 0,
      imported: 0,
      importable: 0,
      renamed: 0,
      duplicates: 0,
      mergeCandidates: 0,
      autoclean: 0,
      errors: 0,
      items: [],
      renamedItems: [],
      mergeItems: [],
      issues: []
    };

    for (const row of rows) {
      const parsed = this._parseAiJsonLoose(row.text);
      const rawSym = parsed?.obj?.simptom || parsed?.obj?.symptom || parsed?.obj || {};
      const rawName = this._cleanupAiJsonText((rawSym?.naziv || rawSym?.name || rawSym?.simptom || '(bez naziva)').toString());

      if (!parsed || !parsed.obj) {
        result.errors++;
        result.issues.push({
          lineNo: row.lineNo,
          kind: 'JSON',
          message: 'Neispravan JSON red.',
          loc: this._getJsonErrorLocation((parsed && parsed.parseSource) || row.text, parsed && parsed.error)
        });
        continue;
      }

      if (parsed.mode && parsed.mode !== 'raw') result.autoclean++;

      const prep = this._buildUserSymptomItemFromAiObject(parsed.obj, {
        usedIds,
        usedNames,
        allowSuffix: true
      });

      if (!prep.ok) {
        if (prep.duplicate) {
          result.duplicates++;
          result.mergeCandidates++;
          result.mergeItems.push({ lineNo: row.lineNo, naziv: rawName, reason: prep.reason });
          result.issues.push({ lineNo: row.lineNo, kind: 'DUPLIKAT', message: prep.reason });
        } else {
          result.errors++;
          result.issues.push({ lineNo: row.lineNo, kind: 'VALIDACIJA', message: prep.reason });
        }
        continue;
      }

      result.valid++;
      usedIds.add(prep.id);
      if (prep.nazivNorm) usedNames.add(prep.nazivNorm);

      if (prep.renamed) {
        result.renamed++;
        result.renamedItems.push({ lineNo: row.lineNo, naziv: prep.naziv, oldId: prep.originalId, id: prep.id });
      } else {
        result.importable++;
        result.items.push({ lineNo: row.lineNo, id: prep.id, naziv: prep.naziv });
      }

      if (Array.isArray(prep.warnings)) {
        prep.warnings.forEach(w => result.issues.push({ lineNo: row.lineNo, kind: 'UPOZORENJE', message: w }));
      }
    }

    const report = this._buildJsonlBatchReport(result);
    this.lastAiJsonlBatchReport = {
      generatedAt: new Date().toISOString(),
      text: report,
      data: { ...result, mode: 'preview' }
    };
    const footer = `
      <div style="display:flex; gap:10px; flex-wrap:wrap; margin-top:12px;">
        <button class="btn-full" style="margin-top:0; padding:10px; background:#1f8b4c;" onclick="window.app && window.app.importAIJsonlBatch && window.app.importAIJsonlBatch()">📦 Uvezi batch sada</button>
        <button class="btn-full" style="margin-top:0; padding:10px; background:#8e44ad;" onclick="window.app && window.app.exportLastJsonlBatchReportMd && window.app.exportLastJsonlBatchReportMd()">💾 Export MD report</button>
        <button class="btn-full" style="margin-top:0; padding:10px; background:#16a085;" onclick="window.app && window.app.exportLastJsonlBatchReportJson && window.app.exportLastJsonlBatchReportJson()">💾 Export JSON report</button>
        <button class="btn-full" style="margin-top:0; padding:10px; background:#95a5a6;" onclick="window.app && window.app.closeTextModal && window.app.closeTextModal()">Zatvori</button>
      </div>
    `;
    this.openTextModal('👁️ JSONL dedupe preview', report, footer);
  }

  diagnoseAIJsonlBatch() {
    return this._runAiJsonlBatch(false);
  }

  importAIJsonlBatch() {
    return this._runAiJsonlBatch(true);
  }

  copyAIBatchResult() {
    const ta = document.getElementById('ai_batch_result');
    if (!ta) return;
    const txt = (ta.value || '').trim();
    if (!txt) return alert('Nema JSONL batch sadržaja za kopiranje.');
    try {
      navigator.clipboard.writeText(txt).then(() => alert('JSONL batch kopiran ✅'));
    } catch (_) {
      try {
        ta.focus();
        ta.select();
        document.execCommand('copy');
        alert('JSONL batch kopiran ✅');
      } catch (e) {
        alert('Ne mogu da kopiram (dozvole).');
      }
    }
  }

  async importAIResult() {
    const raw = (document.getElementById('ai_result')?.value || "").trim();
    if (!raw) return alert("Zalepi AI odgovor (JSON) u polje ispod.");

    let parsed;
    try {
      parsed = this._parseAiJsonLoose(raw);
    } catch (_) {
      parsed = null;
    }
    if (!parsed || !parsed.obj) {
      this._showAiJsonFixModal((parsed && parsed.parseSource) || raw, "Ne mogu da pročitam JSON. Proveri navodnike, markdown linkove ili dodatni tekst.", parsed);
      return;
    }
    const obj = parsed.obj;
    this._applyAiJsonCleanupToTextarea(parsed);

    const prep = this._buildUserSymptomItemFromAiObject(obj, {
      usedIds: new Set((this.userSymptoms || []).map(x => x.id)),
      usedNames: new Set((this.userSymptoms || []).map(x => this._normalizeSymptomNameForDedupe(x?.simptom || x?.naziv || ''))),
      allowSuffix: true
    });

    if (!prep.ok) return alert(prep.reason || "Ne mogu da pripremim simptom za uvoz.");

    const list = Array.isArray(this.userSymptoms) ? this.userSymptoms : [];
    list.unshift(prep.item);
    this.userSymptoms = list;
    await this.saveUserSymptoms();
    this.applyUserDataToCatalog();
    this.renderUserSymptomsList();
    this.renderSystemPresets();

    alert("Uvezeno u 'Moji simptomi' ✅");
    this.nav('mysymptoms');
    setTimeout(() => this.openModal(prep.id), 50);
  }

  /* ===================== UTIL ===================== */

  escapeHtml(str) {
    return (str || "").toString()
      .replace(/&/g, "&amp;")
      .replace(/</g, "&lt;")
      .replace(/>/g, "&gt;")
      .replace(/\"/g, "&quot;")
      .replace(/'/g, "&#39;");
  }

  escapeAttr(str) {
    // attribute-safe (already escapes quotes)
    return this.escapeHtml(str);
  }

  escapeJsString(str) {
    return (str ?? '').toString()
      .replace(/\\/g, '\\\\')
      .replace(/'/g, "\\'")
      .replace(/\r/g, '')
      .replace(/\n/g, '\\n')
      .replace(/&/g, '&amp;')
      .replace(/"/g, '&quot;')
      .replace(/</g, '&lt;')
      .replace(/>/g, '&gt;');
  }

  getMKB10Code(mkb10) {
    if (!mkb10) return "";
    if (typeof mkb10 === "string") {
      const s = mkb10.trim();
      if (!s) return "";
      // Prefer first token that looks like a code (e.g., M06.9)
      const m = s.match(/[A-Z][0-9]{2}(?:\.[0-9])?/i);
      return (m ? m[0] : s.split(/[\s—-]/)[0]).trim();
    }
    const code = (mkb10.code || mkb10.sifra || mkb10.sifra_mkb || "").toString().trim();
    return code;
  }

  getMKB10Desc(mkb10) {
    if (!mkb10 || typeof mkb10 === "string") return "";
    const d = (mkb10.opis || mkb10.naziv || mkb10.description || "").toString().trim();
    return d;
  }


  getMKB10Url(mkb10) {
    if (!mkb10 || typeof mkb10 !== "object") return "";
    const u = (mkb10?.izvor?.url || mkb10?.source?.url || mkb10?.izvor_url || "").toString().trim();
    return u;
  }

  
  formatMKB10Html(mkb10, opts = {}) {
    const includeLink = opts.includeLink !== false;
    const showEmpty = opts.showEmpty === true;

    const empty = showEmpty ? `<span class="muted">nije popunjeno</span>` : "";
    if (!mkb10) return empty;

    if (typeof mkb10 === "string") {
      const s = mkb10.trim();
      return s ? this.escapeHtml(s) : empty;
    }

    const code = this.getMKB10Code(mkb10);
    const desc = this.getMKB10Desc(mkb10);
    const sys = (mkb10?.izvor?.sistem || mkb10?.izvor?.system || "").toString().trim();
    const url = this.getMKB10Url(mkb10);

    const main = (code && desc)
      ? `${this.escapeHtml(code)} — ${this.escapeHtml(desc)}`
      : this.escapeHtml(code || desc);

    if (!main) return empty;

    let html = main;
    if (sys) html += ` <span class="muted" style="font-size:0.85rem; color:#666;">(${this.escapeHtml(sys)})</span>`;
    if (includeLink && url) {
      html += `<div class="muted" style="margin-top:6px; font-size:0.85rem; color:#666;">🔗 <a href="${this.escapeAttr(url)}" target="_blank" rel="noopener noreferrer">Izvor MKB-10</a></div>`;
    }
    return html;
  }


  // Backward-compatible wrapper name (used across UI)
  formatMKB10(mkb10){
    return this.formatMKB10Html(mkb10);
  }

  setModalFreqEnabled(index, enabled){
    const i = Number(index);
    if(!Number.isFinite(i)) return;
    if(!this._modalItem || !this._modalItem.frekvencije || !this._modalItem.frekvencije[i]) return;
    this._modalItem.frekvencije[i].enabled = !!enabled;

    // live update modal totals
    const enabledCount = (this._modalItem.frekvencije || []).filter(f=>f.enabled!==false).length;
    const estMin = enabledCount * (Number(this._modalItem.freq_duration_min)||0);
    const el = document.getElementById("m-proto");
    if(el){
      const loops = Number(this._modalItem.freq_loops)||1;
      const totalMin = estMin * loops;
      el.textContent = `${enabledCount} frekv. • ${this._modalItem.freq_duration_min} min x ${loops} = ~${totalMin} min`;
    }
  }

  toggleNowList(){
    if(!this.ui.nowPlayingPanel) return;

    // 🍏 iOS PRO (Rendered): WebKit has unreliable touch/scroll for checkboxes inside fixed dock.
    // Always open a fullscreen modal for skipping in this mode.
    try {
      if (this._isIOS && this.isIosBgRenderedEnabled && this.isIosBgRenderedEnabled()) {
        this.openRenderedFreqModal();
        return;
      }
    } catch(_) {}
    const open = this.ui.nowPlayingPanel.style.display !== "none";
    this.ui.nowPlayingPanel.style.display = open ? "none" : "block";

    // Jasno dugme (da ne izgleda kao "belo ništa")
    const btnNowList = document.getElementById('btn-nowlist');
    if (btnNowList) {
      btnNowList.innerHTML = open ? '▸ FREKVENCIJE' : '▾ FREKVENCIJE';
      btnNowList.setAttribute('aria-expanded', (!open).toString());
    }

    // Render list when opening (esp. iOS PRO Render mode uses fullSequence)
    if(!open) this.renderNowPlayingList();
  }


  /* ===================== iOS PRO: Frequency skip modal ===================== */

  _ensureRenderedFreqModal(){
    if (this._renderedFreqModal) return this._renderedFreqModal;
    const wrap = document.createElement('div');
    wrap.id = 'ios-pro-freq-modal';
    wrap.style.cssText = [
      'position:fixed','inset:0','z-index:99999','display:none',
      'background:rgba(0,0,0,0.35)','backdrop-filter:saturate(120%) blur(2px)'
    ].join(';');
    wrap.innerHTML = `
      <div style="position:absolute; left:0; right:0; bottom:0; max-height:86vh; background:#fff; border-top-left-radius:18px; border-top-right-radius:18px; box-shadow:0 -8px 24px rgba(0,0,0,.25); overflow:hidden;">
        <div style="display:flex; align-items:center; justify-content:space-between; padding:14px 14px 10px; border-bottom:1px solid rgba(0,0,0,.08);">
          <div style="font-size:20px; font-weight:800; color:#1f4e79;">🎵 Frekvencije (preskakanje)</div>
          <button id="ios-pro-freq-close" style="border:0; background:#f2f4f7; border-radius:12px; width:44px; height:36px; font-size:20px;">✕</button>
        </div>
        <div id="ios-pro-freq-hint" style="padding:10px 14px; font-size:14px; color:#334;">Odčekiraj da preskočiš (važi od sledećeg segmenta u PRO režimu).</div>
        <div id="ios-pro-freq-list" style="overflow:auto; -webkit-overflow-scrolling:touch; padding:6px 10px 12px; max-height:56vh;"></div>
        <div style="padding:12px 14px 16px; border-top:1px solid rgba(0,0,0,.08);">
          <button id="ios-pro-freq-done" style="width:100%; border:0; background:#22a34a; color:#fff; font-weight:800; border-radius:14px; padding:14px 12px; font-size:18px;">ZATVORI</button>
        </div>
      </div>
    `;
    document.body.appendChild(wrap);

    const close = () => { wrap.style.display = 'none'; };
    wrap.addEventListener('click', (e)=>{ if(e.target === wrap) close(); });
    const b1 = wrap.querySelector('#ios-pro-freq-close');
    const b2 = wrap.querySelector('#ios-pro-freq-done');
    if (b1) b1.addEventListener('click', close);
    if (b2) b2.addEventListener('click', close);

    this._renderedFreqModal = wrap;
    return wrap;
  }

  openRenderedFreqModal(){
    // only meaningful in iOS PRO rendered mode
    if (!this._rendered || !this._rendered.active) {
      this.showToast('ℹ️ Lista frekvencija nije dostupna (nema aktivne liste).', { timeoutMs: 4000 });
      return;
    }
    if (!this._renderedSkipSet) this._renderedSkipSet = new Set();

    const wrap = this._ensureRenderedFreqModal();
    const listEl = wrap.querySelector('#ios-pro-freq-list');
    const hintEl = wrap.querySelector('#ios-pro-freq-hint');
    if (hintEl) hintEl.textContent = 'Odčekiraj da preskočiš (važi od sledećeg segmenta u PRO režimu).';

    const seqFull = this._rendered.fullSequence || this._rendered.sequence || [];
    const cur = Number(this._lastStats?.currentIndex ?? this._rendered.lastIndex ?? this._rendered.segStartIndex ?? 0);
    const start = Math.max(0, Math.min(seqFull.length, cur));

    let html = '';
    for (let i = start; i < seqFull.length; i++) {
      const f = seqFull[i] || {};
      const hz = (f.hz ?? f.value ?? '').toString();
      const desc = (f.svrha || f.funkcija || f.desc || f.note || '').toString();
      const src = (f.izvor || f.src || '').toString();
      const enabled = !this._renderedSkipSet.has(i);
      html += `
        <div style="padding:10px 8px; border-bottom:1px solid rgba(0,0,0,.06);">
          <label style="display:flex; gap:10px; align-items:flex-start;">
            <input data-idx="${i}" type="checkbox" ${enabled ? 'checked' : ''} style="transform:scale(1.25); margin-top:3px;">
            <div>
              <div style="font-weight:800; font-size:16px;">${this.escapeHtml(hz)} Hz</div>
              ${desc ? `<div style="color:#556; font-size:13px; margin-top:2px;">${this.escapeHtml(desc)}</div>` : ''}
              ${src ? `<div style="color:#778; font-size:12px; margin-top:2px;">Izvor: ${this.escapeHtml(src)}</div>` : ''}
            </div>
          </label>
        </div>`;
    }
    if (!html) html = '<div style="padding:12px; color:#667;">Nema preostalih frekvencija.</div>';
    if (listEl) listEl.innerHTML = html;

    // bind events
    if (listEl) {
      listEl.querySelectorAll('input[type="checkbox"][data-idx]').forEach(cb => {
        cb.addEventListener('change', (e) => {
          const idx = Number(e.target.getAttribute('data-idx'));
          const on = !!e.target.checked;
          this.setRenderedFreqEnabled(idx, on);
        });
      });
    }

    wrap.style.display = 'block';
  }

  setRenderedFreqEnabled(globalIndex, enabled){
    const i = Number(globalIndex);
    if (!Number.isFinite(i)) return;
    if (!this._renderedSkipSet) this._renderedSkipSet = new Set();
    if (enabled) this._renderedSkipSet.delete(i);
    else this._renderedSkipSet.add(i);

    // Update dock preview (still disabled there, but reflects state visually)
    try { this.renderNowPlayingList({ currentIndex: Number(this._lastStats?.currentIndex ?? this._rendered?.segStartIndex ?? 0) }); } catch(_) {}
    try { this.showToast('🍏 PRO: preskakanje važi od sledećeg segmenta.', { timeoutMs: 3500 }); } catch(_) {}
  }


  /* ===== v15.7.1.5 — Dock Loop controls ===== */

  toggleDockLoopPanel(){
    const panel = document.getElementById('dock-loop-panel');
    const btn = document.getElementById('btn-dock-loop');
    if(!panel || !btn) return;

    const open = panel.style.display !== 'none';
    panel.style.display = open ? 'none' : 'block';
    btn.setAttribute('aria-expanded', (!open).toString());

    if(!open) this._syncRepeatControlsFromState('dock');
  }

  _syncRepeatControlsFromState(ctx){
    const s = { count: this.repeat?.count || 1, infinite: !!this.repeat?.infinite, scope: this.repeat?.scope || REPEAT_SCOPE_ITEM };
    this._setRepeatControls(ctx, s);
  }

  _setRepeatControls(ctx, s){
    // ctx: 'dock' | 'playlist' | 'modal'
    const isModal = (ctx === 'modal');
    const isDock = (ctx === 'dock');

    const idCount = isModal ? 'm-repeat-count' : (isDock ? 'p-repeat-count' : 'pl-repeat-count');
    const idInf = isModal ? 'm-repeat-infinite' : (isDock ? 'p-repeat-infinite' : 'pl-repeat-infinite');
    const nameScope = isModal ? 'm-repeat-scope' : (isDock ? 'p-repeat-scope' : 'pl-repeat-scope');

    try {
      const elC = document.getElementById(idCount);
      if (elC) elC.value = String(Math.max(1, Math.min(200, Number(s?.count) || 1)));
    } catch(_) {}

    try {
      const elI = document.getElementById(idInf);
      if (elI) elI.checked = !!s?.infinite;
    } catch(_) {}

    try {
      const scope = (s?.scope === REPEAT_SCOPE_QUEUE) ? REPEAT_SCOPE_QUEUE : REPEAT_SCOPE_ITEM;
      const el = document.querySelector(`input[name="${nameScope}"][value="${scope}"]`);
      if (el) el.checked = true;
    } catch(_) {}
  }

  onDockRepeatChange(){
    const s = this._readRepeatSettings('dock');
    this._applyRepeatSettings(s);

    // Keep other UIs in sync
    this._setRepeatControls('playlist', s);
    this._setRepeatControls('modal', s);

    // Persist
    try { localStorage.setItem('sinet_repeat_settings', JSON.stringify(s)); } catch(_) {}

    try { this._renderRepeatStatus(); } catch(_) {}

    try {
      if (this.isPlaying && this.isPlaying()) {
        this.showToast('🔁 Loop podešen. Važi odmah (najkasnije od sledećeg ciklusa).', { timeoutMs: 3500 });
      }
    } catch(_) {}
  }

  _restoreRepeatFromStorage(){
    try {
      const raw = localStorage.getItem('sinet_repeat_settings');
      if (!raw) return;
      const s = JSON.parse(raw);
      if (!s) return;

      this._applyRepeatSettings(s);
      this._setRepeatControls('dock', s);
      this._setRepeatControls('playlist', s);
      this._setRepeatControls('modal', s);
    } catch(_) {}
  }

  toggleFreqEnabled(index, enabled){
    const i = Number(index);
    if(!Number.isFinite(i)) return;
    if(!this.audio || !this.audio.currentSequence) return;

    this.audio.setEnabled(i, !!enabled);

    const stats = this.audio.getStats ? this.audio.getStats() : (this.audio.getState ? this.audio.getState() : null);
    if(stats && stats.currentIndex === i && !enabled){
      this.audio.skipCurrent();
    }
    this.renderNowPlayingList();
  }

  renderNowPlayingList(statsOverride=null){
    if(!this.ui.nowPlayingList) return;

    // iOS PRO (Rendered Segments): audio.currentSequence is not used → show list from rendered fullSequence.
    if((!this.audio || !this.audio.currentSequence) && this._rendered && this._rendered.active){
      const seqFull = this._rendered.fullSequence || this._rendered.sequence || [];
      const stats = statsOverride || this._lastStats || { currentIndex: Number(this._rendered.segStartIndex||0) };
      const cur = Math.max(0, Math.min(seqFull.length-1, Number(stats.currentIndex||0)));

      try {
        const hintEl = document.getElementById('nowplaying-hint');
        if (hintEl) hintEl.textContent = '🍏 iOS PRO (Render): lista je pregled. Preskakanje se primenjuje od sledećeg segmenta.';
      } catch(_) {}

      let html = "";
      for(let i=cur;i<seqFull.length;i++){
        const f = seqFull[i] || {};
        const hz = f.hz ?? f.value ?? "";
        const desc = (f.svrha || f.funkcija || f.desc || f.note || "").toString();
        const src = (f.izvor || f.src || "").toString();
        html += `
          <div class="np-row ${i===cur ? "active" : ""}" id="rfreq_${i}">
            <label>
              <input type="checkbox" checked disabled>
              <div>
                <div><b>${hz} Hz</b></div>
                ${desc ? `<div class="muted">${desc}</div>` : ""}
                ${src ? `<div class="muted">Izvor: ${src}</div>` : ""}
              </div>
            </label>
          </div>`;
      }
      this.ui.nowPlayingList.innerHTML = html || '<div class="muted" style="opacity:0.9; padding:8px;">Nema preostalih frekvencija.</div>';
      return;
    }

    if(!this.audio || !this.audio.currentSequence){
      try { const hintEl = document.getElementById('nowplaying-hint'); if (hintEl) hintEl.textContent = 'Odčekiraj da preskočiš (može i tokom rada).'; } catch(_) {}
      this.ui.nowPlayingList.innerHTML = "";
      return;
    }

    try { const hintEl = document.getElementById('nowplaying-hint'); if (hintEl) hintEl.textContent = 'Odčekiraj da preskočiš (može i tokom rada).'; } catch(_) {}
    const stats = statsOverride || (this.audio.getStats ? this.audio.getStats() : {currentIndex:0});
    const seq = this.audio.currentSequence || [];
    const cur = Number(stats.currentIndex||0);

    let html = "";
    for(let i=cur;i<seq.length;i++){
      const f = seq[i] || {};
      const hz = f.hz ?? f.value ?? "";
      const desc = (f.svrha || f.funkcija || f.desc || f.note || "").toString();
      const src = (f.izvor || f.src || "").toString();
      const enabled = this.audio.isEnabled ? this.audio.isEnabled(i) : true;
      html += `
        <div class="np-row ${i===cur ? "active" : ""} ${enabled ? "" : "disabled"}" id="freq_${i}">
          <label>
            <input type="checkbox" ${enabled ? "checked" : ""} onchange="window.app.toggleFreqEnabled(${i}, this.checked)">
            <div>
              <div><b>${hz} Hz</b></div>
              ${desc ? `<div class="muted">${desc}</div>` : ""}
              ${src ? `<div class="muted">Izvor: ${src}</div>` : ""}
            </div>
          </label>
        </div>`;
    }
    this.ui.nowPlayingList.innerHTML = html;
  }


  
  // iOS setting: experimental background mode (best-effort only)
  isIosBgExperimentalEnabled() { return !!this._iosBgExperimental; }

  setIosBgExperimental(enabled) {
    const val = !!enabled;
    this._iosBgExperimental = val;
    try { localStorage.setItem("sinet_ios_bg_experimental", val ? "1" : "0"); } catch(_) {}
    try { if (this._iosKeeper) this._iosKeeper.enabled = (this._isIOS && (val || this._iosBgRendered)); } catch(_) {}

    // If experimental is ON, force-disable PRO rendered mode (avoid conflicts)
    if (val && this._iosBgRendered) {
      this._iosBgRendered = false;
      try { localStorage.setItem("sinet_ios_bg_rendered", "0"); } catch(_) {}
      try { const el2 = document.getElementById("ios-bg-render-toggle"); if (el2) el2.checked = false; } catch(_) {}
    }

    // keeper enabled follows the final effective state
    try { if (this._iosKeeper) this._iosKeeper.enabled = (this._isIOS && (this._iosBgExperimental || this._iosBgRendered)); } catch(_) {}


    // If playing, re-apply routing immediately
    try {
      if (this.audio && this.audio.isPlaying) {
        this._teardownPlaybackSession("ios_toggle");
        this._ensurePlaybackSession();
      }
    } catch(_) {}

    try {
      const el = document.getElementById("ios-bg-toggle");
      if (el) el.checked = val;
    } catch(_) {}

    this.showToast(val
      ? "🍏 iOS: pozadinski zvuk uključen (eksperimentalno). Ako čuješ pulsiranje, isključi opciju."
      : "🍏 iOS: pozadinski zvuk isključen (stabilno). U pozadini može stati (Safari ograničenje).",
      { timeoutMs: 6500 }
    );
  }


  setIosBgRendered(enabled) {
    const val = !!enabled;
    this._iosBgRendered = val;
    try { localStorage.setItem("sinet_ios_bg_rendered", val ? "1" : "0"); } catch(_) {}

    // keeper enabled follows rendered/experimental state
    try { if (this._iosKeeper) this._iosKeeper.enabled = (this._isIOS && (val || this._iosBgExperimental)); } catch(_) {}

    // If rendered is ON, force-disable experimental MediaStream route (avoid pulsing conflicts)
    if (val && this._iosBgExperimental) {
      this._iosBgExperimental = false;
      try { localStorage.setItem("sinet_ios_bg_experimental", "0"); } catch(_) {}
      try { const el = document.getElementById("ios-bg-toggle"); if (el) el.checked = false; } catch(_) {}
    }

    // keeper enabled follows the final effective state
    try { if (this._iosKeeper) this._iosKeeper.enabled = (this._isIOS && (this._iosBgExperimental || this._iosBgRendered)); } catch(_) {}

    // Update UI checkbox
    try {
      const el2 = document.getElementById("ios-bg-render-toggle");
      if (el2) el2.checked = val;
    } catch(_) {}

    // If playing, re-apply routing immediately
    try {
      if (this.audio && (this.audio.isPlaying || this._isRenderedPlaying?.())) {
        this._teardownPlaybackSession("ios_render_toggle");
        this._ensurePlaybackSession();
      }
    } catch(_) {}

    this.showToast(enabled
      ? "🍏 iOS: PRO režim uključen — protokol se renderuje u WAV (RAM) i može da nastavi u pozadini (web-only, bez garancije)."
      : "🍏 iOS: PRO režim isključen.",
      { timeoutMs: 8000 }
    );
  }

  // v15.7.1.5: Bugfix — v15.6.8 referenced isIosBgRenderedEnabled() in several
  // places (playback start / settings), but the method was missing. On iPhone
  // this throws and prevents the "Play frekvencije" panel from opening.
  isIosBgRenderedEnabled() {
    return !!this._iosBgRendered;
  }

  // Defensive alias (mixed naming in older patches)
  isIOSBgRenderedEnabled() {
    return this.isIosBgRenderedEnabled();
  }

/* ===================== iOS / Background Hardening ===================== */

  _ensurePlaybackSession() {
    // iOS: browsers are sandboxed; background/lock-screen playback is NOT guaranteed.
    // Default behavior (stable): pause when app goes to background.
    // Optional (experimental): route WebAudio output through HTMLMediaElement (MediaStream).
    if (this._isIOS) {
      const exp = this.isIosBgExperimentalEnabled();
      const pro = this.isIosBgRenderedEnabled();

      // iOS: unlock / keep audio session alive via a silent HTMLAudio loop.
      // IMPORTANT: must be started from a user gesture at least once.
      try {
        if ((exp || pro) && this._iosKeeper) {
          this._iosKeeper.enabled = true;
          this._iosKeeper.start();
        } else if (this._iosKeeper) {
          this._iosKeeper.stop();
        }
      } catch(_) {}

// Prime hidden media elements (per-element unlock on iOS can be strict).
// We prime with SILENT_WAV to avoid audible noise, but it still counts as a user-initiated play.
try {
  if (pro) {
    const el = this._ensureIosRenderedEl();
    if (el && el.paused) {
      el.src = SILENT_WAV_DATA_URI;
      el.loop = true;
      el.preload = "auto";
      el.playsInline = true;
      el.setAttribute && el.setAttribute("playsinline", "");
      el.volume = 0.0001;
      el.muted = false;
      const pr = el.play();
      if (pr && pr.catch) pr.catch(()=>{});
    }
  } else if (exp && !pro) {
    const el2 = this._ensureIosMediaOutEl();
    if (el2 && el2.paused) {
      el2.src = SILENT_WAV_DATA_URI;
      el2.loop = true;
      el2.preload = "auto";
      el2.playsInline = true;
      el2.setAttribute && el2.setAttribute("playsinline", "");
      el2.volume = 0.0001;
      el2.muted = false;
      const pm = el2.play();
      if (pm && pm.catch) pm.catch(()=>{});
    }
  }
} catch(_) {}

      // Configure output routing
      try {
        if (this.audio && typeof this.audio.disableMediaOutput === "function") {
          this.audio.disableMediaOutput();
        }
        if (exp && !pro && this.audio && typeof this.audio.enableMediaOutput === "function") {
          const outEl = this._ensureIosMediaOutEl();
          this.audio.enableMediaOutput(outEl);
        }
      } catch(_) {}

      // One-time UX hint (dismissible)
      try {
        const dismissed = localStorage.getItem("sinet_ios_bg_hint") === "1";
        if (!dismissed && !this._iosHintShown) {
          this._iosHintShown = true;
          this.showToast(
            exp
              ? "🍏 iOS (eksperimentalno): u pozadini može doći do pulsiranja ili prekida. Ako smeta, isključi opciju u Podešavanjima."
              : "🍏 iOS: Safari uglavnom pauzira zvuk u pozadini/na lock-screen. Ovo je stabilan režim.",
            {
              timeoutMs: 9000,
              actionLabel: "Ne prikazuj više",
              action2Onclick: "localStorage.setItem('sinet_ios_bg_hint','1')"
            }
          );
        }
      } catch(_) {}
    }

    // Keep AudioContext resumed while playing (best-effort)
    if (this._ctxHeartbeat) return;
    this._ctxHeartbeat = setInterval(() => {
      this._resumeAudioContextBestEffort();
    }, 1500);
  }

  _teardownPlaybackSession(reason="") {
    if (this._ctxHeartbeat) {
      clearInterval(this._ctxHeartbeat);
      this._ctxHeartbeat = null;
    }

    // Always stop iOS helper elements; keep routing clean between sessions
    // iOS PRO: stop rendered track if any
    try { if (this._rendered && this._rendered.active) this._stopRenderedTrack(); } catch(_) {}
    try { if (this._iosKeeper) this._iosKeeper.stop(); } catch(_) {}
    try { if (this.audio && typeof this.audio.disableMediaOutput === "function") this.audio.disableMediaOutput(); } catch(_) {}
    try {
      if (this._iosMediaOutEl) {
        this._iosMediaOutEl.pause?.();
        try { this._iosMediaOutEl.srcObject = null; } catch(_) {}
      }
    } catch(_) {}
  }

  _resumeAudioContextBestEffort() {
    try {
      const ctx = this.audio?.audioContext || null;
      if (ctx && ctx.state === "suspended") {
        ctx.resume().catch(() => {});
      }
    } catch(_) {}
  }

  _bindBackgroundRecovery() {
    // When page becomes visible again, try to resume context (some iOS builds suspend it).
    const kick = () => this._resumeAudioContextBestEffort();

    document.addEventListener("visibilitychange", () => {
      // iOS note:
      // - We do NOT force-pause on background anymore (some iOS builds can actually keep <audio> playing).
      // - If Safari still suspends audio, we will notify the user AFTER they return to the app.
      if (document.hidden) {
        try {
          if (this._isIOS && (this.audio?.isPlaying || this._rendered?.active || this._isRenderedPlaying?.())) {
            this._iosBgWasPlaying = true;
          }
        } catch(_) {}
        return;
      }

      kick();

      try {
        if (this._isIOS && this._iosBgWasPlaying) {
          this._iosBgWasPlaying = false;

          const playingNow = !!(this.audio?.isPlaying || this._rendered?.active || this._isRenderedPlaying?.());
          if (!playingNow) {
            const proOn = !!(this.isIosBgRenderedEnabled && this.isIosBgRenderedEnabled());
            const msg = proOn
              ? "🍏 iOS: Safari je pauzirao zvuk u pozadini. Tapni ▶ za nastavak."
              : "🍏 iOS: Safari je pauzirao zvuk u pozadini. Preporuka: uključi 🍏 iPhone PRO (Render) u Podešavanjima, pa ▶.";

            this.showToast(msg, {
              timeoutMs: 12000,
              actionLabel: "Podešavanja",
              actionNav: "settings",
              action2Label: proOn ? "" : "Uključi PRO",
              action2Onclick: proOn ? "" : "window.app && window.app.setIosBgRendered(true)"
            });
          }
        }
      } catch(_) {}
    });


window.addEventListener("pageshow", kick);
    window.addEventListener("focus", kick);
    window.addEventListener("resume", kick);
  }

  _setupMediaSession() {
    try {
      if (!("mediaSession" in navigator)) return;
      const ms = navigator.mediaSession;

      const safe = (fn) => () => { try { fn(); } catch(_) {} };

      ms.setActionHandler?.("play", safe(() => {
        if (!this.audio.isPlaying) this.togglePlayPause();
      }));
      ms.setActionHandler?.("pause", safe(() => {
        if (this.audio.isPlaying) this.togglePlayPause();
      }));
      ms.setActionHandler?.("stop", safe(() => {
        this.stopPlayer(true);
      }));
      ms.setActionHandler?.("previoustrack", safe(() => {
        if (this.isPlaylistActive) this.playPlaylistItem(Math.max(0, this.currentPlaylistIndex - 1));
      }));
      ms.setActionHandler?.("nexttrack", safe(() => {
        if (this.isPlaylistActive) this.playPlaylistItem(this.currentPlaylistIndex + 1);
      }));

      this._mediaSessionReady = true;
    } catch(_) {
      this._mediaSessionReady = false;
    }
  }

  _updateMediaSessionMetadata(freqObj, stats) {
    if (!this._mediaSessionReady) return;
    try {
      const hz = Number(freqObj?.value ?? freqObj?.hz) || 0;
      const symptom = (this.selectedItem?.simptom || "SINET Terapija").toString();
      const pos = (Number(stats?.currentIndex) || 0) + 1;
      const total = Number(stats?.totalItems) || 0;

      const title = hz ? `${hz} Hz  (${pos}/${total})` : `SINET Terapija (${pos}/${total})`;
      const artist = "SINET Audio Lekar";
      const album = symptom;

      navigator.mediaSession.metadata = new MediaMetadata({
        title,
        artist,
        album
      });
    } catch(_) {}
  }

  formatTime(sec) {
    const n = Number(sec);
    if (!Number.isFinite(n)) return '∞';
    const s = Math.max(0, Math.floor(n||0));
    const m = Math.floor(s/60);
    const r = s%60;
    return `${m}:${r<10 ? "0"+r : r}`;
  }
}

const app = new App();


// === DS Generator integration (safe global hooks) ===
window.openDSGenerator = function(){
  try{
    const a = window.app;
    const itemId = (a && a.selectedItem && a.selectedItem.id) ? a.selectedItem.id : '';
    const back = encodeURIComponent(window.location.href);
    const url = `DS-Generator.html?catalog=./data/SINET_STL.json${itemId ? `&freqId=${encodeURIComponent(itemId)}` : ''}&back=${back}`;
    window.open(url, '_blank');
  }catch(e){
    console.warn('openDSGenerator failed', e);
    window.open('DS-Generator.html?catalog=./data/SINET_STL.json', '_blank');
  }
};

async function __importDsBridgeIfAny(){
  try{
    const raw = localStorage.getItem('SINET_DS_BRIDGE');
    if(!raw) return false;

    const payload = JSON.parse(raw);
    if(!payload) return false;
    try{ if(payload.sharedContext && window.SINET_SharedContext && window.SINET_SharedContext.refreshSharedContext){ window.SINET_SharedContext.refreshSharedContext('sinet', payload.sharedContext); } }catch(_){ }
let p0 = null;
let bridgeGuide = null;
let bridgeMeta = null;

// Accept:
// - SINET_DS_TO_PROTOCOL_v1 (from DS/Anamneza "Ubaci u SINET")
// - SINET_SHAREPACK_v1 (downloaded share pack)
if(payload.format === 'SINET_DS_TO_PROTOCOL_v1' && payload.protocol){
  p0 = payload.protocol;
  bridgeGuide = payload.guideHtml || payload.guide || (payload.protocol && payload.protocol._guideHtml) || null;
  bridgeMeta = payload.meta || payload._meta || (payload.protocol && (payload.protocol._meta || payload.protocol.meta)) || null;
} else if(payload.format === 'SINET_SHAREPACK_v1' && payload.protocol){
  p0 = payload.protocol;
  bridgeGuide = payload.guideHtml || payload.guide || (payload.protocol && payload.protocol._guideHtml) || null;
  bridgeMeta = payload.meta || payload._meta || (payload.protocol && (payload.protocol._meta || payload.protocol.meta)) || null;
} else if(payload.protocol){
  // backward compat: plain {protocol: {...}}
  p0 = payload.protocol;
  bridgeGuide = payload.guideHtml || payload.guide || (payload.protocol && payload.protocol._guideHtml) || null;
  bridgeMeta = payload.meta || payload._meta || (payload.protocol && (payload.protocol._meta || payload.protocol.meta)) || null;
} else {
  return false;
}


    if(!p0 || typeof p0 !== 'object' || !Array.isArray(p0.steps) || p0.steps.length===0){
      localStorage.removeItem('SINET_DS_BRIDGE');
      window.app?.showToast?.('⚠️ DS Generator: protokol je prazan ili neispravan.', {timeoutMs: 9000});
      return false;
    }

    const a = window.app;
    if(!a) return false;

    // Ensure db exists
    if(!a.db && window.db) a.db = window.db;

    const p = JSON.parse(JSON.stringify(p0));
    if(bridgeGuide && !p._guideHtml) p._guideHtml = String(bridgeGuide);
    if(bridgeMeta && !p._meta) p._meta = bridgeMeta;

    if(!p.id) p.id = a._newProtoId?.() || (`DS_${Date.now()}`);
    if(!p.name) p.name = 'DS protokol';

    // Normalize dates for IndexedDB / UI
    const now = Date.now();
    if(!p.createdAt || (typeof p.createdAt === 'string')) p.createdAt = now;
    p.updatedAt = now;
    if(!Number.isFinite(p.loopCount)) p.loopCount = 1;

    // Avoid id clash
    try{
      await a.loadProtocolsFromDB?.();
      if(Array.isArray(a.protocols) && a.protocols.some(x=>x.id===p.id)){
        p.id = a._newProtoId?.() || (`DS_${Date.now()}`);
      }
    }catch(_){}

    // Persist
    if(a.db?.putProtocol){
      await a.db.putProtocol(p);
    }else{
      a.protocols = Array.isArray(a.protocols) ? a.protocols : [];
      a.protocols.unshift(p);
    }

    // Cleanup bridge
    localStorage.removeItem('SINET_DS_BRIDGE');

    // Reload + show
    try{ await a.loadProtocolsFromDB?.(); }catch(_){}
    try{ a.nav?.('protocols'); }catch(_){}
    try{ a.renderProtocolsUI?.(); }catch(_){}

    a.showToast?.(`✅ DS Generator: protokol dodat u "Moji protokoli" (${p.name}).`, {timeoutMs: 10000});
    return true;
  }catch(e){
    console.warn('__importDsBridgeIfAny failed', e);
    return false;
  }
}

async function __importUserSymptomBridgeIfAny(){
  try{
    const raw = localStorage.getItem('SINET_USER_SYMPTOM_BRIDGE');
    if(!raw) return false;
    const payload = JSON.parse(raw || '{}');
    let item = payload?.item || payload;
    if(!item || typeof item !== 'object') return false;

    const a = window.app;
    if (!a) return false;

    const baseId = String(item.id || ('usr-' + (item.simptom || item.naziv || 'simptom'))).toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'') || ('usr-' + Date.now());
    let id = baseId;
    const used = new Set((a.userSymptoms || []).map(x => x.id));
    if (used.has(id)) {
      let k = 2;
      while (used.has(`${id}-${k}`)) k++;
      id = `${id}-${k}`;
    }

    const freqs = window.SINET_FrequencySchema?.normalizeFrequencies ? window.SINET_FrequencySchema.normalizeFrequencies(Array.isArray(item.frekvencije) ? item.frekvencije : []) : (Array.isArray(item.frekvencije) ? item.frekvencije.map(f => ({
      hz: Number(f.hz ?? f.value ?? '') || '',
      svrha: String(f.svrha || f.funkcija || f.opis || f.naziv || '').trim(),
      izvor: typeof f.izvor === 'string' ? f.izvor : String(f?.izvor?.url || f?.izvor?.delo || '').trim()
    })).filter(f => f.hz || f.svrha || f.izvor) : []);

    const out = {
      id,
      simptom: String(item.simptom || item.naziv || 'Uvezeni simptom').trim(),
      oblast: String(item.oblast || 'MOJI SIMPTOMI').trim(),
      opis: String(item.opis || item.description || '').trim(),
      mkb10: typeof item.mkb10 === 'string' ? item.mkb10 : String(item?.mkb10?.sifra || '').trim(),
      izvor: typeof item.izvor === 'string' ? item.izvor : String(item?.izvor?.url || item?.izvor?.delo || '').trim(),
      frekvencije: freqs
    };

    a.userSymptoms = Array.isArray(a.userSymptoms) ? a.userSymptoms : [];
    a.userSymptoms.unshift(out);
    await a.saveUserSymptoms?.();
    a.applyUserDataToCatalog?.();
    a.renderUserSymptomsList?.();
    try{ a.renderSystemPresets?.(); }catch(_){}
    try{ if (document.getElementById('page-catalog')?.classList?.contains('active')) a.showCatalogHome?.(); }catch(_){}
    try{ a.nav?.('mysymptoms'); }catch(_){}
    try{ a.showToast?.(`✅ Uvezeno u "Moji simptomi": ${out.simptom}`, { timeoutMs: 10000 }); }catch(_){}

    localStorage.removeItem('SINET_USER_SYMPTOM_BRIDGE');
    return true;
  }catch(e){
    console.warn('__importUserSymptomBridgeIfAny failed', e);
    return false;
  }
}


function setupMobileHeaderCompact(){
  try{
    if (!(window.matchMedia && window.matchMedia('(max-width: 520px)').matches)) return;
    const header = document.querySelector('header');
    const main = document.querySelector('main');
    if (!header || !main) return;
    let ticking = false;
    const apply = () => {
      const y = main.scrollTop || 0;
      if (y > 24) document.body.classList.add('header-compact');
      else document.body.classList.remove('header-compact');
    };
    const onScroll = () => {
      if (ticking) return;
      ticking = true;
      requestAnimationFrame(() => { ticking = false; apply(); });
    };
    main.addEventListener('scroll', onScroll, { passive:true });
    apply();
  }catch(_){}
}



/* ===================== v16.0.0.25 — MKB + Freq Modal + AI/DS History ===================== */
(function(){
  const AI_HISTORY_KEY = 'SINET_AI_HISTORY_V1';
  const DS_HISTORY_KEY = 'sinetHistory';
  const DS_EDIT_STATE_KEY = 'SINET_DS_EDIT_STATE';
  const FREQ_RECENT_KEY = 'SINET_FREQ_RECENT_V1';

  App.prototype._downloadJson = function(obj, filename) {
    const blob = new Blob([JSON.stringify(obj, null, 2)], { type: 'application/json;charset=utf-8' });
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = filename;
    a.click();
    URL.revokeObjectURL(a.href);
  };

  App.prototype._ensureMkbData = async function() {
    if (this._mkbReady) return this._mkbReady;
    this._mkbReady = (async () => {
      try {
        const [mkbRes, dxRes] = await Promise.all([
          fetch('data/mkb10_sr.json', { cache: 'no-store' }),
          fetch('data/sinet_dx_index.json', { cache: 'no-store' })
        ]);
        const mkbJson = mkbRes.ok ? await mkbRes.json() : { entries: [] };
        const dxJson = dxRes.ok ? await dxRes.json() : {};
        this._mkbEntries = Array.isArray(mkbJson?.entries) ? mkbJson.entries : [];
        this._mkbDxIndex = (dxJson && typeof dxJson === 'object') ? dxJson : {};
        this._mkbSearch = this._mkbEntries.map((e, idx) => ({
          idx,
          code: String(e.code || '').trim(),
          title: String(e.title || '').trim(),
          norm: this._norm((e.code || '') + ' ' + (e.title || ''))
        }));
        this.log('DATA', 'MKB Data Loaded', String(this._mkbEntries.length));
      } catch (e) {
        this._mkbEntries = [];
        this._mkbDxIndex = {};
        this._mkbSearch = [];
        this.log('DATA', 'MKB Data Load Failed', e?.message || String(e));
      }
    })();
    return this._mkbReady;
  };

  App.prototype._findItemById = function(id) {
    return (Array.isArray(this.catalogItems) ? this.catalogItems : []).find(it => String(it?.id||'') === String(id||'')) || null;
  };

  App.prototype._mkbTitleByCode = function(code) {
    const c = String(code || '').trim().toUpperCase();
    if (!c) return '';
    const hit = (this._mkbEntries || []).find(e => String(e.code || '').toUpperCase() === c);
    return hit ? String(hit.title || '') : '';
  };

  App.prototype._mkbSuggestionForText = function(text, limit = 8) {
    const q = this._norm(text || '');
    if (!q) return [];
    const direct = [];
    for (const row of (this._mkbSearch || [])) {
      if (row.code && this._norm(row.code) === q) return [row];
      if (!row.norm) continue;
      if (row.norm.includes(q) || q.includes(row.code.toLowerCase())) direct.push({ row, score: 120 });
    }
    const tokens = Array.from(new Set(q.split(/\s+/).filter(t => t && t.length > 2)));
    const scored = [];
    for (const row of (this._mkbSearch || [])) {
      let score = 0;
      for (const t of tokens) {
        if (row.norm.includes(t)) score += (row.title.toLowerCase().includes(t) ? 8 : 4);
      }
      if (score > 0) scored.push({ row, score });
    }
    const merged = direct.concat(scored)
      .sort((a,b) => (b.score - a.score) || a.row.code.localeCompare(b.row.code))
      .map(x => x.row);
    const uniq = [];
    const seen = new Set();
    for (const r of merged) {
      if (seen.has(r.code)) continue;
      seen.add(r.code);
      uniq.push(r);
      if (uniq.length >= limit) break;
    }
    return uniq;
  };

  App.prototype._mkbSuggestionForItem = function(item) {
    const code = this.getMKB10Code?.(item?.mkb10) || (typeof item?.mkb10 === 'string' ? item.mkb10 : '');
    if (code) {
      const title = this._mkbTitleByCode(code);
      return code ? { code, title, source: 'existing' } : null;
    }
    const title = String(item?.naziv || item?.title || '');
    const opis = String(item?.opis || '');
    const oblast = String(item?.oblast || '');
    const q = [title, opis, oblast].filter(Boolean).join(' • ');
    const hits = this._mkbSuggestionForText(q, 1);
    if (hits[0]) return { code: hits[0].code, title: hits[0].title, source: 'suggested' };
    return null;
  };

  App.prototype._ensureWorkbenchEnhancements = async function() {
    await this._ensureMkbData();
    const mkbInput = document.getElementById('wb-f-mkb10');
    if (mkbInput && !document.getElementById('wb-mkb-actions')) {
      const wrap = document.createElement('div');
      wrap.id = 'wb-mkb-actions';
      wrap.style.cssText = 'display:grid; gap:8px; grid-template-columns:1fr 1fr; margin-top:8px;';
      wrap.innerHTML = `
        <button type="button" class="wb-btn wb-btn-secondary" onclick="app.openMkbPicker()">🏷 MKB lista</button>
        <button type="button" class="wb-btn wb-btn-secondary" onclick="app.workbenchSuggestMkb()">✨ Predlog</button>
      `;
      mkbInput.insertAdjacentElement('afterend', wrap);
      const prev = document.createElement('div');
      prev.id = 'wb-mkb-preview';
      prev.style.cssText = 'margin-top:8px; padding:10px; border:1px solid #edf2f7; border-radius:12px; background:#f8fbff; font-size:0.9rem; color:#234;';
      wrap.insertAdjacentElement('afterend', prev);
    }
    this.renderWorkbenchMkbPreview();
  };

  App.prototype.renderWorkbenchMkbPreview = function() {
    const box = document.getElementById('wb-mkb-preview');
    if (!box) return;
    const code = String(document.getElementById('wb-f-mkb10')?.value || '').trim();
    if (!code) {
      const sug = this._mkbSuggestionForItem(this._wbFormItem || {});
      box.innerHTML = sug ? `<b>Predlog:</b> ${this.escapeHtml(sug.code)} — ${this.escapeHtml(sug.title || '')}` : '<span style="color:#667085;">MKB nije upisan. Klikni <b>MKB lista</b> ili <b>Predlog</b>.</span>';
      return;
    }
    const title = this._mkbTitleByCode(code) || '';
    box.innerHTML = `<b>${this.escapeHtml(code)}</b>${title ? ' — ' + this.escapeHtml(title) : ''}`;
  };

  App.prototype.workbenchSuggestMkb = async function() {
    await this._ensureMkbData();
    const sug = this._mkbSuggestionForItem(this._wbFormItem || {});
    if (!sug) {
      this._wbMsg('Nema jasnog MKB predloga za ovu stavku.', 'err');
      this.renderWorkbenchMkbPreview();
      return;
    }
    const el = document.getElementById('wb-f-mkb10');
    if (el) el.value = sug.code;
    this.workbenchFormChanged();
    this.renderWorkbenchMkbPreview();
    this._wbMsg(`MKB predlog: ${sug.code}`, '');
  };

  App.prototype._ensureMkbModal = function() {
    let modal = document.getElementById('mkb-picker-modal');
    if (modal) return modal;
    modal = document.createElement('div');
    modal.id = 'mkb-picker-modal';
    modal.style.cssText = 'position:fixed; inset:0; display:none; align-items:center; justify-content:center; background:rgba(15,23,42,.55); padding:14px; z-index:10070;';
    modal.innerHTML = `
      <div class="sinet-picker-modal">
        <div class="sinet-picker-head">
          <div><div class="sinet-picker-title">🏷 MKB-10 lista</div><div class="sinet-picker-sub">Pretraga po šifri ili nazivu.</div></div>
          <button type="button" class="sinet-picker-close" onclick="app.closeMkbPicker()">✖</button>
        </div>
        <div class="sinet-picker-body">
          <input id="mkb-picker-search" class="sinet-picker-search" type="text" placeholder="npr. E06.3, štitna, bol, nesanica..." oninput="app.renderMkbPickerList(this.value)" />
          <div id="mkb-picker-list" class="sinet-picker-list"></div>
        </div>
      </div>`;
    modal.addEventListener('click', (ev) => { if (ev.target === modal) this.closeMkbPicker(); });
    document.body.appendChild(modal);
    return modal;
  };

  App.prototype.openMkbPicker = async function(seed='') {
    await this._ensureMkbData();
    const modal = this._ensureMkbModal();
    const input = document.getElementById('mkb-picker-search');
    const seedValue = seed || document.getElementById('wb-f-mkb10')?.value || document.getElementById('wb-f-naziv')?.value || '';
    if (input) input.value = String(seedValue || '');
    this.renderMkbPickerList(seedValue);
    modal.style.display = 'flex';
    setTimeout(() => input?.focus(), 30);
  };

  App.prototype.closeMkbPicker = function() {
    const modal = document.getElementById('mkb-picker-modal');
    if (modal) modal.style.display = 'none';
  };

  App.prototype.renderMkbPickerList = function(query='') {
    const box = document.getElementById('mkb-picker-list');
    if (!box) return;
    const hits = query ? this._mkbSuggestionForText(query, 80) : (this._mkbSearch || []).slice(0, 80);
    box.innerHTML = hits.length ? hits.map(h => `<button type="button" class="sinet-picker-item" onclick="app.applyMkbToWorkbench('${this.escapeJsString(h.code)}')"><div class="sinet-picker-item-title">${this.escapeHtml(h.code)} — ${this.escapeHtml(h.title)}</div></button>`).join('') : '<div class="sinet-picker-empty">Nema rezultata.</div>';
  };

  App.prototype.applyMkbToWorkbench = function(code) {
    const el = document.getElementById('wb-f-mkb10');
    if (el) el.value = String(code || '');
    this.workbenchFormChanged();
    this.renderWorkbenchMkbPreview();
    this.closeMkbPicker();
  };

  App.prototype._ensureFreqPickerModal = function() {
    let modal = document.getElementById('freq-picker-modal');
    if (modal) return modal;
    modal = document.createElement('div');
    modal.id = 'freq-picker-modal';
    modal.style.cssText = 'position:fixed; inset:0; display:none; align-items:center; justify-content:center; background:rgba(15,23,42,.55); padding:14px; z-index:10060;';
    modal.innerHTML = `
      <div class="sinet-picker-modal">
        <div class="sinet-picker-head">
          <div><div class="sinet-picker-title">🎧 Frequency Picker</div><div class="sinet-picker-sub">Pretraga po Hz, nazivu, opisu ili tagovima.</div></div>
          <button type="button" class="sinet-picker-close" onclick="app.closeFreqPickerModal()">✖</button>
        </div>
        <div class="sinet-picker-body">
          <input id="freq-picker-search" class="sinet-picker-search" type="text" placeholder="npr. 528, theta, san, focus..." oninput="app.renderFreqPickerList(this.value)" />
          <div id="freq-picker-recent" class="sinet-picker-chips"></div>
          <div id="freq-picker-list" class="sinet-picker-list"></div>
        </div>
      </div>`;
    modal.addEventListener('click', (ev) => { if (ev.target === modal) this.closeFreqPickerModal(); });
    document.body.appendChild(modal);
    return modal;
  };

  App.prototype._loadFreqRecent = function() {
    try { const raw = localStorage.getItem(FREQ_RECENT_KEY); return raw ? JSON.parse(raw) : []; } catch(_) { return []; }
  };
  App.prototype._saveFreqRecent = function(arr) {
    try { localStorage.setItem(FREQ_RECENT_KEY, JSON.stringify((arr || []).slice(0, 12))); } catch(_) {}
  };
  App.prototype._rememberFreqRecent = function(hz) {
    const v = String(hz || '').trim();
    if (!v) return;
    const list = this._loadFreqRecent().filter(x => String(x) !== v);
    list.unshift(v);
    this._saveFreqRecent(list);
  };
  App.prototype._getFreqCatalogEntries = function(query='') {
    const catalog = (this.freqSchema && this.freqSchema._catalog) ? this.freqSchema._catalog : {};
    const norm = this._norm(query || '');
    const rows = Object.keys(catalog).map(hz => {
      const m = catalog[hz] || {};
      return { hz, naziv: String(m.naziv||''), opis: String(m.opis||''), izvor: String(m.izvor||''), tags: Array.isArray(m.tags) ? m.tags : [], norm: this._norm([hz, m.naziv, m.opis, (Array.isArray(m.tags)?m.tags.join(' '):'')].join(' ')) };
    });
    if (!norm) return rows.slice(0, 120);
    const out = [];
    for (const row of rows) {
      if (row.norm.includes(norm)) out.push(row);
      if (out.length >= 120) break;
    }
    return out;
  };

  App.prototype.openFreqPickerFor = function(target='workbench') {
    this._freqPickerTarget = target;
    const modal = this._ensureFreqPickerModal();
    const input = document.getElementById('freq-picker-search');
    if (input) input.value = '';
    this.renderFreqPickerRecent();
    this.renderFreqPickerList('');
    modal.style.display = 'flex';
    setTimeout(() => input?.focus(), 30);
  };
  App.prototype.closeFreqPickerModal = function() {
    const modal = document.getElementById('freq-picker-modal');
    if (modal) modal.style.display = 'none';
  };
  App.prototype.renderFreqPickerRecent = function() {
    const box = document.getElementById('freq-picker-recent');
    if (!box) return;
    const rec = this._loadFreqRecent();
    if (!rec.length) { box.innerHTML = ''; return; }
    box.innerHTML = '<div class="sinet-picker-chip-label">Nedavno:</div>' + rec.map(hz => `<button type="button" class="sinet-picker-chip" onclick="app.freqPickerChoose('${this.escapeJsString(hz)}')">${this.escapeHtml(hz)} Hz</button>`).join('');
  };
  App.prototype.renderFreqPickerList = function(query='') {
    const box = document.getElementById('freq-picker-list');
    if (!box) return;
    const rows = this._getFreqCatalogEntries(query);
    box.innerHTML = rows.length ? rows.map(row => `
      <div class="sinet-picker-item freq-item">
        <div class="sinet-picker-item-title">🎧 ${this.escapeHtml(row.hz)} Hz — ${this.escapeHtml(row.naziv || 'Frekvencija')}</div>
        ${row.opis ? `<div class="sinet-picker-item-sub">${this.escapeHtml(row.opis.slice(0, 220))}</div>` : ''}
        <div class="sinet-picker-item-foot">
          ${row.izvor ? `<span>${this.escapeHtml(row.izvor.slice(0, 120))}</span>` : '<span>Bez dodatnog izvora</span>'}
          <button type="button" class="sinet-picker-add" onclick="app.freqPickerChoose('${this.escapeJsString(row.hz)}')">➕ Dodaj</button>
        </div>
      </div>`).join('') : '<div class="sinet-picker-empty">Nema rezultata za ovu pretragu.</div>';
  };
  App.prototype.freqPickerChoose = function(hz) {
    const v = String(hz || '').trim();
    if (!v) return;
    this._rememberFreqRecent(v);
    const meta = (this.freqSchema && this.freqSchema.getMeta) ? this.freqSchema.getMeta(v) : {};
    if (this._freqPickerTarget === 'user') {
      const idx = this.addUserFreqRow({ hz: v, naziv: meta?.naziv || '', opis: meta?.opis || '', izvor: meta?.izvor || '' });
      const rowHz = document.querySelector(`[data-fhz="${idx}"]`);
      rowHz?.focus();
    } else {
      if (!this._wbFormItem) this._wbFormItem = { frekvencije: [] };
      if (!Array.isArray(this._wbFormItem.frekvencije)) this._wbFormItem.frekvencije = [];
      this._wbFormItem.frekvencije.push({ hz: v, naziv: meta?.naziv || '', opis: meta?.opis || '', izvor: meta?.izvor || '' });
      this.workbenchFormChanged();
      this.workbenchRenderFreqList();
    }
    this.closeFreqPickerModal();
  };
  App.prototype.workbenchOpenFreqPicker = function() { this.openFreqPickerFor('workbench'); };
  App.prototype.userSymptomOpenFreqPicker = function() { this.openFreqPickerFor('user'); };

  App.prototype._aiHistory = function() {
    try { const raw = localStorage.getItem(AI_HISTORY_KEY); const arr = raw ? JSON.parse(raw) : []; return Array.isArray(arr) ? arr : []; } catch(_) { return []; }
  };
  App.prototype._saveAiHistory = function(arr) {
    try { localStorage.setItem(AI_HISTORY_KEY, JSON.stringify((arr || []).slice(0, 40))); } catch(_) {}
  };
  App.prototype._getCurrentAiState = function() {
    return {
      symptom: String(document.getElementById('ai_symptom')?.value || '').trim(),
      desc: String(document.getElementById('ai_desc')?.value || '').trim(),
      age: String(document.getElementById('ai_age')?.value || '').trim(),
      sex: String(document.getElementById('ai_sex')?.value || 'nebitno').trim(),
      med: String(document.getElementById('ai_med')?.value || '').trim(),
      goal: String(document.getElementById('ai_goal')?.value || '').trim(),
      mkb: String(document.getElementById('ai_mkb')?.value || '').trim(),
      prompt: String(document.getElementById('ai_prompt')?.value || '').trim(),
      aiResult: String(document.getElementById('ai_result')?.value || '').trim(),
      batchResult: String(document.getElementById('ai_batch_result')?.value || '').trim()
    };
  };
  App.prototype._upsertAiHistory = function(patch = {}) {
    const items = this._aiHistory();
    const base = this._getCurrentAiState();
    const id = patch.id || ('aih-' + Date.now());
    const entry = Object.assign({ id, createdAt: new Date().toISOString() }, base, patch, { updatedAt: new Date().toISOString() });
    const idx = items.findIndex(x => x.id === id);
    if (idx >= 0) items[idx] = Object.assign({}, items[idx], entry);
    else items.unshift(entry);
    this._saveAiHistory(items);
    this._lastAiHistoryId = id;
    return entry;
  };
  App.prototype.renderAiHistory = function() {
    const box = document.getElementById('ai-history-list');
    if (!box) return;
    const items = this._aiHistory();
    if (!items.length) { box.innerHTML = '<div class="muted">Još nema sačuvanih AI pokušaja.</div>'; return; }
    box.innerHTML = items.map((it, idx) => `
      <div class="hist-card">
        <div class="hist-title">${this.escapeHtml(it.symptom || '(bez naziva)')}</div>
        <div class="hist-sub">${this.escapeHtml([it.mkb, it.sex, it.age ? it.age + ' g.' : '', it.updatedAt ? new Date(it.updatedAt).toLocaleString('sr-RS') : ''].filter(Boolean).join(' • '))}</div>
        ${it.goal ? `<div class="hist-desc">🎯 ${this.escapeHtml(it.goal.slice(0, 160))}</div>` : ''}
        <div class="hist-actions">
          <button type="button" onclick="app.aiHistoryLoad(${idx})">↩ Učitaj</button>
          <button type="button" onclick="app.aiHistoryRegenerate(${idx})">🔁 Regenerate</button>
          <button type="button" onclick="app.aiHistoryShowJson(${idx})">👁 JSON</button>
          ${it.importedId ? `<button type="button" onclick="app.aiHistoryOpenWorkbench(${idx})">🧰 Workbench</button>` : ''}
          <button type="button" onclick="app.aiHistoryDelete(${idx})">🗑 Obriši</button>
        </div>
      </div>`).join('');
  };
  App.prototype.aiHistoryLoad = function(index) {
    const it = this._aiHistory()[Number(index)];
    if (!it) return;
    const map = { ai_symptom:'symptom', ai_desc:'desc', ai_age:'age', ai_sex:'sex', ai_med:'med', ai_goal:'goal', ai_mkb:'mkb', ai_prompt:'prompt', ai_result:'aiResult', ai_batch_result:'batchResult' };
    Object.keys(map).forEach(id => {
      const el = document.getElementById(id);
      if (el) el.value = String(it[map[id]] || '');
    });
    this._lastAiHistoryId = it.id;
    this.showToast('AI zapis učitan.', { timeoutMs: 2600 });
  };
  App.prototype.aiHistoryRegenerate = function(index) { this.aiHistoryLoad(index); this.generateAIPrompt(); };
  App.prototype.aiHistoryShowJson = function(index) {
    const it = this._aiHistory()[Number(index)];
    if (!it) return;
    this.openTextModal('🤖 AI istorija — JSON', JSON.stringify(it, null, 2));
  };
  App.prototype.aiHistoryDelete = function(index) {
    const items = this._aiHistory();
    items.splice(Number(index), 1);
    this._saveAiHistory(items);
    this.renderAiHistory();
  };
  App.prototype.aiHistoryOpenWorkbench = function(index) {
    const it = this._aiHistory()[Number(index)];
    if (!it || !it.importedId) return;
    this.nav('workbench');
    setTimeout(() => {
      const el = document.getElementById('wb-id');
      if (el) el.value = it.importedId;
      this.workbenchOpenById();
    }, 70);
  };

  App.prototype.generateAIBatchPrompt = function(count = 100, sessionLimit = 100) {
    const symptom = String(document.getElementById('ai_symptom')?.value || '').trim();
    const desc = String(document.getElementById('ai_desc')?.value || '').trim();
    const prompt = `TI SI “Medical STL batch generator” za SINET Audio Lekar.\nGeneriši tačno ${count} novih stavki simptoma/problema na srpskom jeziku (latinica), bez duplikata.\nFORMAT IZLAZA: NDJSON (svaki red 1 validan JSON objekat), bez dodatnog teksta.\nPoštuj SINET STL v1.1 polja.\nU jednoj sesiji vrati najviše ${sessionLimit} objekata. Ako ostaje još, korisnik će poslati CONTINUE.\nTematski fokus: ${symptom || 'opšti simptomi i stanja'}.\nDodatni opis / smernice: ${desc || 'jasno, kratko, za starije osobe, sa proverljivim izvorima.'}`;
    const out = document.getElementById('ai_batch_prompt');
    if (out) out.value = prompt;
    this._upsertAiHistory({ id: this._lastAiHistoryId, batchPrompt: prompt, batchCount: count, sessionLimit });
    this.renderAiHistory();
  };
  App.prototype.copyAIBatchPrompt = function() {
    const ta = document.getElementById('ai_batch_prompt');
    if (!ta || !ta.value) return alert('Prvo generiši batch prompt.');
    try { navigator.clipboard.writeText(ta.value).then(() => this.showToast('Batch prompt kopiran ✅')); }
    catch(_) { ta.select(); document.execCommand('copy'); }
  };

  App.prototype._ensureAIHistoryUi = function() {
    const page = document.getElementById('page-ai');
    if (!page) return;
    if (!document.getElementById('ai_mkb')) {
      const goal = document.getElementById('ai_goal');
      if (goal) {
        const wrap = document.createElement('div');
        wrap.innerHTML = '<label style="font-weight:bold;">5) MKB-10 (opciono)</label><input id="ai_mkb" placeholder="npr. E06.3, R52..." style="width:100%; padding:12px; border:1px solid #ddd; border-radius:8px; margin:6px 0 10px 0;">';
        goal.insertAdjacentElement('afterend', wrap);
      }
    }
    if (!document.getElementById('ai-batch-prompt-card')) {
      const host = page.querySelector('.welcome-card');
      const card = document.createElement('div');
      card.id = 'ai-batch-prompt-card';
      card.style.cssText = 'margin-top:16px; padding-top:12px; border-top:1px dashed #ececec;';
      card.innerHTML = `
        <label style="font-weight:bold;">📦 Batch prompt generator</label>
        <div style="display:grid; gap:8px; grid-template-columns:repeat(4,1fr); margin-top:8px;">
          <button class="btn-full" style="margin-top:0; background:#556ee6;" onclick="app.generateAIBatchPrompt(100,100)">100</button>
          <button class="btn-full" style="margin-top:0; background:#556ee6;" onclick="app.generateAIBatchPrompt(200,100)">200</button>
          <button class="btn-full" style="margin-top:0; background:#556ee6;" onclick="app.generateAIBatchPrompt(500,100)">500</button>
          <button class="btn-full" style="margin-top:0; background:#556ee6;" onclick="app.generateAIBatchPrompt(1000,100)">1000</button>
        </div>
        <textarea id="ai_batch_prompt" rows="7" placeholder="Ovde će se pojaviti NDJSON batch prompt..." style="width:100%; padding:12px; border:1px solid #ddd; border-radius:8px; margin-top:10px;"></textarea>
        <button class="btn-full" style="background:#2ecc71;" onclick="app.copyAIBatchPrompt()">📋 Kopiraj batch prompt</button>`;
      const target = document.getElementById('ai_result')?.parentElement;
      target?.insertAdjacentElement('beforebegin', card);
    }
    if (!document.getElementById('ai-history-card')) {
      const host = page.querySelector('.welcome-card');
      const card = document.createElement('div');
      card.id = 'ai-history-card';
      card.style.cssText = 'margin-top:16px; padding-top:12px; border-top:1px dashed #ececec;';
      card.innerHTML = '<label style="font-weight:bold;">🕘 AI Upitnik — istorija / reopen / regenerate</label><div id="ai-history-list" style="margin-top:10px;"></div>';
      host?.appendChild(card);
    }
  };

  App.prototype._dsHistory = function() {
    try { const raw = localStorage.getItem(DS_HISTORY_KEY); const arr = raw ? JSON.parse(raw) : []; return Array.isArray(arr) ? arr : []; } catch(_) { return []; }
  };
  App.prototype._saveDsHistory = function(arr) {
    try { localStorage.setItem(DS_HISTORY_KEY, JSON.stringify(arr || [])); } catch(_) {}
  };
  App.prototype.renderDsHistoryCard = function() {
    const host = document.getElementById('ds-history-list');
    if (!host) return;
    const items = this._dsHistory();
    if (!items.length) { host.innerHTML = '<div class="muted">Još nema sačuvanih DS vodiča.</div>'; return; }
    host.innerHTML = items.map((it, idx) => `
      <div class="hist-card">
        <div class="hist-title">${this.escapeHtml(it.name || 'Pacijent')}</div>
        <div class="hist-sub">${this.escapeHtml([it.mkb || 'Bez MKB', it.date || ''].filter(Boolean).join(' • '))}</div>
        <div class="hist-actions">
          <button type="button" onclick="app.dsHistoryPreview(${idx})">👁 Pogledaj</button>
          <button type="button" onclick="app.dsHistoryOpen(${idx}, false)">↩ Reopen / Edit</button>
          <button type="button" onclick="app.dsHistoryOpen(${idx}, true)">🔁 Regenerate</button>
          <button type="button" onclick="app.dsHistoryDelete(${idx})">🗑 Obriši</button>
        </div>
      </div>`).join('');
  };
  App.prototype.dsHistoryPreview = function(index) {
    const it = this._dsHistory()[Number(index)];
    if (!it) return;
    this.openTextModal('🧾 DS istorija — HTML preview', String(it.html || '').slice(0, 4000));
  };
  App.prototype.dsHistoryOpen = function(index, regen=false) {
    const it = this._dsHistory()[Number(index)];
    if (!it) return;
    try { localStorage.setItem(DS_EDIT_STATE_KEY, JSON.stringify(it.state || {})); } catch(_) {}
    const back = encodeURIComponent(window.location.href);
    location.href = `DS-Generator.html?catalog=./data/SINET_STL.json&restore=1${regen ? '&regen=1' : ''}&back=${back}`;
  };
  App.prototype.dsHistoryDelete = function(index) {
    const items = this._dsHistory();
    items.splice(Number(index), 1);
    this._saveDsHistory(items);
    this.renderDsHistoryCard();
  };
  App.prototype._ensureSettingsAdvancedCards = async function() {
    await this._ensureMkbData();
    const page = document.getElementById('page-settings');
    if (!page) return;
    if (!document.getElementById('settings-mkb-card')) {
      const card = document.createElement('div');
      card.className = 'welcome-card';
      card.id = 'settings-mkb-card';
      card.style.textAlign = 'left';
      card.innerHTML = '<h4 style="margin-top:0;">🏷 MKB standardizacija UI</h4><div id="mkb-settings-stats" class="muted" style="margin-bottom:10px;"></div><div id="mkb-missing-list"></div>';
      const audit = document.getElementById('settings-audit-card');
      audit?.insertAdjacentElement('afterend', card);
    }
    if (!document.getElementById('settings-ds-history-card')) {
      const card = document.createElement('div');
      card.className = 'welcome-card';
      card.id = 'settings-ds-history-card';
      card.style.textAlign = 'left';
      card.innerHTML = '<h4 style="margin-top:0;">🧾 DS Generator — istorija / reopen / regenerate</h4><div id="ds-history-list"></div>';
      const mkb = document.getElementById('settings-mkb-card');
      mkb?.insertAdjacentElement('afterend', card);
    }
    this.renderMkbSettings();
    this.renderDsHistoryCard();
  };

  App.prototype.renderMkbSettings = function() {
    const stats = document.getElementById('mkb-settings-stats');
    const list = document.getElementById('mkb-missing-list');
    if (!stats || !list) return;
    const all = Array.isArray(this.catalogItems) ? this.catalogItems.filter(it => !String(it?.id||'').startsWith('usr-')) : [];
    const withMkb = all.filter(it => !!this.getMKB10Code?.(it.mkb10));
    const missing = all.filter(it => !this.getMKB10Code?.(it.mkb10));
    const userMissing = (Array.isArray(this.userSymptoms) ? this.userSymptoms : []).filter(it => !this.getMKB10Code?.(it.mkb10));
    stats.innerHTML = `<b>Katalog:</b> ${all.length} • <b>sa MKB:</b> ${withMkb.length} • <b>bez MKB:</b> ${missing.length} • <b>Moji simptomi bez MKB:</b> ${userMissing.length}`;
    list.innerHTML = missing.slice(0, 30).map(it => {
      const sug = this._mkbSuggestionForItem(it);
      return `<div class="hist-card"><div class="hist-title">${this.escapeHtml(it.naziv || it.id || '')}</div><div class="hist-sub">${this.escapeHtml(it.id || '')} • ${this.escapeHtml(it.oblast || '')}</div>${sug ? `<div class="hist-desc">Predlog: <b>${this.escapeHtml(sug.code)}</b> — ${this.escapeHtml(sug.title || '')}</div>` : '<div class="hist-desc">Bez sigurnog predloga.</div>'}<div class="hist-actions"><button type="button" onclick="app.openWorkbenchWithId('${this.escapeJsString(it.id)}')">🧰 Otvori u Workbench</button>${sug ? `<button type="button" onclick="app.applySuggestedMkbToItem('${this.escapeJsString(it.id)}','${this.escapeJsString(sug.code)}')">✨ Primeni predlog</button>` : ''}</div></div>`;
    }).join('') || '<div class="muted">Sve katalog stavke već imaju MKB.</div>';
  };
  App.prototype.openWorkbenchWithId = function(id) {
    this.nav('workbench');
    setTimeout(() => {
      const el = document.getElementById('wb-id');
      if (el) el.value = id;
      this.workbenchOpenById();
    }, 70);
  };
  App.prototype.applySuggestedMkbToItem = async function(id, code) {
    const item = this._findItemById(id);
    if (!item) return;
    if (!this.overrides || typeof this.overrides !== 'object') this.overrides = {};
    const merged = JSON.parse(JSON.stringify((this.overrides[id]) ? this._deepMerge(item, this.overrides[id]) : item));
    merged.mkb10 = code;
    this.overrides[id] = merged;
    await this.saveOverrides();
    this.applyUserDataToCatalog();
    this.renderMkbSettings();
    this.showToast(`MKB predlog primenjen: ${code}`, { timeoutMs: 3500, actionLabel:'Workbench', actionNav:'workbench' });
  };

  const origPrepareAIUI = App.prototype.prepareAIUI;
  App.prototype.prepareAIUI = function() {
    if (origPrepareAIUI) origPrepareAIUI.apply(this, arguments);
    try { this._ensureAIHistoryUi(); } catch(_) {}
    try { this.renderAiHistory(); } catch(_) {}
  };

  const origInit = App.prototype.init;
  App.prototype.init = async function() {
    await origInit.apply(this, arguments);
    try { await this._ensureMkbData(); } catch(_) {}
    try { this._ensureAIHistoryUi(); this.renderAiHistory(); } catch(_) {}
    try { await this._ensureSettingsAdvancedCards(); } catch(_) {}
    try { await this._ensureWorkbenchEnhancements(); } catch(_) {}
  };

  const origNav = App.prototype.nav;
  App.prototype.nav = function(pageId, opts={}) {
    const r = origNav.apply(this, arguments);
    if (pageId === 'settings') { try { this._ensureSettingsAdvancedCards(); } catch(_) {} }
    if (pageId === 'workbench') { try { this._ensureWorkbenchEnhancements(); } catch(_) {} }
    if (pageId === 'ai') { try { this._ensureAIHistoryUi(); this.renderAiHistory(); } catch(_) {} }
    return r;
  };

  const origWorkbenchPopulateForm = App.prototype.workbenchPopulateForm;
  App.prototype.workbenchPopulateForm = function(item) {
    const r = origWorkbenchPopulateForm.apply(this, arguments);
    try { this.renderWorkbenchMkbPreview(); } catch(_) {}
    return r;
  };

  const origWorkbenchFormChanged = App.prototype.workbenchFormChanged;
  App.prototype.workbenchFormChanged = function() {
    const r = origWorkbenchFormChanged.apply(this, arguments);
    try { this.renderWorkbenchMkbPreview(); } catch(_) {}
    return r;
  };

  const origShowUserSymptomForm = App.prototype.showUserSymptomForm;
  App.prototype.showUserSymptomForm = function(editId = null) {
    const r = origShowUserSymptomForm.apply(this, arguments);
    try {
      const host = document.getElementById('us_freqs');
      const row = host?.parentElement?.querySelector('[data-us-freq-actions]');
      if (!row && host) {
        const actions = host.nextElementSibling;
        if (actions) {
          actions.setAttribute('data-us-freq-actions', '1');
          const btn = document.createElement('button');
          btn.className = 'btn-full';
          btn.style.cssText = 'flex:1; margin-top:0; background:#556ee6;';
          btn.type = 'button';
          btn.textContent = '🎧 Izaberi iz liste';
          btn.onclick = () => this.userSymptomOpenFreqPicker();
          actions.insertBefore(btn, actions.firstChild.nextSibling);
        }
      }
    } catch(_) {}
    return r;
  };

  const origAddUserFreqRow = App.prototype.addUserFreqRow;
  App.prototype.addUserFreqRow = function(prefill = null) {
    const box = document.getElementById('us_freqs');
    if (!box) return -1;
    const idx = box.querySelectorAll('[data-fhz]').length;
    const p = prefill || {};
    if (!prefill) {
      origAddUserFreqRow.apply(this, arguments);
      return idx;
    }
    const row = document.createElement('div');
    row.innerHTML = window.SINET_ItemShell?.renderFrequencyEditorRows
      ? window.SINET_ItemShell.renderFrequencyEditorRows([{ hz:p.hz||'', naziv:p.naziv||'', funkcija:p.opis||'', izvor:p.izvor||'' }])
          .replaceAll('data-fhz="0"', `data-fhz="${idx}"`)
          .replaceAll('data-fnm="0"', `data-fnm="${idx}"`)
          .replaceAll('data-fdc="0"', `data-fdc="${idx}"`)
          .replaceAll('data-fsrc="0"', `data-fsrc="${idx}"`)
      : `<div style="display:flex; gap:8px; margin:6px 0;"><input data-fhz="${idx}" placeholder="Hz" value="${this.escapeAttr(p.hz||'')}" style="width:90px; padding:10px; border:1px solid #ddd; border-radius:8px;"><input data-fnm="${idx}" placeholder="Naziv frekvencije" value="${this.escapeAttr(p.naziv||'')}" style="flex:1; padding:10px; border:1px solid #ddd; border-radius:8px;"></div><textarea data-fdc="${idx}" placeholder="Opis / funkcija" style="width:100%; padding:10px; border:1px solid #ddd; border-radius:8px; margin:6px 0;">${this.escapeHtml(p.opis||'')}</textarea><input data-fsrc="${idx}" placeholder="Izvor (link ili tekst)" value="${this.escapeAttr(p.izvor||'')}" style="width:100%; padding:10px; border:1px solid #ddd; border-radius:8px; margin-bottom:6px;">`;
    box.appendChild(row);
    return idx;
  };

  const origGenerateAIPrompt = App.prototype.generateAIPrompt;
  App.prototype.generateAIPrompt = function() {
    const r = origGenerateAIPrompt.apply(this, arguments);
    try { this._upsertAiHistory({ id: this._lastAiHistoryId }); } catch(_) {}
    try { this.renderAiHistory(); } catch(_) {}
    return r;
  };

  const origImportAIResult = App.prototype.importAIResult;
  App.prototype.importAIResult = async function() {
    const before = Array.isArray(this.userSymptoms) ? this.userSymptoms.length : 0;
    const r = await origImportAIResult.apply(this, arguments);
    try {
      const after = Array.isArray(this.userSymptoms) ? this.userSymptoms.length : 0;
      const importedId = after > before ? (this.userSymptoms[0]?.id || '') : '';
      this._upsertAiHistory({ id: this._lastAiHistoryId, importedId, aiResult: String(document.getElementById('ai_result')?.value || '') });
      this.renderAiHistory();
    } catch(_) {}
    return r;
  };

  const origCopyAIPrompt = App.prototype.copyAIPrompt;
  App.prototype.copyAIPrompt = function() {
    const r = origCopyAIPrompt.apply(this, arguments);
    try { this._upsertAiHistory({ id: this._lastAiHistoryId }); this.renderAiHistory(); } catch(_) {}
    return r;
  };

})();

window.addEventListener('DOMContentLoaded', async () => {
  await app.init();
  setupMobileHeaderCompact();
  try { await __importDsBridgeIfAny(); } catch(e) { console.warn('DS import call failed', e); }
  try { await __importUserSymptomBridgeIfAny(); } catch(e) { console.warn('User symptom import call failed', e); }
});
window.app = app;
window.nav = (id) => app.nav(id);


function getApp(){
  return window.app || window.__app || null;
}
window.getApp = getApp;

window.resumeSession = function(id){
  const a = getApp();
  if (a && typeof a.resumeSavedSession === 'function') return a.resumeSavedSession(String(id||''));
  console.warn("resumeSession(): app not ready");
};
window.openAuditBatch = function(batchId){
  const a = getApp();
  if (a && typeof a.openAuditBatch === 'function') return a.openAuditBatch(String(batchId||''));
  console.warn("openAuditBatch(): app not ready");
};


// ---- SINET Studio (Symptom Studio) ----
window.studioGenerate100 = function(){
  const a = getApp();
  if (a && typeof a.studioGeneratePrompts === 'function') return a.studioGeneratePrompts(1, 100);
  alert("Studio nije spreman.");
};
window.studioGenerate1000 = function(){
  const a = getApp();
  if (a && typeof a.studioGeneratePrompts === 'function') return a.studioGeneratePrompts(10, 100);
  alert("Studio nije spreman.");
};
window.studioValidate = function(){
  const a = getApp();
  if (a && typeof a.studioValidate === 'function') return a.studioValidate();
  alert("Studio nije spreman.");
};
window.studioCommit = function(){
  const a = getApp();
  if (a && typeof a.studioCommitToUserSymptoms === 'function') return a.studioCommitToUserSymptoms();
  alert("Studio nije spreman.");
};
window.studioExport = function(){
  const a = getApp();
  if (a && typeof a.studioExportPatch === 'function') return a.studioExportPatch();
  alert("Studio nije spreman.");
};
window.studioClear = function(){
  const a = getApp();
  if (a && typeof a.studioClear === 'function') return a.studioClear();
  alert("Studio nije spreman.");
};

window.deleteSession = function(id){
  const a = getApp();
  if (a && typeof a.deleteSavedSession === 'function') return a.deleteSavedSession(String(id||''));
  console.warn("deleteSession(): app not ready");
};

window.setSessionNote = function(id){
  const a = getApp();
  if (a && typeof a.setSessionNote === 'function') return a.setSessionNote(String(id||''));
  console.warn("setSessionNote(): app not ready");
};
window.openSessionAudit = function(id){
  const a = getApp();
  if (a && typeof a.openSessionAudit === 'function') return a.openSessionAudit(String(id||''));
  console.warn("openSessionAudit(): app not ready");
};


window.playFromModal = () => app.playFromModal();
window.toggleFavoriteFromModal = () => app.toggleFavoriteFromModal();
window.addToPlaylistFromModal = () => app.addToPlaylistFromModal();
window.addToMySymptomsFromModal = () => app.addToMySymptomsFromModal();
window.createProtocolFromModal = () => app.createProtocolFromModal();
window.closeModal = () => app.closeModal();
window.openAcupressureViewer = (itemId, idx) => app.openAcupressureViewer(itemId, idx);
window.closeAcupressureViewer = () => app.closeAcupressureViewer();


// === SINET v14.7 – GLOBAL UI HOOKS (for inline onclick in index.html) ===
(function(){
  const getApp = () => window.app || window.__app || null;

  window.startProtocol = function(){
    const a = getApp();
    if (a && typeof a.startProtocol === 'function') return a.startProtocol();
    console.warn("startProtocol(): app not ready");
  };

  window.togglePause = function(){
    const a = getApp();
    if (a && typeof a.togglePlayPause === 'function') return a.togglePlayPause();
    console.warn("togglePause(): app not ready");
  };

  window.stopAudio = function(){
    const a = getApp();
    if (a && typeof a.stopPlayer === 'function') return a.stopPlayer(!(a.isResumeEnabled && a.isResumeEnabled()));
    // fallback: try audio engine directly
    if (a && a.audio && typeof a.audio.stop === 'function') return a.audio.stop();
    console.warn("stopAudio(): app not ready");
  };

  window.resetAppCache = async function(){
    try {
      if ('serviceWorker' in navigator) {
        const regs = await navigator.serviceWorker.getRegistrations();
        for (const r of regs) await r.unregister();
      }
      if (window.caches && typeof caches.keys === 'function') {
        const keys = await caches.keys();
        for (const k of keys) await caches.delete(k);
      }
    } catch (e) {
      console.warn("resetAppCache failed", e);
    }
    // Hard reload
    try { location.reload(); } catch(_) {}
  };
})();



// Global helpers for inline HTML onclick
window.toggleNowList = () => window.app && window.app.toggleNowList();
window.toggleDockLoopPanel = () => window.app && window.app.toggleDockLoopPanel();
window.onDockRepeatChange = () => window.app && window.app.onDockRepeatChange();
window.clearPlaylist = () => window.app && window.app.clearPlaylist();
window.doSearch = (v) => window.app && window.app.filterCatalog(v);
window.generateAIPrompt = () => window.app && window.app.generateAIPrompt();
window.copyAIPrompt = () => window.app && window.app.copyAIPrompt();
window.exportCatalogTxt = (mode) => window.app && window.app.exportCatalogTxt(mode);
window.exportCatalogSTL = (mode) => window.app && window.app.exportCatalogSTL(mode);


// Protocols (global onclick helpers)
window.newProtocol = () => window.app && window.app.openProtocolEditor(null);
window.createProtocolFromFavorites = () => window.app && window.app.createProtocolFromFavorites();
window.createProtocolFromQueue = () => window.app && window.app.createProtocolFromQueue();
window.exportProtocols = () => window.app && window.app.exportProtocols();
window.importProtocols = () => window.app && window.app.importProtocolsPrompt();


/* ===================== v16.0.0.26 — LAB Bridge skeleton + Book/Pro render + Lab Snapshot ===================== */
(function(){
  const LAB_HISTORY_KEY = 'SINET_LAB_HISTORY_V1';
  const LAB_SNAPSHOT_KEY = 'SINET_LAB_SNAPSHOTS_V1';
  const LAB_LAST_REPORT_KEY = 'SINET_LAB_LAST_REPORT_V1';

  function safeParse(raw, fallback) {
    try { return JSON.parse(raw); } catch(_) { return fallback; }
  }
  function arrify(v) {
    if (Array.isArray(v)) return v.filter(Boolean);
    if (v === null || v === undefined || v === '') return [];
    return [v];
  }
  function slugify(s) {
    return String(s || 'lab')
      .normalize('NFD').replace(/[\u0300-\u036f]/g, '')
      .toLowerCase().replace(/[^a-z0-9]+/g, '-')
      .replace(/^-+|-+$/g, '').slice(0, 64) || 'lab';
  }
  function pick(obj, keys, fallback='') {
    if (!obj || typeof obj !== 'object') return fallback;
    for (const k of keys) {
      const parts = String(k).split('.');
      let cur = obj;
      let ok = true;
      for (const p of parts) {
        if (cur && Object.prototype.hasOwnProperty.call(cur, p)) cur = cur[p];
        else { ok = false; break; }
      }
      if (!ok) continue;
      if (cur !== undefined && cur !== null && cur !== '') return cur;
    }
    return fallback;
  }

  App.prototype._labHistory = function() {
    return safeParse(localStorage.getItem(LAB_HISTORY_KEY) || '[]', []);
  };
  App.prototype._saveLabHistory = function(arr) {
    try { localStorage.setItem(LAB_HISTORY_KEY, JSON.stringify(Array.isArray(arr) ? arr : [])); } catch(_) {}
  };
  App.prototype._labSnapshots = function() {
    return safeParse(localStorage.getItem(LAB_SNAPSHOT_KEY) || '[]', []);
  };
  App.prototype._saveLabSnapshots = function(arr) {
    try { localStorage.setItem(LAB_SNAPSHOT_KEY, JSON.stringify(Array.isArray(arr) ? arr : [])); } catch(_) {}
  };

  App.prototype._ensureLabBridgeStyles = function() {
    if (document.getElementById('lab-bridge-styles')) return;
    const st = document.createElement('style');
    st.id = 'lab-bridge-styles';
    st.textContent = `
      .lab-grid{display:grid;gap:12px}
      @media(min-width:760px){.lab-grid.two{grid-template-columns:1fr 1fr}}
      .lab-input,.lab-textarea,.lab-select{width:100%;padding:12px;border:1px solid #d8e1ea;border-radius:12px;background:#fff;font:inherit}
      .lab-textarea{min-height:110px;resize:vertical}
      .lab-actions{display:flex;gap:10px;flex-wrap:wrap;margin-top:10px}
      .lab-btn{flex:1;min-width:180px;padding:12px;border:none;border-radius:12px;font-weight:900;color:#fff;cursor:pointer}
      .lab-btn.secondary{background:#4a90e2}.lab-btn.primary{background:#2ecc71}.lab-btn.warn{background:#8e44ad}.lab-btn.dark{background:#0f766e}.lab-btn.ghost{background:#eef4fb;color:#234;border:1px solid #cfe0f6}
      .lab-kpi{display:grid;gap:10px;grid-template-columns:repeat(auto-fit,minmax(180px,1fr));margin-top:10px}
      .lab-kpi-card{background:#f8fbff;border:1px solid #dce9f8;border-radius:14px;padding:12px}
      .lab-kpi-card b{display:block;color:#18466f;margin-bottom:4px}
      .lab-report-shell{display:grid;gap:14px}
      .lab-report-head{background:linear-gradient(135deg,#0f766e,#2563eb);color:#fff;border-radius:18px;padding:18px}
      .lab-report-head h2{margin:0 0 8px 0;font-size:1.45rem}.lab-report-head p{margin:0;opacity:.95}
      .lab-report-section{background:#fff;border:1px solid #e9eef4;border-radius:16px;padding:16px;box-shadow:0 2px 6px rgba(15,23,42,.04)}
      .lab-report-section h3{margin:0 0 10px 0;color:#17405f}.lab-report-section h4{margin:12px 0 6px 0;color:#24557d}
      .lab-pill{display:inline-flex;align-items:center;padding:6px 10px;border-radius:999px;background:#eef6ff;border:1px solid #d7e6fb;color:#22415f;font-size:.85rem;font-weight:800;margin:0 8px 8px 0}
      .lab-card-list{display:grid;gap:10px}
      .lab-find-card{background:#f9fbfd;border:1px solid #e6edf5;border-radius:14px;padding:12px}
      .lab-find-card .meta{font-size:.85rem;color:#567}
      .lab-prio-high{background:#fff2f0;border-color:#ffd0c8}.lab-prio-medium{background:#fff8e8;border-color:#ffe3a3}.lab-prio-low{background:#f2fbf5;border-color:#cdeed7}
      .lab-hist-card{background:#fff;border:1px solid #e8edf4;border-radius:14px;padding:12px;margin-top:10px}
      .lab-hist-title{font-weight:900;color:#1f3f5b}.lab-hist-sub{font-size:.88rem;color:#678;margin-top:4px}.lab-hist-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px}
      .lab-hist-actions button{padding:8px 10px;border-radius:10px;border:1px solid #d7e1eb;background:#fff;font-weight:800;cursor:pointer}
      .lab-inline-note{font-size:.85rem;color:#667;margin-top:8px}
      .lab-filebox{padding:12px;border:1px dashed #b8cbe0;border-radius:14px;background:#f9fcff}
      .lab-json-box{font-family:monospace;font-size:.88rem;background:#fbfdff}
      .lab-empty{padding:12px;border:1px dashed #d7e1eb;border-radius:12px;color:#678;background:#fbfdff}
      .lab-snapshot-chip{display:inline-flex;gap:6px;align-items:center;padding:7px 10px;border-radius:999px;border:1px solid #dce5ef;background:#fff;margin:0 8px 8px 0;font-size:.86rem}
    `;
    document.head.appendChild(st);
  };

  App.prototype._ensureLabBridgePage = function() {
    if (document.getElementById('page-labs')) return document.getElementById('page-labs');
    const main = document.querySelector('main');
    if (!main) return null;
    const page = document.createElement('div');
    page.id = 'page-labs';
    page.className = 'section';
    page.innerHTML = `
      <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:10px; gap:10px; flex-wrap:wrap;">
        <h2 style="margin:0;">🧪 LAB Bridge</h2>
        <button class="btn-mini" onclick="nav('home')">🏠 Početni ekran</button>
      </div>
      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">SINET standardni laboratorijski izveštaj</h4>
        <p style="color:#666; margin-top:6px;">Učitaj tekst/PDF metapodatke, unesi kontekst i generiši <b>SINET MASTER PROMPT</b>. Kada AI vrati strukturisani JSON, ovde ga pretvaramo u profesionalni <b>Book/Pro</b> izveštaj, čuvamo istoriju i pravimo <b>Lab Snapshot</b> za buduću vezu sa Anamnezom.</p>
      </div>
      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">1) Ulaz / kontekst</h4>
        <div class="lab-grid two">
          <div>
            <label><b>Ime / oznaka pacijenta</b></label>
            <input id="lab_patient_name" class="lab-input" type="text" placeholder="npr. Gordana U. / Pacijent A" />
          </div>
          <div>
            <label><b>Datum nalaza</b></label>
            <input id="lab_report_date" class="lab-input" type="date" />
          </div>
          <div>
            <label><b>Godine</b></label>
            <input id="lab_age" class="lab-input" type="text" placeholder="npr. 44" />
          </div>
          <div>
            <label><b>Pol</b></label>
            <select id="lab_sex" class="lab-select"><option value="nebitno">nebitno</option><option value="ženski">ženski</option><option value="muški">muški</option></select>
          </div>
        </div>
        <div style="height:10px"></div>
        <div class="lab-filebox">
          <label><b>Prilog nalaza</b></label>
          <div class="lab-inline-note">Offline demo: TXT/MD/HTML/JSON može direktno da učita tekst. PDF trenutno čuva naziv fajla i priprema prostor za budući parser.</div>
          <input id="lab_file_input" type="file" accept=".txt,.md,.json,.html,.htm,.pdf" onchange="app.labHandleInputFiles(this)" style="margin-top:10px;" />
          <div id="lab-file-meta" class="lab-inline-note">Nema učitanog priloga.</div>
        </div>
        <div style="height:10px"></div>
        <label><b>Simptomi / razlog analize</b></label>
        <textarea id="lab_symptoms" class="lab-textarea" placeholder="Šta pacijent oseća, zašto je urađena laboratorija, šta je cilj tumačenja..."></textarea>
        <label><b>Anamneza / porodična anamneza / prethodni nalazi</b></label>
        <textarea id="lab_anamnesis" class="lab-textarea" placeholder="Ranije bolesti, porodična anamneza, pritisak, težina, PCOS, bubrezi, štitna... "></textarea>
        <label><b>Terapija / suplementi / napomene</b></label>
        <textarea id="lab_therapy" class="lab-textarea" placeholder="Lekovi, suplementi, vitamin D, metformin, gvožđe, antibiotici... "></textarea>
        <label><b>Cilj korisnika</b></label>
        <textarea id="lab_goal" class="lab-textarea" placeholder="npr. jasnije tumačenje, dalje dijagnostikovanje, plan kontrole, upozorenja..."></textarea>
        <label><b>Sirovi tekst nalaza / ručni unos</b></label>
        <textarea id="lab_raw_text" class="lab-textarea" style="min-height:180px;" placeholder="Nalepi tekst nalaza, izveštaj laboratorije ili prepis ključnih parametara sa referentnim vrednostima..."></textarea>
        <div class="lab-actions">
          <button class="lab-btn warn" type="button" onclick="app.generateLabPrompt()">🧠 Generiši SINET Lab Prompt</button>
          <button class="lab-btn primary" type="button" onclick="app.copyLabPrompt()">📋 Kopiraj prompt</button>
          <button class="lab-btn ghost" type="button" onclick="app.labLoadSampleFromLastPdf()">📄 Ubaci primer iz poslednjeg PDF nalaza</button>
        </div>
        <textarea id="lab_prompt" class="lab-textarea lab-json-box" style="min-height:240px; margin-top:10px;" placeholder="Ovde će se pojaviti master prompt za AI..."></textarea>
      </div>
      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">2) AI JSON rezultat → Book/Pro izveštaj</h4>
        <label><b>AI rezultat (JSON)</b></label>
        <textarea id="lab_ai_json" class="lab-textarea lab-json-box" style="min-height:220px;" placeholder='Nalepi strukturisani JSON iz AI-a. Preporuka: koristi SINET Lab Master Prompt i vrati validan JSON.'></textarea>
        <div class="lab-actions">
          <button class="lab-btn secondary" type="button" onclick="app.cleanLabJson()">🧹 Očisti JSON</button>
          <button class="lab-btn dark" type="button" onclick="app.renderLabReportFromInput()">🪄 Renderuj izveštaj</button>
          <button class="lab-btn primary" type="button" onclick="app.saveCurrentLabCase()">💾 Sačuvaj slučaj</button>
        </div>
      </div>
      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">3) Standardni SINET Book/Pro prikaz</h4>
        <div class="lab-actions">
          <button class="lab-btn dark" type="button" onclick="app.openLabReportWindow()">🪟 Otvori puni izveštaj</button>
          <button class="lab-btn secondary" type="button" onclick="app.downloadCurrentLabHtml()">⬇ HTML</button>
          <button class="lab-btn primary" type="button" onclick="app.saveCurrentLabSnapshot()">🧩 Sačuvaj Lab Snapshot</button>
          <button class="lab-btn ghost" type="button" onclick="app.exportCurrentLabJson()">⬇ JSON</button>
        </div>
        <div id="lab-report-preview" style="margin-top:12px;"></div>
      </div>
      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">4) Istorija laboratorijskih izveštaja</h4>
        <div id="lab-history-list"></div>
      </div>
      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">5) Lab Snapshot osnova</h4>
        <div class="lab-inline-note">Snapshot čuva kratak sažetak za buduću vezu sa Anamnezom. Ovo je baza za kasniji “Lab Snapshot” tab u dijagnostici.</div>
        <div id="lab-snapshot-list" style="margin-top:10px;"></div>
      </div>`;
    const helpPage = document.getElementById('page-help');
    if (helpPage) main.insertBefore(page, helpPage);
    else main.appendChild(page);
    return page;
  };

  App.prototype._ensureLabBridgeEntryPoints = function() {
    if (!document.getElementById('menu-link-labs')) {
      const settingsLink = Array.from(document.querySelectorAll('a.menu-link')).find(a => /Podešavanja/.test(a.textContent || ''));
      if (settingsLink && settingsLink.parentElement && settingsLink.parentElement.parentElement) {
        const li = document.createElement('li');
        li.innerHTML = '<a id="menu-link-labs" class="menu-link" data-label="lab bridge laboratorija" href="#" onclick="nav(\'labs\'); closeMenu();">🧪 LAB Bridge</a>';
        settingsLink.parentElement.parentElement.insertBefore(li, settingsLink.parentElement);
      }
    }
    const legacyAiBtn = document.getElementById('ai-open-lab-btn');
    if (legacyAiBtn) legacyAiBtn.remove();
  };

  App.prototype._ensureLabBridgeUI = function() {
    this._ensureLabBridgeStyles();
    this._ensureLabBridgePage();
    this._ensureLabBridgeEntryPoints();
    if (this.ui && this.ui.screens) this.ui.screens.labs = document.getElementById('page-labs');
  };

  App.prototype.collectLabInput = function() {
    const fileMeta = safeParse(document.getElementById('lab-file-meta')?.dataset?.payload || 'null', null);
    return {
      id: 'lab-' + Date.now(),
      created_at: new Date().toISOString(),
      patient: {
        name: String(document.getElementById('lab_patient_name')?.value || '').trim(),
        age: String(document.getElementById('lab_age')?.value || '').trim(),
        sex: String(document.getElementById('lab_sex')?.value || 'nebitno').trim(),
        report_date: String(document.getElementById('lab_report_date')?.value || '').trim()
      },
      context: {
        symptoms: String(document.getElementById('lab_symptoms')?.value || '').trim(),
        anamnesis: String(document.getElementById('lab_anamnesis')?.value || '').trim(),
        therapy: String(document.getElementById('lab_therapy')?.value || '').trim(),
        goal: String(document.getElementById('lab_goal')?.value || '').trim()
      },
      attachment: fileMeta,
      raw_text: String(document.getElementById('lab_raw_text')?.value || '').trim()
    };
  };

  App.prototype.labHandleInputFiles = async function(input) {
    const file = input?.files?.[0];
    const meta = document.getElementById('lab-file-meta');
    if (!file || !meta) return;
    const info = { name: file.name, size: file.size, type: file.type || '', lastModified: file.lastModified || 0 };
    meta.dataset.payload = JSON.stringify(info);
    const ext = (file.name.split('.').pop() || '').toLowerCase();
    if (['txt','md','json','html','htm'].includes(ext) || /^text\//.test(file.type || '')) {
      const raw = await file.text();
      let text = raw;
      if (ext === 'html' || ext === 'htm' || /html/.test(file.type || '')) {
        try {
          const doc = new DOMParser().parseFromString(raw, 'text/html');
          text = String(doc.body?.innerText || raw).trim();
        } catch(_) {}
      }
      const ta = document.getElementById('lab_raw_text');
      if (ta) ta.value = text;
      meta.innerHTML = `Učitano: <b>${this.escapeHtml(file.name)}</b> • ${(file.size/1024).toFixed(1)} KB`;
      this.showToast('LAB prilog učitan u tekstualni editor ✅');
      return;
    }
    if (ext === 'pdf' || /pdf/.test(file.type || '')) {
      const ta = document.getElementById('lab_raw_text');
      if (ta && !ta.value.trim()) ta.value = `[PDF prilog] ${file.name}\n\nNapomena: offline demo za sada ne parsira PDF automatski. Nalepi tekst nalaza ili koristi ovaj prilog kao referencu.`;
      meta.innerHTML = `PDF priložen: <b>${this.escapeHtml(file.name)}</b> • ${(file.size/1024).toFixed(1)} KB • parser dolazi u sledećem talasu.`;
      this.showToast('PDF je zabeležen. Za sada nalepi tekst nalaza ili prepis vrednosti.');
      return;
    }
    meta.innerHTML = `Prilog: <b>${this.escapeHtml(file.name)}</b> • format nije direktno podržan za tekstualni import.`;
  };

  App.prototype.labLoadSampleFromLastPdf = async function() {
    const ta = document.getElementById('lab_raw_text');
    if (!ta || ta.value.trim()) return;
    ta.value = `Pacijent: ženski pol\nNalaz: glukoza natašte uredna, insulin povišeniji, HOMA indeks povišen, vitamin D nizak, HDL blago niži, kreatinin pri gornjoj granici, eGFR graničan ali iznad 60, urin koncentrisaniji bez jasnih znakova infekcije.\n\nDodaj referentne vrednosti i kompletne brojke iz laboratorije pre konačnog tumačenja.`;
    this.showToast('Ubačen primer strukture nalaza za brži start.');
  };

  App.prototype.generateLabPrompt = function() {
    const input = this.collectLabInput();
    const schema = {
      title: 'string',
      subtitle: 'string',
      doctor_summary: ['string'],
      user_summary: ['string'],
      quick_actions: ['string'],
      key_findings: [{ param: 'string', value: 'string', status: 'uredno|sniženo|povišeno|granično', importance: 'visoki|srednji|niski', why_it_matters: 'string' }],
      sections: {
        hematologija: ['string'],
        glukoza_insulin: ['string'],
        bubrezi: ['string'],
        jetra: ['string'],
        lipidi: ['string'],
        vitamini_minerali: ['string'],
        hormoni: ['string'],
        urin: ['string'],
        ostalo: ['string']
      },
      next_steps: ['string'],
      scenarios: ['string'],
      faq: [{ q: 'string', a: 'string' }],
      treatment_and_control: ['string'],
      alternative_support: ['string'],
      red_flags: ['string'],
      monitoring_plan: ['string'],
      doctor_notes: ['string'],
      sources: ['string']
    };
    const prompt = `Ti si SINET Audio Lekar – Laboratorijski analitičar / medicinsko-informativni asistent.\n\nRadiš detaljan, profesionalan, racionalan i smiren SINET laboratorijski izveštaj na srpskom jeziku (latinica).\n\nPRAVILA:\n- Ne postavljaj konačnu dijagnozu samo iz laboratorije.\n- Ne izmišljaj podatke.\n- Jasno odvoji: uredno / granično / odstupanje / prioritet kontrole.\n- Piši i za lekara i za korisnika.\n- Uvek uključi dalje racionalne korake, red flags i oprez sa alternativnim metodama.\n- Vrati VALIDAN JSON, bez markdown-a i bez dodatnog teksta.\n\nOBAVEZNA STRUKTURA JSON IZLAZA:\n${JSON.stringify(schema, null, 2)}\n\nULAZNI PODACI:\n${JSON.stringify(input, null, 2)}\n\nNAPOMENA:\n- Ako neki blok nije primenljiv, vrati prazan niz umesto izmišljanja.\n- Ako laboratorija nije potpuna, jasno napiši šta nedostaje i kako to utiče na pouzdanost tumačenja.\n- U sekciji treatment_and_control navedi standardni medicinski okvir + blagu podršku (ishrana, san, hidracija, fizička aktivnost, oprezni čajevi), ali ne agresivne alternativne metode.\n- Stil treba da bude SINET Book/Pro, pogodan za HTML render.`;
    const out = document.getElementById('lab_prompt');
    if (out) out.value = prompt;
    this.showToast('SINET Lab Prompt generisan ✅', { timeoutMs: 3200 });
    this.log('USER', 'Lab Prompt Generated', input.patient?.name || 'LAB');
  };

  App.prototype.copyLabPrompt = function() {
    const ta = document.getElementById('lab_prompt');
    if (!ta || !ta.value.trim()) return alert('Prvo generiši prompt.');
    ta.select();
    document.execCommand('copy');
    this.showToast('Lab prompt kopiran ✅');
  };

  App.prototype.cleanLabJson = function() {
    const ta = document.getElementById('lab_ai_json');
    const raw = (ta?.value || '').trim();
    if (!raw) return alert('Nema LAB JSON sadržaja.');
    const parsed = this._parseAiJsonLoose ? this._parseAiJsonLoose(raw) : null;
    if (!parsed || !parsed.obj) return alert('LAB JSON nije uspešno očišćen.');
    ta.value = JSON.stringify(parsed.obj, null, 2);
    this.showToast(`LAB JSON očišćen ✅ (${parsed.mode})`);
  };

  App.prototype._normalizeLabReportObject = function(obj, inputCtx) {
    const root = (obj && typeof obj === 'object' && obj.report && typeof obj.report === 'object') ? obj.report : obj;
    const patient = pick(root, ['patient','meta.patient'], {}) || {};
    const quick = arrify(pick(root, ['quick_actions','quickActions','actions'], []));
    const findings = arrify(pick(root, ['key_findings','keyFindings','findings'], [])).map((it) => {
      if (typeof it === 'string') return { param: it, value: '', status: '', importance: '', why_it_matters: '' };
      return {
        param: pick(it, ['param','name','label'], ''),
        value: pick(it, ['value','result'], ''),
        status: pick(it, ['status','flag'], ''),
        importance: pick(it, ['importance','priority'], ''),
        why_it_matters: pick(it, ['why_it_matters','why','note','description'], '')
      };
    });
    const sections = pick(root, ['sections','detailed_sections','details'], {});
    const report = {
      title: pick(root, ['title'], 'SINET laboratorijski izveštaj'),
      subtitle: pick(root, ['subtitle'], 'Book/Pro tumačenje laboratorijskih nalaza'),
      patient: {
        name: pick(patient, ['name'], inputCtx?.patient?.name || ''),
        age: pick(patient, ['age'], inputCtx?.patient?.age || ''),
        sex: pick(patient, ['sex'], inputCtx?.patient?.sex || ''),
        report_date: pick(patient, ['report_date','date'], inputCtx?.patient?.report_date || '')
      },
      doctor_summary: arrify(pick(root, ['doctor_summary','doctorSummary'], [])),
      user_summary: arrify(pick(root, ['user_summary','userSummary','patient_summary'], [])),
      quick_actions: quick,
      key_findings: findings,
      sections: (sections && typeof sections === 'object') ? sections : {},
      next_steps: arrify(pick(root, ['next_steps','nextSteps','diagnostics','further_steps'], [])),
      scenarios: arrify(pick(root, ['scenarios','scenario_guide'], [])),
      faq: arrify(pick(root, ['faq'], [])).map((it) => typeof it === 'string' ? { q: it, a: '' } : { q: pick(it, ['q','question'], ''), a: pick(it, ['a','answer'], '') }),
      treatment_and_control: arrify(pick(root, ['treatment_and_control','treatmentControl','care_plan'], [])),
      alternative_support: arrify(pick(root, ['alternative_support','support','alternatives'], [])),
      red_flags: arrify(pick(root, ['red_flags','redFlags','alarm_signs'], [])),
      monitoring_plan: arrify(pick(root, ['monitoring_plan','follow_up_plan','plan'], [])),
      doctor_notes: arrify(pick(root, ['doctor_notes','doctorNotes'], [])),
      sources: arrify(pick(root, ['sources','references'], [])),
      raw: obj,
      input: inputCtx || null,
      created_at: new Date().toISOString()
    };
    return report;
  };

  App.prototype._labParagraphsHtml = function(value, empty='Nema podataka.') {
    const items = arrify(value).flatMap(v => Array.isArray(v) ? v : [v]).filter(Boolean);
    if (!items.length) return `<div class="lab-empty">${this.escapeHtml(empty)}</div>`;
    return items.map(v => `<p style="margin:0 0 8px 0; line-height:1.55;">${this.escapeHtml(String(v))}</p>`).join('');
  };

  App.prototype._labListHtml = function(value, empty='Nema podataka.') {
    const items = arrify(value).flatMap(v => Array.isArray(v) ? v : [v]).filter(Boolean);
    if (!items.length) return `<div class="lab-empty">${this.escapeHtml(empty)}</div>`;
    return `<ul style="margin:0; padding-left:18px; line-height:1.55;">${items.map(v => `<li>${this.escapeHtml(String(v))}</li>`).join('')}</ul>`;
  };

  App.prototype._labFindingsHtml = function(findings) {
    const list = arrify(findings);
    if (!list.length) return '<div class="lab-empty">Ključni nalazi još nisu prikazani.</div>';
    return `<div class="lab-card-list">${list.map((it) => {
      const pr = String(it.importance || '').toLowerCase();
      const cls = pr.includes('vis') || pr.includes('high') ? 'lab-prio-high' : (pr.includes('sred') || pr.includes('medium') ? 'lab-prio-medium' : 'lab-prio-low');
      return `<div class="lab-find-card ${cls}"><div style="font-weight:900; color:#173e5d;">${this.escapeHtml(it.param || 'Nalaz')}</div><div class="meta">${this.escapeHtml([it.value, it.status, it.importance].filter(Boolean).join(' • '))}</div>${it.why_it_matters ? `<div style="margin-top:8px; line-height:1.55;">${this.escapeHtml(it.why_it_matters)}</div>` : ''}</div>`;
    }).join('')}</div>`;
  };

  App.prototype._labFaqHtml = function(faq) {
    const items = arrify(faq);
    if (!items.length) return '<div class="lab-empty">FAQ nije popunjen.</div>';
    return items.map((it) => `<details class="help-details"><summary>${this.escapeHtml(it.q || 'Pitanje')}</summary><div style="margin-top:8px; line-height:1.55;">${this.escapeHtml(it.a || '')}</div></details>`).join('');
  };

  App.prototype._labSectionsHtml = function(sections) {
    if (!sections || typeof sections !== 'object' || !Object.keys(sections).length) return '<div class="lab-empty">Detaljna objašnjenja po oblastima nisu dostavljena.</div>';
    return Object.entries(sections).map(([key, val]) => `<div style="margin-bottom:12px;"><h4>${this.escapeHtml(String(key).replace(/_/g,' '))}</h4>${this._labParagraphsHtml(val)}</div>`).join('');
  };

  App.prototype._labReportBodyHtml = function(report) {
    const patientLine = [report.patient?.name, report.patient?.age ? `${report.patient.age} god.` : '', report.patient?.sex, report.patient?.report_date].filter(Boolean).join(' • ');
    return `
      <div class="lab-report-shell">
        <section class="lab-report-head">
          <h2>${this.escapeHtml(report.title || 'SINET laboratorijski izveštaj')}</h2>
          <p>${this.escapeHtml(report.subtitle || '')}</p>
          ${patientLine ? `<div style="margin-top:10px; font-weight:700;">${this.escapeHtml(patientLine)}</div>` : ''}
        </section>
        <section class="lab-report-section">
          <h3>Quick action kartice — šta je najracionalnije uraditi prvo</h3>
          ${arrify(report.quick_actions).length ? `<div>${arrify(report.quick_actions).map(v => `<span class="lab-pill">${this.escapeHtml(String(v))}</span>`).join('')}</div>` : '<div class="lab-empty">Nema definisanih brzih akcija.</div>'}
        </section>
        <section class="lab-report-section"><h3>Sažetak za kliničko razmišljanje</h3>${this._labParagraphsHtml(report.doctor_summary, 'Nema sažetka za lekara.')}</section>
        <section class="lab-report-section"><h3>Sažetak za mirnije i lakše razumevanje</h3>${this._labParagraphsHtml(report.user_summary, 'Nema korisničkog sažetka.')}</section>
        <section class="lab-report-section"><h3>Ključni nalazi — brzo čitanje</h3>${this._labFindingsHtml(report.key_findings)}</section>
        <section class="lab-report-section"><h3>Detaljno objašnjenje po oblastima</h3>${this._labSectionsHtml(report.sections)}</section>
        <section class="lab-report-section"><h3>Predlog dalje dijagnostike — racionalno i prioritetno</h3>${this._labListHtml(report.next_steps, 'Nema predloženih narednih koraka.')}</section>
        <section class="lab-report-section"><h3>Scenario vodič — racionalno odlučivanje korak po korak</h3>${this._labListHtml(report.scenarios, 'Nema scenario vodiča.')}</section>
        <section class="lab-report-section"><h3>FAQ — praktična pitanja koja će korisnik verovatno postaviti</h3>${this._labFaqHtml(report.faq)}</section>
        <section class="lab-report-section"><h3>Način lečenja i kontrole — detaljno, racionalno i po prioritetu</h3>${this._labListHtml(report.treatment_and_control, 'Nema definisanog plana lečenja/kontrole.')}</section>
        <section class="lab-report-section"><h3>Alternativna pomoć, čajevi i narodni pristupi — šta realno može da pomogne, a šta ne</h3>${this._labListHtml(report.alternative_support, 'Nema dodatne blage podrške.')}</section>
        <section class="lab-report-section"><h3>Kada obavezno lekar uživo — crvene zastavice i pragovi za pregled</h3>${this._labListHtml(report.red_flags, 'Nema dodatih red flags stavki.')}</section>
        <section class="lab-report-section"><h3>Plan praćenja — kalendar koji korisnik može da isprati</h3>${this._labListHtml(report.monitoring_plan, 'Plan praćenja nije dodat.')}</section>
        <section class="lab-report-section"><h3>Doctor form + beleške</h3>${this._labListHtml(report.doctor_notes, 'Nema dodatih beleški.')}</section>
        <section class="lab-report-section"><h3>Izvori i smernice korišćene za tumačenje</h3>${this._labListHtml(report.sources, 'Izvori nisu upisani.')}</section>
        <section class="lab-report-section"><div class="lab-inline-note"><b>Napomena:</b> Ovaj izveštaj je medicinsko-informativan i ne zamenjuje pregled lekara, dijagnozu niti individualni terapijski plan.</div></section>
      </div>`;
  };

  App.prototype._labReportDocument = function(report) {
    const body = this._labReportBodyHtml(report);
    return `<!doctype html><html lang="sr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>${this.escapeHtml(report.title || 'SINET Lab Report')}</title><style>body{font-family:Arial,sans-serif;background:#f4f8fb;color:#193247;margin:0;padding:20px}.wrap{max-width:1024px;margin:0 auto}.lab-report-shell{display:grid;gap:14px}.lab-report-head{background:linear-gradient(135deg,#0f766e,#2563eb);color:#fff;border-radius:18px;padding:18px}.lab-report-head h2{margin:0 0 8px 0;font-size:1.65rem}.lab-report-section{background:#fff;border:1px solid #e9eef4;border-radius:16px;padding:16px;box-shadow:0 2px 6px rgba(15,23,42,.04)}.lab-report-section h3{margin:0 0 10px 0;color:#17405f}.lab-report-section h4{margin:12px 0 6px 0;color:#24557d}.lab-pill{display:inline-flex;align-items:center;padding:6px 10px;border-radius:999px;background:#eef6ff;border:1px solid #d7e6fb;color:#22415f;font-size:.85rem;font-weight:800;margin:0 8px 8px 0}.lab-card-list{display:grid;gap:10px}.lab-find-card{background:#f9fbfd;border:1px solid #e6edf5;border-radius:14px;padding:12px}.lab-prio-high{background:#fff2f0;border-color:#ffd0c8}.lab-prio-medium{background:#fff8e8;border-color:#ffe3a3}.lab-prio-low{background:#f2fbf5;border-color:#cdeed7}.lab-empty{padding:12px;border:1px dashed #d7e1eb;border-radius:12px;color:#678;background:#fbfdff}.lab-inline-note{font-size:.9rem;color:#456}details{border:1px solid #e7edf3;border-radius:12px;padding:10px 12px;background:#fcfeff;margin-bottom:10px}summary{cursor:pointer;font-weight:800}ul{line-height:1.55}p{line-height:1.6;margin:0 0 8px 0}@media print{body{background:#fff;padding:0}.wrap{max-width:none}}</style></head><body><div class="wrap">${body}</div></body></html>`;
  };

  App.prototype.renderLabReportToDom = function(report) {
    const host = document.getElementById('lab-report-preview');
    if (!host) return;
    if (!report) {
      host.innerHTML = '<div class="lab-empty">Još nema renderovanog laboratorijskog izveštaja. Generiši prompt, zalepi AI JSON i klikni <b>Renderuj izveštaj</b>.</div>';
      return;
    }
    host.innerHTML = this._labReportBodyHtml(report);
  };

  App.prototype.renderLabReportFromInput = function() {
    const ta = document.getElementById('lab_ai_json');
    const raw = String(ta?.value || '').trim();
    if (!raw) return alert('Nalepi AI JSON rezultat.');
    const parsed = this._parseAiJsonLoose ? this._parseAiJsonLoose(raw) : null;
    if (!parsed || !parsed.obj) return alert('LAB JSON nije validan.');
    if (ta) ta.value = JSON.stringify(parsed.obj, null, 2);
    const report = this._normalizeLabReportObject(parsed.obj, this.collectLabInput());
    this._labCurrentReport = report;
    try { localStorage.setItem(LAB_LAST_REPORT_KEY, JSON.stringify(report)); } catch(_) {}
    this.renderLabReportToDom(report);
    this.showToast('SINET Lab report renderovan ✅');
    this.log('USER', 'Lab Report Render', report.patient?.name || 'LAB');
  };

  App.prototype.openLabReportWindow = function() {
    const report = this._labCurrentReport || safeParse(localStorage.getItem(LAB_LAST_REPORT_KEY) || 'null', null);
    if (!report) return alert('Nema laboratorijskog izveštaja za prikaz.');
    const html = this._labReportDocument(report);
    const w = window.open('', '_blank');
    if (!w) return alert('Browser je blokirao novi prozor.');
    w.document.open();
    w.document.write(html);
    w.document.close();
  };

  App.prototype.downloadCurrentLabHtml = function() {
    const report = this._labCurrentReport || safeParse(localStorage.getItem(LAB_LAST_REPORT_KEY) || 'null', null);
    if (!report) return alert('Nema laboratorijskog izveštaja za preuzimanje.');
    const html = this._labReportDocument(report);
    const filename = `SINET_LAB_REPORT_${slugify(report.patient?.name || report.title)}_${new Date().toISOString().slice(0,10)}.html`;
    this._downloadText(html, filename);
  };

  App.prototype.exportCurrentLabJson = function() {
    const report = this._labCurrentReport || safeParse(localStorage.getItem(LAB_LAST_REPORT_KEY) || 'null', null);
    if (!report) return alert('Nema LAB JSON sadržaja za izvoz.');
    this._downloadJson(report, `SINET_LAB_REPORT_${slugify(report.patient?.name || report.title)}_${new Date().toISOString().slice(0,10)}.json`);
  };

  App.prototype.saveCurrentLabCase = function() {
    const report = this._labCurrentReport || safeParse(localStorage.getItem(LAB_LAST_REPORT_KEY) || 'null', null);
    if (!report) return alert('Prvo renderuj laboratorijski izveštaj.');
    const input = this.collectLabInput();
    const aiRaw = String(document.getElementById('lab_ai_json')?.value || '').trim();
    const items = this._labHistory();
    const record = {
      id: 'labcase-' + Date.now(),
      created_at: new Date().toISOString(),
      title: report.title,
      patient_name: report.patient?.name || input.patient?.name || '',
      report_date: report.patient?.report_date || input.patient?.report_date || '',
      input,
      report,
      ai_json: aiRaw
    };
    items.unshift(record);
    this._saveLabHistory(items.slice(0, 40));
    this.renderLabHistory();
    this.showToast('LAB slučaj sačuvan ✅', { timeoutMs: 3200 });
    this.log('USER', 'Lab Case Saved', record.patient_name || record.id);
  };

  App.prototype._buildLabSnapshotFromReport = function(report) {
    const keyFindings = arrify(report.key_findings).slice(0, 5).map(it => [it.param, it.status || it.importance].filter(Boolean).join(' • ')).filter(Boolean);
    return {
      id: 'labsnap-' + Date.now(),
      created_at: new Date().toISOString(),
      title: report.title || 'Lab Snapshot',
      patient_name: report.patient?.name || '',
      report_date: report.patient?.report_date || '',
      quick_summary: arrify(report.user_summary).slice(0, 3),
      key_findings: keyFindings,
      next_steps: arrify(report.next_steps).slice(0, 5),
      red_flags: arrify(report.red_flags).slice(0, 5),
      source: 'LAB Bridge',
      lab_report_ref: slugify(report.patient?.name || report.title)
    };
  };

  App.prototype.saveCurrentLabSnapshot = function() {
    const report = this._labCurrentReport || safeParse(localStorage.getItem(LAB_LAST_REPORT_KEY) || 'null', null);
    if (!report) return alert('Prvo renderuj laboratorijski izveštaj.');
    const snap = this._buildLabSnapshotFromReport(report);
    const items = this._labSnapshots();
    items.unshift(snap);
    this._saveLabSnapshots(items.slice(0, 30));
    try { localStorage.setItem('ANAMNEZA_LAB_SNAPSHOT_LAST', JSON.stringify(snap)); } catch(_) {}
    this.renderLabSnapshots();
    this.showToast('Lab Snapshot sačuvan ✅', { timeoutMs: 3200, actionLabel:'LAB Bridge', actionNav:'labs' });
    this.log('USER', 'Lab Snapshot Saved', snap.patient_name || snap.id);
  };

  App.prototype.renderLabHistory = function() {
    const host = document.getElementById('lab-history-list');
    if (!host) return;
    const items = this._labHistory();
    if (!items.length) { host.innerHTML = '<div class="lab-empty">Još nema sačuvanih laboratorijskih slučajeva.</div>'; return; }
    host.innerHTML = items.map((it, idx) => `<div class="lab-hist-card"><div class="lab-hist-title">${this.escapeHtml(it.patient_name || it.title || 'LAB slučaj')}</div><div class="lab-hist-sub">${this.escapeHtml([it.report_date, it.created_at ? it.created_at.slice(0,16).replace('T',' ') : ''].filter(Boolean).join(' • '))}</div><div class="lab-hist-actions"><button type="button" onclick="app.labHistoryLoad(${idx})">↩ Učitaj</button><button type="button" onclick="app.labHistoryViewJson(${idx})">👁 JSON</button><button type="button" onclick="app.labHistoryOpenHtml(${idx})">🪟 HTML</button><button type="button" onclick="app.labHistorySnapshot(${idx})">🧩 Snapshot</button><button type="button" onclick="app.labHistoryDelete(${idx})">🗑 Obriši</button></div></div>`).join('');
  };

  App.prototype.renderLabSnapshots = function() {
    const host = document.getElementById('lab-snapshot-list');
    if (!host) return;
    const items = this._labSnapshots();
    if (!items.length) { host.innerHTML = '<div class="lab-empty">Još nema Lab Snapshot zapisa.</div>'; return; }
    host.innerHTML = items.map((it, idx) => `<div class="lab-hist-card"><div class="lab-hist-title">${this.escapeHtml(it.patient_name || it.title || 'Snapshot')}</div><div class="lab-hist-sub">${this.escapeHtml([it.report_date, it.created_at ? it.created_at.slice(0,16).replace('T',' ') : ''].filter(Boolean).join(' • '))}</div><div style="margin-top:8px;">${arrify(it.key_findings).map(v => `<span class="lab-snapshot-chip">${this.escapeHtml(String(v))}</span>`).join('') || '<span class="lab-inline-note">Nema ključnih nalaza.</span>'}</div><div class="lab-hist-actions"><button type="button" onclick="app.viewLabSnapshot(${idx})">👁 Prikaži</button><button type="button" onclick="app.exportLabSnapshot(${idx})">⬇ JSON</button><button type="button" onclick="app.deleteLabSnapshot(${idx})">🗑 Obriši</button></div></div>`).join('');
  };

  App.prototype.viewLabSnapshot = function(index) {
    const it = this._labSnapshots()[Number(index)];
    if (!it) return;
    this.openTextModal('🧩 Lab Snapshot', JSON.stringify(it, null, 2));
  };

  App.prototype.exportLabSnapshot = function(index) {
    const it = this._labSnapshots()[Number(index)];
    if (!it) return;
    this._downloadJson(it, `SINET_LAB_SNAPSHOT_${slugify(it.patient_name || it.title)}_${new Date().toISOString().slice(0,10)}.json`);
  };
  App.prototype.deleteLabSnapshot = function(index) {
    const items = this._labSnapshots();
    items.splice(Number(index), 1);
    this._saveLabSnapshots(items);
    this.renderLabSnapshots();
  };

  App.prototype.labHistoryLoad = function(index) {
    const it = this._labHistory()[Number(index)];
    if (!it) return;
    this.nav('labs');
    setTimeout(() => {
      const set = (id, val) => { const el = document.getElementById(id); if (el) el.value = val || ''; };
      set('lab_patient_name', it.input?.patient?.name || '');
      set('lab_report_date', it.input?.patient?.report_date || '');
      set('lab_age', it.input?.patient?.age || '');
      set('lab_sex', it.input?.patient?.sex || 'nebitno');
      set('lab_symptoms', it.input?.context?.symptoms || '');
      set('lab_anamnesis', it.input?.context?.anamnesis || '');
      set('lab_therapy', it.input?.context?.therapy || '');
      set('lab_goal', it.input?.context?.goal || '');
      set('lab_raw_text', it.input?.raw_text || '');
      set('lab_ai_json', it.ai_json || JSON.stringify(it.report?.raw || it.report || {}, null, 2));
      this._labCurrentReport = it.report || null;
      try { localStorage.setItem(LAB_LAST_REPORT_KEY, JSON.stringify(it.report || null)); } catch(_) {}
      this.renderLabReportToDom(it.report || null);
      const meta = document.getElementById('lab-file-meta');
      if (meta) {
        meta.dataset.payload = JSON.stringify(it.input?.attachment || null);
        meta.innerHTML = it.input?.attachment?.name ? `Prilog: <b>${this.escapeHtml(it.input.attachment.name)}</b>` : 'Nema učitanog priloga.';
      }
    }, 60);
  };
  App.prototype.labHistoryViewJson = function(index) {
    const it = this._labHistory()[Number(index)];
    if (!it) return;
    this.openTextModal('🧪 LAB istorija — JSON', JSON.stringify(it.report || it, null, 2));
  };
  App.prototype.labHistoryOpenHtml = function(index) {
    const it = this._labHistory()[Number(index)];
    if (!it || !it.report) return;
    const html = this._labReportDocument(it.report);
    const w = window.open('', '_blank');
    if (!w) return alert('Browser je blokirao novi prozor.');
    w.document.open(); w.document.write(html); w.document.close();
  };
  App.prototype.labHistorySnapshot = function(index) {
    const it = this._labHistory()[Number(index)];
    if (!it || !it.report) return;
    this._labCurrentReport = it.report;
    this.saveCurrentLabSnapshot();
  };
  App.prototype.labHistoryDelete = function(index) {
    const items = this._labHistory();
    items.splice(Number(index), 1);
    this._saveLabHistory(items);
    this.renderLabHistory();
  };

  App.prototype.prepareLabBridgeUI = function() {
    this._ensureLabBridgeUI();
    this.renderLabHistory();
    this.renderLabSnapshots();
    const last = this._labCurrentReport || safeParse(localStorage.getItem(LAB_LAST_REPORT_KEY) || 'null', null);
    this.renderLabReportToDom(last);
    const date = document.getElementById('lab_report_date');
    if (date && !date.value) date.value = new Date().toISOString().slice(0,10);
  };

  const origCacheUI = App.prototype.cacheUI;
  App.prototype.cacheUI = function() {
    origCacheUI.apply(this, arguments);
    this._ensureLabBridgeUI();
    this.ui.screens.labs = document.getElementById('page-labs');
  };

  const origInit26 = App.prototype.init;
  App.prototype.init = async function() {
    await origInit26.apply(this, arguments);
    try { this.prepareLabBridgeUI(); } catch(_) {}
  };

  const origNav26 = App.prototype.nav;
  App.prototype.nav = function(pageId, opts={}) {
    const r = origNav26.apply(this, arguments);
    if (pageId === 'labs') { try { this.prepareLabBridgeUI(); } catch(_) {} }
    return r;
  };
})();

/* ===================== v16.0.0.27 — Zdravstveni Karton Core + GDPR + Copy JSON + QA/versioning ===================== */
(function(){
  const HEALTH_RECORD_KEY = 'SINET_HEALTH_RECORD_CORE_V1';
  const LAB_PROMPT_VERSION = 'SINET_MASTER_PROMPT_v1.1';
  const LAB_SCHEMA_VERSION = 'SINET_LAB_SCHEMA_v1.1';
  const LAB_RENDER_VERSION = 'SINET_LAB_RENDER_v16.0.0.40';

  function safeParse(raw, fallback) {
    try { return JSON.parse(raw); } catch(_) { return fallback; }
  }
  function arrify(v) {
    if (Array.isArray(v)) return v.filter(Boolean);
    if (v === null || v === undefined || v === '') return [];
    return [v];
  }
  function slugify(s) {
    return String(s || 'item')
      .normalize('NFD').replace(/[\u0300-\u036f]/g, '')
      .toLowerCase().replace(/[^a-z0-9]+/g, '-')
      .replace(/^-+|-+$/g, '').slice(0, 64) || 'item';
  }
  function uid(prefix) {
    return `${prefix}-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
  }
  function dedupeById(list) {
    const seen = new Set();
    return arrify(list).filter((it) => {
      const id = String(it?.id || '');
      if (!id) return true;
      if (seen.has(id)) return false;
      seen.add(id);
      return true;
    });
  }
  function todayIso() {
    return new Date().toISOString().slice(0, 10);
  }
  function sanitizeFreeText(raw) {
    let text = String(raw || '');
    text = text.replace(/^(\s*(?:Pacijent|Patient|Ime i prezime|Name)\s*:\s*).+$/gim, '$1[PRIVATNO]');
    text = text.replace(/\b\d{13}\b/g, '[JMBG]');
    text = text.replace(/\b[\w.+-]+@[\w.-]+\.[A-Za-z]{2,}\b/g, '[EMAIL]');
    text = text.replace(/\+?\d[\d\s\/()-]{6,}\d/g, '[TELEFON]');
    text = text.replace(/\[PDF prilog\]\s+[^\n]+/g, '[PDF prilog] [GDPR-NEUTRALNO]');
    return text.trim();
  }

  App.prototype._getHealthRecord = function() {
    const raw = safeParse(localStorage.getItem(HEALTH_RECORD_KEY) || 'null', null) || {};
    return {
      id: raw.id || 'sinet-health-record-core-v1',
      created_at: raw.created_at || new Date().toISOString(),
      updated_at: raw.updated_at || new Date().toISOString(),
      profile: {
        display_name: raw.profile?.display_name || 'Korisnik',
        age: raw.profile?.age || '',
        sex: raw.profile?.sex || '',
        notes: raw.profile?.notes || ''
      },
      current_snapshot: {
        summary: arrify(raw.current_snapshot?.summary),
        priorities: arrify(raw.current_snapshot?.priorities),
        next_steps: arrify(raw.current_snapshot?.next_steps),
        red_flags: arrify(raw.current_snapshot?.red_flags),
        last_report_date: raw.current_snapshot?.last_report_date || '',
        last_lab_snapshot_id: raw.current_snapshot?.last_lab_snapshot_id || '',
        last_update: raw.current_snapshot?.last_update || ''
      },
      default_analyses: arrify(raw.default_analyses).length ? arrify(raw.default_analyses) : [
        'KKS', 'Glukoza natašte', 'Insulin', 'HOMA-IR', 'HbA1c',
        'Lipidni status', 'AST', 'ALT', 'Kreatinin', 'eGFR', 'Urin', 'Vitamin D'
      ],
      lab_snapshots: arrify(raw.lab_snapshots),
      medical_events: arrify(raw.medical_events),
      therapy_records: arrify(raw.therapy_records),
      attachments: arrify(raw.attachments),
      specialist_views: safeParse(JSON.stringify(raw.specialist_views || {}), {}),
      analytics: safeParse(JSON.stringify(raw.analytics || {}), {}),
      audit: {
        last_sync_source: raw.audit?.last_sync_source || '',
        last_lab_snapshot_id: raw.audit?.last_lab_snapshot_id || '',
        last_render_version: raw.audit?.last_render_version || LAB_RENDER_VERSION
      }
    };
  };

  App.prototype._saveHealthRecord = function(record) {
    const next = Object.assign({}, record || {}, { updated_at: new Date().toISOString() });
    try { localStorage.setItem(HEALTH_RECORD_KEY, JSON.stringify(next)); } catch(_) {}
    return next;
  };

  App.prototype._neutralLabAttachmentName = function(fileName, mimeType='') {
    const ext = (String(fileName || '').split('.').pop() || '').toLowerCase();
    const byMime = /pdf/i.test(mimeType || '') ? 'pdf' : (/html/i.test(mimeType || '') ? 'html' : 'txt');
    const finalExt = (ext && /^[a-z0-9]{1,6}$/.test(ext)) ? ext : byMime;
    return `lab_prilog.${finalExt || 'dat'}`;
  };

  App.prototype.gdprSanitizeLabText = function(raw) {
    return sanitizeFreeText(raw);
  };

  App.prototype._sanitizeLabInputForPrompt = function(input) {
    const clean = safeParse(JSON.stringify(input || {}), {});
    if (clean.patient) {
      clean.patient.display_name = clean.patient.name ? 'Pacijent' : 'Korisnik';
      clean.patient.name = clean.patient.name ? 'Pacijent' : '';
    }
    if (clean.attachment) {
      clean.attachment = Object.assign({}, clean.attachment, {
        name: undefined,
        display_name: clean.attachment.display_name || clean.attachment.neutral_name || 'lab_prilog',
        gdpr_sanitized: true
      });
    }
    clean.raw_text = this.gdprSanitizeLabText(clean.raw_text || '');
    return clean;
  };

  App.prototype._labVersioningMeta = function(inputCtx, existingMeta={}) {
    return {
      report_id: existingMeta.report_id || uid('lab-report'),
      prompt_version: existingMeta.prompt_version || LAB_PROMPT_VERSION,
      schema_version: existingMeta.schema_version || LAB_SCHEMA_VERSION,
      render_version: LAB_RENDER_VERSION,
      source_type: existingMeta.source_type || (inputCtx?.attachment ? (inputCtx.attachment.source_type || 'local_upload') : (inputCtx?.raw_text ? 'manual_text' : 'manual')),
      generated_at: new Date().toISOString(),
      privacy: {
        gdpr_sanitized: true,
        hidden_attachment_names: true,
        neutral_prompt_identity: true
      }
    };
  };

  App.prototype._labQaGate = function(report) {
    const issues = [];
    const findings = arrify(report?.key_findings);
    if (!String(report?.title || '').trim()) issues.push('Nedostaje naslov izveštaja.');
    if (!arrify(report?.doctor_summary).length) issues.push('Nedostaje doctor_summary.');
    if (!arrify(report?.user_summary).length) issues.push('Nedostaje user_summary.');
    if (!findings.length) issues.push('Nema key_findings stavki.');
    if (!arrify(report?.next_steps).length) issues.push('Nema next_steps preporuka.');
    if (!arrify(report?.red_flags).length) issues.push('Nema red_flags napomena.');
    return {
      status: issues.length ? 'warn' : 'pass',
      issues,
      checked_at: new Date().toISOString()
    };
  };

  const origNormalizeLabReportObject27 = App.prototype._normalizeLabReportObject;
  App.prototype._normalizeLabReportObject = function(obj, inputCtx) {
    const report = origNormalizeLabReportObject27.apply(this, arguments);
    const rootMeta = safeParse(JSON.stringify((obj && obj.metadata) || (obj && obj.report && obj.report.metadata) || {}), {});
    report.metadata = this._labVersioningMeta(inputCtx, rootMeta);
    report.qa = this._labQaGate(report);
    report.metadata.qa_status = report.qa.status;
    report.metadata.qa_issues = arrify(report.qa.issues).slice(0, 12);
    return report;
  };

  const origLabReportBodyHtml27 = App.prototype._labReportBodyHtml;
  App.prototype._labReportBodyHtml = function(report) {
    const base = origLabReportBodyHtml27.apply(this, arguments);
    const qa = report?.qa || this._labQaGate(report || {});
    const meta = report?.metadata || {};
    const versionBlock = `
      <section class="lab-report-section">
        <h3>Versioning + QA gate</h3>
        <div class="lab-kpi">
          <div class="lab-kpi-card"><b>QA status</b>${this.escapeHtml(qa.status || 'unknown')}</div>
          <div class="lab-kpi-card"><b>prompt_version</b>${this.escapeHtml(meta.prompt_version || LAB_PROMPT_VERSION)}</div>
          <div class="lab-kpi-card"><b>schema_version</b>${this.escapeHtml(meta.schema_version || LAB_SCHEMA_VERSION)}</div>
          <div class="lab-kpi-card"><b>render_version</b>${this.escapeHtml(meta.render_version || LAB_RENDER_VERSION)}</div>
        </div>
        ${arrify(qa.issues).length ? `<div style="margin-top:12px;">${this._labListHtml(qa.issues, 'QA bez napomena.')}</div>` : '<div class="lab-inline-note" style="margin-top:12px;">QA gate nije prijavio strukturne probleme.</div>'}
      </section>`;
    return base.replace(/<\/div>\s*$/, `${versionBlock}</div>`);
  };

  const origGenerateLabPrompt27 = App.prototype.generateLabPrompt;
  App.prototype.generateLabPrompt = function() {
    const input = this.collectLabInput();
    const safeInput = this._sanitizeLabInputForPrompt(input);
    const schema = {
      title: 'string',
      subtitle: 'string',
      doctor_summary: ['string'],
      user_summary: ['string'],
      quick_actions: ['string'],
      key_findings: [{ param: 'string', value: 'string', status: 'uredno|sniženo|povišeno|granično', importance: 'visoki|srednji|niski', why_it_matters: 'string' }],
      sections: {
        hematologija: ['string'],
        glukoza_insulin: ['string'],
        bubrezi: ['string'],
        jetra: ['string'],
        lipidi: ['string'],
        vitamini_minerali: ['string'],
        hormoni: ['string'],
        urin: ['string'],
        ostalo: ['string']
      },
      next_steps: ['string'],
      scenarios: ['string'],
      faq: [{ q: 'string', a: 'string' }],
      treatment_and_control: ['string'],
      alternative_support: ['string'],
      red_flags: ['string'],
      monitoring_plan: ['string'],
      doctor_notes: ['string'],
      sources: ['string'],
      metadata: {
        report_id: 'string',
        prompt_version: LAB_PROMPT_VERSION,
        schema_version: LAB_SCHEMA_VERSION,
        source_type: 'manual_text|local_upload|pdf_reference|unknown'
      }
    };
    const prompt = `Ti si SINET Audio Lekar – Laboratorijski analitičar / medicinsko-informativni asistent.\n\nRadiš detaljan, profesionalan, racionalan i smiren SINET laboratorijski izveštaj na srpskom jeziku (latinica).\n\nPRAVILA:\n- Ne postavljaj konačnu dijagnozu samo iz laboratorije.\n- Ne izmišljaj podatke.\n- Jasno odvoji: uredno / granično / odstupanje / prioritet kontrole.\n- Piši i za lekara i za korisnika.\n- Uvek uključi dalje racionalne korake, red flags i oprez sa alternativnim metodama.\n- U metadata obavezno upiši prompt_version i schema_version.\n- Vrati VALIDAN JSON, bez markdown-a i bez dodatnog teksta.\n\nOBAVEZNA STRUKTURA JSON IZLAZA:\n${JSON.stringify(schema, null, 2)}\n\nULAZNI PODACI (GDPR neutralisani za prompt):\n${JSON.stringify(safeInput, null, 2)}\n\nNAPOMENA:\n- Ako neki blok nije primenljiv, vrati prazan niz umesto izmišljanja.\n- Ako laboratorija nije potpuna, jasno napiši šta nedostaje i kako to utiče na pouzdanost tumačenja.\n- U sekciji treatment_and_control navedi standardni medicinski okvir + blagu podršku (ishrana, san, hidracija, fizička aktivnost, oprezni čajevi), ali ne agresivne alternativne metode.\n- Stil treba da bude SINET Book/Pro, pogodan za HTML render.\n- Ne vraćaj pravo ime iz priloga ili naziva fajla.`;
    const out = document.getElementById('lab_prompt');
    if (out) out.value = prompt;
    this.showToast('SINET Lab Prompt generisan ✅', { timeoutMs: 3200 });
    this.log('USER', 'Lab Prompt Generated v16.0.0.27', safeInput.patient?.display_name || 'LAB');
  };

  const origLabHandleInputFiles27 = App.prototype.labHandleInputFiles;
  App.prototype.labHandleInputFiles = async function(input) {
    const file = input?.files?.[0];
    const meta = document.getElementById('lab-file-meta');
    if (!file || !meta) return;
    const neutralName = this._neutralLabAttachmentName(file.name, file.type || '');
    const info = {
      display_name: neutralName,
      neutral_name: neutralName,
      ext: (String(file.name || '').split('.').pop() || '').toLowerCase(),
      size: file.size,
      type: file.type || '',
      lastModified: file.lastModified || 0,
      gdpr_sanitized: true,
      source_type: /pdf/i.test(file.type || '') || /\.pdf$/i.test(file.name || '') ? 'pdf_reference' : 'local_upload'
    };
    meta.dataset.payload = JSON.stringify(info);
    const ext = info.ext;
    if (['txt','md','json','html','htm'].includes(ext) || /^text\//.test(file.type || '')) {
      const raw = await file.text();
      let text = raw;
      if (ext === 'html' || ext === 'htm' || /html/.test(file.type || '')) {
        try {
          const doc = new DOMParser().parseFromString(raw, 'text/html');
          text = String(doc.body?.innerText || raw).trim();
        } catch(_) {}
      }
      const ta = document.getElementById('lab_raw_text');
      if (ta) ta.value = this.gdprSanitizeLabText(text);
      meta.innerHTML = `Učitano: <b>${this.escapeHtml(neutralName)}</b> • ${(file.size/1024).toFixed(1)} KB • GDPR sanitize ON`;
      this.showToast('LAB prilog učitan i neutralisan za prikaz ✅');
      return;
    }
    if (ext === 'pdf' || /pdf/.test(file.type || '')) {
      const ta = document.getElementById('lab_raw_text');
      if (ta && !ta.value.trim()) ta.value = '[PDF prilog] [GDPR-NEUTRALNO]\n\nNapomena: offline demo za sada ne parsira PDF automatski. Nalepi tekst nalaza ili koristi ovaj prilog kao referencu.';
      meta.innerHTML = `PDF priložen: <b>${this.escapeHtml(neutralName)}</b> • ${(file.size/1024).toFixed(1)} KB • parser dolazi u sledećem talasu • GDPR sanitize ON`;
      this.showToast('PDF je zabeležen sa neutralnim imenom.');
      return;
    }
    meta.innerHTML = `Prilog: <b>${this.escapeHtml(neutralName)}</b> • format nije direktno podržan za tekstualni import.`;
  };

  const origRenderLabReportFromInput27 = App.prototype.renderLabReportFromInput;
  App.prototype.renderLabReportFromInput = function() {
    const ta = document.getElementById('lab_ai_json');
    const raw = String(ta?.value || '').trim();
    if (!raw) return alert('Nalepi AI JSON rezultat.');
    const parsed = this._parseAiJsonLoose ? this._parseAiJsonLoose(raw) : null;
    if (!parsed || !parsed.obj) return alert('LAB JSON nije validan.');
    if (ta) ta.value = JSON.stringify(parsed.obj, null, 2);
    const report = this._normalizeLabReportObject(parsed.obj, this.collectLabInput());
    this._labCurrentReport = report;
    try { localStorage.setItem('SINET_LAB_LAST_REPORT_V1', JSON.stringify(report)); } catch(_) {}
    this.renderLabReportToDom(report);
    this.showToast(`SINET Lab report renderovan ✅ (QA: ${report.qa?.status || 'n/a'})`);
    this.log('USER', 'Lab Report Render v16.0.0.27', report.metadata?.report_id || report.patient?.name || 'LAB');
    try { this.renderHealthRecord(); } catch(_) {}
  };

  App.prototype.copyCurrentLabJson = async function() {
    const report = this._labCurrentReport || safeParse(localStorage.getItem('SINET_LAB_LAST_REPORT_V1') || 'null', null);
    if (!report) return alert('Nema LAB JSON sadržaja za kopiranje.');
    const text = JSON.stringify(report, null, 2);
    try {
      if (navigator.clipboard && navigator.clipboard.writeText) await navigator.clipboard.writeText(text);
      else throw new Error('clipboard-api-unavailable');
      this.showToast('KOPIRAJ JSON REZULTAT ✅');
    } catch(_) {
      this.openTextModal('📋 KOPIRAJ JSON REZULTAT', text);
    }
  };

  const origDownloadCurrentLabHtml27 = App.prototype.downloadCurrentLabHtml;
  App.prototype.downloadCurrentLabHtml = function() {
    const report = this._labCurrentReport || safeParse(localStorage.getItem('SINET_LAB_LAST_REPORT_V1') || 'null', null);
    if (!report) return alert('Nema laboratorijskog izveštaja za preuzimanje.');
    const html = this._labReportDocument(report);
    const filename = `SINET_LAB_REPORT_${slugify(report.metadata?.report_id || report.title)}_${todayIso()}.html`;
    this._downloadText(html, filename);
  };

  const origExportCurrentLabJson27 = App.prototype.exportCurrentLabJson;
  App.prototype.exportCurrentLabJson = function() {
    const report = this._labCurrentReport || safeParse(localStorage.getItem('SINET_LAB_LAST_REPORT_V1') || 'null', null);
    if (!report) return alert('Nema LAB JSON sadržaja za izvoz.');
    this._downloadJson(report, `SINET_LAB_REPORT_${slugify(report.metadata?.report_id || report.title)}_${todayIso()}.json`);
  };

  App.prototype._syncHealthRecordFromLabSnapshot = function(snap) {
    if (!snap) return;
    const hr = this._getHealthRecord();
    hr.profile.display_name = snap.patient_name || hr.profile.display_name || 'Korisnik';
    hr.current_snapshot = {
      summary: arrify(snap.quick_summary).slice(0, 3),
      priorities: arrify(snap.key_findings).slice(0, 5),
      next_steps: arrify(snap.next_steps).slice(0, 5),
      red_flags: arrify(snap.red_flags).slice(0, 5),
      last_report_date: snap.report_date || '',
      last_lab_snapshot_id: snap.id || '',
      last_update: new Date().toISOString()
    };
    hr.lab_snapshots = dedupeById([snap, ...arrify(hr.lab_snapshots)]).slice(0, 50);
    hr.audit.last_sync_source = 'LAB Bridge';
    hr.audit.last_lab_snapshot_id = snap.id || '';
    hr.audit.last_render_version = LAB_RENDER_VERSION;
    this._saveHealthRecord(hr);
  };

  const origSaveCurrentLabSnapshot27 = App.prototype.saveCurrentLabSnapshot;
  App.prototype.saveCurrentLabSnapshot = function() {
    const before = this._labSnapshots ? this._labSnapshots() : [];
    origSaveCurrentLabSnapshot27.apply(this, arguments);
    const after = this._labSnapshots ? this._labSnapshots() : [];
    const snap = after[0] || before[0] || null;
    if (snap) this._syncHealthRecordFromLabSnapshot(snap);
    try { this.renderHealthRecord(); } catch(_) {}
  };

  App.prototype._ensureHealthRecordStyles = function() {
    if (document.getElementById('health-record-styles')) return;
    const st = document.createElement('style');
    st.id = 'health-record-styles';
    st.textContent = `
      .hr-list-card{background:#fff;border:1px solid #e8edf4;border-radius:14px;padding:12px;margin-top:10px}
      .hr-sub{font-size:.88rem;color:#678;margin-top:4px}
      .hr-mini{font-size:.85rem;color:#456}
      .hr-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px}
      .hr-actions button{padding:8px 10px;border-radius:10px;border:1px solid #d7e1eb;background:#fff;font-weight:800;cursor:pointer}
      .hr-tag{display:inline-flex;align-items:center;border:1px solid #dce5ef;border-radius:999px;padding:6px 10px;margin:0 8px 8px 0;background:#fff}
      .hr-grid{display:grid;gap:12px}
      .hr-attach{display:inline-flex;align-items:center;gap:8px;border:1px dashed #cfe0f2;border-radius:12px;padding:6px 10px;margin:0 8px 8px 0;background:#fbfdff;cursor:pointer}
      .hr-attach small{color:#678;font-weight:800}
      .hr-thumb{width:28px;height:28px;border-radius:8px;object-fit:cover;border:1px solid #e1ebf5;background:#fff}
      .hr-attach-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px}
      .hr-attach-actions button{padding:7px 10px;border-radius:10px;border:1px solid #d7e1eb;background:#fff;font-weight:900;cursor:pointer}
      .hr-modal-backdrop{position:fixed;inset:0;background:rgba(0,0,0,.25);z-index:9999;display:none;align-items:center;justify-content:center;padding:14px}
      .hr-modal{background:#fff;border-radius:16px;max-width:860px;width:100%;max-height:86vh;overflow:auto;border:1px solid #e6eef8;box-shadow:0 20px 60px rgba(0,0,0,.15)}
      .hr-modal header{display:flex;justify-content:space-between;align-items:center;gap:10px;padding:12px 14px;border-bottom:1px solid #eef3fa;position:sticky;top:0;background:#fff}
      .hr-modal header h3{margin:0;font-size:1.05rem}
      .hr-modal .body{padding:14px}
      .hr-modal .preview{border:1px solid #eef3fa;border-radius:14px;overflow:hidden;background:#fbfdff}
      .hr-modal iframe{width:100%;height:62vh;border:0}
      .hr-modal .meta{display:grid;gap:10px;margin-top:12px}
      @media(min-width:760px){.hr-modal .meta{grid-template-columns:1fr 1fr}}

    `;
    document.head.appendChild(st);
  };

  App.prototype._ensureHealthRecordPage = function() {
    if (document.getElementById('page-health-record')) return document.getElementById('page-health-record');
    const main = document.querySelector('main');
    if (!main) return null;
    const page = document.createElement('div');
    page.id = 'page-health-record';
    page.className = 'section';
    page.innerHTML = `
      <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom:10px; gap:10px; flex-wrap:wrap;">
        <h2 style="margin:0;">🗂 Zdravstveni Karton</h2>
        <div style="display:flex; gap:8px; flex-wrap:wrap;">
          <button class="btn-mini" onclick="nav('labs')">🧪 LAB Bridge</button>
          <button class="btn-mini" onclick="nav('home')">🏠 Početni ekran</button>
        </div>
      </div>
      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">Centralni model sistema</h4>
        <p style="color:#666; margin-top:6px;">Anamneza ostaje <b>current snapshot</b>, a <b>Zdravstveni Karton</b> čuva vremensku liniju: laboratorije, događaje, terapije, trendove i AI sažetke.</p>
      </div>
      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">1) Trenutni snapshot</h4>
        <div id="health-record-summary"></div>
      </div>
      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">2) Default Analyses</h4>
        <div class="lab-inline-note">Ovo je početni spisak analiza koje želiš da pratiš kroz vreme. Jedna analiza po liniji.</div>
        <textarea id="health-default-analyses" class="lab-textarea" style="min-height:170px;"></textarea>
        <div class="lab-actions">
          <button class="lab-btn primary" type="button" onclick="app.saveHealthRecordDefaultAnalyses()">💾 Sačuvaj Default Analyses</button>
          <button class="lab-btn ghost" type="button" onclick="app.resetHealthRecordDefaultAnalyses()">↺ Vrati preporučeni spisak</button>
          <button class="lab-btn secondary" type="button" onclick="app.exportHealthRecordJson()">⬇ JSON kartona</button>
        </div>
      </div>
      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">3) Medicinski događaji / encounter log</h4>
        <div class="hr-grid two">
          <div>
            <label><b>Datum</b></label>
            <input id="health-event-date" class="lab-input" type="date" />
          </div>
          <div>
            <label><b>Tip događaja</b></label>
            <select id="health-event-type" class="lab-select">
              <option value="follow_up_control">follow-up control</option>
              <option value="doctor_report">doctor report</option>
              <option value="lab_panel">lab panel</option>
              <option value="imaging_report">imaging report</option>
              <option value="hospital_event">hospital event</option>
              <option value="symptom_episode">symptom episode</option>
            </select>
          </div>
        </div>
        <label><b>Naslov / razlog</b></label>
        <input id="health-event-title" class="lab-input" type="text" placeholder="npr. internistička kontrola, UZ abdomena, novi bol u vratu..." />
        <label><b>Napomena</b></label>
        <textarea id="health-event-notes" class="lab-textarea" placeholder="kratak opis nalaza, preporuke, dogovorene kontrole..."></textarea>
        <div class="lab-actions">
          <button class="lab-btn dark" type="button" onclick="app.addHealthRecordEvent()">➕ Dodaj događaj</button>
        </div>
        <div id="health-event-list"></div>
      </div>
      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">4) Terapijski sloj</h4>
        <div class="hr-grid two">
          <div>
            <label><b>Datum</b></label>
            <input id="health-therapy-date" class="lab-input" type="date" />
          </div>
          <div>
            <label><b>Tip terapije</b></label>
            <select id="health-therapy-type" class="lab-select">
              <option value="lek">lek</option>
              <option value="suplement">suplement</option>
              <option value="audio_terapija">audio terapija</option>
              <option value="frekvencijski_protokol">frekvencijski protokol</option>
              <option value="relaks">relaks</option>
              <option value="san">san</option>
              <option value="bol">bol</option>
              <option value="vitamin_mineral">vitamin/mineral podrška</option>
            </select>
          </div>
        </div>
        <label><b>Naziv / razlog</b></label>
        <input id="health-therapy-name" class="lab-input" type="text" placeholder="npr. vitamin D, metformin, relaks audio, 40 min loop..." />
        <div class="hr-grid two">
          <div>
            <label><b>Trajanje</b></label>
            <input id="health-therapy-duration" class="lab-input" type="text" placeholder="npr. 14 dana / 40 min x 2" />
          </div>
          <div>
            <label><b>Subjektivni efekat</b></label>
            <input id="health-therapy-effect" class="lab-input" type="text" placeholder="npr. blago bolje, bez efekta, potrebno pratiti" />
          </div>
        </div>
        <label><b>Napomena</b></label>
        <textarea id="health-therapy-notes" class="lab-textarea" placeholder="zašto je uvedeno, šta se pratilo, kakav je odgovor...\n"></textarea>
        <div class="lab-actions">
          <button class="lab-btn primary" type="button" onclick="app.addHealthRecordTherapy()">➕ Dodaj terapijski zapis</button>
        </div>
        <div id="health-therapy-list"></div>
      </div>
      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">5) Lab Snapshot istorija</h4>
        <div class="lab-actions">
          <button class="lab-btn secondary" type="button" onclick="app.importLatestLabSnapshotIntoHealthRecord()">🧩 Uvezi poslednji Lab Snapshot</button>
          <button class="lab-btn ghost" type="button" onclick="app.viewHealthRecordJson()">👁 Prikaži JSON kartona</button>
        </div>
        <div id="health-lab-snapshot-list"></div>
      </div>

      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">6) Compare + Trend (2–3 nalaza)</h4>
        <div class="lab-inline-note">Izaberi 2 ili 3 Lab Snapshot-a i dobićeš brzi pregled promena (tagovi/ključni nalazi) i specialist pregled.</div>
        <div class="hr-grid two">
          <div>
            <label><b>Snapshot A</b></label>
            <select id="hr-compare-a" class="lab-select"></select>
          </div>
          <div>
            <label><b>Snapshot B</b></label>
            <select id="hr-compare-b" class="lab-select"></select>
          </div>
        </div>
        <div style="margin-top:10px;">
          <label><b>Snapshot C (opciono)</b></label>
          <select id="hr-compare-c" class="lab-select"></select>
        </div>
        <div class="lab-actions">
          <button class="lab-btn dark" type="button" onclick="app.runHealthRecordCompare()">🔍 Uporedi</button>
          <button class="lab-btn ghost" type="button" onclick="app.clearHealthRecordCompare()">↺ Reset</button>
        </div>
        <div id="hr-compare-out"></div>
      </div>

      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">7) Specialist views (auto)</h4>
        <div class="lab-inline-note">Ovo je “quick view” sloj za specijaliste. Ne menja lekarski pregled — služi za prioritet kontrole i organizaciju pitanja.</div>
        <div id="hr-specialist-views"></div>
        <div class="lab-actions" style="margin-top:10px;">
          <button class="lab-btn secondary" type="button" onclick="app.exportSpecialistViewsHtml()">🖨 Print/HTML specijalistički sažetak</button>
          <button class="lab-btn ghost" type="button" onclick="app.copySpecialistViewsJson()">📋 Kopiraj specialist JSON</button>
        </div>
      </div>

      <div class="welcome-card" style="text-align:left;">
        <h4 style="margin-top:0;">8) Prilozi (attachments)</h4>
        <div class="lab-inline-note">Prilozi se vezuju za događaje ili terapije. Manji fajlovi dobijaju preview; veći se čuvaju kao metadata-only (da ne “pukne” localStorage).</div>
        <div class="hr-grid two">
          <div>
            <label><b>Veži prilog za</b></label>
            <select id="hr-attach-target-type" class="lab-select">
              <option value="event">Medicinski događaj</option>
              <option value="therapy">Terapijski zapis</option>
            </select>
          </div>
          <div>
            <label><b>Izaberi zapis</b></label>
            <select id="hr-attach-target-id" class="lab-select"></select>
          </div>
        </div>
        <input id="hr-attach-file" type="file" class="lab-input" style="padding:10px;" />
        <div class="lab-actions">
          <button class="lab-btn primary" type="button" onclick="app.attachFileToHealthRecord()">📎 Dodaj prilog</button>
        </div>
        <div id="hr-attachments-list"></div>
      </div>

      <div id="hr-attach-modal" class="hr-modal-backdrop" onclick="app.closeHealthRecordAttachmentModal(event)">
        <div class="hr-modal" onclick="event.stopPropagation()">
          <header>
            <h3 id="hr-attach-modal-title">📎 Prilog</h3>
            <button class="btn-mini" onclick="app.hideHealthRecordAttachmentModal()">✖</button>
          </header>
          <div class="body">
            <div id="hr-attach-modal-preview" class="preview"></div>
            <div id="hr-attach-modal-meta" class="meta"></div>
            <div class="hr-attach-actions">
              <button type="button" onclick="app.downloadCurrentAttachment()">⬇ Preuzmi</button>
              <button type="button" onclick="app.deleteCurrentAttachment()">🗑 Obriši</button>
            </div>
          </div>
        </div>
      </div>

    `;
    const helpPage = document.getElementById('page-help');
    if (helpPage) main.insertBefore(page, helpPage);
    else main.appendChild(page);
    return page;
  };

  App.prototype._ensureHealthRecordEntryPoints = function() {
    if (!document.getElementById('menu-link-health-record')) {
      const labsLink = document.getElementById('menu-link-labs');
      const anchor = labsLink?.parentElement || Array.from(document.querySelectorAll('a.menu-link')).find(a => /Podešavanja/.test(a.textContent || ''))?.parentElement;
      if (anchor && anchor.parentElement) {
        const li = document.createElement('li');
        li.innerHTML = '<a id="menu-link-health-record" class="menu-link" data-label="zdravstveni karton" href="#" onclick="nav(\'health-record\'); closeMenu();">🗂 Zdravstveni Karton</a>';
        anchor.parentElement.insertBefore(li, anchor.nextSibling);
      }
    }
    const page = document.getElementById('page-labs');
    if (page && !document.getElementById('lab-open-health-record-btn')) {
      const actionsWrap = page.querySelectorAll('.welcome-card .lab-actions')[2];
      if (actionsWrap) {
        const btn = document.createElement('button');
        btn.id = 'lab-open-health-record-btn';
        btn.type = 'button';
        btn.className = 'lab-btn ghost';
        btn.textContent = '🗂 Otvori Zdravstveni Karton';
        btn.onclick = () => this.nav('health-record');
        actionsWrap.appendChild(btn);
      }
    }
  };

  App.prototype._patchLabBridge27 = function() {
    const page = document.getElementById('page-labs');
    if (!page) return;
    if (!document.getElementById('lab-gdpr-note')) {
      const fileBox = page.querySelector('.lab-filebox');
      if (fileBox) {
        const note = document.createElement('div');
        note.id = 'lab-gdpr-note';
        note.className = 'lab-inline-note';
        note.style.marginTop = '10px';
        note.innerHTML = '<b>GDPR sanitize:</b> stvarna imena iz naziva fajlova se ne prikazuju. Prompt koristi neutralan identitet pacijenta.';
        fileBox.appendChild(note);
      }
    }
    if (!document.getElementById('lab-copy-json-btn')) {
      const actionsWrap = page.querySelectorAll('.welcome-card .lab-actions')[1];
      if (actionsWrap) {
        const btn = document.createElement('button');
        btn.id = 'lab-copy-json-btn';
        btn.type = 'button';
        btn.className = 'lab-btn ghost';
        btn.textContent = '📋 KOPIRAJ JSON REZULTAT';
        btn.onclick = () => this.copyCurrentLabJson();
        actionsWrap.appendChild(btn);
      }
    }
  };

  App.prototype.renderHealthRecord = function() {
    const hr = this._getHealthRecord();
    const summaryHost = document.getElementById('health-record-summary');
    const analysesTa = document.getElementById('health-default-analyses');
    const eventList = document.getElementById('health-event-list');
    const therapyList = document.getElementById('health-therapy-list');
    const snapList = document.getElementById('health-lab-snapshot-list');
    if (analysesTa && document.activeElement !== analysesTa) analysesTa.value = hr.default_analyses.join('\n');
    if (summaryHost) {
      summaryHost.innerHTML = `
        <div class="lab-kpi">
          <div class="lab-kpi-card"><b>Profil</b>${this.escapeHtml(hr.profile.display_name || 'Korisnik')}</div>
          <div class="lab-kpi-card"><b>Default analyses</b>${this.escapeHtml(String(hr.default_analyses.length || 0))}</div>
          <div class="lab-kpi-card"><b>Lab snapshots</b>${this.escapeHtml(String(hr.lab_snapshots.length || 0))}</div>
          <div class="lab-kpi-card"><b>Terapijski zapisi</b>${this.escapeHtml(String(hr.therapy_records.length || 0))}</div>
          <div class="lab-kpi-card"><b>Medicinski događaji</b>${this.escapeHtml(String(hr.medical_events.length || 0))}</div>
          <div class="lab-kpi-card"><b>Last update</b>${this.escapeHtml(hr.updated_at ? hr.updated_at.slice(0,16).replace('T',' ') : '-')}</div>
        </div>
        <div style="margin-top:12px;">
          <div class="hr-mini"><b>Current snapshot</b></div>
          <div style="margin-top:8px;">${arrify(hr.current_snapshot.summary).length ? arrify(hr.current_snapshot.summary).map(v => `<span class="hr-tag">${this.escapeHtml(String(v))}</span>`).join('') : '<div class="lab-empty">Još nema sažetka current snapshot-a. Uvezi Lab Snapshot iz LAB Bridge modula.</div>'}</div>
          ${arrify(hr.current_snapshot.priorities).length ? `<div style="margin-top:12px;">${arrify(hr.current_snapshot.priorities).map(v => `<span class="hr-tag">${this.escapeHtml(String(v))}</span>`).join('')}</div>` : ''}
          ${hr.current_snapshot.last_report_date ? `<div class="lab-inline-note" style="margin-top:10px;">Poslednji laboratorijski datum: <b>${this.escapeHtml(hr.current_snapshot.last_report_date)}</b></div>` : ''}
        </div>`;
    }
    if (eventList) {
      eventList.innerHTML = hr.medical_events.length ? hr.medical_events.map((it, idx) => `
        <div class="hr-list-card">
          <div style="font-weight:900; color:#1f3f5b;">${this.escapeHtml(it.title || 'Događaj')}</div>
          <div class="hr-sub">${this.escapeHtml([it.date, it.type].filter(Boolean).join(' • '))}</div>
          ${it.notes ? `<div style="margin-top:8px; line-height:1.55;">${this.escapeHtml(it.notes)}</div>` : ''}
          <div class="hr-actions"><button type="button" onclick="app.deleteHealthRecordEvent(${idx})">🗑 Obriši</button></div>
        </div>`).join('') : '<div class="lab-empty">Još nema medicinskih događaja.</div>';
    }
    if (therapyList) {
      therapyList.innerHTML = hr.therapy_records.length ? hr.therapy_records.map((it, idx) => `
        <div class="hr-list-card">
          <div style="font-weight:900; color:#1f3f5b;">${this.escapeHtml(it.name || 'Terapija')}</div>
          <div class="hr-sub">${this.escapeHtml([it.date, it.type, it.duration].filter(Boolean).join(' • '))}</div>
          ${it.effect ? `<div class="hr-mini" style="margin-top:8px;"><b>Efekat:</b> ${this.escapeHtml(it.effect)}</div>` : ''}
          ${it.notes ? `<div style="margin-top:8px; line-height:1.55;">${this.escapeHtml(it.notes)}</div>` : ''}
          <div class="hr-actions"><button type="button" onclick="app.deleteHealthRecordTherapy(${idx})">🗑 Obriši</button></div>
        </div>`).join('') : '<div class="lab-empty">Još nema terapijskih zapisa.</div>';
    }
    if (snapList) {
      const snaps = hr.lab_snapshots.length ? hr.lab_snapshots : (this._labSnapshots ? this._labSnapshots() : []);
      snapList.innerHTML = snaps.length ? snaps.map((it) => `
        <div class="hr-list-card">
          <div style="font-weight:900; color:#1f3f5b;">${this.escapeHtml(it.patient_name || it.title || 'Lab Snapshot')}</div>
          <div class="hr-sub">${this.escapeHtml([it.report_date, it.created_at ? it.created_at.slice(0,16).replace('T',' ') : ''].filter(Boolean).join(' • '))}</div>
          <div style="margin-top:8px;">${arrify(it.key_findings).map(v => `<span class="hr-tag">${this.escapeHtml(String(v))}</span>`).join('') || '<span class="lab-inline-note">Nema ključnih nalaza.</span>'}</div>
        </div>`).join('') : '<div class="lab-empty">Još nema uvezenih Lab Snapshot-a u kartonu.</div>';
    }
    const eventDate = document.getElementById('health-event-date');
    const therapyDate = document.getElementById('health-therapy-date');
    if (eventDate && !eventDate.value) eventDate.value = todayIso();
    if (therapyDate && !therapyDate.value) therapyDate.value = todayIso();
  };



  App.prototype._renderHealthRecordCompareAndAttachments = function(hr) {
    const snaps = arrify(hr.lab_snapshots).slice(0, 50);
    const optHtml = (s) => `<option value="${this.escapeHtml(String(s.id||''))}">${this.escapeHtml((s.report_date||'') + ' — ' + (s.title||s.patient_name||'Lab Snapshot'))}</option>`;
    const selA = document.getElementById('hr-compare-a');
    const selB = document.getElementById('hr-compare-b');
    const selC = document.getElementById('hr-compare-c');
    const fill = (sel) => {
      if (!sel) return;
      const cur = sel.value || '';
      sel.innerHTML = `<option value="">— izaberi —</option>` + snaps.map(optHtml).join('');
      if (cur) sel.value = cur;
    };
    fill(selA); fill(selB); fill(selC);

    const tType = document.getElementById('hr-attach-target-type');
    const tSel = document.getElementById('hr-attach-target-id');
    if (tType && tSel) {
      const cur = tSel.value || '';
      const mode = String(tType.value || 'event');
      const list = mode === 'therapy' ? arrify(hr.therapy_records) : arrify(hr.medical_events);
      tSel.innerHTML = `<option value="">— izaberi —</option>` + list.map(it => {
        const label = [it.date, (mode==='therapy'? (it.name||'Terapija') : (it.title||'Događaj'))].filter(Boolean).join(' • ');
        return `<option value="${this.escapeHtml(String(it.id||''))}">${this.escapeHtml(label)}</option>`;
      }).join('');
      if (cur) tSel.value = cur;
      if (!tSel.value && list[0]?.id) tSel.value = String(list[0].id);
      tType.onchange = () => { try { this._renderHealthRecordCompareAndAttachments(this._getHealthRecord()); } catch(_) {} };
    }

    const host = document.getElementById('hr-attachments-list');
    if (host) {
      const atts = arrify(hr.attachments);
      host.innerHTML = atts.length ? atts.map(a => `
        <div class="hr-list-card">
          <div style="display:flex;justify-content:space-between;align-items:center;gap:10px;flex-wrap:wrap;">
            <div style="font-weight:900;color:#1f3f5b;">${this.escapeHtml(a.name||'prilog')}</div>
            <div class="hr-sub">${this.escapeHtml([a.kind, (a.size||0)+' B', (a.created_at||'').slice(0,16).replace('T',' ')].filter(Boolean).join(' • '))}</div>
          </div>
          <div style="margin-top:10px;">${this._hrAttachmentChipHtml(a)}</div>
        </div>
      `).join('') : '<div class="lab-empty">Još nema priloga.</div>';
    }
  };

  App.prototype._renderHealthRecordSpecialistViews = function(hr) {
    const host = document.getElementById('hr-specialist-views');
    if (!host) return;
    const sv = hr.specialist_views || {};
    const views = arrify(sv.views);
    if (!views.length) {
      host.innerHTML = '<div class="lab-empty">Nema specialist view-a još. Pokreni Compare (A/B) da se generiše na osnovu izabranog snapshot-a.</div>';
      return;
    }
    host.innerHTML = `
      <div class="lab-inline-note">based_on_snapshot_id: <b>${this.escapeHtml(String(sv.based_on_snapshot_id||''))}</b> • generated_at: ${this.escapeHtml(String(sv.generated_at||''))}</div>
      ${views.map(v => `
        <div class="hr-list-card">
          <div style="font-weight:900;color:#1f3f5b;">${this.escapeHtml(v.title||v.id)}</div>
          ${arrify(v.focus).length ? `<div style="margin-top:10px;">${arrify(v.focus).map(x=>`<span class="hr-tag">${this.escapeHtml(String(x))}</span>`).join('')}</div>` : '<div class="hr-sub">Nema eksplicitnih tagova za ovaj view (ili nije prepoznato u nalazu).</div>'}
          ${arrify(v.red_flags).length ? `<div style="margin-top:10px;"><b>Red flags</b>${arrify(v.red_flags).map(x=>`<div class="hr-mini">• ${this.escapeHtml(String(x))}</div>`).join('')}</div>` : ''}
          ${arrify(v.next_steps).length ? `<div style="margin-top:10px;"><b>Sledeći koraci</b>${arrify(v.next_steps).map(x=>`<div class="hr-mini">• ${this.escapeHtml(String(x))}</div>`).join('')}</div>` : ''}
          ${arrify(v.questions).length ? `<div style="margin-top:10px;"><b>Pitanja za lekara</b>${arrify(v.questions).map(x=>`<div class="hr-mini">• ${this.escapeHtml(String(x))}</div>`).join('')}</div>` : ''}
        </div>
      `).join('')}
    `;
  };

  App.prototype.saveHealthRecordDefaultAnalyses = function() {
    const ta = document.getElementById('health-default-analyses');
    const items = String(ta?.value || '').split(/\n+/).map(v => v.trim()).filter(Boolean);
    const hr = this._getHealthRecord();
    hr.default_analyses = items;
    this._saveHealthRecord(hr);
    this.renderHealthRecord();
    this.showToast('Default Analyses sačuvan ✅');
  };

  App.prototype.resetHealthRecordDefaultAnalyses = function() {
    const hr = this._getHealthRecord();
    hr.default_analyses = ['KKS', 'Glukoza natašte', 'Insulin', 'HOMA-IR', 'HbA1c', 'Lipidni status', 'AST', 'ALT', 'Kreatinin', 'eGFR', 'Urin', 'Vitamin D'];
    this._saveHealthRecord(hr);
    this.renderHealthRecord();
  };

  App.prototype.addHealthRecordEvent = function() {
    const date = String(document.getElementById('health-event-date')?.value || '').trim();
    const type = String(document.getElementById('health-event-type')?.value || '').trim();
    const title = String(document.getElementById('health-event-title')?.value || '').trim();
    const notes = String(document.getElementById('health-event-notes')?.value || '').trim();
    if (!title) return alert('Upiši naslov događaja.');
    const hr = this._getHealthRecord();
    hr.medical_events.unshift({ id: uid('event'), date: date || todayIso(), type, title, notes, created_at: new Date().toISOString() });
    this._saveHealthRecord(hr);
    ['health-event-title','health-event-notes'].forEach(id => { const el = document.getElementById(id); if (el) el.value = ''; });
    this.renderHealthRecord();
    this.showToast('Medicinski događaj dodat ✅');
  };

  App.prototype.deleteHealthRecordEvent = function(index) {
    const hr = this._getHealthRecord();
    hr.medical_events.splice(Number(index), 1);
    this._saveHealthRecord(hr);
    this.renderHealthRecord();
  };

  App.prototype.addHealthRecordTherapy = function() {
    const date = String(document.getElementById('health-therapy-date')?.value || '').trim();
    const type = String(document.getElementById('health-therapy-type')?.value || '').trim();
    const name = String(document.getElementById('health-therapy-name')?.value || '').trim();
    const duration = String(document.getElementById('health-therapy-duration')?.value || '').trim();
    const effect = String(document.getElementById('health-therapy-effect')?.value || '').trim();
    const notes = String(document.getElementById('health-therapy-notes')?.value || '').trim();
    if (!name) return alert('Upiši naziv terapije/zapisa.');
    const hr = this._getHealthRecord();
    hr.therapy_records.unshift({ id: uid('therapy'), date: date || todayIso(), type, name, duration, effect, notes, created_at: new Date().toISOString() });
    this._saveHealthRecord(hr);
    ['health-therapy-name','health-therapy-duration','health-therapy-effect','health-therapy-notes'].forEach(id => { const el = document.getElementById(id); if (el) el.value = ''; });
    this.renderHealthRecord();
    this.showToast('Terapijski zapis dodat ✅');
  };

  App.prototype.deleteHealthRecordTherapy = function(index) {
    const hr = this._getHealthRecord();
    hr.therapy_records.splice(Number(index), 1);
    this._saveHealthRecord(hr);
    this.renderHealthRecord();
  };

  App.prototype.importLatestLabSnapshotIntoHealthRecord = function() {
    const snap = this._labSnapshots ? this._labSnapshots()[0] : null;
    if (!snap) return alert('Još nema sačuvanog Lab Snapshot-a.');
    this._syncHealthRecordFromLabSnapshot(snap);
    this.renderHealthRecord();
    this.showToast('Poslednji Lab Snapshot uvezen u karton ✅');
  };

  App.prototype.viewHealthRecordJson = function() {
    this.openTextModal('🗂 Zdravstveni Karton — JSON', JSON.stringify(this._getHealthRecord(), null, 2));
  };

  App.prototype.exportHealthRecordJson = function() {
    const hr = this._getHealthRecord();
    this._downloadJson(hr, `SINET_ZDRAVSTVENI_KARTON_${slugify(hr.profile?.display_name || 'korisnik')}_${todayIso()}.json`);
  };

  /* ===== v16.0.0.31 — Runtime hotfix: template escape / cache bump ===== */

  App.prototype._hrMaxInlineBytes = function() { return 320 * 1024; }; // 320 KB
  App.prototype._hrReadFileAsDataURL = function(file) {
    return new Promise((resolve, reject) => {
      const fr = new FileReader();
      fr.onload = () => resolve(String(fr.result || ''));
      fr.onerror = () => reject(fr.error || new Error('File read error'));
      fr.readAsDataURL(file);
    });
  };

  App.prototype._hrGuessKind = function(mime, name) {
    const m = String(mime || '').toLowerCase();
    const n = String(name || '').toLowerCase();
    if (m.startsWith('image/')) return 'image';
    if (m.includes('pdf') || n.endsWith('.pdf')) return 'pdf';
    if (m.includes('html') || n.endsWith('.html') || n.endsWith('.htm')) return 'html';
    if (m.startsWith('text/') || n.endsWith('.txt') || n.endsWith('.md') || n.endsWith('.json')) return 'text';
    return 'bin';
  };

  App.prototype._hrEnsureAttachmentIndex = function() {
    if (this._hrAttachIndexReady) return;
    this._hrAttachIndexReady = true;
    this._hrCurrentAttachmentId = null;
  };

  App.prototype._hrGetAttachmentById = function(hr, id) {
    const list = Array.isArray(hr.attachments) ? hr.attachments : [];
    return list.find(a => String(a.id) === String(id)) || null;
  };

  App.prototype._hrLinkAttachmentToTarget = function(hr, targetType, targetId, attachmentId) {
    const tId = String(targetId || '');
    if (!tId) return;
    if (targetType === 'event') {
      const ev = hr.medical_events.find(e => String(e.id) === tId);
      if (!ev) return;
      ev.attachment_ids = Array.isArray(ev.attachment_ids) ? ev.attachment_ids : [];
      if (!ev.attachment_ids.includes(attachmentId)) ev.attachment_ids.unshift(attachmentId);
    } else if (targetType === 'therapy') {
      const th = hr.therapy_records.find(t => String(t.id) === tId);
      if (!th) return;
      th.attachment_ids = Array.isArray(th.attachment_ids) ? th.attachment_ids : [];
      if (!th.attachment_ids.includes(attachmentId)) th.attachment_ids.unshift(attachmentId);
    }
  };

  App.prototype.attachFileToHealthRecord = async function() {
    const fileEl = document.getElementById('hr-attach-file');
    const file = fileEl && fileEl.files && fileEl.files[0] ? fileEl.files[0] : null;
    if (!file) return alert('Izaberi fajl.');
    const targetType = String(document.getElementById('hr-attach-target-type')?.value || 'event');
    const targetId = String(document.getElementById('hr-attach-target-id')?.value || '');
    if (!targetId) return alert('Izaberi zapis (događaj/terapija).');

    const hr = this._getHealthRecord();
    hr.attachments = Array.isArray(hr.attachments) ? hr.attachments : [];

    const kind = this._hrGuessKind(file.type, file.name);
    const neutralName = this._neutralLabAttachmentName(file.name, file.type);
    const baseMeta = {
      id: uid('att'),
      created_at: new Date().toISOString(),
      name: neutralName,
      original_name_redacted: true,
      mime: file.type || '',
      size: file.size || 0,
      kind,
      inline: false,
      data_url: null,
      text_excerpt: null
    };

    try {
      if ((file.size || 0) <= this._hrMaxInlineBytes()) {
        const dataUrl = await this._hrReadFileAsDataURL(file);
        baseMeta.inline = true;
        baseMeta.data_url = dataUrl;
        if (kind === 'text' || kind === 'html') {
          const raw = atob(String(dataUrl.split(',')[1] || ''));
          baseMeta.text_excerpt = this.gdprSanitizeLabText(raw.slice(0, 12000));
        }
      }
    } catch (e) {
      baseMeta.inline = false;
      baseMeta.data_url = null;
    }

    hr.attachments.unshift(baseMeta);
    this._hrLinkAttachmentToTarget(hr, targetType, targetId, baseMeta.id);
    hr.attachments = hr.attachments.slice(0, 120);
    this._saveHealthRecord(hr);
    if (fileEl) fileEl.value = '';
    this.renderHealthRecord();
    this.showToast('Prilog dodat ✅');
  };

  App.prototype._hrAttachmentChipHtml = function(att) {
    const kind = String(att.kind || 'bin');
    const label = kind === 'image' ? '🖼' : (kind === 'pdf' ? '📄' : (kind === 'text' ? '📝' : (kind === 'html' ? '🌐' : '📦')));
    const thumb = (kind === 'image' && att.inline && att.data_url) ? `<img class="hr-thumb" src="${att.data_url}" alt="thumb" />` : '';
    return `<span class="hr-attach" onclick="app.showHealthRecordAttachment('${this.escapeHtml(String(att.id))}')">${thumb}<b>${label}</b><small>${this.escapeHtml(att.name || 'prilog')}</small></span>`;
  };

  App.prototype.showHealthRecordAttachment = function(attId) {
    this._hrEnsureAttachmentIndex();
    const hr = this._getHealthRecord();
    const att = this._hrGetAttachmentById(hr, attId);
    if (!att) return alert('Prilog nije pronađen.');
    this._hrCurrentAttachmentId = att.id;

    const modal = document.getElementById('hr-attach-modal');
    const title = document.getElementById('hr-attach-modal-title');
    const prev = document.getElementById('hr-attach-modal-preview');
    const meta = document.getElementById('hr-attach-modal-meta');
    if (!modal || !title || !prev || !meta) return;

    title.textContent = `📎 ${att.name || 'Prilog'}`;
    prev.innerHTML = '';
    const kind = String(att.kind || 'bin');

    if (att.inline && att.data_url) {
      if (kind === 'image') prev.innerHTML = `<img src="${att.data_url}" alt="preview" style="width:100%;height:auto;display:block;" />`;
      else if (kind === 'pdf') prev.innerHTML = `<iframe src="${att.data_url}"></iframe>`;
      else if (kind === 'html') prev.innerHTML = `<iframe sandbox="allow-same-origin" src="${att.data_url}"></iframe>`;
      else if (kind === 'text') prev.innerHTML = `<pre style="margin:0;padding:12px;white-space:pre-wrap;line-height:1.5;">${this.escapeHtml(att.text_excerpt || '[tekst]')}</pre>`;
      else prev.innerHTML = `<div style="padding:12px;">Inline fajl je sačuvan, ali nema preview za ovaj tip.</div>`;
    } else {
      prev.innerHTML = `<div style="padding:12px;">Ovaj prilog je sačuvan kao <b>metadata-only</b> (prevelik ili nepodržan za inline). Preuzimanje nije dostupno bez ponovnog uploada.</div>`;
    }

    meta.innerHTML = `
      <div class="lab-kpi-card"><b>Tip</b>${this.escapeHtml(kind)}</div>
      <div class="lab-kpi-card"><b>MIME</b>${this.escapeHtml(att.mime || '-')}</div>
      <div class="lab-kpi-card"><b>Veličina</b>${this.escapeHtml(String(att.size || 0))} B</div>
      <div class="lab-kpi-card"><b>Datum</b>${this.escapeHtml((att.created_at || '').slice(0,16).replace('T',' ') || '-')}</div>
    `;
    modal.style.display = 'flex';
  };

  App.prototype.hideHealthRecordAttachmentModal = function() {
    const modal = document.getElementById('hr-attach-modal');
    if (modal) modal.style.display = 'none';
    this._hrCurrentAttachmentId = null;
  };

  App.prototype.closeHealthRecordAttachmentModal = function(evt) {
    if (evt && evt.target && evt.target.id === 'hr-attach-modal') this.hideHealthRecordAttachmentModal();
  };

  App.prototype.downloadCurrentAttachment = function() {
    const id = this._hrCurrentAttachmentId;
    if (!id) return;
    const hr = this._getHealthRecord();
    const att = this._hrGetAttachmentById(hr, id);
    if (!att) return;
    if (!att.inline || !att.data_url) return alert('Nema inline sadržaja za preuzimanje (metadata-only).');
    const a = document.createElement('a');
    a.href = att.data_url;
    a.download = att.name || 'prilog';
    document.body.appendChild(a);
    a.click();
    a.remove();
  };

  App.prototype.deleteCurrentAttachment = function() {
    const id = this._hrCurrentAttachmentId;
    if (!id) return;
    if (!confirm('Obrisati prilog?')) return;
    const hr = this._getHealthRecord();
    hr.attachments = Array.isArray(hr.attachments) ? hr.attachments : [];
    hr.attachments = hr.attachments.filter(a => String(a.id) !== String(id));
    hr.medical_events.forEach(e => { e.attachment_ids = (Array.isArray(e.attachment_ids)? e.attachment_ids: []).filter(x => String(x) !== String(id)); });
    hr.therapy_records.forEach(t => { t.attachment_ids = (Array.isArray(t.attachment_ids)? t.attachment_ids: []).filter(x => String(x) !== String(id)); });
    this._saveHealthRecord(hr);
    this.hideHealthRecordAttachmentModal();
    this.renderHealthRecord();
    this.showToast('Prilog obrisan ✅');
  };

  App.prototype._hrComputeSpecialistViews = function(snapshot) {
    const tags = arrify(snapshot?.key_findings || []);
    const next = arrify(snapshot?.next_steps || []);
    const red = arrify(snapshot?.red_flags || []);
    const all = (tags.join(' ') + ' ' + next.join(' ') + ' ' + red.join(' ')).toLowerCase();

    function pick(keys) {
      const out = [];
      keys.forEach(k => { if (all.includes(k.toLowerCase())) out.push(k); });
      return out;
    }

    const views = [
      { id:'internista', title:'Internista', focus: pick(['glukoza','insulin','homa','lipid','trigliceridi','hdl','ldl','krvni pritisak']), questions:[
        'Da li postoji potreba za dodatnim testovima (HbA1c, OGTT, apoB)?',
        'Da li su potrebne kontrole u 3 meseca ili ranije?'
      ]},
      { id:'endokrinolog', title:'Endokrinolog / metabolizam', focus: pick(['homa','insulin','tsh','ft4','vitamin d']), questions:[
        'Da li je potrebno ciljano ispitivanje štitaste žlezde ili metabolizma?',
        'Koji je plan praćenja (kontrola, terapija, ishrana)?'
      ]},
      { id:'nefrolog', title:'Nefrolog / bubrezi', focus: pick(['egfr','kreatinin','cistatin','urin','albumin']), questions:[
        'Da li eGFR trend zahteva dodatnu obradu (ACR, urin sediment)?',
        'Koji je optimalan interval kontrole kreatinina/eGFR?'
      ]},
      { id:'gastro', title:'Gastro / hepatologija', focus: pick(['alt','ast','jetra','gama','bilirubin']), questions:[
        'Da li su potrebne dodatne analize jetre ili UZ abdomena?',
        'Da li postoji sumnja na masnu jetru i koji je plan kontrole?'
      ]},
      { id:'kardiolog', title:'Kardiolog / vaskularni rizik', focus: pick(['ldl','hdl','trigliceridi','chol','hs-crp']), questions:[
        'Procena kardiovaskularnog rizika i ciljevi za lipide.',
        'Da li je potreban EKG/UZ srca ili terapijske korekcije?'
      ]},
      { id:'hematologija', title:'Hematologija / KKS', focus: pick(['kks','hemoglobin','leukociti','trombociti','crp']), questions:[
        'Da li KKS/upalni parametri zahtevaju ponavljanje ili dodatnu dijagnostiku?',
        'Koji je interval praćenja?'
      ]}
    ];

    return views.map(v => Object.assign({}, v, { relevant: v.focus.length || red.length, red_flags: red.slice(0,5), next_steps: next.slice(0,5) }));
  };

  App.prototype.runHealthRecordCompare = function() {
    const hr = this._getHealthRecord();
    const snaps = hr.lab_snapshots || [];
    const ida = document.getElementById('hr-compare-a')?.value || '';
    const idb = document.getElementById('hr-compare-b')?.value || '';
    const idc = document.getElementById('hr-compare-c')?.value || '';
    const a = snaps.find(s => String(s.id) === String(ida)) || null;
    const b = snaps.find(s => String(s.id) === String(idb)) || null;
    const c = snaps.find(s => String(s.id) === String(idc)) || null;
    if (!a || !b) return alert('Izaberi A i B snapshot.');

    function setOfTags(s){ return new Set(arrify(s?.key_findings || []).map(x => String(x))); }
    const sa = setOfTags(a), sb = setOfTags(b), sc = c ? setOfTags(c) : null;

    function diff(s1, s2){
      const added=[], removed=[], same=[];
      s2.forEach(v => { if (!s1.has(v)) added.push(v); else same.push(v); });
      s1.forEach(v => { if (!s2.has(v)) removed.push(v); });
      return { added, removed, same };
    }
    const ab = diff(sa, sb);
    const ac = sc ? diff(sa, sc) : null;

    const out = document.getElementById('hr-compare-out');
    if (out) {
      const card = (title, list) => `<div class="hr-list-card"><div style="font-weight:900;color:#1f3f5b;">${this.escapeHtml(title)}</div><div style="margin-top:8px;">${list.length? list.map(v=>`<span class="hr-tag">${this.escapeHtml(v)}</span>`).join('') : '<span class="lab-inline-note">nema</span>'}</div></div>`;
      out.innerHTML = `
        <div class="hr-grid two">
          ${card('Novo u B (u odnosu na A)', ab.added)}
          ${card('Nestalo iz B (u odnosu na A)', ab.removed)}
        </div>
        ${c ? `<div class="hr-grid two" style="margin-top:12px;">
          ${card('Novo u C (u odnosu na A)', (ac?.added||[]))}
          ${card('Nestalo iz C (u odnosu na A)', (ac?.removed||[]))}
        </div>` : ''}
      `;
    }

    const ref = c && c.report_date && b.report_date && c.report_date > b.report_date ? c : b;
    hr.specialist_views = {
      based_on_snapshot_id: ref.id,
      generated_at: new Date().toISOString(),
      views: this._hrComputeSpecialistViews(ref)
    };
    this._saveHealthRecord(hr);
    this.renderHealthRecord();
  };

  App.prototype.clearHealthRecordCompare = function() {
    const out = document.getElementById('hr-compare-out');
    if (out) out.innerHTML = '';
    ['hr-compare-a','hr-compare-b','hr-compare-c'].forEach(id => { const el=document.getElementById(id); if (el) el.value=''; });
  };

  App.prototype.copySpecialistViewsJson = function() {
    const hr = this._getHealthRecord();
    const payload = hr.specialist_views || {};
    const txt = JSON.stringify(payload, null, 2);
    navigator.clipboard.writeText(txt).then(()=>this.showToast('Kopirano ✅')).catch(()=>alert('Clipboard nije dostupan.'));
  };

  App.prototype.exportSpecialistViewsHtml = function() {
    const hr = this._getHealthRecord();
    const sv = hr.specialist_views || {};
    const views = arrify(sv.views);
    const title = `SINET Specialist Views — ${this.escapeHtml(hr.profile?.display_name || 'Korisnik')} — ${todayIso()}`;
    const body = `
      <h1 style="margin:0 0 10px 0;">${title}</h1>
      <div style="color:#567;margin-bottom:14px;">based_on_snapshot_id: <b>${this.escapeHtml(String(sv.based_on_snapshot_id||''))}</b> • generated_at: ${this.escapeHtml(String(sv.generated_at||''))}</div>
      ${views.map(v=>`
        <section style="border:1px solid #e6eef8;border-radius:14px;padding:12px;margin:0 0 12px 0;">
          <h2 style="margin:0 0 8px 0;">${this.escapeHtml(v.title||v.id)}</h2>
          ${arrify(v.focus).length? `<div style="margin:0 0 8px 0;">${arrify(v.focus).map(x=>`<span style="display:inline-block;border:1px solid #dce5ef;border-radius:999px;padding:6px 10px;margin:0 8px 8px 0;">${this.escapeHtml(String(x))}</span>`).join('')}</div>`:''}
          ${arrify(v.red_flags).length? `<div style="margin:8px 0;"><b>Red flags:</b><ul>${arrify(v.red_flags).map(x=>`<li>${this.escapeHtml(String(x))}</li>`).join('')}</ul></div>`:''}
          ${arrify(v.next_steps).length? `<div style="margin:8px 0;"><b>Sledeći koraci:</b><ul>${arrify(v.next_steps).map(x=>`<li>${this.escapeHtml(String(x))}</li>`).join('')}</ul></div>`:''}
          ${arrify(v.questions).length? `<div style="margin:8px 0;"><b>Pitanja za lekara:</b><ul>${arrify(v.questions).map(x=>`<li>${this.escapeHtml(String(x))}</li>`).join('')}</ul></div>`:''}
          <div style="color:#777;font-size:.92rem;margin-top:8px;">Napomena: informativno, ne zamenjuje pregled.</div>
        </section>
      `).join('')}
    `;
    const html = `<!doctype html><html><head><meta charset="utf-8"/><meta name="viewport" content="width=device-width, initial-scale=1"/><title>${title}</title></head><body style="font-family:system-ui,-apple-system,Segoe UI,Roboto,Ubuntu,Arial,sans-serif;line-height:1.5;padding:14px;background:#fbfdff;">${body}</body></html>`;
    const blob = new Blob([html], { type: 'text/html;charset=utf-8' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `SINET_SPECIALIST_VIEWS_${slugify(hr.profile?.display_name||'korisnik')}_${todayIso()}.html`;
    document.body.appendChild(a);
    a.click();
    a.remove();
    setTimeout(()=>URL.revokeObjectURL(url), 1500);
  };


  App.prototype.prepareHealthRecordUI = function() {
    this._ensureLabBridgeUI();
    this._ensureHealthRecordStyles();
    this._ensureHealthRecordPage();
    this._ensureHealthRecordEntryPoints();
    if (this.ui && this.ui.screens) this.ui.screens['health-record'] = document.getElementById('page-health-record');
    this.renderHealthRecord();
  };

  const origPrepareLabBridgeUI27 = App.prototype.prepareLabBridgeUI;
  App.prototype.prepareLabBridgeUI = function() {
    origPrepareLabBridgeUI27.apply(this, arguments);
    this._patchLabBridge27();
    this._ensureHealthRecordEntryPoints();
  };

  const origCacheUI27 = App.prototype.cacheUI;
  App.prototype.cacheUI = function() {
    origCacheUI27.apply(this, arguments);
    try { this.prepareHealthRecordUI(); } catch(_) {}
  };

  const origInit27 = App.prototype.init;
  App.prototype.init = async function() {
    await origInit27.apply(this, arguments);
    try { this.prepareHealthRecordUI(); } catch(_) {}
  };

  const origNav27 = App.prototype.nav;
  App.prototype.nav = function(pageId, opts={}) {
    const r = origNav27.apply(this, arguments);
    if (pageId === 'health-record') { try { this.prepareHealthRecordUI(); } catch(_) {} }
    if (pageId === 'labs') { try { this._patchLabBridge27(); } catch(_) {} }
    return r;
  };
})();


/* ===================== v16.0.0.37 — Health block + specialist mobile views + numeric trends + AI workflow + attachments UX Pro ===================== */
(function(){
  const HR_SELECTED_SPECIALIST_KEY = 'SINET_HR_SELECTED_SPECIALIST';
  const HR_ATTACHMENT_FILTER_KEY = 'SINET_HR_ATTACHMENT_FILTER';
  const HR_TREND_PARAM_KEY = 'SINET_HR_TREND_PARAM';
  const HEALTH_RECORD_KEY = 'SINET_HEALTH_RECORD_CORE_V1';
  const LAB_LAST_REPORT_KEY = 'SINET_LAB_LAST_REPORT_V1';

  function safeParse(raw, fallback) {
    try { return JSON.parse(raw); } catch(_) { return fallback; }
  }
  function arrify(v) {
    if (Array.isArray(v)) return v.filter(Boolean);
    if (v === null || v === undefined || v === '') return [];
    return [v];
  }
  function slugify(s) {
    return String(s || 'item')
      .normalize('NFD').replace(/[\u0300-\u036f]/g, '')
      .toLowerCase().replace(/[^a-z0-9]+/g, '-')
      .replace(/^-+|-+$/g, '').slice(0, 64) || 'item';
  }
  function uid(prefix) {
    return `${prefix}-${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
  }
  function uniqueBy(list, keyFn) {
    const out = [];
    const seen = new Set();
    arrify(list).forEach((it) => {
      const key = String(keyFn(it) || '');
      if (key && seen.has(key)) return;
      if (key) seen.add(key);
      out.push(it);
    });
    return out;
  }
  function stripHtml(raw) {
    return String(raw || '').replace(/<[^>]+>/g, ' ').replace(/\s+/g, ' ').trim();
  }
  function normalizeText(raw) {
    return String(raw || '').replace(/\s+/g, ' ').trim();
  }
  function normalizeLabel(raw) {
    const base = normalizeText(raw)
      .replace(/\b\(([^)]*)\)\s*$/,'')
      .replace(/\s*[-–—]\s*(high|low|normal|borderline|povišeno|sniženo|granično)$/i,'')
      .trim();
    return base || normalizeText(raw);
  }
  function parseNumericValue(raw) {
    if (raw === null || raw === undefined || raw === '') return null;
    if (typeof raw === 'number' && Number.isFinite(raw)) return raw;
    let s = String(raw).trim();
    if (!s) return null;
    if (/^\d{4}-\d{2}-\d{2}/.test(s)) return null;
    s = s.replace(/\u00a0/g, ' ');
    const match = s.match(/-?\d+(?:[.,]\d+)?/);
    if (!match) return null;
    const num = Number(match[0].replace(',', '.'));
    return Number.isFinite(num) ? num : null;
  }
  function fmtNum(n) {
    if (!Number.isFinite(n)) return '—';
    const abs = Math.abs(n);
    if (abs >= 100 || Number.isInteger(n)) return String(Math.round(n));
    if (abs >= 10) return n.toFixed(1).replace(/\.0$/, '');
    return n.toFixed(2).replace(/0+$/,'').replace(/\.$/,'');
  }
  function spark(values) {
    const vals = values.filter(v => Number.isFinite(v));
    if (!vals.length) return '';
    const chars = '▁▂▃▄▅▆▇█';
    const min = Math.min(...vals);
    const max = Math.max(...vals);
    if (min === max) return vals.map(() => '▅').join('');
    return vals.map(v => {
      const idx = Math.max(0, Math.min(chars.length - 1, Math.round(((v - min) / (max - min)) * (chars.length - 1))));
      return chars[idx];
    }).join('');
  }
  function trendDirection(first, last) {
    if (!Number.isFinite(first) || !Number.isFinite(last)) return '—';
    const delta = last - first;
    if (Math.abs(delta) < 1e-9) return '→ stabilno';
    return delta > 0 ? '↗ porast' : '↘ pad';
  }


  App.prototype._removeLegacyHomeLabBridgeDuplicates41 = function() {
    try {
      const frequent = document.getElementById('frequent-actions');
      if (frequent) {
        Array.from(frequent.querySelectorAll('button')).forEach((btn) => {
          const txt = String(btn.textContent || '').toLowerCase();
          if (txt.includes('lab bridge')) btn.remove();
        });
      }
      const legacyAiBtn = document.getElementById('ai-open-lab-btn');
      if (legacyAiBtn) legacyAiBtn.remove();
    } catch (_) {}
  };

  App.prototype._ensureHealthHomeEntryPoints37 = function() {
    const home = document.getElementById('page-home');
    if (!home || document.getElementById('sinet-health-home-card')) return;
    const card = document.createElement('div');
    card.id = 'sinet-health-home-card';
    card.style.cssText = 'background:white; padding:16px; border-radius:12px; margin-bottom:15px; box-shadow:0 2px 5px rgba(0,0,0,0.05);';
    card.innerHTML = `
      <h3 style="margin:0 0 10px 0; color:var(--primary);">🩺 Zdravlje i nalazi</h3>
      <p style="margin:0 0 12px 0; color:#666;">Glavni tok: <b>LAB Bridge → Zdravstveni Karton → Anamneza</b>. Mobilno, brzo i bez traženja po meniju.</p>
      <div style="display:flex; flex-wrap:wrap; gap:10px;">
        <button class="btn-full" style="flex:1; min-width:220px; padding:12px; background:#0f766e;" onclick="nav('labs')">🧪 LAB Bridge</button>
        <button class="btn-full" style="flex:1; min-width:220px; padding:12px; background:#2563eb;" onclick="nav('health-record')">🗂 Zdravstveni Karton</button>
        <button class="btn-full" style="flex:1; min-width:220px; padding:12px;" onclick="openQuickPage('anamneza.html',null,'🩺 Anamneza (Dijagnostika)')">🩺 Anamneza</button>
      </div>`;
    const frequent = document.getElementById('frequent-actions');
    if (frequent && frequent.parentElement) frequent.parentElement.insertBefore(card, frequent.nextSibling);
    else home.insertBefore(card, home.querySelector('#pwa-install-promo') || home.firstChild?.nextSibling || null);
  };

  App.prototype._ensureHealthRecordEnhancements37 = function() {
    this._ensureHealthHomeEntryPoints37();
    const page = document.getElementById('page-health-record');
    if (!page) return;
    if (!document.getElementById('hr-trend-card')) {
      const trendCard = document.createElement('div');
      trendCard.id = 'hr-trend-card';
      trendCard.className = 'welcome-card';
      trendCard.style.textAlign = 'left';
      trendCard.innerHTML = `
        <h4 style="margin-top:0;">6b) Numeric trend (kompletan LAB JSON)</h4>
        <div class="lab-inline-note">Parser pokušava da pročita numeriku iz <b>key_findings</b> i iz dubljih slojeva LAB JSON-a. Najkorisnije radi na novim snapshot-ima sačuvanim od ove verzije naviše.</div>
        <div class="hr-grid two" style="margin-top:10px;">
          <div>
            <label><b>Parametar</b></label>
            <select id="hr-trend-param" class="lab-select"></select>
          </div>
          <div>
            <label><b>Broj snapshot-a sa numerikom</b></label>
            <div id="hr-trend-kpi" class="lab-input" style="background:#fbfdff;">—</div>
          </div>
        </div>
        <div class="lab-actions">
          <button class="lab-btn dark" type="button" onclick="app.refreshHealthRecordTrend()">📈 Osveži trend</button>
          <button class="lab-btn ghost" type="button" onclick="app.copyHealthRecordTrendJson()">📋 Kopiraj trend JSON</button>
        </div>
        <div id="hr-trend-out"></div>`;
      const compareCard = Array.from(page.querySelectorAll('.welcome-card')).find(el => /Compare \+ Trend/i.test(el.textContent || ''));
      if (compareCard && compareCard.parentElement) compareCard.parentElement.insertBefore(trendCard, compareCard.nextSibling);
      else page.appendChild(trendCard);
    }
    if (!document.getElementById('hr-ai-workflow-card')) {
      const aiCard = document.createElement('div');
      aiCard.id = 'hr-ai-workflow-card';
      aiCard.className = 'welcome-card';
      aiCard.style.textAlign = 'left';
      aiCard.innerHTML = `
        <h4 style="margin-top:0;">7b) Specialist AI workflow</h4>
        <div class="lab-inline-note">Generiše <b>prompt pack</b>, pitanja za lekara i istoriju rada iz aktivne Anamneze + Zdravstvenog Kartona.</div>
        <div class="hr-grid two" style="margin-top:10px;">
          <div>
            <label><b>Specijalista</b></label>
            <select id="hr-ai-specialist" class="lab-select"></select>
          </div>
          <div>
            <label><b>Aktivna Anamneza</b></label>
            <div id="hr-ai-anam-status" class="lab-input" style="background:#fbfdff;">Nije učitana.</div>
          </div>
        </div>
        <div class="lab-actions">
          <button class="lab-btn secondary" type="button" onclick="app.importActiveAnamnezaIntoHealthRecord()">🩺 Uvezi aktivnu Anamnezu</button>
          <button class="lab-btn dark" type="button" onclick="app.generateSpecialistAiWorkflow()">🧠 Generiši prompt pack</button>
          <button class="lab-btn ghost" type="button" onclick="app.copySpecialistAiPrompt()">📋 Kopiraj prompt pack</button>
          <button class="lab-btn primary" type="button" onclick="app.exportSpecialistAiPrompt()">⬇ TXT</button>
        </div>
        <textarea id="hr-ai-prompt" class="lab-textarea lab-json-box" style="min-height:220px;" placeholder="Ovde se pojavljuje specialist prompt pack..."></textarea>
        <div id="hr-ai-history"></div>`;
      const svCard = Array.from(page.querySelectorAll('.welcome-card')).find(el => /Specialist views/i.test(el.textContent || ''));
      if (svCard && svCard.parentElement) svCard.parentElement.insertBefore(aiCard, svCard.nextSibling);
      else page.appendChild(aiCard);
    }
  };

  const origEnsureHealthStyles37 = App.prototype._ensureHealthRecordStyles;
  App.prototype._ensureHealthRecordStyles = function() {
    origEnsureHealthStyles37.apply(this, arguments);
    if (document.getElementById('health-record-styles-v37')) return;
    const st = document.createElement('style');
    st.id = 'health-record-styles-v37';
    st.textContent = `
      .hr-specialist-toolbar{display:flex;gap:10px;flex-wrap:wrap;align-items:center;margin:10px 0 14px}
      .hr-chip-row{display:flex;gap:8px;flex-wrap:wrap}
      .hr-chip{border:1px solid #d7e1eb;background:#fff;border-radius:999px;padding:8px 12px;font-weight:800;cursor:pointer}
      .hr-chip.active{background:#0f766e;color:#fff;border-color:#0f766e}
      .hr-specialist-grid{display:grid;gap:12px}
      @media(min-width:960px){.hr-specialist-grid.desktop-grid{grid-template-columns:repeat(2,minmax(0,1fr));}}
      .hr-thumb-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(136px,1fr));gap:12px;margin-top:10px}
      .hr-attach-card{background:#fff;border:1px solid #e8edf4;border-radius:16px;padding:12px;box-shadow:0 2px 6px rgba(15,23,42,.04)}
      .hr-attach-thumb-wrap{height:110px;border-radius:12px;background:#f7fbff;border:1px solid #e3edf6;display:flex;align-items:center;justify-content:center;overflow:hidden;cursor:pointer}
      .hr-attach-thumb-wrap img{width:100%;height:100%;object-fit:cover}
      .hr-attach-thumb-fallback{font-size:2rem}
      .hr-filter-row{display:flex;gap:8px;flex-wrap:wrap;margin:10px 0}
      .hr-filter-btn{border:1px solid #d7e1eb;background:#fff;border-radius:999px;padding:8px 12px;font-weight:800;cursor:pointer}
      .hr-filter-btn.active{background:#2563eb;color:#fff;border-color:#2563eb}
      .hr-drawer-backdrop{position:fixed;inset:0;background:rgba(15,23,42,.35);z-index:9999;display:none}
      .hr-drawer-backdrop.open{display:block}
      .hr-drawer-panel{position:absolute;right:0;top:0;bottom:0;width:min(92vw,540px);background:#fff;border-left:1px solid #e6eef8;box-shadow:-10px 0 30px rgba(15,23,42,.18);display:flex;flex-direction:column}
      .hr-drawer-panel .body{padding:14px;overflow:auto}
      .hr-drawer-panel .preview{border:1px solid #eef3fa;border-radius:14px;overflow:hidden;background:#fbfdff}
      .hr-drawer-panel iframe{width:100%;height:52vh;border:0}
      .hr-trend-table{width:100%;border-collapse:collapse;margin-top:12px;font-size:.95rem}
      .hr-trend-table th,.hr-trend-table td{padding:8px 10px;border-bottom:1px solid #eef3fa;text-align:left;vertical-align:top}
      .hr-trend-table th{color:#456;background:#fbfdff}
      .hr-kpi-row{display:grid;gap:10px;grid-template-columns:repeat(auto-fit,minmax(150px,1fr));margin-top:12px}
      .hr-spark{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:1.2rem;letter-spacing:1px}
      .hr-history-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:8px}
      .hr-history-actions button{padding:7px 10px;border-radius:10px;border:1px solid #d7e1eb;background:#fff;font-weight:800;cursor:pointer}
      @media(max-width:759px){
        .hr-drawer-panel{left:0;right:0;top:auto;bottom:0;width:100%;max-height:82vh;border-left:none;border-top:1px solid #e6eef8;border-radius:18px 18px 0 0;box-shadow:0 -16px 36px rgba(15,23,42,.22)}
        .hr-specialist-grid.mobile-single{grid-template-columns:1fr}
      }
    `;
    document.head.appendChild(st);
  };

  App.prototype._labExtractNumericItems = function(report) {
    const items = [];
    const seen = new Set();
    const add = (labelRaw, valueRaw, unitRaw, meta={}) => {
      const label = normalizeLabel(labelRaw);
      const numeric = parseNumericValue(valueRaw);
      if (!label || !Number.isFinite(numeric)) return;
      const unit = normalizeText(unitRaw || '');
      const key = slugify(label) + '|' + unit.toLowerCase();
      if (seen.has(key)) return;
      seen.add(key);
      items.push({
        key,
        label,
        numeric_value: numeric,
        value_raw: normalizeText(valueRaw),
        unit,
        status: normalizeText(meta.status || ''),
        importance: normalizeText(meta.importance || ''),
        source: meta.source || 'raw'
      });
    };

    arrify(report?.key_findings).forEach((it) => {
      if (!it || typeof it !== 'object') return;
      add(it.param || it.name || it.test, it.numeric_value ?? it.value ?? it.result, it.unit || it.units, {
        status: it.status,
        importance: it.importance,
        source: 'key_findings'
      });
    });

    const visit = (node, depth=0) => {
      if (depth > 8 || node === null || node === undefined) return;
      if (Array.isArray(node)) { node.forEach((x) => visit(x, depth + 1)); return; }
      if (typeof node !== 'object') return;
      const label = node.param || node.name || node.test || node.title || node.label || node.analyte || node.marker;
      const value = node.numeric_value ?? node.result_value ?? node.result ?? node.value ?? node.current;
      const unit = node.unit || node.units || node.measure_unit || node.reference_unit;
      if (label !== undefined && value !== undefined) {
        add(label, value, unit, { status: node.status || node.flag || node.interpretation, source: 'raw' });
      }
      Object.keys(node).forEach((k) => {
        if (/^(param|name|test|title|label|analyte|marker|numeric_value|result_value|result|value|current|unit|units|measure_unit|reference_unit|status|flag|interpretation)$/i.test(k)) return;
        visit(node[k], depth + 1);
      });
    };
    visit(report?.raw || report);

    const index = {};
    items.forEach((it) => { index[it.key] = { label: it.label, numeric_value: it.numeric_value, unit: it.unit, status: it.status, importance: it.importance, source: it.source }; });
    return { items, index };
  };

  const origBuildLabSnapshot37 = App.prototype._buildLabSnapshotFromReport;
  App.prototype._buildLabSnapshotFromReport = function(report) {
    const base = origBuildLabSnapshot37.apply(this, arguments);
    const numeric = this._labExtractNumericItems(report || {});
    base.key_findings_struct = arrify(report?.key_findings).map((it) => {
      if (!it || typeof it !== 'object') return { value: String(it) };
      return {
        param: normalizeText(it.param || it.name || ''),
        value: normalizeText(it.value || it.result || ''),
        numeric_value: Number.isFinite(parseNumericValue(it.numeric_value ?? it.value ?? it.result)) ? parseNumericValue(it.numeric_value ?? it.value ?? it.result) : null,
        unit: normalizeText(it.unit || it.units || ''),
        status: normalizeText(it.status || ''),
        importance: normalizeText(it.importance || '')
      };
    });
    base.report_id = normalizeText(report?.metadata?.report_id || '');
    base.qa_status = normalizeText(report?.qa?.status || report?.metadata?.qa_status || '');
    base.snapshot_schema_version = '2';
    base.numeric_version = '1';
    base.numeric = numeric;
    return base;
  };

  App.prototype._hrGetTrendDataset = function(hr) {
    const snaps = arrify(hr?.lab_snapshots).slice().sort((a, b) => String(a.report_date || a.created_at || '').localeCompare(String(b.report_date || b.created_at || '')));
    const labelMap = new Map();
    const seriesMap = new Map();
    snaps.forEach((snap) => {
      const numeric = snap.numeric && snap.numeric.items ? snap.numeric : { items: [] };
      arrify(numeric.items).forEach((item) => {
        const key = String(item.key || slugify(item.label) + '|' + String(item.unit || '').toLowerCase());
        labelMap.set(key, { key, label: item.label || key, unit: item.unit || '' });
        if (!seriesMap.has(key)) seriesMap.set(key, []);
        seriesMap.get(key).push({
          snapshot_id: snap.id,
          report_date: snap.report_date || '',
          created_at: snap.created_at || '',
          title: snap.title || snap.patient_name || 'Lab Snapshot',
          value: Number(item.numeric_value),
          unit: item.unit || '',
          status: item.status || ''
        });
      });
    });
    const params = Array.from(labelMap.values()).sort((a, b) => a.label.localeCompare(b.label, 'sr'));
    return { params, seriesMap, snaps };
  };

  App.prototype._renderHealthRecordTrend = function(hr) {
    const host = document.getElementById('hr-trend-out');
    const sel = document.getElementById('hr-trend-param');
    const kpi = document.getElementById('hr-trend-kpi');
    if (!host || !sel || !kpi) return;
    const ds = this._hrGetTrendDataset(hr || this._getHealthRecord());
    const current = localStorage.getItem(HR_TREND_PARAM_KEY) || sel.value || '';
    sel.innerHTML = ds.params.length ? ds.params.map((p) => `<option value="${this.escapeHtml(p.key)}">${this.escapeHtml(p.label)}${p.unit ? ' (' + this.escapeHtml(p.unit) + ')' : ''}</option>`).join('') : '<option value="">— nema numerike —</option>';
    if (current && Array.from(sel.options).some(o => o.value === current)) sel.value = current;
    if (!sel.value && sel.options.length) sel.value = sel.options[0].value;
    sel.onchange = () => { try { localStorage.setItem(HR_TREND_PARAM_KEY, sel.value || ''); } catch(_) {} this.refreshHealthRecordTrend(); };
    const paramKey = sel.value || '';
    const series = paramKey ? (ds.seriesMap.get(paramKey) || []) : [];
    kpi.textContent = String(series.length || 0);
    if (!series.length) {
      host.innerHTML = '<div class="lab-empty">Još nema dovoljno numeričkih snapshot-a za trend. Sačuvaj nove LAB snapshot-e iz ove verzije naviše.</div>';
      return;
    }
    const values = series.map(x => Number(x.value)).filter(Number.isFinite);
    const first = values[0];
    const last = values[values.length - 1];
    const delta = Number.isFinite(first) && Number.isFinite(last) ? last - first : null;
    const pct = Number.isFinite(first) && first !== 0 && Number.isFinite(last) ? ((last - first) / first) * 100 : null;
    const param = ds.params.find(p => p.key === paramKey) || { label: paramKey, unit: '' };
    const enough = series.length >= 6;
    host.innerHTML = `
      <div class="hr-kpi-row">
        <div class="lab-kpi-card"><b>Parametar</b>${this.escapeHtml(param.label)}${param.unit ? ' (' + this.escapeHtml(param.unit) + ')' : ''}</div>
        <div class="lab-kpi-card"><b>Trend</b>${this.escapeHtml(trendDirection(first, last))}</div>
        <div class="lab-kpi-card"><b>Promena</b>${delta === null ? '—' : this.escapeHtml((delta > 0 ? '+' : '') + fmtNum(delta) + (param.unit ? ' ' + param.unit : ''))}</div>
        <div class="lab-kpi-card"><b>% promena</b>${pct === null ? '—' : this.escapeHtml((pct > 0 ? '+' : '') + fmtNum(pct) + '%')}</div>
        <div class="lab-kpi-card"><b>6+ snapshot-a</b>${enough ? 'da ✅' : 'ne još'}</div>
      </div>
      <div class="hr-list-card">
        <div style="font-weight:900;color:#1f3f5b;">Sparkline</div>
        <div class="hr-spark" style="margin-top:8px;">${this.escapeHtml(spark(values) || '—')}</div>
        <div class="hr-sub" style="margin-top:6px;">Prvi: <b>${this.escapeHtml(fmtNum(first))}</b>${param.unit ? ' ' + this.escapeHtml(param.unit) : ''} • Poslednji: <b>${this.escapeHtml(fmtNum(last))}</b>${param.unit ? ' ' + this.escapeHtml(param.unit) : ''}</div>
      </div>
      <table class="hr-trend-table">
        <thead><tr><th>Datum</th><th>Snapshot</th><th>Vrednost</th><th>Status</th></tr></thead>
        <tbody>
          ${series.slice().reverse().map(row => `<tr>
            <td>${this.escapeHtml(row.report_date || (row.created_at || '').slice(0,10) || '-')}</td>
            <td>${this.escapeHtml(row.title || row.snapshot_id || 'Lab Snapshot')}</td>
            <td><b>${this.escapeHtml(fmtNum(Number(row.value)))}</b>${row.unit ? ' ' + this.escapeHtml(row.unit) : ''}</td>
            <td>${this.escapeHtml(row.status || '—')}</td>
          </tr>`).join('')}
        </tbody>
      </table>`;
    this._hrLastTrendPayload = {
      generated_at: new Date().toISOString(),
      param,
      summary: { first, last, delta, pct, direction: trendDirection(first, last), spark: spark(values), count: series.length },
      series
    };
  };

  App.prototype.refreshHealthRecordTrend = function() {
    this._renderHealthRecordTrend(this._getHealthRecord());
  };

  App.prototype.copyHealthRecordTrendJson = function() {
    const payload = this._hrLastTrendPayload || null;
    if (!payload) return alert('Prvo osveži trend.');
    navigator.clipboard.writeText(JSON.stringify(payload, null, 2)).then(() => this.showToast('Trend JSON kopiran ✅')).catch(() => alert('Clipboard nije dostupan.'));
  };

  const origRenderHealthRecord37 = App.prototype.renderHealthRecord;
  App.prototype.renderHealthRecord = function() {
    origRenderHealthRecord37.apply(this, arguments);
    const hr = this._getHealthRecord();
    try { this._ensureHealthRecordEnhancements37(); } catch(_) {}
    try { this._renderHealthRecordCompareAndAttachments(hr); } catch(_) {}
    try { this._renderHealthRecordSpecialistViews(hr); } catch(_) {}
    try { this._renderHealthRecordTrend(hr); } catch(_) {}
    try { this._renderSpecialistAiWorkflow(hr); } catch(_) {}
  };

  const origComputeSpecialistViews37 = App.prototype._hrComputeSpecialistViews;
  App.prototype._hrComputeSpecialistViews = function(snapshot) {
    const base = origComputeSpecialistViews37.apply(this, arguments) || [];
    const keyText = [
      ...arrify(snapshot?.key_findings),
      ...arrify(snapshot?.next_steps),
      ...arrify(snapshot?.red_flags),
      ...arrify(snapshot?.quick_summary),
      ...arrify(snapshot?.numeric?.items).map(it => `${it.label} ${it.status || ''}`)
    ].join(' ').toLowerCase();
    const pick = (keys) => uniqueBy(keys.filter(k => keyText.includes(String(k).toLowerCase())).map(k => normalizeLabel(k)), x => x.toLowerCase());
    const extra = [
      { id:'ginekolog', title:'Ginekolog', focus: pick(['ginekolog','menstru','pcos','estradiol','progesteron','fsh','lh','prolaktin']), questions:['Da li hormonski profil ili ciklus zahtevaju dodatnu obradu?','Da li postoji potreba za UZ i dodatnim ginekološkim kontrolama?'] },
      { id:'radiolog', title:'Radiolog / snimanja', focus: pick(['uz abdomena','ultrazvuk','rtg','ct','mri','radiolog','snimanje']), questions:['Koji imaging pregled je najracionalniji sledeći korak?','Da li postoje jasne indikacije i prioritet snimanja?'] },
      { id:'oftalmolog', title:'Oftalmolog', focus: pick(['vid','retina','oftalmolog','oči','glaukom']), questions:['Da li simptomi ili rizici zahtevaju oftalmološki pregled?','Da li su potrebni dodatni testovi vida / očnog pritiska?'] },
      { id:'dentista', title:'Dentista / stomatolog', focus: pick(['zubi','dent','stomat','parodont','vilica']), questions:['Da li postoje znaci oralnog fokusa ili upale koje treba proveriti?','Da li je potreban stomatološki pregled / ortopan?'] },
      { id:'imunolog', title:'Imunolog', focus: pick(['autoimuno','crp','imun','alerg','igg','iga','igm']), questions:['Da li obrazac nalaza i simptoma traži imunološku obradu?','Koje dodatne imunološke analize imaju najviše smisla?'] }
    ].map(v => Object.assign({}, v, { relevant: v.focus.length || arrify(snapshot?.red_flags).length, red_flags: arrify(snapshot?.red_flags).slice(0,5), next_steps: arrify(snapshot?.next_steps).slice(0,5) }));
    return uniqueBy([...base, ...extra], x => x.id);
  };

  App.prototype._renderHealthRecordSpecialistViews = function(hr) {
    const host = document.getElementById('hr-specialist-views');
    if (!host) return;
    const sv = hr.specialist_views || {};
    const views = arrify(sv.views);
    if (!views.length) {
      host.innerHTML = '<div class="lab-empty">Nema specialist view-a još. Pokreni Compare da se generiše na osnovu izabranog snapshot-a.</div>';
      return;
    }
    let selected = localStorage.getItem(HR_SELECTED_SPECIALIST_KEY) || views[0].id;
    if (!views.some(v => v.id === selected)) selected = views[0].id;
    const isMobile = window.matchMedia && window.matchMedia('(max-width: 759px)').matches;
    const selectHtml = `<select id="hr-specialist-mobile-select" class="lab-select">${views.map(v => `<option value="${this.escapeHtml(v.id)}">${this.escapeHtml(v.title || v.id)}</option>`).join('')}</select>`;
    const chipsHtml = views.map(v => `<button type="button" class="hr-chip ${v.id === selected ? 'active' : ''}" onclick="app.selectHealthRecordSpecialist('${this.escapeHtml(v.id)}')">${this.escapeHtml(v.title || v.id)}</button>`).join('');
    const renderCard = (v) => `
      <div class="hr-list-card">
        <div style="font-weight:900;color:#1f3f5b;">${this.escapeHtml(v.title || v.id)}</div>
        ${arrify(v.focus).length ? `<div style="margin-top:10px;">${arrify(v.focus).map(x => `<span class="hr-tag">${this.escapeHtml(String(x))}</span>`).join('')}</div>` : '<div class="hr-sub">Nema eksplicitno prepoznatih tagova za ovaj prikaz.</div>'}
        ${arrify(v.red_flags).length ? `<div style="margin-top:10px;"><b>Red flags</b>${arrify(v.red_flags).map(x=>`<div class="hr-mini">• ${this.escapeHtml(String(x))}</div>`).join('')}</div>` : ''}
        ${arrify(v.next_steps).length ? `<div style="margin-top:10px;"><b>Sledeći koraci</b>${arrify(v.next_steps).map(x=>`<div class="hr-mini">• ${this.escapeHtml(String(x))}</div>`).join('')}</div>` : ''}
        ${arrify(v.questions).length ? `<div style="margin-top:10px;"><b>Pitanja za lekara</b>${arrify(v.questions).map(x=>`<div class="hr-mini">• ${this.escapeHtml(String(x))}</div>`).join('')}</div>` : ''}
      </div>`;
    const shown = isMobile ? views.filter(v => v.id === selected) : views;
    host.innerHTML = `
      <div class="lab-inline-note">based_on_snapshot_id: <b>${this.escapeHtml(String(sv.based_on_snapshot_id || ''))}</b> • generated_at: ${this.escapeHtml(String(sv.generated_at || ''))}</div>
      <div class="hr-specialist-toolbar">
        <div style="min-width:${isMobile ? '100%' : '220px'}; flex:${isMobile ? '1 1 100%' : '0 0 260px'};">${selectHtml}</div>
        <div class="hr-chip-row">${chipsHtml}</div>
      </div>
      <div class="hr-specialist-grid ${isMobile ? 'mobile-single' : 'desktop-grid'}">${shown.map(renderCard).join('')}</div>`;
    const sel = document.getElementById('hr-specialist-mobile-select');
    if (sel) {
      sel.value = selected;
      sel.onchange = () => this.selectHealthRecordSpecialist(sel.value || '');
    }
    const aiSel = document.getElementById('hr-ai-specialist');
    if (aiSel) {
      aiSel.innerHTML = views.map(v => `<option value="${this.escapeHtml(v.id)}">${this.escapeHtml(v.title || v.id)}</option>`).join('');
      aiSel.value = selected;
    }
  };

  App.prototype.selectHealthRecordSpecialist = function(id) {
    try { localStorage.setItem(HR_SELECTED_SPECIALIST_KEY, String(id || '')); } catch(_) {}
    const aiSel = document.getElementById('hr-ai-specialist');
    if (aiSel && id) aiSel.value = id;
    this._renderHealthRecordSpecialistViews(this._getHealthRecord());
  };

  App.prototype._renderHealthRecordCompareAndAttachments = function(hr) {
    const snaps = arrify(hr.lab_snapshots).slice(0, 50);
    const optHtml = (s) => `<option value="${this.escapeHtml(String(s.id||''))}">${this.escapeHtml((s.report_date||'') + ' — ' + (s.title||s.patient_name||'Lab Snapshot'))}</option>`;
    ['hr-compare-a','hr-compare-b','hr-compare-c'].forEach((id) => {
      const sel = document.getElementById(id);
      if (!sel) return;
      const cur = sel.value || '';
      sel.innerHTML = `<option value="">— izaberi —</option>` + snaps.map(optHtml).join('');
      if (cur) sel.value = cur;
    });
    const tType = document.getElementById('hr-attach-target-type');
    const tSel = document.getElementById('hr-attach-target-id');
    if (tType && tSel) {
      const cur = tSel.value || '';
      const mode = String(tType.value || 'event');
      const list = mode === 'therapy' ? arrify(hr.therapy_records) : arrify(hr.medical_events);
      tSel.innerHTML = `<option value="">— izaberi —</option>` + list.map(it => {
        const label = [it.date, (mode === 'therapy' ? (it.name || 'Terapija') : (it.title || 'Događaj'))].filter(Boolean).join(' • ');
        return `<option value="${this.escapeHtml(String(it.id||''))}">${this.escapeHtml(label)}</option>`;
      }).join('');
      if (cur) tSel.value = cur;
      if (!tSel.value && list[0]?.id) tSel.value = String(list[0].id);
      tType.onchange = () => { try { this._renderHealthRecordCompareAndAttachments(this._getHealthRecord()); } catch(_) {} };
    }
    const host = document.getElementById('hr-attachments-list');
    if (!host) return;
    const filter = localStorage.getItem(HR_ATTACHMENT_FILTER_KEY) || 'all';
    const atts = arrify(hr.attachments);
    const relatedMap = {};
    arrify(hr.medical_events).forEach(ev => arrify(ev.attachment_ids).forEach(id => { relatedMap[String(id)] = { type:'event', title: ev.title || 'Događaj', date: ev.date || '' }; }));
    arrify(hr.therapy_records).forEach(th => arrify(th.attachment_ids).forEach(id => { if (!relatedMap[String(id)]) relatedMap[String(id)] = { type:'therapy', title: th.name || 'Terapija', date: th.date || '' }; }));
    const filtered = atts.filter((a) => {
      if (filter === 'all') return true;
      const rel = relatedMap[String(a.id)] || null;
      return rel && rel.type === filter;
    });
    const btn = (id, label) => `<button type="button" class="hr-filter-btn ${filter === id ? 'active' : ''}" onclick="app.setHealthRecordAttachmentFilter('${id}')">${label}</button>`;
    host.innerHTML = `
      <div class="hr-filter-row">
        ${btn('all','Sve')}
        ${btn('event','Događaji')}
        ${btn('therapy','Terapije')}
      </div>
      <div class="lab-inline-note">Prikaz: <b>${this.escapeHtml(String(filtered.length))}</b> / ${this.escapeHtml(String(atts.length))} priloga</div>
      <div class="hr-thumb-grid">
        ${filtered.length ? filtered.map((att) => {
          const rel = relatedMap[String(att.id)] || null;
          const kind = String(att.kind || 'bin');
          const thumb = (kind === 'image' && att.inline && att.data_url)
            ? `<img src="${att.data_url}" alt="thumb" />`
            : `<div class="hr-attach-thumb-fallback">${kind === 'pdf' ? '📄' : kind === 'html' ? '🌐' : kind === 'text' ? '📝' : '📦'}</div>`;
          return `<div class="hr-attach-card">
            <div class="hr-attach-thumb-wrap" onclick="app.showHealthRecordAttachment('${this.escapeHtml(String(att.id))}')">${thumb}</div>
            <div style="font-weight:900;color:#1f3f5b;margin-top:10px;">${this.escapeHtml(att.name || 'prilog')}</div>
            <div class="hr-sub">${this.escapeHtml([att.kind, (att.size||0)+' B'].join(' • '))}</div>
            ${rel ? `<div class="hr-mini" style="margin-top:8px;"><b>${rel.type === 'event' ? 'Događaj' : 'Terapija'}:</b> ${this.escapeHtml([rel.date, rel.title].filter(Boolean).join(' • '))}</div>` : '<div class="hr-mini" style="margin-top:8px;">Nije vezan za cilj.</div>'}
            <div class="hr-history-actions"><button type="button" onclick="app.showHealthRecordAttachment('${this.escapeHtml(String(att.id))}')">👁 Otvori</button></div>
          </div>`;
        }).join('') : '<div class="lab-empty">Još nema priloga za ovaj filter.</div>'}
      </div>`;
  };

  App.prototype.setHealthRecordAttachmentFilter = function(mode) {
    try { localStorage.setItem(HR_ATTACHMENT_FILTER_KEY, String(mode || 'all')); } catch(_) {}
    this._renderHealthRecordCompareAndAttachments(this._getHealthRecord());
  };

  App.prototype.showHealthRecordAttachment = function(attId) {
    this._hrEnsureAttachmentIndex && this._hrEnsureAttachmentIndex();
    const hr = this._getHealthRecord();
    const att = this._hrGetAttachmentById(hr, attId);
    if (!att) return alert('Prilog nije pronađen.');
    this._hrCurrentAttachmentId = att.id;
    const modal = document.getElementById('hr-attach-modal');
    const title = document.getElementById('hr-attach-modal-title');
    const prev = document.getElementById('hr-attach-modal-preview');
    const meta = document.getElementById('hr-attach-modal-meta');
    if (!modal || !title || !prev || !meta) return;
    modal.classList.add('hr-drawer-backdrop','open');
    const panel = modal.querySelector('.hr-modal');
    if (panel) panel.classList.add('hr-drawer-panel');
    title.textContent = `📎 ${att.name || 'Prilog'}`;
    prev.innerHTML = '';
    const kind = String(att.kind || 'bin');
    if (att.inline && att.data_url) {
      if (kind === 'image') prev.innerHTML = `<img src="${att.data_url}" alt="preview" style="width:100%;height:auto;display:block;" />`;
      else if (kind === 'pdf') prev.innerHTML = `<iframe src="${att.data_url}"></iframe>`;
      else if (kind === 'html') prev.innerHTML = `<iframe sandbox="allow-same-origin" src="${att.data_url}"></iframe>`;
      else if (kind === 'text') prev.innerHTML = `<pre style="margin:0;padding:12px;white-space:pre-wrap;line-height:1.5;">${this.escapeHtml(att.text_excerpt || '[tekst]')}</pre>`;
      else prev.innerHTML = '<div style="padding:12px;">Inline fajl je sačuvan, ali nema preview za ovaj tip.</div>';
    } else {
      prev.innerHTML = '<div style="padding:12px;">Ovaj prilog je sačuvan kao <b>metadata-only</b> (prevelik ili nepodržan za inline). Preuzimanje nije dostupno bez ponovnog uploada.</div>';
    }
    let relation = 'Nije vezan.';
    arrify(hr.medical_events).forEach(ev => { if (arrify(ev.attachment_ids).includes(att.id)) relation = `Događaj • ${[ev.date, ev.title].filter(Boolean).join(' • ')}`; });
    arrify(hr.therapy_records).forEach(th => { if (arrify(th.attachment_ids).includes(att.id)) relation = `Terapija • ${[th.date, th.name].filter(Boolean).join(' • ')}`; });
    meta.innerHTML = `
      <div class="lab-kpi-card"><b>Tip</b>${this.escapeHtml(kind)}</div>
      <div class="lab-kpi-card"><b>MIME</b>${this.escapeHtml(att.mime || '-')}</div>
      <div class="lab-kpi-card"><b>Veličina</b>${this.escapeHtml(String(att.size || 0))} B</div>
      <div class="lab-kpi-card"><b>Datum</b>${this.escapeHtml((att.created_at || '').slice(0,16).replace('T',' ') || '-')}</div>
      <div class="lab-kpi-card"><b>Veza</b>${this.escapeHtml(relation)}</div>
      <div class="lab-kpi-card"><b>Fallback</b>${att.inline ? 'inline preview' : 'metadata-only'}</div>`;
    modal.style.display = 'block';
  };

  const origHideAtt37 = App.prototype.hideHealthRecordAttachmentModal;
  App.prototype.hideHealthRecordAttachmentModal = function() {
    const modal = document.getElementById('hr-attach-modal');
    if (modal) {
      modal.style.display = 'none';
      modal.classList.remove('open');
    }
    origHideAtt37 && origHideAtt37.apply(this, arguments);
  };

  App.prototype._getActiveAnamnezaForHealthRecord = function() {
    try {
      if (window.SINET_ProfilesCore && window.SINET_ProfilesCore.getActiveProfile) {
        const p = window.SINET_ProfilesCore.getActiveProfile();
        if (p) return p;
      }
    } catch(_) {}
    return safeParse(localStorage.getItem('ANAMNEZA_LAST_PATIENT') || 'null', null);
  };

  App.prototype.importActiveAnamnezaIntoHealthRecord = function() {
    const active = this._getActiveAnamnezaForHealthRecord();
    if (!active) return alert('Nema aktivne Anamneze / profila.');
    const hr = this._getHealthRecord();
    hr.profile.display_name = active.displayName || active.ime_prezime || active.ime || hr.profile.display_name || 'Korisnik';
    hr.profile.age = active.age || active.starost || hr.profile.age || '';
    hr.profile.sex = active.sex || active.pol || hr.profile.sex || '';
    hr.profile.notes = [active.primaryDiagnosis, active.noteSummary, active.diagnoza, active.licna_anamneza].filter(Boolean).join(' • ').slice(0, 1000);
    hr.analytics = hr.analytics || {};
    hr.analytics.active_anamneza = active;
    this._saveHealthRecord(hr);
    this.renderHealthRecord();
    this.showToast('Aktivna Anamneza uvezena ✅');
  };

  App.prototype._topNumericLinesForSpecialist = function(hr, specialistView) {
    const ds = this._hrGetTrendDataset(hr);
    const focusText = arrify(specialistView?.focus).join(' ').toLowerCase();
    const candidates = ds.params.filter(p => !focusText || focusText.includes((p.label || '').toLowerCase()) || (specialistView?.title || '').toLowerCase().includes((p.label || '').toLowerCase().slice(0, 5)));
    const picked = (candidates.length ? candidates : ds.params).slice(0, 5);
    return picked.map((p) => {
      const series = ds.seriesMap.get(p.key) || [];
      if (!series.length) return null;
      const first = Number(series[0].value);
      const last = Number(series[series.length - 1].value);
      return `${p.label}${p.unit ? ` (${p.unit})` : ''}: ${fmtNum(first)} → ${fmtNum(last)} (${trendDirection(first, last)})`;
    }).filter(Boolean);
  };

  App.prototype._buildSpecialistAiPromptPack = function(hr, specialistId) {
    const sv = hr.specialist_views || {};
    const views = arrify(sv.views);
    const selected = views.find(v => String(v.id) === String(specialistId)) || views[0] || null;
    const anam = hr.analytics?.active_anamneza || this._getActiveAnamnezaForHealthRecord() || null;
    const questions = uniqueBy([
      ...arrify(selected?.questions),
      ...arrify(selected?.next_steps).map(x => `Da li ${String(x).replace(/[.。]+$/,'').toLowerCase()}?`),
      ...arrify(selected?.red_flags).map(x => `Kako tumačiti red flag: ${x}?`)
    ], x => String(x).toLowerCase()).slice(0, 12);
    const numericLines = this._topNumericLinesForSpecialist(hr, selected);
    const prompt = [
      `SINET Specialist AI Workflow — ${selected?.title || 'Specijalista'}`,
      '',
      'Uloga: medicinsko-informativni asistent. Ne postavljaj konačnu dijagnozu samo iz ovih podataka.',
      '',
      '1) Pacijent / profil',
      `- Ime/oznaka: ${hr.profile?.display_name || 'Korisnik'}`,
      `- Godine: ${hr.profile?.age || ''}`,
      `- Pol: ${hr.profile?.sex || ''}`,
      anam ? `- Aktivna Anamneza: ${normalizeText(anam.displayName || anam.ime_prezime || anam.ime || 'profil')}` : '- Aktivna Anamneza: nije dostupna',
      anam ? `- Anamneza sažetak: ${normalizeText(anam.noteSummary || anam.diagnoza || anam.licna_anamneza || '')}` : '',
      '',
      '2) Current snapshot',
      ...arrify(hr.current_snapshot?.summary).map(x => `- ${x}`),
      ...arrify(hr.current_snapshot?.priorities).map(x => `- Prioritet: ${x}`),
      hr.current_snapshot?.last_report_date ? `- Poslednji datum nalaza: ${hr.current_snapshot.last_report_date}` : '',
      '',
      `3) Fokus za ${selected?.title || 'specijalistu'}`,
      ...arrify(selected?.focus).map(x => `- Fokus: ${x}`),
      ...arrify(selected?.red_flags).map(x => `- Red flag: ${x}`),
      ...arrify(selected?.next_steps).map(x => `- Sledeći korak: ${x}`),
      '',
      '4) Numerički trendovi',
      ...(numericLines.length ? numericLines.map(x => `- ${x}`) : ['- Nema dovoljno numerike za trend.']),
      '',
      '5) Zadatak za AI',
      '- Napiši sažetak za izabranog specijalistu.',
      '- Odvoji: uredno / granično / odstupanje / prioritet kontrole.',
      '- Izvuci racionalna pitanja za lekara i red flags.',
      '- Ne uvoditi terapiju bez jasne napomene da pregled odlučuje.',
      '',
      '6) Pitanja za lekara',
      ...questions.map(x => `- ${x}`)
    ].filter(Boolean).join('\n');
    return {
      id: uid('specialist-ai'),
      generated_at: new Date().toISOString(),
      specialist_id: selected?.id || '',
      specialist_title: selected?.title || '',
      based_on_snapshot_id: sv.based_on_snapshot_id || '',
      patient: { display_name: hr.profile?.display_name || '', age: hr.profile?.age || '', sex: hr.profile?.sex || '' },
      questions_for_doctor: questions,
      numeric_trends: numericLines,
      prompt_text: prompt
    };
  };

  App.prototype.generateSpecialistAiWorkflow = function() {
    const hr = this._getHealthRecord();
    const sel = document.getElementById('hr-ai-specialist');
    const specialistId = String(sel?.value || localStorage.getItem(HR_SELECTED_SPECIALIST_KEY) || '');
    if (!arrify(hr.specialist_views?.views).length) return alert('Prvo generiši Specialist views kroz Compare.');
    const pack = this._buildSpecialistAiPromptPack(hr, specialistId);
    const ta = document.getElementById('hr-ai-prompt');
    if (ta) ta.value = pack.prompt_text;
    hr.analytics = hr.analytics || {};
    hr.analytics.specialist_ai_history = [pack, ...arrify(hr.analytics.specialist_ai_history)].slice(0, 20);
    this._saveHealthRecord(hr);
    this._hrLastAiPrompt = pack;
    this._renderSpecialistAiWorkflow(this._getHealthRecord());
    this.showToast('Specialist prompt pack spreman ✅');
  };

  App.prototype.copySpecialistAiPrompt = function() {
    const ta = document.getElementById('hr-ai-prompt');
    const txt = String(ta?.value || this._hrLastAiPrompt?.prompt_text || '').trim();
    if (!txt) return alert('Prvo generiši prompt pack.');
    navigator.clipboard.writeText(txt).then(() => this.showToast('Prompt pack kopiran ✅')).catch(() => alert('Clipboard nije dostupan.'));
  };

  App.prototype.exportSpecialistAiPrompt = function() {
    const txt = String(document.getElementById('hr-ai-prompt')?.value || this._hrLastAiPrompt?.prompt_text || '').trim();
    if (!txt) return alert('Prvo generiši prompt pack.');
    const sel = document.getElementById('hr-ai-specialist');
    const sid = String(sel?.value || 'specialist');
    this._downloadText(txt, `SINET_SPECIALIST_PROMPT_${slugify(sid)}_${new Date().toISOString().slice(0,10)}.txt`);
  };

  App.prototype.loadSpecialistAiHistory = function(id) {
    const hr = this._getHealthRecord();
    const item = arrify(hr.analytics?.specialist_ai_history).find(x => String(x.id) === String(id)) || null;
    if (!item) return;
    const ta = document.getElementById('hr-ai-prompt');
    if (ta) ta.value = item.prompt_text || '';
    this._hrLastAiPrompt = item;
    const sel = document.getElementById('hr-ai-specialist');
    if (sel && item.specialist_id) sel.value = item.specialist_id;
  };

  App.prototype.deleteSpecialistAiHistory = function(id) {
    const hr = this._getHealthRecord();
    hr.analytics = hr.analytics || {};
    hr.analytics.specialist_ai_history = arrify(hr.analytics.specialist_ai_history).filter(x => String(x.id) !== String(id));
    this._saveHealthRecord(hr);
    this._renderSpecialistAiWorkflow(this._getHealthRecord());
  };

  App.prototype._renderSpecialistAiWorkflow = function(hr) {
    const anamStatus = document.getElementById('hr-ai-anam-status');
    const aiSel = document.getElementById('hr-ai-specialist');
    const historyHost = document.getElementById('hr-ai-history');
    if (anamStatus) {
      const anam = hr.analytics?.active_anamneza || this._getActiveAnamnezaForHealthRecord();
      anamStatus.textContent = anam ? [anam.displayName || anam.ime_prezime || anam.ime || 'profil', anam.primaryDiagnosis || anam.diagnoza || ''].filter(Boolean).join(' • ') : 'Nije učitana.';
    }
    const views = arrify(hr.specialist_views?.views);
    if (aiSel) {
      const selected = localStorage.getItem(HR_SELECTED_SPECIALIST_KEY) || views[0]?.id || '';
      aiSel.innerHTML = views.length ? views.map(v => `<option value="${this.escapeHtml(v.id)}">${this.escapeHtml(v.title || v.id)}</option>`).join('') : '<option value="">— nema specialist views —</option>';
      if (selected) aiSel.value = selected;
      aiSel.onchange = () => { this.selectHealthRecordSpecialist(aiSel.value || ''); };
    }
    if (historyHost) {
      const hist = arrify(hr.analytics?.specialist_ai_history);
      historyHost.innerHTML = hist.length ? `<div style="margin-top:12px;"><b>Istorija prompt paketa</b>${hist.map(it => `
        <div class="hr-list-card">
          <div style="font-weight:900;color:#1f3f5b;">${this.escapeHtml(it.specialist_title || it.specialist_id || 'Specijalista')}</div>
          <div class="hr-sub">${this.escapeHtml((it.generated_at || '').slice(0,16).replace('T',' '))}</div>
          <div class="hr-history-actions">
            <button type="button" onclick="app.loadSpecialistAiHistory('${this.escapeHtml(String(it.id))}')">↩ Učitaj</button>
            <button type="button" onclick="app.deleteSpecialistAiHistory('${this.escapeHtml(String(it.id))}')">🗑 Obriši</button>
          </div>
        </div>`).join('')}</div>` : '<div class="lab-inline-note" style="margin-top:12px;">Istorija će se pojaviti posle prvog generisanja.</div>';
    }
  };



App.prototype._openControlCenterDoc39 = function() {
  return this.openDoc('./docs/protokoli/37_SINET_CONTROL_CENTER_INSTALL_v1.0_SR.html', '🎛 SINET Control Center + Install');
};

App.prototype._ensureControlCenterHomeEntryPoints39 = function() {
  const home = document.getElementById('page-home');
  if (!home || document.getElementById('sinet-control-center-home-card')) return;
  const card = document.createElement('div');
  card.id = 'sinet-control-center-home-card';
  card.style.cssText = 'background:white; padding:16px; border-radius:12px; margin-bottom:15px; box-shadow:0 2px 5px rgba(0,0,0,0.05);';
  card.innerHTML = `
    <h3 style="margin:0 0 10px 0; color:var(--primary);">🎛 SINET Control Center (van Audio Lekar)</h3>
    <p style="margin:0 0 12px 0; color:#666;">Ovo je <b>spoljašnji alat</b> za instalaciju, desktop launcher, GitHub/Netlify publish, backup i lokalni start projekata. Ne nalazi se kao radni modul unutar Audio Lekara, već kao poseban paket u <code>tools/control-center/</code>.</p>
    <div style="display:flex; flex-wrap:wrap; gap:10px;">
      <button class="btn-full" style="flex:1; min-width:220px; padding:12px; background:#334155;" onclick="app._openControlCenterDoc39()">📘 Vodič + instalacija</button>
      <button class="btn-full" style="flex:1; min-width:220px; padding:12px; background:#0f766e;" onclick="app.openDoc('./docs/control-center/SINET_Control_Center_v1.html','🎛 SINET Control Center')">🧰 Control Center dokumentacija</button>
      <button class="btn-full" style="flex:1; min-width:220px; padding:12px; background:#2563eb;" onclick="app.openDoc('./tools/control-center/README_SINET_CONTROL_CENTER_v1.md','README — SINET Control Center')">📄 README / putanje</button>
    </div>`;
  const promo = document.getElementById('pwa-install-promo');
  if (promo && promo.parentElement) promo.parentElement.insertBefore(card, promo);
  else home.appendChild(card);
};

  const origPrepareHealth37 = App.prototype.prepareHealthRecordUI;
  App.prototype.prepareHealthRecordUI = function() {
    origPrepareHealth37.apply(this, arguments);
    try { this._ensureHealthHomeEntryPoints37(); } catch(_) {}
    try { this._removeLegacyHomeLabBridgeDuplicates41(); } catch(_) {}
    try { this._ensureControlCenterHomeEntryPoints39(); } catch(_) {}
    try { this._ensureHealthRecordEnhancements37(); } catch(_) {}
  };

  const origNav37 = App.prototype.nav;
  App.prototype.nav = function(pageId, opts={}) {
    const r = origNav37.apply(this, arguments);
    if (pageId === 'home' || pageId === 'health-record' || pageId === 'labs') {
      try { this._ensureHealthHomeEntryPoints37(); } catch(_) {}
      try { this._ensureControlCenterHomeEntryPoints39(); } catch(_) {}
      try { this._ensureHealthRecordEnhancements37(); } catch(_) {}
    }
    return r;
  };
})();

/* ===================== v16.0.0.73 — Global nav/menu/order hardening ===================== */
(function(){
  function removeIfExists(id){
    try{ const el = document.getElementById(id); if (el) el.remove(); }catch(_){ }
  }

  App.prototype._removeHomeMenuClutter71 = function(){
    try {
      removeIfExists('sinet-health-home-card');
      removeIfExists('sinet-control-center-home-card');
      removeIfExists('ai-open-lab-btn');
      const frequent = document.getElementById('frequent-actions');
      if (frequent) {
        Array.from(frequent.querySelectorAll('button')).forEach((btn) => {
          const txt = String(btn.textContent || '').toLowerCase();
          if (txt.includes('lab bridge') || txt.includes('zdravstveni karton') || txt.includes('control center')) btn.remove();
        });
      }
    } catch(_) {}
  };

  App.prototype._ensureHealthHomeEntryPoints37 = function(){
    this._removeHomeMenuClutter71();
    return null;
  };
  App.prototype._ensureControlCenterHomeEntryPoints39 = function(){
    this._removeHomeMenuClutter71();
    return null;
  };

  const origCacheUI71 = App.prototype.cacheUI;
  App.prototype.cacheUI = function(){
    const r = origCacheUI71.apply(this, arguments);
    try {
      if (this.ui && this.ui.screens) {
        this.ui.screens['labs'] = document.getElementById('page-labs');
        this.ui.screens['health-record'] = document.getElementById('page-health-record');
      }
    } catch(_) {}
    return r;
  };

  App.prototype._ensureSpaPageTools = function(){
    try {
      if (!document.getElementById('sinet-spa-nav-style-71')) {
        const st = document.createElement('style');
        st.id = 'sinet-spa-nav-style-71';
        st.textContent = [
          '.sinet-spa-nav{display:flex;gap:8px;flex-wrap:wrap;align-items:center;margin:10px 0 14px;padding:10px 12px;border:1px solid rgba(15,118,110,.18);border-radius:14px;background:linear-gradient(180deg,#ffffff 0%,#f8fffd 100%);box-shadow:0 6px 18px rgba(15,23,42,.05);}',
          '.sinet-spa-nav .btn-mini{display:inline-flex;align-items:center;justify-content:center;min-height:42px;padding:10px 14px;border-radius:12px;font-weight:900;border:1px solid #cbd5e1;background:#fff;color:#0f172a;cursor:pointer;}',
          '.sinet-spa-nav .btn-mini.primary{background:#0f766e;border-color:#0f766e;color:#fff;}',
          '.sinet-spa-nav .sinet-spa-note{margin-left:auto;font-size:.9rem;color:#475569;}',
          '@media (max-width:640px){.sinet-spa-nav .btn-mini{flex:1 1 calc(50% - 8px)} .sinet-spa-nav .sinet-spa-note{width:100%;margin-left:0;}}'
        ].join('');
        document.head.appendChild(st);
      }
      const pages = ['catalog','settings','system','studio','favorites','playlist','protocols','mysymptoms','ai','admin','workbench','sessions','help','labs','health-record'];
      const mkBtn = (label, cls, handler) => {
        const b = document.createElement('button');
        b.type = 'button';
        b.className = cls;
        b.textContent = label;
        b.addEventListener('click', handler);
        return b;
      };
      pages.forEach((pageId) => {
        const page = document.getElementById('page-' + pageId);
        if (!page) return;
        let bar = page.querySelector('[data-sinet-spa-nav="1"]');
        if (!bar) {
          bar = document.createElement('div');
          bar.className = 'sinet-spa-nav';
          bar.setAttribute('data-sinet-spa-nav','1');
          const back = mkBtn('⬅ Nazad','btn-mini', () => this.goBack());
          const home = mkBtn('🏠 Početak','btn-mini primary', () => this.nav('home'));
          const note = document.createElement('div');
          note.className = 'sinet-spa-note';
          note.textContent = 'Brz povratak na prethodni ekran ili na početnu.';
          bar.appendChild(back);
          bar.appendChild(home);
          bar.appendChild(note);
        }
        const heading = page.querySelector('h1, h2, h3');
        if (heading) heading.insertAdjacentElement('afterend', bar);
        else if (page.firstElementChild !== bar) page.prepend(bar);
      });
    } catch(_) {}
  };

  App.prototype._ensureMenuPriorityOrder = function(){
    try {
      const root = document.getElementById('nav-overlay');
      if (!root) return;
      const shortcuts = document.getElementById('menu-my-shortcuts');
      if (shortcuts) {
        shortcuts.style.gridTemplateColumns = 'repeat(2, minmax(0,1fr))';
        shortcuts.innerHTML = [
          '<button class="btn-full" style="margin:0; padding:12px; background:#2ecc71;" onclick="nav(\'mysymptoms\'); closeMenu();">🧬 MOJI SIMPTOMI</button>',
          '<button class="btn-full" style="margin:0; padding:12px; background:#f1c40f; color:#2c3e50;" onclick="nav(\'favorites\'); closeMenu();">⭐ MOJI FAVORITI</button>',
          '<button class="btn-full" style="margin:0; padding:12px; background:#1abc9c;" onclick="nav(\'protocols\'); closeMenu();">📑 MOJI PROTOKOLI</button>',
          '<button class="btn-full" style="margin:0; padding:12px; background:#2563eb;" onclick="nav(\'playlist\'); closeMenu();">📋 LISTA</button>'
        ].join('');
      }

      let catalogBox = document.getElementById('menu-katalog-priority-71');
      if (!catalogBox) {
        catalogBox = document.createElement('div');
        catalogBox.id = 'menu-katalog-priority-71';
        catalogBox.style.cssText = 'display:grid;grid-template-columns:1fr;gap:8px;margin:0 0 12px 0;';
        catalogBox.innerHTML = '<button class="btn-full" style="margin:0; padding:13px; background:#0f62fe;" onclick="nav(\'catalog\'); closeMenu();">📚 OTVORI KATALOG</button>';
        const anchor = shortcuts || document.getElementById('nav-menu');
        if (anchor && anchor.parentElement) anchor.insertAdjacentElement('afterend', catalogBox);
      }

      const navMenu = document.getElementById('nav-menu');
      if (!navMenu) return;
      const sec = (txt) => `<li class="menu-section-label" style="list-style:none; margin:12px 0 6px; padding:8px 12px; border-radius:10px; background:#f8fafc; color:#0f172a; font-weight:900; font-size:.92rem;">${txt}</li>`;
      const hr = '<li style="list-style:none; margin:10px 0;"><hr style="border:0; border-top:1px solid #e5e7eb; margin:0;"></li>';
      const link = (label, text, onclick) => `<li><a class="menu-link" data-label="${label}" href="#" onclick="${onclick}">${text}</a></li>`;
      navMenu.innerHTML = [
        sec('GLAVNI MENI'),
        link('moji simptomi','🧬 MOJI SIMPTOMI', "nav('mysymptoms'); closeMenu(); return false;"),
        link('moji favoriti','⭐ MOJI FAVORITI', "nav('favorites'); closeMenu(); return false;"),
        link('moji protokoli','📑 MOJI PROTOKOLI', "nav('protocols'); closeMenu(); return false;"),
        link('lista queue','📋 LISTA', "nav('playlist'); closeMenu(); return false;"),
        link('otvori katalog','📚 OTVORI KATALOG', "nav('catalog'); closeMenu(); return false;"),
        hr,
        link('anamneza dijagnostika','🩺 ANAMNEZA', "openQuickPage('anamneza.html',null,'🩺 Anamneza (Dijagnostika)'); closeMenu(); return false;"),
        link('zdravstveni karton','🗂 ZDRAVSTVENI KARTON', "nav('health-record'); closeMenu(); return false;"),
        link('lab bridge','🧪 LAB BRIDGE', "nav('labs'); closeMenu(); return false;"),
        hr,
        sec('BRZI LINKOVI (PRVA POMOĆ) / NAJVAŽNIJI ALATI'),
        link('antiparazitski program prva pomoc','🦠 ANTIPARAZITSKI PROGRAM', "openQuickPage('pages/antiparazitski.html','https://vera-srecko-analiza.netlify.app/','🦠 Antiparazitski (Prva pomoć)'); closeMenu(); return false;"),
        link('integrativna biblioteka','🧭 INTEGRATIVNA BIBLIOTEKA', "openQuickPage('pages/integrativna_biblioteka.html',null,'🧭 Integrativna biblioteka'); closeMenu(); return false;"),
        link('integrativni vodic ra sake','🧩 INTEGRATIVNI VODIČ - RA ŠAKE', "openQuickPage('pages/integrativni_vodic_RA_sake.html',null,'🧩 Integrativni vodič – RA šake'); closeMenu(); return false;"),
        link('integrativni vodic generator','🧾 INTEGRATIVNI VODIČ - GENERATOR', "openQuickPage('pages/integrativni_vodic.html',null,'🧾 Integrativni vodič (generator)'); closeMenu(); return false;"),
        link('akupunktura','🪡 AKUPUNKTURA', "openQuickPage('pages/akupunktura.html',null,'🪡 Akupunktura (stručni modul)'); closeMenu(); return false;"),
        link('tai chi seniori','🧘 TAI CHI ZA SENIORE', "openQuickPage('pages/tai_chi.html',null,'🧘 Tai Chi (za seniore)'); closeMenu(); return false;"),
        hr,
        sec('SISTEM / PODEŠAVANJA / ADMIN'),
        link('podesavanja','⚙️ PODEŠAVANJA', "nav('settings'); closeMenu(); return false;"),
        link('admin dijagnostika','🛡️ ADMIN DIJAGNOSTIKA', "nav('admin'); closeMenu(); return false;"),
        link('content studio','🧪 CONTENT STUDIO', "nav('studio'); closeMenu(); return false;"),
        link('instaliranje aplikacije za rad bez interneta instaliraj sada','📲 INSTALIRAJ SADA', "document.getElementById('pwa-install-btn') && document.getElementById('pwa-install-btn').click(); closeMenu(); return false;"),
        hr,
        sec('INSTALACIJA ZA KORISNIKA (1 KLIK)'),
        link('otvori vodic za instalaciju','⬇️ OTVORI VODIČ ZA INSTALACIJU', "window.app && window.app.openDoc && window.app.openDoc('./docs/control-center/SINET_One_Click_Install_v2.html','⬇️ Instalacija za korisnika (1 klik)'); closeMenu(); return false;"),
        link('control centar install','🎛 CONTROL CENTAR + INSTALL', "window.app && window.app.openDoc && window.app.openDoc('./docs/protokoli/37_SINET_CONTROL_CENTER_INSTALL_v1.0_SR.html','🎛 SINET Control Center + Install'); closeMenu(); return false;"),
        hr,
        sec('SINET CONTROL CENTER (van Audio Lekar)'),
        link('vodic instalacija control center','📘 VODIČ + INSTALACIJA', "window.app && window.app.openDoc && window.app.openDoc('./docs/protokoli/37_SINET_CONTROL_CENTER_INSTALL_v1.0_SR.html','🎛 SINET Control Center + Install'); closeMenu(); return false;"),
        link('control centar dokumentacija','🧰 CONTROL CENTAR DOKUMENTACIJA', "window.app && window.app.openDoc && window.app.openDoc('./docs/control-center/SINET_Control_Center_v1.html','🎛 SINET Control Center'); closeMenu(); return false;"),
        link('readme putanja','📄 README / PUTANJA', "window.app && window.app.openDoc && window.app.openDoc('./tools/control-center/README_SINET_CONTROL_CENTER_v1.md','README — SINET Control Center'); closeMenu(); return false;")
      ].join('');
    } catch(_) {}
  };

  const origPrepareHealth71 = App.prototype.prepareHealthRecordUI;
  App.prototype.prepareHealthRecordUI = function(){
    const r = origPrepareHealth71.apply(this, arguments);
    try { this._removeHomeMenuClutter71(); } catch(_) {}
    try { this._ensureSpaPageTools(); } catch(_) {}
    return r;
  };

  const origInit71 = App.prototype.init;
  App.prototype.init = async function(){
    const r = await origInit71.apply(this, arguments);
    try { this._removeHomeMenuClutter71(); } catch(_) {}
    try { this._ensureSpaPageTools(); } catch(_) {}
    try { this._ensureMenuPriorityOrder(); } catch(_) {}
    return r;
  };

  const origNav71 = App.prototype.nav;
  App.prototype.nav = function(pageId, opts={}){
    const r = origNav71.apply(this, arguments);
    try { this._removeHomeMenuClutter71(); } catch(_) {}
    try { this._ensureSpaPageTools(); } catch(_) {}
    try { this._ensureMenuPriorityOrder(); } catch(_) {}
    try {
      if (this.ui && this.ui.screens) {
        this.ui.screens['labs'] = document.getElementById('page-labs');
        this.ui.screens['health-record'] = document.getElementById('page-health-record');
      }
    } catch(_) {}
    return r;
  };
})();


/* ===================== v16.0.0.73b — Docs/support sibling remap ===================== */
(function(){
  function remapDocHref71(href){
    const raw = String(href || '').trim();
    if (!raw) return raw;
    if (/^https?:/i.test(raw) || raw.startsWith('data:') || raw.startsWith('blob:')) return raw;
    const clean = raw.replace(/^\.\//,'');
    if (clean.startsWith('docs/')) return '../SINET_Audio_Lekar_DOCS_SUPPORT/' + clean;
    if (clean.startsWith('tools/')) return '../SINET_Audio_Lekar_DOCS_SUPPORT/' + clean;
    if (/\.(md|txt|html)$/i.test(clean) && !clean.startsWith('pages/') && !clean.startsWith('data/') && !clean.startsWith('js/') && !clean.startsWith('css/')) {
      return '../SINET_Audio_Lekar_DOCS_SUPPORT/' + clean;
    }
    return raw;
  }
  const origOpenDoc71 = App.prototype.openDoc;
  App.prototype.openDoc = function(href, title = 'Dokument'){
    return origOpenDoc71.call(this, remapDocHref71(href), title);
  };
})();

/* ===================== v16.0.0.73 — Clean home + unified menu model ===================== */
(function(){
  const V = '16.0.0.73';

  function removeIfExists(id){
    try{ const el=document.getElementById(id); if(el) el.remove(); }catch(_){ }
  }

  function safeCloseMenu(){
    try{ if(window.closeMenu) window.closeMenu(); }catch(_){ }
  }

  App.prototype._cleanupHomeToEntryOnly72 = function(){
    try {
      ['system-entry-card','home-resume-panel','frequent-actions','quick-links','install-center-promo','pwa-install-promo','pwa-install-promo-ios'].forEach(removeIfExists);
      const mainBox = document.getElementById('home-main-content');
      if (mainBox) {
        mainBox.style.display = 'none';
        mainBox.setAttribute('data-sinet-hidden-main','1');
      }
      const dedicationInsideMain = mainBox && mainBox.querySelector('.welcome-card');
      if (dedicationInsideMain) dedicationInsideMain.remove();
    } catch(_) {}
  };

  App.prototype._ensureHomeDedication72 = function(){
    try {
      const home = document.getElementById('page-home');
      const selector = document.getElementById('home-entry-selector');
      if (!home || !selector) return;
      let box = document.getElementById('home-dedication-always');
      if (!box) {
        box = document.createElement('div');
        box.id = 'home-dedication-always';
        box.className = 'welcome-card';
        box.style.cssText = 'background:white;padding:14px;border-radius:12px;margin-bottom:15px;box-shadow:0 2px 5px rgba(0,0,0,0.05);text-align:center;';
        box.innerHTML = '<div class="dedication-box"><p class="dedication-main">❤️ Posvećeno mojoj Veri, mome Srećku!</p><p class="dedication-sub">Uz svo poštovanje i svu ljubav.</p></div>';
        selector.insertAdjacentElement('afterend', box);
      }
    } catch(_) {}
  };

  App.prototype._ensureUnifiedMenu72 = function(){
    try {
      removeIfExists('menu-priority-shortcuts');
      removeIfExists('menu-katalog-priority-71');
      const navMenu = document.getElementById('nav-menu');
      if (!navMenu) return;
      const sec = (txt) => `<li class="menu-section-label" style="list-style:none; margin:12px 0 6px; padding:8px 12px; border-radius:10px; background:#f8fafc; color:#0f172a; font-weight:900; font-size:.92rem;">${txt}</li>`;
      const hr = '<li style="list-style:none; margin:10px 0;"><hr style="border:0; border-top:1px solid #e5e7eb; margin:0;"></li>';
      const link = (label, text, onclick) => `<li><a class="menu-link" data-label="${label}" href="#" onclick="${onclick}">${text}</a></li>`;
      navMenu.setAttribute('data-sinet-unified-menu', V);
      navMenu.innerHTML = [
        sec('GLAVNI MENI'),
        link('moji simptomi','🧬 MOJI SIMPTOMI', "nav('mysymptoms'); closeMenu(); return false;"),
        link('moji favoriti','⭐ MOJI FAVORITI', "nav('favorites'); closeMenu(); return false;"),
        link('moji protokoli','📑 MOJI PROTOKOLI', "nav('protocols'); closeMenu(); return false;"),
        link('lista','📋 LISTA', "nav('playlist'); closeMenu(); return false;"),
        link('otvori katalog','📚 OTVORI KATALOG', "nav('catalog'); closeMenu(); return false;"),
        hr,
        link('anamneza','🩺 ANAMNEZA', "openQuickPage('anamneza.html',null,'🩺 Anamneza (Dijagnostika)'); closeMenu(); return false;"),
        link('zdravstveni karton','🗂 ZDRAVSTVENI KARTON', "nav('health-record'); closeMenu(); return false;"),
        link('lab bridge','🧪 LAB BRIDGE', "nav('labs'); closeMenu(); return false;"),
        hr,
        sec('BRZI LINKOVI (PRVA POMOĆ) / NAJVAŽNIJI ALATI'),
        link('brzi linkovi prva pomoc','⚡ BRZI LINKOVI (PRVA POMOĆ)', "window.openQuickLinks && window.openQuickLinks(); closeMenu(); return false;"),
        link('antiparazitski program','🦠 ANTIPARAZITSKI PROGRAM', "openQuickPage('pages/antiparazitski.html','https://vera-srecko-analiza.netlify.app/','🦠 Antiparazitski (Prva pomoć)'); closeMenu(); return false;"),
        hr,
        link('integrativna biblioteka','🧭 INTEGRATIVNA BIBLIOTEKA', "openQuickPage('pages/integrativna_biblioteka.html',null,'🧭 Integrativna biblioteka'); closeMenu(); return false;"),
        link('integrativni vodic ra sake','🧩 INTEGRATIVNI VODIČ - RA ŠAKE', "openQuickPage('pages/integrativni_vodic_RA_sake.html',null,'🧩 Integrativni vodič – RA šake'); closeMenu(); return false;"),
        link('integrativni vodic generator','🧾 INTEGRATIVNI VODIČ - GENERATOR', "openQuickPage('pages/integrativni_vodic.html',null,'🧾 Integrativni vodič (generator)'); closeMenu(); return false;"),
        hr,
        link('akupunktura','🪡 AKUPUNKTURA', "openQuickPage('pages/akupunktura.html',null,'🪡 Akupunktura (stručni modul)'); closeMenu(); return false;"),
        link('tai chi seniori','🧘 TAI CHI ZA SENIORE', "openQuickPage('pages/tai_chi.html',null,'🧘 Tai Chi (za seniore)'); closeMenu(); return false;"),
        hr,
        sec('SISTEM / PODEŠAVANJA / ADMIN'),
        link('podesavanja','⚙️ PODEŠAVANJA', "nav('settings'); closeMenu(); return false;"),
        link('admin dijagnostika','🛡️ ADMIN DIJAGNOSTIKA', "nav('admin'); closeMenu(); return false;"),
        link('content studio','🧪 CONTENT STUDIO', "nav('studio'); closeMenu(); return false;"),
        link('instaliraj sada','📲 INSTALIRAJ SADA', "document.getElementById('pwa-install-btn') && document.getElementById('pwa-install-btn').click(); closeMenu(); return false;"),
        hr,
        sec('INSTALACIJA ZA KORISNIKA (1 KLIK)'),
        link('otvori vodic za instalaciju','⬇️ OTVORI VODIČ ZA INSTALACIJU', "window.app && window.app.openDoc && window.app.openDoc('./docs/control-center/SINET_One_Click_Install_v2.html','⬇️ Instalacija za korisnika (1 klik)'); closeMenu(); return false;"),
        link('control centar install','🎛 CONTROL CENTAR + INSTALL', "window.app && window.app.openDoc && window.app.openDoc('./docs/protokoli/37_SINET_CONTROL_CENTER_INSTALL_v1.0_SR.html','🎛 SINET Control Center + Install'); closeMenu(); return false;"),
        hr,
        sec('SINET CONTROL CENTER (van Audio Lekar)'),
        link('vodic instalacija','📘 VODIČ + INSTALACIJA', "window.app && window.app.openDoc && window.app.openDoc('./docs/protokoli/37_SINET_CONTROL_CENTER_INSTALL_v1.0_SR.html','🎛 SINET Control Center + Install'); closeMenu(); return false;"),
        link('control centar dokumentacija','🧰 CONTROL CENTAR DOKUMENTACIJA', "window.app && window.app.openDoc && window.app.openDoc('./docs/control-center/SINET_Control_Center_v1.html','🎛 SINET Control Center'); closeMenu(); return false;"),
        link('readme putanja','📄 README / PUTANJA', "window.app && window.app.openDoc && window.app.openDoc('./tools/control-center/README_SINET_CONTROL_CENTER_v1.md','README — SINET Control Center'); closeMenu(); return false;")
      ].join('');
    } catch(_) {}
  };

  const origSetHomeEntryMode72 = App.prototype.setHomeEntryMode;
  App.prototype.setHomeEntryMode = function(mode){
    if (mode === 'main') {
      try { this.homeEntryMode = null; } catch(_) {}
      try { origSetHomeEntryMode72.call(this, null); } catch(_) {}
      try { this._cleanupHomeToEntryOnly72(); } catch(_) {}
      try { this._ensureHomeDedication72(); } catch(_) {}
      try { if (window.toggleMenu) window.toggleMenu(); } catch(_) {}
      return;
    }
    const r = origSetHomeEntryMode72.apply(this, arguments);
    try { this._cleanupHomeToEntryOnly72(); } catch(_) {}
    try { this._ensureHomeDedication72(); } catch(_) {}
    return r;
  };

  const origApplyHomeEntryMode72 = App.prototype.applyHomeEntryMode;
  App.prototype.applyHomeEntryMode = function(){
    const r = origApplyHomeEntryMode72.apply(this, arguments);
    try { this._cleanupHomeToEntryOnly72(); } catch(_) {}
    try { this._ensureHomeDedication72(); } catch(_) {}
    return r;
  };

  const origInit72 = App.prototype.init;
  App.prototype.init = async function(){
    const r = await origInit72.apply(this, arguments);
    try { this._cleanupHomeToEntryOnly72(); } catch(_) {}
    try { this._ensureHomeDedication72(); } catch(_) {}
    try { this._ensureUnifiedMenu72(); } catch(_) {}
    try {
      const mainBtn = document.getElementById('home-entry-main');
      if (mainBtn) mainBtn.onclick = function(ev){ if(ev) ev.preventDefault(); try{ if(window.toggleMenu) window.toggleMenu(); }catch(_){} return false; };
    } catch(_) {}
    return r;
  };

  const origNav72 = App.prototype.nav;
  App.prototype.nav = function(pageId, opts={}){
    const r = origNav72.apply(this, arguments);
    try { this._cleanupHomeToEntryOnly72(); } catch(_) {}
    try { this._ensureHomeDedication72(); } catch(_) {}
    try { this._ensureUnifiedMenu72(); } catch(_) {}
    return r;
  };

  window.openQuickLinks = function(){
    try { nav('home'); } catch(_) {}
    setTimeout(() => {
      try { if (window.app && window.app.setHomeEntryMode) window.app.setHomeEntryMode('firstaid'); } catch(_) {}
      try { if (typeof window.scrollTo === 'function') window.scrollTo({ top: 0, behavior: 'smooth' }); } catch(_) {}
    }, 80);
  };
})();

/* ===================== v16.0.0.73 — GLAVNI MENI cards + HAMBURGER list ===================== */
(function(){
  const V = '16.0.0.73';

  function actionNav(page){ return `nav('${page}')`; }
  function actionQuick(page, fallback, title){ return `openQuickPage('${page}',${fallback ? "'"+fallback+"'" : 'null'},'${title.replace(/'/g,"\\'")}')`; }
  function actionDoc(href, title){ return `window.app && window.app.openDoc && window.app.openDoc('${href}','${title.replace(/'/g,"\\'")}')`; }

  App.prototype._getSinetMenuModel73 = function(){
    return [
      {
        title: 'RAD SA APLIKACIJOM',
        items: [
          { key:'mysymptoms', list:'🧬 MOJI SIMPTOMI', card:'🧬 Moji simptomi', desc:'Lični simptomi koje pratiš i dopunjuješ.', color:'#22c55e', action: actionNav('mysymptoms') },
          { key:'favorites', list:'⭐ MOJI FAVORITI', card:'⭐ Moji favoriti', desc:'Brz pristup najčešće korišćenim stavkama.', color:'#f59e0b', action: actionNav('favorites') },
          { key:'protocols', list:'📑 MOJI PROTOKOLI', card:'📑 Moji protokoli', desc:'Pregled i organizacija protokola.', color:'#14b8a6', action: actionNav('protocols') },
          { key:'playlist', list:'📋 LISTA', card:'📋 Lista', desc:'Tvoja radna queue lista.', color:'#2563eb', action: actionNav('playlist') },
          { key:'catalog', list:'📚 OTVORI KATALOG', card:'📚 Otvori katalog', desc:'Pretraga i izbor simptoma iz kataloga.', color:'#0f62fe', action: actionNav('catalog') },
        ]
      },
      {
        title: 'ZDRAVLJE / NALAZI / LAB',
        items: [
          { key:'anamneza', list:'🩺 ANAMNEZA', card:'🩺 Anamneza', desc:'Dijagnostika i unos anamneze.', color:'#ef4444', action: "openQuickPage('anamneza.html',null,'🩺 Anamneza (Dijagnostika)')" },
          { key:'health-record', list:'🗂 ZDRAVSTVENI KARTON', card:'🗂 Zdravstveni karton', desc:'Tvoj pregled zdravstvenih podataka.', color:'#8b5cf6', action: actionNav('health-record') },
          { key:'labs', list:'🧪 LAB BRIDGE', card:'🧪 Lab Bridge', desc:'Laboratorija i tumačenje nalaza.', color:'#06b6d4', action: actionNav('labs') },
        ]
      },
      {
        title: 'PRVA POMOĆ / INTEGRATIVNI ALATI',
        items: [
          { key:'quicklinks', list:'⚡ BRZI LINKOVI (PRVA POMOĆ)', card:'⚡ Brzi linkovi (Prva pomoć)', desc:'Najbrži ulaz u hitne korisne sadržaje.', color:'#dc2626', action: 'window.openQuickLinks && window.openQuickLinks()' },
          { key:'antiparazitski', list:'🦠 ANTIPARAZITSKI PROGRAM', card:'🦠 Antiparazitski program', desc:'Poseban vodič i pomoćni modul.', color:'#059669', action: "openQuickPage('pages/antiparazitski.html','https://vera-srecko-analiza.netlify.app/','🦠 Antiparazitski (Prva pomoć)')" },
          { key:'biblioteka', list:'🧭 INTEGRATIVNA BIBLIOTEKA', card:'🧭 Integrativna biblioteka', desc:'Biblioteka integrativnih sadržaja.', color:'#7c3aed', action: "openQuickPage('pages/integrativna_biblioteka.html',null,'🧭 Integrativna biblioteka')" },
          { key:'ra-sake', list:'🧩 INTEGRATIVNI VODIČ - RA ŠAKE', card:'🧩 Integrativni vodič – RA šake', desc:'Ciljani vodič za RA šake.', color:'#9333ea', action: "openQuickPage('pages/integrativni_vodic_RA_sake.html',null,'🧩 Integrativni vodič – RA šake')" },
          { key:'generator', list:'🧾 INTEGRATIVNI VODIČ - GENERATOR', card:'🧾 Integrativni vodič – generator', desc:'Generator integrativnog vodiča.', color:'#6366f1', action: "openQuickPage('pages/integrativni_vodic.html',null,'🧾 Integrativni vodič (generator)')" },
          { key:'akupunktura', list:'🪡 AKUPUNKTURA', card:'🪡 Akupunktura', desc:'Stručni modul i vodič.', color:'#0f766e', action: "openQuickPage('pages/akupunktura.html',null,'🪡 Akupunktura (stručni modul)')" },
          { key:'tai-chi', list:'🧘 TAI CHI ZA SENIORE', card:'🧘 Tai Chi za seniore', desc:'Vežbe i smernice za seniore.', color:'#475569', action: "openQuickPage('pages/tai_chi.html',null,'🧘 Tai Chi (za seniore)')" },
        ]
      },
      {
        title: 'SISTEM / PODEŠAVANJA / ADMIN',
        items: [
          { key:'settings', list:'⚙️ PODEŠAVANJA', card:'⚙️ Podešavanja', desc:'Audio, jezik, prikaz i ostale opcije.', color:'#64748b', action: actionNav('settings') },
          { key:'admin', list:'🛡️ ADMIN DIJAGNOSTIKA', card:'🛡️ Admin dijagnostika', desc:'Sistemski alati i dijagnostika.', color:'#0f766e', action: actionNav('admin') },
          { key:'studio', list:'🧪 CONTENT STUDIO', card:'🧪 Content Studio', desc:'Symptom Studio i uvoz/izvoz patch-eva.', color:'#6d28d9', action: actionNav('studio') },
          { key:'install-now', list:'📲 INSTALIRAJ SADA', card:'📲 Instaliraj sada', desc:'PWA instalacija za rad bez interneta.', color:'#ea580c', action: "document.getElementById('pwa-install-btn') && document.getElementById('pwa-install-btn').click()" },
        ]
      },
      {
        title: 'INSTALACIJA / CONTROL CENTER',
        items: [
          { key:'install-guide', list:'⬇️ OTVORI VODIČ ZA INSTALACIJU', card:'⬇️ Vodič za instalaciju', desc:'Koraci za korisničku instalaciju.', color:'#2563eb', action: actionDoc('./docs/control-center/SINET_One_Click_Install_v2.html','⬇️ Instalacija za korisnika (1 klik)') },
          { key:'cc-install', list:'🎛 CONTROL CENTAR + INSTALL', card:'🎛 Control Centar + Install', desc:'Jedinstven ulaz za install tok i control center.', color:'#1d4ed8', action: actionDoc('./docs/protokoli/37_SINET_CONTROL_CENTER_INSTALL_v1.0_SR.html','🎛 SINET Control Center + Install') },
          { key:'cc-docs', list:'🧰 CONTROL CENTAR DOKUMENTACIJA', card:'🧰 Control Centar dokumentacija', desc:'Povezana dokumentacija control centra.', color:'#334155', action: actionDoc('./docs/control-center/SINET_Control_Center_v1.html','🎛 SINET Control Center') },
          { key:'readme-path', list:'📄 README / PUTANJA', card:'📄 README / putanja', desc:'README i putanja do dodatnih alata.', color:'#475569', action: actionDoc('./tools/control-center/README_SINET_CONTROL_CENTER_v1.md','README — SINET Control Center') },
        ]
      }
    ];
  };

  App.prototype._ensureDualMenuModel73 = function(){
    try {
      const model = this._getSinetMenuModel73();
      const navMenu = document.getElementById('nav-menu');
      if (navMenu) {
        const sec = (txt) => `<li class="menu-section-label" style="list-style:none; margin:12px 0 6px; padding:8px 12px; border-radius:10px; background:#f8fafc; color:#0f172a; font-weight:900; font-size:.92rem;">${txt}</li>`;
        const hr = '<li style="list-style:none; margin:10px 0;"><hr style="border:0; border-top:1px solid #e5e7eb; margin:0;"></li>';
        const link = (label, text, onclick) => `<li><a class="menu-link" data-label="${label}" href="#" onclick="${onclick}; closeMenu(); return false;">${text}</a></li>`;
        const parts = [sec('HAMBURGER MENI')];
        model.forEach((section, idx) => {
          if (idx > 0) parts.push(hr);
          parts.push(sec(section.title));
          section.items.forEach(item => parts.push(link(item.key, item.list, item.action)));
        });
        navMenu.innerHTML = parts.join('');
        navMenu.setAttribute('data-sinet-dual-menu', V);
      }

      const page = document.getElementById('page-system');
      if (page) {
        if (!document.getElementById('sinet-menu-cards-style-73')) {
          const st = document.createElement('style');
          st.id = 'sinet-menu-cards-style-73';
          st.textContent = [
            '.sinet-menu-intro{margin:0 0 12px;color:#475569;font-size:.98rem;}',
            '.sinet-card-menu-section{margin:14px 0 18px;}',
            '.sinet-card-menu-section h3{margin:0 0 10px;font-size:1rem;color:#0f172a;}',
            '.sinet-card-menu-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:12px;}',
            '.sinet-menu-card{display:flex;flex-direction:column;gap:8px;border:none;border-radius:18px;padding:16px;min-height:124px;text-align:left;color:#fff;box-shadow:0 10px 24px rgba(15,23,42,.12);cursor:pointer;}',
            '.sinet-menu-card .title{font-size:1rem;font-weight:900;line-height:1.2;}',
            '.sinet-menu-card .desc{font-size:.88rem;line-height:1.35;opacity:.96;}',
            '.sinet-menu-card:hover{transform:translateY(-1px);}',
            '.sinet-system-footer{margin-top:8px;display:flex;justify-content:flex-end;}',
            '@media (max-width:640px){.sinet-menu-card{min-height:114px;padding:14px;}}'
          ].join('');
          document.head.appendChild(st);
        }
        const h2 = page.querySelector('h2');
        let shell = page.querySelector('[data-sinet-card-menu="1"]');
        if (!shell) {
          shell = document.createElement('div');
          shell.setAttribute('data-sinet-card-menu','1');
          if (h2) h2.insertAdjacentElement('afterend', shell);
          else page.prepend(shell);
        }
        shell.innerHTML = '<p class="sinet-menu-intro">GLAVNI MENI otvara kartice istog redosleda kao hamburger meni, ali preglednije za rad i mobilnu upotrebu.</p>' +
          model.map(section => '<section class="sinet-card-menu-section"><h3>'+section.title+'</h3><div class="sinet-card-menu-grid">' +
            section.items.map(item => `<button type="button" class="sinet-menu-card" style="background:${item.color};" onclick="${item.action}; return false;"><span class="title">${item.card}</span><span class="desc">${item.desc}</span></button>`).join('') +
          '</div></section>').join('') +
          '<div class="sinet-system-footer"></div>';

        Array.from(page.children).forEach((child) => {
          if (child === h2 || child === shell || (child.getAttribute && child.getAttribute('data-sinet-spa-nav') === '1')) return;
          if (child.tagName === 'SCRIPT' || child.tagName === 'STYLE') return;
          child.style.display = 'none';
          child.setAttribute('data-sinet-hidden-old-system','1');
        });
      }

      const mainBtn = document.getElementById('home-entry-main');
      if (mainBtn) {
        mainBtn.textContent = '🗂 GLAVNI MENI';
        mainBtn.onclick = function(ev){ if (ev) ev.preventDefault(); try{ nav('system'); }catch(_){ } return false; };
      }
    } catch(_) {}
  };

  const origInit73 = App.prototype.init;
  App.prototype.init = async function(){
    const r = await origInit73.apply(this, arguments);
    try { this._ensureDualMenuModel73(); } catch(_) {}
    return r;
  };

  const origNav73 = App.prototype.nav;
  App.prototype.nav = function(pageId, opts={}){
    const r = origNav73.apply(this, arguments);
    try { this._ensureDualMenuModel73(); } catch(_) {}
    return r;
  };
})();
